# Scan 测试数据生成说明

本文记录 `Scan/` 文件夹中的离线资料如何被构造成 AI 监检平台的完整测试流程数据。

## 目标

把 `Scan/` 下已经完成 OCR 的文件，模拟成项目 `P-2026-GDLNG-002` 中真实上传、提交、等待监检审查的数据。

目标状态：

- 施工方已上传并提交相关资料。
- 无损机构已上传并提交无损检测资料。
- OCR 已完成，文件详情能看到 OCR 字段和原文。
- 文件已完成切片和离线向量化。
- OCR 结果经过资料打靶，自动关联到可命中的业务审查点。
- 监检人员登录后能看到待审查、待人工确认节点和待办。
- 不再出现所有文件默认挂到“产品质量证明书”的情况。

## 入口脚本

脚本位置：

```bash
backend/scripts/import_scan_test_scenario.py
```

执行方式：

```bash
cd /Volumes/7up/github/knowledgetools
backend/.venv/bin/python backend/scripts/import_scan_test_scenario.py --apply
```

场景标签：

```text
scan-test-scenario-v1
```

脚本通过这个标签标记本次测试数据，便于重复导入时清理旧数据、重新生成流程。

## 文件如何落盘

脚本不会只在数据库里写 mock URL，而是把原始文件复制到正常上传文件路径：

```text
output/document_uploads/P-2026-GDLNG-002/{uploadSessionId}/{documentVersionId}/{fileName}
```

生成的 `storageKey` 形如：

```text
local://output/document_uploads/P-2026-GDLNG-002/UPS-SCAN-CONTRACTOR-V1/DV-xxx-V1/xxx.pdf
```

因此文件预览、原文接口读取的是实际文件，不是 `mock://preview/...`。

## 生成的核心数据

每个 Scan 文件会生成或更新以下数据：

- `documents`：项目文档主记录。
- `document_versions`：文件版本、hash、大小、存储位置。
- `knowledge_files`：知识库文件入口。
- `knowledge_tasks`：OCR、切片、向量化任务记录。
- `ocr_jobs`：OCR 任务。
- `ocr_parse_results`：OCR 解析结果。
- `extracted_fields`：OCR 字段。
- `evidence_links`：OCR 证据片段。
- `knowledge_chunks`：文本分片。
- `knowledge_vectors`：离线 hash 向量。

文档会挂到项目：

```text
P-2026-GDLNG-002
```

并带上：

```text
scenarioTag = scan-test-scenario-v1
```

## 文件到节点的显式映射

脚本内的 `FILE_MAPPINGS` 定义了第一层人工映射。

映射字段包括：

- `role`：上传角色，施工方或无损机构。
- `nodeId`：关联业务节点。
- `requirementId`：资料需求项。
- `materialCategory`：资料类别。
- `materialTypeCode`：资料类型编码。

示例：

```text
20260623104703.pdf -> 节点 16，产品质量证明文件，施工方上传
20260623104730.pdf -> 节点 16，产品质量证明文件，施工方上传
20260623104828.pdf -> 节点 16，产品质量证明文件，施工方上传
20260623105636.pdf -> 节点 40，无损检测报告，无损机构上传
```

这一步生成 `node_bindings`，相当于文件已经走过正常上传后的资料绑定路径。

## OCR 打靶自动关联

显式映射完成后，脚本会对每个文件执行：

```python
run_material_targeting(...)
```

它根据 OCR 文本、资料类别、业务审查点规则，自动生成：

- `material_targeting_runs`：每个文件的打靶运行记录。
- `node_evidence_links`：文件命中的业务审查点证据。
- 自动生成的 `node_bindings`：当 OCR 证据命中其他业务节点时，补充自动绑定。

因此现在有两类关联：

- 显式导入绑定：来自 `FILE_MAPPINGS`。
- 自动打靶绑定：来自 OCR 命中业务审查点。

这就是“打靶”逻辑：上传资料不是只挂一个固定节点，而是根据 OCR 内容分配到它能支撑的审查点。

## 上传会话

脚本会生成两个上传会话：

```text
UPS-SCAN-CONTRACTOR-V1
UPS-SCAN-NDT-V1
```

施工方会话包含施工方文件，无损机构会话包含无损检测文件。上传会话状态为 `已完成`，用于让前端施工方/无损机构侧能看到正常上传后的文件列表。

## 角色和可见范围

脚本会补 `project_members` 中的节点范围：

- 施工方：粤海安装工程有限公司，李工。
- 无损机构：粤检无损检测，王工。
- 监检人员：可看到待审查、待人工确认节点和待办。

施工方节点范围来自施工方上传文件映射到的节点；无损机构节点范围包含节点 40。

## 流程数据

导入不是只补文件，还会补齐业务流程数据：

- 施工方提交记录：按节点生成 `submissions`。
- 无损检测提交记录：生成无损检测报告、记录和提交。
- 监检待办：生成 `todos`。
- 节点状态：
  - 施工方相关节点：`待人工确认`
  - 无损检测节点 40：`待审查`
- 项目状态：`在监检审查中`

这样监检平台能看到真实的待办和待审查节点。

## 当前 PostgreSQL 中的结果

最近一次迁移后，当前 PostgreSQL 中的 Scan 测试数据统计为：

```text
Scan 文档：30
文档版本：30
上传会话：2
节点绑定：52
提交流程：12
监检待办：12
打靶运行：30
节点证据：84
OCR job：30
OCR parse result：30
Scan 向量：4524
```

## 校验 SQL

当前服务已切到 PostgreSQL 后，可以用以下 SQL 校验：

```sql
select 'scan_docs|' || count(*)
from aicheck_state
where collection = 'documents'
  and payload->>'projectId' = 'P-2026-GDLNG-002'
  and payload->>'scenarioTag' = 'scan-test-scenario-v1';

select 'scan_upload_sessions|' || count(*)
from aicheck_state
where collection = 'upload_sessions'
  and payload->>'projectId' = 'P-2026-GDLNG-002'
  and payload->>'scenarioTag' = 'scan-test-scenario-v1';

select 'scan_bindings|' || count(*)
from aicheck_state
where collection = 'node_bindings'
  and payload->>'projectId' = 'P-2026-GDLNG-002'
  and payload->>'scenarioTag' = 'scan-test-scenario-v1';

select 'scan_targeting_runs|' || count(*)
from aicheck_state
where collection = 'material_targeting_runs'
  and payload->>'projectId' = 'P-2026-GDLNG-002'
  and payload->>'scenarioTag' = 'scan-test-scenario-v1';

select 'scan_node_evidence|' || count(*)
from aicheck_state
where collection = 'node_evidence_links'
  and payload->>'projectId' = 'P-2026-GDLNG-002'
  and payload->>'scenarioTag' = 'scan-test-scenario-v1';

select 'scan_todos|' || count(*)
from aicheck_state
where collection = 'todos'
  and payload->>'projectId' = 'P-2026-GDLNG-002'
  and payload->>'scenarioTag' = 'scan-test-scenario-v1';
```

## 接口校验

后端健康检查应显示 PostgreSQL：

```bash
curl -s http://127.0.0.1:8000/api/healthz
```

关键字段：

```text
databaseBackend = postgres
postgresEnabled = true
sqliteEnabled = false
```

检查项目文档列表：

```bash
curl -s 'http://127.0.0.1:8000/api/projects/P-2026-GDLNG-002/documents?page=1&pageSize=5'
```

检查节点包和打靶证据：

```bash
curl -s 'http://127.0.0.1:8000/api/projects/P-2026-GDLNG-002/nodes/16/package?role=inspection'
curl -s 'http://127.0.0.1:8000/api/projects/P-2026-GDLNG-002/nodes/40/package?role=inspection'
```

## 注意事项

- 当前业务状态已经迁移到 PostgreSQL；SQLite 文件仍作为本地备份存在。
- `knowledge_vectors` 已进入 PostgreSQL 的 JSONB 状态表。
- 本机 PostgreSQL 尚未安装 `pgvector` 扩展，因此没有生成 `knowledge_vector_index` 物理向量索引表。
- 若要启用 PostgreSQL 物理向量检索，需要先安装 `pgvector`，再重建向量索引。

