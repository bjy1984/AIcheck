# rules 目录清单

整理日期：2026-07-02

## 目录结构

| 路径 | 内容 |
| --- | --- |
| `业务规则.md` | 当前业务规则文件副本 |
| `NB_T_47013_8_2025_vs_old_update_changes.md` | `NB/T 47013.8-2025` 相较旧版的更新变化记录 |
| `nbt47013_8_2025_update_images/` | 上述更新变化记录使用的 9 张附图 |
| `standards/` | `业务规则.md` 中涉及的标准、规范、规程、规则、公告类文件 |
| `reports/` | 标准齐套性、`NB/T 47013` 拆分和更新核对报告 |

## 纳入范围

本次按 `业务规则.md` 和 `output/files_standard_completeness_report.md` 的匹配结果，复制了以下类别文件：

- TSG 法规、规程、规则、公告。
- GB/GB-T 标准。
- NB/T、JB/T、SY/T 行业标准。
- `NB/T 47013` 原合集及已拆分分册目录。
- `NB/T 47013.8-2025` 更新变化记录及附图。

## 重要说明

- 本次采用复制方式整理，原 `files/`、`output/` 下文件保留不动，避免破坏既有引用。
- 当前工作区未找到 `NB/T 47013.8-2025` 的完整 PDF；按用户要求，已在 `standards/` 中放入 `NB_T 47013.8-2025 承压设备无损检测 第8部分 泄漏检测（临时替代-更新变化记录）.md`，暂时替代完整 PDF 用于规则库检索。取得完整 PDF 后应替换为正式标准文件。
- `NB/T 47013` 目录中已包含当前工作区已有的 `NB/T 47013.3-2023`、`NB/T 47013.11-2023` 等新增分册文件。
- 2026-07-02 复核后补入 `特种设备检验人员考核规则.docx`，用于覆盖 `TSG Z8001-2019` 的第1号修改单引用；详见 `reports/rules_standard_completeness_recheck.md`。
