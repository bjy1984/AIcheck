# 下一批任務（2026-09-10 開列）

依據是當天量出來的數字，不是印象。每項都寫清楚：**做什麼、為什麼是這個順序、卡不卡、怎麼算做完**。

---

## A. 只等你一個動作就能做的（我這邊已就緒）

### A1. 換通義密鑰 → 跑通 R43 端到端　【**最高優先**】

**現況**：`AICHECK_LLM_API_KEY` 是 `sk-ws-` 開頭 116 字元，阿里雲回 `invalid_api_key`。DeepSeek 回退已棄用。AI 審查、一鍵分析、文件分類自 2026-09-09 04:11 起全停。

**你要做的**：阿里雲百煉簽一把新 key（`sk-` 開頭約 35 字元）寫進 `AICHECK_LLM_API_KEY`，重啟 API 與四個 worker。

**我接著做的**：
1. `scripts/model_reachability_probe.py` 確認通了
2. 對 `0常用管道元件核查记录-施工单位填写.doc` 跑 `run_domain_judgment.py --persist`
3. R43 判據拿到三個判斷布林 → 出真實結論（passed／failed／evidence_insufficient 都算成功，重點是**不再是「什麼都沒有」**）
4. 用 `export_review_acceptance_fixture.py` 導出成驗收證據

**算做完**：一條規則對一份真實文件走完 OCR → 分類 → 對映 → agent 填事實 → 凍結判據判定 → 導出 fixture，全鏈有據可查。
**規模**：拿到密鑰後約半天。
**這是整批裡唯一能證明「這套系統能審真圖紙」的任務。**

### A2. 索取施工記錄類資料

**現況**：121 份文件全是設計、許可、資格、質量證明、檢測報告。**0 份**防腐施工記錄／陰極保護測試記錄／支吊架安裝記錄／驗收表。

**影響**：R44、R46、R47、R49、R50、R52～R55、R68 等約 25 條施工類規則**沒有紙可讀**，補再多程式也沒用。352 個判斷布林裡約七成是現場觀察，記錄這些觀察的文件不在語料裡。

**你要做的**：向工程方要一套完整的施工記錄。哪怕只有一個工程的一類記錄，就能讓對應規則活過來。

---

## B. 不等任何人，我現在就能做的

### B1. 開工位模式並重跑真實審查

**為什麼**：生產庫**沒有任何一次運行**有 `documentScopeSnapshot`（工位模式從沒開過，且開了不會給既有運行補快照）。沒有凍結的文件範圍，`export_review_acceptance_fixture.py` 就導不出任何東西——第 8 項驗收因此一步都動不了。

**做什麼**：生產開 `AICHECK_WORKSTATIONS_ENABLED`，對兩個真實工程重跑審查，讓運行帶上凍結範圍。

**算做完**：至少一次真實運行帶 `documentScopeSnapshot`，並能被導出器接受。
**注意**：這會產生新的審查運行，屬於生產寫入，**動手前我會先問你**。

### ~~B2. 把 R63 的壓力管道級別表接上~~ → 改列為 **C4，卡在 OCR 品質**

**試過了，做不了。** 把那張表 28 列全撈出來看，它不是「表頭錯位」這麼簡單：

```
第0列: {"压力管道级别": "损伤比例", "操作压力": "操作温度",
        "管路等级": "介质名称", "管路起点": "管路终点", ...,
        "GC2": "RT10%", "M1E": "王虎悦", "0.275": "60°C"}
第2列: {"BOM A": "1", "BOM A_12": "无缝钢管", "BOM A_13": "25",
        "BOM A_14": "0.7M", "BOM A_15": "S30408"}
第4列: {"管路起点": "S30408", "操作压力": "25", ...}
```

這是一張**施工圖**，頁面上同時有管道特性表和材料清單（BOM），OCR 把兩張表併成一張，
欄名一半是真實表頭、一半是資料值。同一個鍵 `管路起点` 在某列裡裝的是 `S30408`
（一個材質牌號）。

**沒有任何簽名能可靠地從這種結構裡取值。** 硬寫一條只會在真實素材上默默配錯——
和我當天撤掉的那條種子資料簽名是同一類錯誤。

**真正要做的**是修 OCR 對「一頁多表」的切分，讓管道特性表和 BOM 各自成表。
那是 OCR 管線的工作，不是簽名的工作，規模也大得多。

### B3. 清掉已棄用的 DeepSeek 回退配置

**為什麼**：DeepSeek 棄用後 `AICHECK_LLM_FALLBACK_*` 是死重，而且**有害**——主供應商壞掉時錯誤會先變成一次 402，看起來像餘額問題而不是密鑰問題。今天就差點誤導我。

**做什麼**：從 `aicheck-runtime.env` 移除那組變數。**這要動 runtime.env，按規矩不歸我做，需要你或運維執行。**

### B4. 把可達性探針掛進定期巡檢

**為什麼**：密鑰失效整整一天沒人發現，是查別的事撞上的。探針寫好了但還沒排程。

**算做完**：cron 裡有一條，失敗會告警。

---

## C. 需要人工判斷才能開始的（我不該自己猜）

### C1. 三條掛錯的條款重新指認

`R43`、`R10`、`R51` 的規則包裡記著條款掛錯。例：R43 登記的 GB/T 19285-2026 4.2／5.1 經核對是「腐蝕防護系統檢驗前的管段劃分」與「建設期檢驗一般規定」，**不含材料質量證明文件的要求**；看起來想引的是 5.3.4 資料驗收，但那條已登記在 R44 名下。

**需要領域專家指認正確條款。** 我不會憑印象改——今天已經因為「拿種子資料當真實表頭」撤過一條東西。

### C2. 剩下 22 條規則要先寫凍結判據

R01～R09、R19、R24、R38 等 22 條**既沒有事實構建器，也沒有凍結判據**。寫判據要逐條核對標準原文，而且照這裡的慣例要標 `humanVerified: false` 加明確 limitations，等人簽字。

**需要你指定從哪幾條開始**，我一條一條做，每條附可核對的條文出處。

### C3. GB/T 8163、GB/T 12771、GB/T 5310 的化學成分

OCR 出來的表自相矛盾（GB/T 8163 表 6 把 Q390 C 級磷硫讀成 1.70，錳橫跨欄位滲漏），**故意沒有錄入**。需要乾淨的 PDF 或人工轉錄。

---

## 建議順序

1. **A1**（給密鑰）—— 唯一能證明整條鏈通的
2. **B1**（開工位重跑）—— 解鎖驗收導出，但要你點頭
3. **A2**（要施工記錄）—— 前置時間長，越早提越好
4. **B3 / B4**（清死配置、掛探針）—— 便宜，防止同類事故重演
5. **C1 / C2**—— 等你指定範圍
6. **C4**（修 OCR 一頁多表切分）—— 規模大，且會影響既有解析結果，要單獨評估

---

## 一句話總結

**程式這邊該建的都建了**：判據生產側三層、離線重放 47/69、導出器、探針，全量回歸 5252 通過 0 失敗。

**卡住的三件事都不在程式裡**：一把有效的密鑰、一套施工記錄、幾條要人工指認的條款。
