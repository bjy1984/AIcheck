# Jev v3 分支实施状态（2026-09-22）

分支：`codex/jev-integration-v3`。此文件记录代码已做和仍需验收的范围；研究数字仍以
[`2026-09-22-jev-plan-v3-final.md`](2026-09-22-jev-plan-v3-final.md) 为准。

## 已接入代码、未开放资料出境

- 第 2 期的新守卫：Jev state 只接收归属本工程的冻结文件版本；对同一事实有相反
  `passed` 的规则检查，会同时排除两边并留下冲突清单。GC2 样例有回归测试。
  单份文件超过 40,000 字会显式返回版本 ID，不截断原文。
- 第 3 期的表格分类：在 Temporal `review.llm` worker 上、事实构建前执行；仅对
  R24–R34 调用。表格类型、每行角色共用整份文件原文。`r24_r34_facts.py` 只接受
  固定 `jev-1.13.0`、与当前表格哈希匹配、把握值 ≥ 0.90 的分类；其余逐行回退
  原启发式。力学试验表不再作为 R25/R29/R32 的 WPS/PQR/热处理记录行；R26
  焊材证明的力学数据保留。
- 第 4 期的影子路径：Qwen 输出增加逐字摘自发现文本的 `claims`；Jev 用整节点原文
  核对。未取得监检校准时只保存 `jevClaimChecks`，不撤下发现。校准后如另行开启
  拒绝开关并配置阈值，被明确否定或原文没有的主张会从可见描述撤下，整条降级人工。
- 第 5 期的影子路径：每原子项保存 `jevSecondOpinions`，含结构化选择、
  把握值、与确定性层是否一致，以及仅供提示的支持页。原规则判定完全不变。
  原工作台已能显示第二意见并按分歧、低把握排序，但未校准时不会把该字段写入
  工作台的 `atomicCheckOutcomes`。
  进队列／出队列阈值必须在校准后显式配置，灰区沿用上次队列状态。
- 所有 Jev 请求固定 `jev-1.13.0`，只通过
  `AICHECK_JEV_ENABLED=true`、`AICHECK_JEV_DATA_EGRESS_APPROVED=true` 和全新
  `AICHECK_JEV_API_KEY` 三项同时具备，并单独启用对应阶段，才可发起。
  第 3/4/5 期分别用 `AICHECK_JEV_TABLE_CLASSIFICATION_ENABLED`、
  `AICHECK_JEV_CLAIM_SHADOW_ENABLED`、`AICHECK_JEV_SECOND_OPINION_ENABLED`。
  旧 `JEV_KEY` 不读取。
  inline 请求模式一律跳过新增步骤。测试用录制/伪造响应，不向外网发送工程资料。

## 发布前还缺什么

1. **正式资料出境合规决定与密钥轮换**。2026-09-23 用户允许 `test2/` 和七项目 OCR
   作为测试资料出境，已用测试资料评估；这不等于正式审查资料获批。提供给测试的密钥也
   出现在聊天中，不能作为生产密钥复用；全新密钥由运维放入
   `/home/dev-bjy/aicheck-secrets.env`。正式 Jev 开关仍保持关闭。
2. **33 题监检标注与阈值校准**。16 道分歧和 17 道 Jev 判通过的原始候选表未在本
   工作树中找到；`scratchpad/shadow/` 也不存在。只有研究报告，不能重造题目或宣称
   0.68／0.72 已经校准。标注后才能设置校准、拒绝和 UI 开关。
3. **上游两个生产缺陷**。设计证 GC1 对 GC2 的双工具矛盾已在本分支按同一
   冻结业务规则修正；Jev state 的矛盾排除守卫仍保留。节点 3 的已知「单位名称被抽成
   评价句」已加窄范围守卫。两处都尚未重放原生产 OCR，不能记为实机修复完成。
4. **实机验收**。需要原始七项目影子产物或获准后的受控回放，核对 R25/29/32 的
   71 条假事实、219 格方向对照、逐句抓错率、R19 八项语义题及大文件分拆。
   单份超过 40,000 字的文件目前会跳过 Jev；不能宣称两格超长已补跑。
5. **费用优化与正式队列**。当前 Qwen 仍对所有进入原流程的节点生成发现；“只给人工
   队列生成解释”的省费改造尚未实施。R19 仍按现有语义路径裁决，Jev 暂不替换它。
6. **清单模式**。`AICHECK_REVIEW_PROMPT_MODE=checklist` 的清单结论受独立的条件一致性
   契约保护；本分支的逐句核对只针对自由发现模式。清单模式会明确记录
   `unsupported_checklist_mode`，不会擅自改写固定条件判定。

## 开关与验收顺序

先验收不出境的规则/表格回退测试；获得出境结论并轮换密钥后，先只开三项基础开关
跑影子数据。33 题经监检标注后，确定并记录拒绝阈值、人工队列进出阈值，再考虑设置：

```text
AICHECK_JEV_CALIBRATION_APPROVED=true
AICHECK_JEV_CLAIM_GATE_ENABLED=true
AICHECK_JEV_CLAIM_REJECT_CONFIDENCE=<经标注确认的值>
AICHECK_JEV_SECOND_OPINION_UI_ENABLED=true
AICHECK_JEV_QUEUE_ENTER_CONFIDENCE=<经标注确认的值>
AICHECK_JEV_QUEUE_EXIT_CONFIDENCE=<经标注确认且高于进入阈值的值>
```

研究计划中的 0.68/0.72 只是待校准候选。支持页只有 Jev 的定位建议，不能替代
现有 `evidenceRefs`。所有代码变更目前只在该分支，未部署。

## 本地验证记录

- Jev 单元测试、条件清单一致性和 P8/H5 清单回归：57 项通过（2026-09-23）。
- 新增 Jev 代码及测试的 Ruff 检查通过；前端单元测试 106 个文件及 Vue 类型检查通过。
- 此前的重点后端回归共 80 项通过。排除既有 `test_openapi_contract.py` 后可收集
  5,428 项；完整后端测试仍未跑通：该文件在收集阶段导入当前实现没有的
  `OpenApiContractError` / `validate_operation`。排除它的一次广泛运行在约 20% 处
  因本分支新增的图顺序断言失败而中断（当时为 1,112 通过、6 跳过、1 失败）；断言
  已修正且单独复测通过，但**不能据此声称全套后端测试通过**。
- 2026-09-22 的单元测试全部使用伪造/录制响应。2026-09-23 在用户允许后，另用
  合成资料及 `test2/` 两份完整 OCR 做了真实 Jev API 评估，详见
  [`2026-09-23-jev-test-evaluation.md`](../verification/2026-09-23-jev-test-evaluation.md)。
  只在评估进程内启用出境和影子开关；没有部署或开启正式裁决／UI 门禁。

## 2026-09-23 开发收口进度

- 已加强正式发现的证据引用校验：引用须解析到本次审查冻结版本、允许页码范围及所属
  工程中的原文锚点；页码、框、引文不能由生成结果自行改写。多条引用有一条失败时不
  部分回填。证据不足的人工提示仍可保留，但无引用的“已证实”发现不得通过。
- 人工拒绝系统自动归属后，系统绑定随之移除；重算保留拒绝记录，避免重复挂回。
  人工确认／拒绝带修订号，旧页面的过期提交返回冲突。这里是应用层保护，数据库
  并发事务仍需另行验收。
- 修复 OpenAPI 契约脚本缺少的校验入口；部署静态契约检查改查更严格的证据错误码。
- 当前改动的相关后端测试 **169 通过**，前端单测 **106 个文件通过**，Vue 类型检查
  通过，巨石文件棘轮通过。完整后端套件未完成：一次正确工作目录的运行在代码继续
  调整时主动中止，当时 621 通过、6 跳过，不能算全套通过。
- Ruff 全仓基线仍未通过：318 条告警对 289 条基线，新增项来自本批未修改的旧文件；
  本批新增模块及相关测试单独 Ruff 检查通过。正式合并前需单独清理这些既存新增项。

## 文件归属的下一批开发（仍为影子模式）

- OCR 后的异步分类任务完成并落库后，可另外派发 Jev 文件归属任务。新任务只在
  `AICHECK_JEV_DOCUMENT_ROUTING_ENABLED=true`、Jev 总开关、资料出境许可、新密钥及
  Celery 派发同时满足时运行；inline／派发不可用时不会在上传请求里同步访问 Jev。
- 每份文件以本工程、当前版本的完整 OCR 为输入，按当前业务包启用的审查点汇总成
  节点选择题。输出每节点三态与把握值、输入指纹、现有归属及分歧；人工拒绝的节点
  不进入建议名单。结果保存在文件的 `jevRoutingShadow`，不改资料类型、绑定、证据链
  或审查结论。旧版本、跨工程、无 OCR、单文件超长、请求整体超长和模板超长均显式
  跳过或标为部分结果。当前默认审查点资产共有 168 项、覆盖 68 个节点；本地预检中
  问题模板收紧后，本地预检 `test2/` 共 20 份中 19 份可发起、1 份（`test2-006`）
  因原文超过 40,000 字被跳过；19 份合计需 66 次完整原文请求，其中 `test2-010`
  最多需 10 次。单文件超过 10 次请求会明确返回预算超限。这里仅计算请求尺寸，
  未实际发送这批文件归属题，也未测准确率与费用。
- 所有调用仍走同一个 `ask_jev`。兼容相同请求／响应契约的 API 可配置
  `AICHECK_JEV_API_URL`；切换域名须另设 `AICHECK_JEV_APPROVED_HOSTS`，且只接受 HTTPS。
  统一客户端按完整请求包（原文加题目）限制 40,000 字，并自动分批、校验每批回答；
  单题仍超限则显式失败。模型仍固定 `jev-1.13.0`，不同协议或模型须重新适配及校准。
- 文件归属的多批请求可能长于普通任务；只给这个任务的 PostgreSQL 锁设 180 秒空闲
  超时，普通任务仍为 60 秒，避免请求未结束时锁先失效，同时保留孤儿锁自动清理。
- 这一批用伪造 Jev 响应验证了整份 OCR、人工否决、输入变化重跑、异步派发、版本
  竞态及故障回退；统一客户端、任务锁和离线容量预检加入后相关后端回归 **106 项通过**。**尚未用七项目真值集衡量归属准确率**，不得把影子建议接入自动
  挂载。正式资料出境许可和密钥轮换的发布前置条件维持不变。

## 节点 3 的错误单位名称守卫

- 单位许可证 OCR 若把「被评定为 A 级机构。」之类明显的评价句填入单位名称，事实
  构建不再把它当主体身份；同份 OCR 有另一条正常单位名称字段时优先用正常字段。
  单位许可证也不再把正文的个人「姓名」回退成持证单位。
- 对被拒的字段记录 `holder_field_rejected`；按许可证号查到平台登记后，保留平台
  查询结果，但不据此冒充主体一致或报主体不一致，也不引入平台证据，判定为证据不足
  交人工核对。真正的正常名称不一致仍保留 `verified_mismatch`。
- 证书事实、平台核对、规则输出相关 **48 项测试通过**。这只覆盖已知错误形态，
  尚未拿节点 3 原生产 OCR 重放，不能推论所有单位名称抽取都已修好。

## GC2 许可覆盖的双工具一致性

- [市场监管总局 2021 年《特种设备生产单位许可目录》注一](https://www.samr.gov.cn/tzsbj/dtzb/gzdt/art/2021/art_ab36c6d55fde4d7daf8264578997607e.html)
  明确 GC1 级覆盖 GC2 级，适用于压力管道设计、安装许可。原 `AC-R01-02`
  的范围工具据此判通过，但 `AC-R01-03` 所用证照工具只按代号完全相等比对，
  把同一 GC1 证错误判为不覆盖 GC2。
- 设计许可证的两项工具现在共用同一 GC1→GC2 规则；证照工具版本升为
  `certificate-validity-cn-v2`，留痕增加每项要求可接受的许可级别。真实工具输出
  注入 Jev state 前不再构成该例的矛盾；原有「遇到矛盾两侧都不注入」守卫未撤除。
- 本批相关 **206 项测试通过**，涵盖 GC1 覆盖 GC2、真正缺级别、非设计证不借用该
  覆盖关系和 Jev state 一致性。旧的线上留痕仍记录历史误判，不自动改写或重跑。
- 该官方目录同时写明 **GCD 级也覆盖 GC2 级**。现已在本分支建立
  `engineering-inspection-r01-v20260923` 和业务包 `2026.09.23`：原始见证文字存入
  `sourceWitness`，新版执行方法与 AC-R01-02／04 明确加入 GCD。冻结绑定参数
  `design-license-scope-cn-v2` 传给范围及证照工具；旧版缺省 `v1` 保持原行为。
  这仍未部署、未重跑历史任务。原生产案例重放及旧项目实库兼容性仍待执行。详情见
  [`2026-09-23-r01-gcd-versioned-coverage.md`](../verification/2026-09-23-r01-gcd-versioned-coverage.md)。
- 新版对话 Agent 的直接范围工具也从任务或工程快照读冻结参数，拒绝模型覆盖版本；
  找不到旧版本快照时拒绝调用，不使用当前包猜测。最终后端全套 **5,409 通过、81
  跳过、0 失败**；条款资产与节点编号审计通过。全仓 Ruff 棘轮仍为 318 / 289，
  属本批未修改旧文件中的告警，另行清理。81 跳过不代表服务实测。

## 文件归属离线评估工具

- 新增仅在本地运行的文件归属对照工具，按工程、文件和版本匹配 `jevRoutingShadow` 与
  监检标注；只将完整影子结果和人工确认的题计入精确率、召回率及阈值扫描。
  未标注节点不当负例；不完整运行、系统预标、缺少影子结果和人工拒绝冲突单列。
  使用方式及标注格式见
  [`2026-09-23-jev-routing-evaluation.md`](../verification/2026-09-23-jev-routing-evaluation.md)。
- 此工具不调用外网、不开生产门禁，也不把既有节点挂载当作真值。当前没有可比的
  文件归属监检标注，故尚无准确率或可批准的发布阈值；原子项级 33 题仍单独待标。

## 2026-09-23 Ruff 棘轮收口

- 全仓新增告警已清掉，基线从 289 收紧到 **287 条**；这代表无新增告警，
  并不代表存量 287 条全部修复。受影响测试 243 通过、1 条环境跳过，基线
  保护测试 5 通过。详情见
  [`2026-09-23-jev-branch-ruff-ratchet.md`](../verification/2026-09-23-jev-branch-ruff-ratchet.md)。
- 上文 318／289 的记录是清理前的历史状态。完整后端 5,409 通过、81 跳过
  发生在本次 lint 整理前；整理后只跑针对性回归。正式资料出境、监检真值集、
  原生产 OCR 重放及七项目验收仍未完成。

## 2026-09-23 文件归属补题

- 先前本地预检的 68 节点是代码当时的实际覆盖，不是 69 节点目标已完成。
  第 69 节点没有资料审查点，但已安装模板要求质量体系评价工作流记录。
  影子路由现在从该工程的节点树与必需资料补上此节点，保留人工评价权；
  停用点、跨工程和不同业务包版本不补回。详情见
  [`2026-09-23-jev-routing-node69.md`](../verification/2026-09-23-jev-routing-node69.md)。
- `test2/` 离线预检更新为 **69 节点、19／20 份可发起、66 次预计请求**；
  这不是实际 Jev 调用或文件归属准确率。人工真值集与正式发布门槛仍未完成。

## 2026-09-23 原生产 OCR 局部回放

- 已从生产状态库**只读**提取一个工程的节点 1、节点 3 历史冻结输入，使用四份原始
  已选 OCR 和两条当时缓存的平台记录做离线回放；原文仅存 0600 临时文件，未入库、
  未提交，也没有发起新的平台或 Jev 请求。具体输入哈希与判定见
  [`2026-09-23-r01-r03-original-ocr-replay.md`](../verification/2026-09-23-r01-r03-original-ocr-replay.md)。
- 同一份原 OCR 让 AC-R01-02／03 在旧冻结绑定与当前包均判通过。AC-R01-04
  原因是已入库的两份设计文件 `pressure_pipe_level=GC2` 未进入
  `designDocument.pipelineGrades`，旧代码返回证据不足；补齐事实与原页引用后，
  两套绑定均判通过。若任一已选设计文件 OCR 未完成、级别缺失或字段混写多个级别，
  仍留证据不足。节点 3 的坏单位名称被拒用，平台核验状态为 `unable_to_verify`，
  不再报 `verified_mismatch`。
- 此处验证的是**当前代码对旧输入的反事实回放**；历史执行快照、当时结论及生产服务
  均未改动。先前“尚未拿原生产 OCR 重放”的记录指回放前状态，现只对这一个工程的
  两个节点完成。七项目全量对照、Jev 阈值及正式发布条件仍待验收。
- 本批最终测试收集 5,499 项；后端完整运行 **5,418 通过、81 跳过、0 失败**；
  改动文件 Ruff 通过，全仓基线 **287／287**。此前 5,409 通过的记录是这批修复前的
  历史结果。
