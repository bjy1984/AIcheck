# Lab 灰度計劃與上線前檢查

2026-09-24　｜　Lab 分支　｜　狀態：關卡 1 完成，**等確認後才進入關卡 2、3**

## 關卡 1：程式收尾（已完成）

- 已合併 PR #26–#41：Jev 插件化與審查意見修正、R35 以後的來源與施工記錄抽取、對象選取、記錄適用性口徑與「未抽取」守門、管道特性表（三種版式）、壓力管道匯總表、管線號公稱直徑比對、R44／R53／R55 記錄、Jev 核對施工記錄、登入頁預設密碼修正。
- 本分支再加：Jev 也核對 R44 塗層類型、R55 支架型式。

**回歸結果（最新 Lab 加上本分支）：**

| 項目 | 結果 |
|---|---|
| 後端全套 | 5,775 通過／0 失敗（81 條 PostgreSQL 整合測試因本機服務未起而跳過；CI 會實跑） |
| ruff 棘輪／巨石棘輪 | 286／286；routes.py、execution.py 都沒變長 |
| 前端 | 單測 111／111、vue-tsc 通過、ESLint 0 錯誤 |
| 快照規則重放 | 133 個工程×規則、361 個原子項、71 條判據：與本輪 Lab 改動**之前**（48dac663）相比**零變化**（節點、原子項、判據三層都比對） |

**更正：**先前幾個 PR 說的「重放結論零變化」只比到了節點層級：重放腳本讀錯了原子項的鍵名，原子項結果是空的。本次修正腳本後，重新在原子項與判據層級比對，結果仍是零變化。

**零變化的含義：**新行為（記錄表重建、適用性、未抽取）要監檢員先選定對象才會生效；快照重放沒有選對象，所以測不到。灰度的瀏覽器驗收必須補上這一段。

## 關卡 2：部署前檢查（待確認後做）

- 從**剛拉取的 Lab 乾淨副本**打包（部署腳本打包本機 HEAD；2026-08-29 曾因舊副本把生產退回舊版）。
- 環境變數只改 `backend/deploy/build_runtime_env.py`，不手改伺服器上的 `aicheck-runtime.env`。
  - `AICHECK_WORKSTATIONS_ENABLED=true`（已是）
  - `AICHECK_REVIEW_PAGE_RANGES_ENABLED`：維持關閉
  - `AICHECK_JEV_ENABLED`：維持關閉；任何工程都不開 Jev 插件
- 部署前備份資料庫，記下目前容器鏡像以便回滾。
- 選沒有批次任務在跑的時段（部署會重建 worker，佇列中的任務會丟失）。

## 關卡 3：灰度部署與驗收（待確認時段）

1. 從 Lab 乾淨副本執行 `AICHECK_DEPLOY_HOST=aicheck-prod-new bash backend/scripts/deploy_to_server.sh`（後端、前端）。
2. 冒煙：健康檢查、登入（**確認登入頁已不預填密碼**）、首頁、打開既有工程。
3. 試點工程 GDLNG、ECD202，在瀏覽器實際操作：
   - GDLNG R47、R68、R53、R55、R44：看到候選對象 → 選一個 → 重新審查 → 寫明的欄位通過、缺的判「未抽取」、**沒有誤判的不符合**、證據頁碼正確。
   - GDLNG R66／R67：選 PL8303-100 → 對到匯總表 PL8303。
   - ECD202：逐管線事實裡出現 A01～A08-PL-02（GC2、可燃、要求泄漏試驗）。
   - 管理後台：新增用戶時密碼欄空白；Jev 開關可見但部署層未開通。
4. 挑 3～5 個既有節點重新審查，結論應與部署前一致；有差異就停下來查。

## 關卡 4：觀察與回滾

- 試點觀察 1～3 天：API／worker 錯誤日誌、審查失敗率、佇列積壓。
- 立即回滾條件：無法解釋的結論變化、登入異常、審查持續失敗、worker 積壓。回滾方式：用備份鏡像重建容器。

## 正式上線前檢查（灰度期間示範帳號保留，方便測試）

- [ ] `AICHECK_ENABLE_DEMO_USERS=false`（生成器目前未設，程式預設為開；admin 已實測可用共用密碼登入）
- [ ] 為真人建立正式帳號；用預設初始密碼建立的帳號首次登入必須改密碼（#36 已實作）
- [ ] 部署 main 的登入修正 #37，或讓 Lab 合回 main
- [ ] main 的 CI 從 2026-09-08 起一直是紅的（#25 草稿），Lab 合回前要先處理
- [ ] 評估是否開 `AICHECK_STRICT_PRODUCTION=true`（會強制檢查示範帳號等設定）
- [ ] 輪換 Jev 金鑰（曾出現在對話中）；開 Jev 前需按工程批准資料外發
