# AICheck 工位制實驗分支

分支：`codex/lab-aicheck-workstations`，基於修復分支 `9bfb6f3`。本分支實作整合計畫，不以建立分支或 A 工位原型作為整體完成。

## 既定契約

- A 焊接 R24–R34；B 設計 R01/R04–R10/R63；C 施工品質 R02/R11/R69；D 材料 R12–R23；E NDT R03/R35–R42/R65；F 防護 R43–R47/R50；G 安裝 R48–R49/R51–R55；H 安全附件 R56–R58；I 系統試驗 R59–R62/R64/R66–R68。
- 每個節點只有一個主責工位；每次模型請求一個角色、一個節點。O 協調者無業務判定權。
- 自動核對＋可追溯人工見證；缺資料不假定不符合，文件存在不等於符合。
- 自由選取有權存取的已上傳文件，預設僅本次，可保存節點掛載；解除掛載不刪文件，固定文件版本與範圍。
- 自由編輯業務規則，預設工程專用草稿；描述、適用性、資料要求、原子項、限值、依據及協作要求皆可修改。試跑比較後發布，prompt 與工具共同使用同一份有效規則。
- 工程與平台規則隔離；自然語言生成草稿，未支援的條件不得冒充可執行判定。
- 新任務固定所有版本，歷史結論不改寫；交接包含工程、對象、節點、文件版本與證據，上游變更僅觸發相關下游待重驗。

## 工作包與完成證據

| 工作包 | 交付／驗收 | 狀態 |
|---|---|---|
| W1 工位基礎 | 九份完整角色 prompt、69 節點唯一映射、版本快照、通用與專用執行路由、工具執行端隔離 | 開發中 |
| W2 文件自由選取 | 搜尋預覽批選、頁碼、僅本次/持久掛載、OCR 狀態、解除掛載、固定輸入、權限及版本測試 | 開發中；已修正明確要求配對與工程節點驗證 |
| W3 規則編輯 | 工程/平台版本隔離、結構化可執行條件、草稿編輯/試跑/差異/發布/回滾、多人衝突、自然語言草稿 | 開發中；工程草稿／條件表單／輔助試跑已實作，正式工具整合與發布未完成 |
| W4 一致執行 | 原子項專屬參數、規則/文件/prompt 快照、資料範圍、完整原子結果與證據保留 | 開發中；規則與文件範圍快照、來源變動阻擋及完整結果已實作，重放與全路徑一致性未完成 |
| W5 工位交接 | 事實/判定/協作三類交接、對象匹配、返修輪次、版本失效、無循環重跑 | 待開發 |
| W6 工位介面 | 工位視圖、文件與規則入口、版本/缺件/衝突/見證/協作、一致性核對與節點結果 | 開發中；規則編輯與輸入變動提示已接入，完整工位工作台未完成 |
| W7 規則補齊 | R10/R11/R35–37/R39–40/R43–58/R63–64/R66–68 共 28 條，正式資料與工具鏈路 | 待開發 |
| W8 標準與原因追蹤 | 26 查詢、252 字串/模板逐項歸因；NB47014 C–F 圖示公式、適用性、測試 | 待開發 |
| W9 可信驗收 | 69 條至少 276 基礎項及邊界/衝突、原 36 差異、可執行發布門檻、服務整合及瀏覽器 | 驗證中；局部契約及元件瀏覽器已驗證，全量業務與服務驗收未完成 |
| W10 品質評估 | 固定現況/工位/完整三組，A 工位案例、双人金標、誤報/漏報/可判定率/證據支持/人工作業時間/成本 | 待開發；付費批次須預算 |
| W11 發布 A | 保留既有 ID 資產及生成/補種；69 條驗收後一次發布；新任務快照與回滾 | 待驗收 |
| W12 發布 B | 維護窗口停寫/在途處理、伺服器備份恢復、dry-run、交易遷移、reconcile、零孤兒及零變更重跑 | 待窗口 |
| W13 後續 | stage2 評估、P4/P8 焊口聯合表與 H6、Ruff 存量、外部平台無結果留痕 | 前置條件分別記錄 |

## Lab 啟用

新建審查使用 `AICHECK_WORKSTATIONS_ENABLED=true` 開啟工位模式；目前為開發開關，沒有修改生產部署預設。切換不會對既有審查補種工位快照。未部署生產、未發布 69 條工具、未執行正式 ID 遷移。

W1 的自動測試覆蓋真實 69 節點/194 綁定、唯一歸屬、快照防修改、錯節點拒絕、缺工具阻擋、prompt 單一角色及執行端越工位工具拒絕。這些不代替 W9 的業務驗收。

後续每批記錄具體測試結果與尚未完成項。工程行為、業務規則改動同步交接與主計畫。工位 prototype 完成後，繼續全部工作包；不縮小總目標。

## 2026-09-08 驗證紀錄

- 工位基礎、文件組織隔離與人工證據鏈回歸：151 passed；Ruff 289 / 289，無新增告警。
- 文件掛載不再按文件順序配對要求。明確指定的 requirementId 必須屬於目前工程與節點，整批驗證成功才寫入；未指定時作為補充文件，不自動指定必傳要求。修改掛載同樣驗證，要求名稱取自伺服器資料。
- API 回歸包含跨工程要求整批拒絕、補充掛載、明確掛載、錯節點修改拒絕及名稱不可偽造。既有文件組織權限流程保持驗證。
- 文件選取介面、本次輸入快照、頁碼及版本管理仍待完成；上述不代表 W2 已驗收。

### 規則範圍基礎

- 有效規則查詢已傳入工程 ID，工程覆寫優先於較新的平台版本；工程沒有已發布覆寫時回到對應業務包的平台版本。
- 發布只能取代相同工程與業務包內的規則，回滾按 ID 或版本名稱選擇時也限制相同範圍。
- 規則範圍與發布相關測試 11 passed。這批是後端選擇／取代邊界；工程規則的建立、列表、編輯權限與介面，以及可執行條件、任務有效規則快照仍未完成，不能視為 W3/W4 已驗收。

### 任務有效規則快照

- Lab 新建 ReviewRun 時凍結工程優先選出的完整規則、工程／節點／業務包範圍及雜湊，並納入 inputHash。缺少對應規則時阻擋建立。
- 規則引擎與 prompt 組裝優先使用此快照；後續修改／發布規則僅供新任務選擇。重取既有任務不補寫快照；無此契約的歷史任務維持原行為。
- 快照遭修改或被套用到其他工程／節點／業務包時拒絕使用。這是完整性檢查，不是數位簽章。
- 相關測試 22 passed，包含建立任務後改規則、既有任務冪等、新任務使用新版及不同 inputHash、prompt 忽略後續規則、快照完整性與範圍。Ruff 289 / 289。
- 本批尚未把自然語言條件編譯為工具的結構化條件；規則快照與靜態工具判定的一致性仍屬 W3/W4 待辦，不能宣稱自由修改限值已生效。

### 原子項專屬工具參數

- 執行器新增 arguments_by_atomic_check，流程 context 對應 argumentsByAtomicCheck，按原子項 ID 再按工具名稱指定輸入；舊 tool_arguments 保留供既有呼叫使用。
- 執行第一個工具前驗證整批原子項／工具歸屬與固定規則參數。未知原子項、未綁工具、重複原子 ID，以及明確參數覆寫 binding 固定參數時拒絕執行。
- 傳給工具的參數深複製，避免工具修改輸入影響後續原子項或原始事實。規則編輯器／事實組裝器產生這份新結構仍待接入，尚未宣稱可自由修改可執行限值。
- 工具執行器測試 23 passed；工位／規則範圍／快照測試 28 passed；Ruff 無新增告警。

### 完整發現與摘要分離

- Lab 工位模式取消生成結果的 8 條截併，清單模式的額外問題也不再截到 3 條；提示要求保留獨立問題與完整引用。
- findingDrafts 保存完整結果，經既有 schema／證據／品質門檻流程檢查，outputHash 計算完整結果。另以 findingSummaryDrafts 保存最多 8 條的顯示摘要，介面展開完整結果仍待接入。
- 摘要生成深複製資料，不再修改完整發現中的降級斷言或引用。歷史模式保留原顯示契約。
- 工位、輸出及清單相關測試 26 項通過；Ruff 無新增告警。這不代表已驗證分片去重或模型本身不漏判，後續仍須完整原子覆蓋驗收。

### 工程規則寫入邊界

- 建立／複製工程草稿及編輯、發布、回滾時檢查工程存在、歸檔狀態、成員節點範圍與業務包一致性；編輯不可移動工程或業務包，工程規則不可複製回平台或其他工程。
- 平台規則可複製成合法工程的草稿，原規則保持原範圍。既有 knowledge:manage 中介層仍控制這些全域路由；一般工程使用者的專用授權入口、列表與讀取隔離仍待接入。
- 相關規則範圍與 API 回歸 16 passed；Ruff 289 / 289。

### 規則讀取與預覽隔離

- 列表先排除無權存取的工程規則再分頁；單筆、差異、操作預覽與正式操作的預覽入口檢查工程成員與節點範圍。平台規則仍沿用全域讀取權限。
- 差異的預設比較版本限定同工程／業務包；發布指紋只包含同範圍受影響規則，其他工程更新不會使預覽失效。預覽工程影響數依工程／業務包過濾。
- 規則範圍、讀寫與預覽回歸 22 passed，Ruff 289 / 289。工程使用者專用授權路由及前端規則編輯入口仍待完成。

### 完整後端回歸啟動方式

與 backend-quality CI 相同，從 backend 工作目錄執行：

```sh
cd backend
.venv/bin/python -m pytest tests --collect-only -q
.venv/bin/python -m pytest tests -q --tb=short
```

倉庫根目錄與 backend 各有 scripts/openapi_contract.py；從倉庫根目錄直接執行後端 pytest 曾載入後端同名模組，導致 OpenApiContractError 匯入失敗。這是啟動路徑問題，不能把收集失敗視為測試通過。完整結果與服務整合環境分開記錄。

### 完整後端回歸結果

- 首輪：3061 passed / 2 failed / 66 skipped。R69 測試仍使用未指定要求的舊式掛載，已改為明確 requirementId；另因 routes.py 及 execution.py 超過巨石基線，抽出規則選擇、差異／預覽、工位初始化、輸出保存及工作佇列設定，沒有調高基線。
- 修正後全量：3063 passed / 66 skipped / 6 warnings，243.84 秒，退出碼 0。完整紀錄：[backend regression](verification/2026-09-08-backend-regression.txt)。
- 本地選擇性跳過不計入 PostgreSQL／MinIO／Temporal 實機驗收。本批無付費模型調用或生產操作。

### 工程草稿 API

- Lab 開關啟用時提供 `/projects/{projectId}/rules/versions` 的 GET／POST、`/{versionId}` 的 PATCH 及 `/{versionId}/fork` 的 POST（亦支援 `/api` 前綴）。
- 限具有效工程成員身份與節點授權的監檢人員；寫入使用 review:save 動作權限。工程 ID 與業務包由伺服器確定，不接受客戶端改身份／狀態／執行欄位。
- 修改強制 If-Match，複製及編輯同時驗證原節點與正規化後的節點，包含 sourceSequence 帶入的節點，避免繞過範圍。
- 列表包含授權節點的平台範本與本工程版本；無權讀取其他工程的版本。只新增草稿操作，正式發布入口、結構化條件編輯、前端介面及試跑仍待完成。
- 新 API 與巨石基線相關 7 項測試通過，Ruff 無新增告警。本批沒有重跑完整後端套件；上一批 3063 passed 不作為本批新增路由的全量驗收證明。

### 工程規則編輯介面

- AIReviewB 監檢工作台新增「工程規則」入口，前端需 `VITE_AICHECK_WORKSTATIONS_ENABLED=true`，後端需啟用同名工位能力開關。
- 可選擇當前節點的工程／平台版本、新建草稿、唯讀查看既有版本、複製成工程草稿及修改草稿文字。唯讀狀態、實際適用節點與「尚未發布」明確顯示。
- 未保存修改在切換版本／關閉時詢問；保存錯誤保留輸入，If-Match 防止覆蓋新版。切換工程或節點時關閉編輯器並忽略舊請求回應，避免把回應帶入另一工程。
- 前端 vue-tsc 全量型別檢查通過；修改檔 ESLint／Stylelint 通過。尚未完成登入帳號下的瀏覽器互動、截圖及端到端驗收，不能視為 W6 已完成。結構化條件、試跑與正式發布仍待接入。

### 結構化條件與草稿試跑

- 工程草稿新增 executionConditions，schemaVersion 為 rule-conditions-v1，checks 指定 id、field、operator、expected 與可選 unit。支援 eq/ne/gt/gte/lt/lte/in；拒絕未知運算、程式欄位、重复 ID 及非有限數值。
- `/projects/{projectId}/rules/versions/{versionId}/trial` 接受 facts：各欄位包含 value、unit、evidenceRefs。使用已儲存條件，不接受請求覆寫門檻；不修改草稿、審查或歷史結果。
- 缺資料／引用、單位及型別不符回傳 evidence_insufficient；不隱式換算單位。結果明確標記 draft_trial、advisoryOnly=true、evidenceVerified=false，試跑引用未作正式證據核驗。
- 條件及 API 試跑測試 14 passed；Ruff 無新增告警。條件表單、適用性／不適用判定、正式事實組裝、工具鏈整合及發布驗收仍待完成，不等同於 69 條正式規則已實作。

### 適用條件與四態試跑

- executionConditions 可增加 applicability，結構與單一 check 相同，先於 checks 執行。適用條件成立才進行判定；有引用且條件不成立時回傳 not_applicable；缺資料、引用或型別不符時回傳 evidence_insufficient。
- 未執行的檢查仍逐項保留 ID、原因及適用性依據，不因不適用而從結果中消失。整份條件先驗證，不能利用不適用分支藏入未支援運算。
- 條件／工程草稿／試跑 API 相關回歸 23 passed，Ruff 無新增告警。仍屬輔助試跑，引用未經正式核驗；複合適用條件、條件表單、正式資料與工具鏈整合仍待開發。

### 複合適用條件

- applicability 現可用 all／any／not 組合既有比較條件，採三值邏輯：not(未知) 仍未知；all 含明確否定可確定不適用；any 含明確肯定可確定適用。結果保留完整子條件及引用，不隱藏未知項。
- 驗證限制深度 8、葉條件最多 200，拒絕空組、混用群組鍵與重複條件 ID；不執行任意程式。
- 條件與草稿試跑回歸 38 passed，Ruff 無新增告警。仍是輔助試跑；條件編輯表單、正式事實／工具鏈及發布門檻尚未完成。

### 判定條件表單

- 工程草稿編輯器已加入結構化條件表單：可新增／移除檢查項，設定欄位、比較運算、文字／數字／是非值、候選列表與單位。
- 適用條件可用巢狀 all／any／not 表單編輯；條件 ID 由前端產生，轉成群組時保留原條件。平台及非草稿版本仍唯讀，複製後編輯。
- 保存包含 executionConditions，載入時深複製以免未保存編輯污染列表版本；既有文本編輯與 If-Match 契約保持。
- vue-tsc 全量型別檢查、修改檔 ESLint／Stylelint、條件模型測試均通過。尚未完成瀏覽器與 API 聯合驗收；試跑操作介面、正式事實／工具鏈及發布仍待完成。

### 草稿試跑介面與版本綁定

- 工程規則編輯器可對已儲存 executionConditions 的本工程版本試跑；表單由檢查／適用條件欄位產生，輸入示例值、單位與示例資料編號。空值可測缺資料，空引用可測缺證據。
- 未保存修改時停用試跑；結果列出規則修訂與各檢查狀態／原因。輸入或規則變更會清除旧結果；切換版本／工程時忽略舊請求回應。
- 試跑 API 現強制 If-Match，過期版本在 evaluator 執行前拒絕。示例資料未經核驗，不保存為工程證據或正式結論。
- API 版本綁定測試 2 passed；前端 vue-tsc、修改檔 ESLint／Stylelint、Ruff 通過。瀏覽器互動驗收及正式資料／工具鏈整合仍未完成。

### 本地瀏覽器與 API 聯合驗收

- 已用獨立 Chrome 設定檔、真實 Vue 元件及 FastAPI 路由完成本地互動驗證，測試可重複執行。認證傳輸使用記憶體 seed 的監檢帳號；沒有使用生產資料或現有瀏覽器登入。
- 通過唯讀平台規則、新建／保存數值條件、不符合／符合／缺引用試跑、清除過期結果、未保存停用試跑、取消關閉保留輸入，以及無瀏覽器 runtime error 檢查。
- 試跑畫面：[截圖](verification/browser/rule-trial-pass.png)。重跑方式：frontend/e2e/lab-rules/README.md。
- 這是元件與真實 API 的聯合測試；完整工作台的正式登入帳號、行動版、複合條件互動及外部服務實機驗收仍未完成。

### 工具資料範圍

- 修正 OCR 解析選取在任務文件清單為空時可能回退全庫的行為。帶 reviewRun 的讀取僅限任務 inputDocumentVersionIds，即使 context 指定更大範圍也不擴張。
- 工位 runtime dispatcher 在讀取或業務工具执行前，拒絕頂層工程／節點與任務不符、任意層級文件版本不屬任務輸入，以及文件版本清單型別錯誤。拒絕結果不帶出範圍外文件資料。
- 工位／runtime 測試 36 passed，工具執行／原始工具留痕回歸 25 passed，Ruff 無新增告警。
- 這是既有 runtime 工具鏈的資料邊界；專用 planner 的本地工具、正式標準資料來源、文件選取完整快照與工位交接仍需逐路驗收。

### 任務文件範圍快照（Lab）

- 新工位任務保存 documentScopeSnapshot，固定工程、節點、業務包與文件版本 ID 清單，雜湊納入 inputHash。任務清單從上游 AI 輸入深複製，避免後續修改共享清單。
- 工位快照讀取與 OCR 選取會驗證文件範圍；清單增刪、身份改動或快照內容與雜湊不符時拒絕繼續。既有無快照任務維持歷史契約。
- 分片保留原任務，在 context 中縮小文件選取；測試確認不會擴張至清單外文件。此雜湊是完整性校驗，並非防管理者重算的數位簽章。
- 文件範圍、工位、規則快照及分片執行回歸 41 passed；Ruff 289 / 289。本批未重跑完整後端。
- 尚待完成檔案內容／OCR 與人工修正版本固定、專用 planner 全路徑驗收及文件選取介面，不代表 W2/W4 已完成。

### OCR／人工修正變動檢查（Lab）

- 新工位任務在文件範圍快照加入 sourceFingerprint，涵蓋已選文件的 OCR 記錄及本工程／節點有效人工修正，並經快照雜湊納入 inputHash。
- 通用 OCR 選取前校驗來源。OCR／修正變動、停用修正或新增解析結果時，以 review_document_sources_changed_recreate_run 阻擋舊任務讀取；重取既有任務不更新指紋，新任務使用當前資料。
- 未選文件及其他工程的修正不影響指紋。當前按完整記錄及既有順序計算，相關記錄僅元資料變動也可能要求重建任務，屬保守處理。
- 單元／工位／快照／分片／巨石基線 51 passed，任務建立契約 2 passed；Ruff 289 / 289，diff check 通過。未重跑完整後端。
- 此批提供變動偵測，尚非歷史輸入重放：完整 OCR／修正版本儲存、物件內容核驗、專用 planner 路徑及對使用者的重建提示仍待完成。無生產部署。

### 輸入變動的正式流程失敗處理（Lab）

- 來源檢查接入正式執行：專用前置審查之前、每個 graph step 之前及 graph 完成後檢查；來源或文件快照不一致轉為 REVIEW_INPUT_CHANGED_RECREATE_RUN，非自動重試錯誤。
- R12／材料等前置審查納入既有統一失敗處理，使失敗狀態、AI 任務錯誤碼、事件及原節點狀態恢復沿用同一流程。已完成／待人工任務仍保留既有返回契約。
- R12／R14／R24 的變動輸入契約測試確認前置審查未呼叫、無新發現、任務失敗與節點狀態恢復；合計相關契約 5 passed。工位／來源／分片／重試／巨石 52 passed；另前置人工核驗及 inline 持久化與重試批次 15 passed（與前批有重疊）。Ruff 289 / 289，diff check 通過。
- 巨石檢查首輪超出 1 行，已抽出 failure_policy.py 並保留 execution.py 相容再匯出，未提高基線。
- 尚未完成使用者介面的專用提示、可重放的完整版本存儲與全後端回歸；中途已保存的草稿保持失敗任務留痕，不作正式通過結論。本批無生產部署。

### 工作台輸入變動提示

- AIReviewB 取得失敗任務的 REVIEW_INPUT_CHANGED_RECREATE_RUN 時，在目前上下文顯示不可關閉的原因提示與「按當前資料重新發起」按鈕文案；目前任務狀態不再被聊天完成覆蓋。
- 沿用既有 requestAiRecheckApi 與每次新 idempotency key，建立使用目前資料的新任務；不使用保留舊快照的 replay。按鈕仍遵守工程／節點 canStartReview 權限及原有正式／缺項預審選擇。
- 成功、執行中任務即使帶舊錯誤碼也不顯示重新發起提示；普通失敗維持原有流程。聊天執行動態仍獨立呈現。
- 新狀態判斷、既有停滯及任務綁定測試通過，全前端 vue-tsc、修改檔 ESLint、diff check 通過。Stylelint 12 errors / 3 warnings，與 HEAD 修改前 stdin 檢查完全相同，皆為未修改的 CSS；未宣稱 Stylelint 通過。
- 本批未做登入工作台的瀏覽器驗收；整體工位介面、自由選文件及完整輸入重放仍待完成。

### 最新完整回歸與契約修正

- 本次以 4cac73d1 為起點，收集 3196 項。首輪 3127 passed / 3 failed / 66 skipped：新增工程規則 API 未更新 OpenAPI 工件，以及部署報告無法追蹤函式內匯入的幂等委派、未列明只讀草稿試跑。
- 工程規則路由改用模組匯入，保留實際既有幂等委派；只讀 trial 列入精確路徑豁免，建立／編輯／複製仍須通過幂等檢查。更新生成 OpenAPI，確認無既有路徑／schema 名稱被刪除。部署與契約 40 passed。
- 第二輪在 fault-proxy 單元測試的真實網路轉發等待，人工中止（1703 passed / 20 skipped，非完整通過）。該測試改用 MockTransport 驗證一次 429 後一次成功轉發及上游呼叫次數，避免使用真實 socket／環境代理。相關 15 passed，不替代實機故障注入。
- 最終完整後端：3130 passed / 66 skipped / 6 warnings，168.40 秒，退出碼 0。原先三項失敗逐項消除，未按失敗總數放行。Ruff 289 / 289。
- 前端單元測試 84 個檔案全部通過。清理工作台既有 CSS 次序／註解告警，Stylelint 與 ESLint 通過。減少動畫及響應式覆寫保留必要 cascade 順序、精確註明 order/order 例外；Chrome 1200px／1600px 及兩種減少動畫設定與修改前結果一致。重跑腳本 frontend/e2e/lab-rules/check-workbench-css.mjs。
- 完整原始紀錄在 docs/lab/verification/regression-4cac73d1/。選擇性跳過仍不算 PostgreSQL／MinIO／Temporal 的實機驗收；沒有付費模型評估、正式 69 條發布、ID 遷移或生產部署。

### 以任務 OCR 資料試跑工程規則

- 工程規則 trial API 可傳 reviewRunId，與手填 facts 互斥。伺服器檢查工程、節點及業務包；只接受有來源指紋的文件快照，沿用範圍與來源變動驗證讀取 OCR／人工修正。
- 按條件欄位名稱精確匹配，保留零值、單位、文件版本、欄位 ID、頁碼、bbox 與 correctionId；不隱式把字串轉數字或轉換單位。缺欄位／定位或多筆同名欄位均輸出 factDiagnostics 並維持證據不足，多筆相同值也不假設同一焊口或材料批次。
- 回應帶 sourceMode=run_ocr、sourceReviewRunId、sourceSnapshotHash，仍是 advisoryOnly=true / evidenceVerified=false：来源定位不是對真實性或法規適用性的人工背書，不保存正式結論，不發布規則。
- 新取證、條件、文件快照、部署及 OpenAPI 回歸 98 passed；試跑 API 3 passed；Ruff 289 / 289，diff check 通過。本批未重跑完整後端，上一批 3130 passed 為修改前紀錄。
- 前端任務來源選擇、對象／欄位映射、正式工具整合仍待完成。此入口可供後續真實案例離線比對，不代表 W3/W4/W10 已完成。

### 任務 OCR 試跑介面

- 工作台將目前 reviewRunId 傳入工程規則編輯器；試跑可選手填示例或目前任務 OCR，沒有任務時停用該選項。請求型別互斥，OCR 模式不送出手填 facts。
- 切換來源／任務清除舊結果，來源任務與欄位診斷以文字顯示；未保存或執行中不重複試跑。缺快照、來源變動提供重新發起審查提示。
- 本地 Chrome＋真實 API 通過原有手填案例、鍵盤開啟來源選單、OCR 成功判定、來源快照回應、來源切換清除結果、缺欄位診斷及未保存保護，無瀏覽器 runtime error。截圖 docs/lab/verification/browser/rule-trial-run-ocr.png。
- 測試初版因夾具缺 tenantId，在請求正規化後觸發來源變動阻擋；已在建立測試快照前補齊夾具，未放寬生產來源校驗。Element Plus 選單內 input 被 placeholder 覆蓋，驗收改用正常鍵盤 Enter 開啟，未強制點擊。
- vue-tsc、修改檔 ESLint／Stylelint、diff check 通過。獨立測試 API／Vite 已停止。此為元件與 API 驗證，完整登入工作台及任意歷史任務選取尚未驗收；正式條件工具整合仍待完成。
