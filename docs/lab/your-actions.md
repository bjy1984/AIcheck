# 需要你做的事（2026-09-10）

只列**你**的動作。我這邊能做的都做完了，每項後面寫清楚做完之後我接手什麼。

---

## 1. 把生產切到 Token Plan　【最急，我這邊已全部驗證】

**結論更正**：你重新生成的 `sk-sp-…` key **是好的**。之前 401 是因為我把它打到按量計費端點
（後來又錯打 Coding Plan 端點）。Token Plan 有自己的域名，三種產品的 key 與地址完全隔離：

```
https://token-plan.cn-beijing.maas.aliyuncs.com/compatible-mode/v1
```

已在伺服器上用臨時容器實測（key 只經環境變數、用完即刪，未寫入任何檔案）：

| 項目 | 結果 |
|---|---|
| `qwen3.7-plus` / `qwen3.8-max` / `qwen3.6-flash` chat | **OK**，模型名與生產現配完全一致，不用改 |
| 視覺（64×64 圖，`qwen3.7-plus` / `qwen3.8-max`） | **OK**，正確認出顏色 |
| `qwen-vl-max` | 404，Token Plan 沒有此模型 → 視覺角色改用 `qwen3.7-plus` |
| `text-embedding-v4` | **404，Token Plan 沒有 embeddings** |
| 舊 `sk-ws-` key（按量計費） | 仍 401，帳號層問題未解 |

**你要做的只有一行**（`/home/dev-bjy/aicheck-secrets.env`，600 權限的憑證檔）：

```
AICHECK_TOKEN_PLAN_API_KEY=<你的 sk-sp- key>
```

**不要手改 `runtime.env`**——它是 `deploy_to_server.sh --backend` 每次用 `build_runtime_env.py`
從憑證檔重新生成的，手改的行下次部署就沒了（這條坑倉庫裡記著 2026-08-14 踩過）。生成器
已改成：憑證裡有這把 key，文本與視覺角色就整體切到 Token Plan 端點、視覺模型改
`qwen3.7-plus`、文本模型名不動、embeddings 留在按量端點。你加完那一行告訴我，
**我來跑 `deploy_to_server.sh --backend`**（按規矩從倉庫根目錄跑）並用探針確認。

**確認**（伺服器上，不花錢；退出碼 0 即成）：
```
docker run --rm --network aicheck-net --env-file /home/dev-bjy/aicheck-runtime.env \
  -v /tmp/aicheck-tests-export/backend:/app:ro -w /app -e PYTHONPATH=/app \
  aicheck-api:local python3 scripts/model_reachability_probe.py
```

**仍然斷的一條：向量化。** `AICHECK_EMBEDDING_API_KEY` 用的是死掉的 `sk-ws-`，Token Plan 又沒有
embeddings。知識庫切片向量化要恢復，仍需一把**按量計費**的普通 `sk-` key，寫進
`AICHECK_EMBEDDING_API_KEY`（`AICHECK_EMBEDDING_API_BASE` 保持 dashscope 不變）。這和帳號層
為何失效是同一個問題，要在百煉控制台查。

**我接手**：探針一綠，立刻跑 R43 端到端。這條鏈到「agent 填值」之前的每一段，今天已在
**生產資料上只讀乾跑證明通了**（途中修掉三個真缺口：無對象選取、20 個工具沒接進執行器、
表格引用沒有原文——見 `verification/2026-09-10-r43-dry-run-three-gaps.md`）。現在
`documentNo`／`materialGrade` 已通過，剩四個判斷布林等 agent 填，那一步只差你的 env。

## 2. 向工程方索取施工記錄類資料

**現況**：生產庫 121 份文件，全是設計文件、許可證、資格證、產品質量證明書、
無損檢測報告、焊接工藝評定、施工方案。**0 份**施工記錄。

**要的東西**（有哪類要哪類，不必一次齊）：

- 防腐施工記錄 / 電火花檢漏記錄
- 陰極保護測試記錄
- 支吊架安裝記錄、補償器安裝記錄
- 管道元件驗收表、隱蔽工程驗收記錄
- 吹掃清洗記錄、壓力試驗記錄

**為什麼非要不可**：R44、R46、R47、R49、R50、R52～R55、R68 等約 25 條規則，
判據要讀的是「陽極回填有沒有空隙」「通電前電纜連了沒」「四點測厚做了沒」這類
**現場觀察**。這些不寫在圖紙上，只寫在施工記錄上。沒有這些紙，補再多程式也沒用。

**前置時間長，建議今天就提。**

---

## 3. 決定：要不要開工位模式重跑真實審查

**現況**：生產庫**沒有任何一次審查運行**帶 `documentScopeSnapshot`（凍結的文件
範圍）。工位模式從沒在生產開過，而且開了**不會**給既有運行補快照。

**影響**：沒有凍結範圍，`export_review_acceptance_fixture.py` 導不出任何東西——
276 情境驗收（第 8 項）一步都動不了。

**要你決定的**：同不同意在生產開 `AICHECK_WORKSTATIONS_ENABLED` 並對兩個真實工程
重跑審查。**這會產生新的審查運行，是生產寫入**，所以我不自己動。

回一句「可以」我就做。

---

## 4. 清掉已棄用的 DeepSeek 回退配置

**為什麼**：DeepSeek 棄用之後 `AICHECK_LLM_FALLBACK_*` 是死重，而且**有害**——
主供應商壞掉時，錯誤會先變成一次 402「餘額不足」冒出來，看起來像充錢就好，
實際上主供應商的密鑰才是壞的。今天就差點把我帶偏。

**你要做的**：從 `/home/dev-bjy/aicheck-runtime.env` 移除
`AICHECK_LLM_FALLBACK_API_BASE`、`AICHECK_LLM_FALLBACK_API_KEY`
及 `AICHECK_LLM_FALLBACK_MODEL_*`，然後重啟同上五個容器。

（按規矩我不手改這個檔，所以這項只能你或運維做。）

---

## 5. 找領域專家指認三條掛錯的條款

規則包裡自己記著這三條掛錯，需要**人**重新指認正確條款，我不會憑印象改：

| 規則 | 問題 |
|---|---|
| **R43** | 登記的 GB/T 19285-2026 4.2／5.1，經核對正文分別是「腐蝕防護系統檢驗前的管段劃分」與「建設期檢驗一般規定」，**不含材料質量證明文件的要求**。看起來想引的是 5.3.4 資料驗收，但那條已登記在 R44 名下 |
| **R10** | 條款對映有誤，待指認 |
| **R51** | `AC-R51-01` 掛的條款與原子項不對應 |

---

## 6. 指定從哪幾條規則開始補凍結判據

**現況**：R01～R09、R19、R24、R38 等 **22 條規則既沒有事實構建器，也沒有凍結判據**。

寫判據要逐條核對標準原文，照這裡的慣例要標 `humanVerified: false` 加明確
limitations，等人簽字。**我一條一條做，每條附可核對的條文出處。**

**你要做的**：告訴我先做哪幾條（建議挑語料裡有對應文件的，否則寫了也跑不了）。

---

## 7. 三份國標的化學成分，要乾淨 PDF 或人工轉錄

GB/T 8163、GB/T 12771、GB/T 5310 的化學成分**故意沒有錄入**——OCR 出來的表
自相矛盾（GB/T 8163 表 6 把 Q390 C 級的磷、硫讀成 1.70，錳橫跨欄位滲漏）。

錄錯的限值比沒有限值危險得多，所以我停在這裡。需要乾淨的 PDF 或人工轉錄。

---

## 8. 確認一個殘留容器

伺服器上 `aicheck-tests-rehearsal-pg` 已經開了約 10 小時，不是我這次會話起的。
測試容器留在生產主機上，建議確認要不要拆。

---

## 優先級

**只做一件的話：第 1 項（換密鑰）。** 它卡住的東西最多，而且做完當天就能看到
一條規則對真實文件跑出真實結論。

**第 2 項前置時間最長，建議同時就提。**

第 3 項回一句話我就能動。第 4、8 項是幾分鐘的事。第 5～7 項要找人，可以慢慢排。
