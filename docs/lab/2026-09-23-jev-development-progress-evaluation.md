# Jev Lab：技術方向調整、方案評分與開發計劃

更新日期：2026-09-23　｜　分支：`codex/jev-integration-v3`　｜　基準提交：`ca3a212e`（本次改動未提交）

> **進度（2026-09-23 收工時）：目前 83／100，目標 91。**已按決定移除「出境合規與後備」並重新換算：原分支 33 分，調整當時 68 分，M0 基線 77 分。M1 已全部完成；M2 完成證書與設計試驗要求兩類；M4 完成資料庫整合測試。
> - **用 Jev：**核對規則所用的事實，目前已接入證書類；R19 的 8 項語意題給第二意見；文件歸屬候選；表格分類；草稿主張的影子核對。
> - **不用 Jev：**任何結論、186 項確定性規則的第二意見、平台查證、計算與門檻比較、判「不適用」、選條款、自動掛載、超長文件、相近人名辨認、每次運行臨時出題。
> - **結論只來自規則、平台核驗和計算。**Jev 只標記「抽取可疑」和「分歧」，交監檢員先看。
> - **下一步：**M3（真實 OCR 上的已知答案實測）決定能否過 90 分，需要你確認送出資料並輸入 Jev 金鑰；M2 其餘事實類別可在本機繼續。

**方向已調整：Jev 只作加強，不作裁決。**結論只來自規則、平台核驗和計算；Jev 只回傳一個選項和把握值、沒有理由，所以它的答案只掛在規則結果旁邊，與規則不一致時提示監檢員先核對原文。查證書、查平台、算日期和數值的檢查項不再送給 Jev。

**本輪實測後的補充：**Jev 在「原文是否寫明某事」這類題上幾乎不錯：核對抽取值 32／32，從合訂本中找出指定證書 8／8。但它在算術和門檻比較上會錯，例如 2.3MPa 對 2.4MPa 的門檻答了「通過」。因此 Jev 最有價值的位置是**核對規則所用的事實有沒有抽對**，而不是判斷檢查項本身。唯一證實的真錯 R02-02 正是一個抽取錯誤：它屬於一整類有效期寫法，9 種寫法都會抽錯，現已修正，並用真實資料驗證只改對了該改的那一份。詳見[能力邊界實測](verification/2026-09-23-jev-boundary-suite.md)。

## 1. 為甚麼要調整

原分支的做法是 Qwen 根據節點模板和 OCR 出題，Jev 選答案，然後**用 Jev 的答案覆寫原子項結果**。評估時發現三個問題：

- **Jev 答了它答不了的題。**194 個原子項中，除 67 個證據定位項外，其餘 127 項都會被 Jev 覆寫，包括 13 項證書／許可／平台核驗和 15 項日期、數值計算。Jev 只讀 OCR，查不了公示平台；算術也應由程式算。
- **低把握也會生效，而且會流進下游。**R09-01 上 Jev 以 0.28 選「通過」，照樣成為該項結果；結果又被寫回 `context["ruleResults"]`，審查草稿、依據校驗和逐項輸出都用它。
- **出錯時會蓋掉規則結論。**不在白名單的工程、出題失敗或 Jev 不可用時，所有原子項會被改成「需人工」，連規則已明確判出的「不符合」也被蓋掉。

Jev 沒有通過或拒絕理由，在監檢語境裡「不符合，0.87」只是一個提示，不是結論。9 月 22 日的[研究](research/2026-09-22-jev-vs-current-verdict-layer.md)已得出「能做，但不該做成裁決層」，原分支後來偏離了這個判斷。

## 2. Jev 用在哪裡、不能用在哪裡

判斷標準只有一條：**Jev 回答「原文是否寫明某事」很可靠，但它不給理由，也不會算。**所以它只用來核對事實、提示分歧，不用來下結論。這條規則已寫成 [jev_usage_policy.py](../../backend/libs/review_orchestrator/jev_usage_policy.py)，並由程式強制執行。

**用 Jev 的地方（都不改結論）**

| 位置 | Jev 做甚麼 | 依據 | 程式 |
|---|---|---|---|
| 證書事實核對（節點 1、2、3、24、38） | 核對規則所用的起止日、證號、持證人是否為原文所寫；答「否」且把握值 ≥0.70 時標記「抽取可疑」，交人工先看 | 核對抽取值 32／32；合訂本中找對指定證書 8／8；R02-02 正是這類錯誤 | [jev_fact_check.py](../../backend/libs/review_orchestrator/jev_fact_check.py)（本輪新增；題目由抽取值按模板生成，不經 Qwen；只送該證書所在的那一份文件） |
| R19 境外材料 8 項語意題 | 第二意見：先判是否適用，再判是否滿足，與 Qwen 的語意判斷對照 | 業務包 194 項中只有這 8 項標為「語意判斷」；題目寫明條件時，適用性題 3／3 答對 | `jev_opinion` 的 R19 影子路徑（凍結題目）；`jev_primary` 現在只放行這一類 |
| 文件歸屬候選 | 提示文件可能屬於哪個節點，由使用者自己保存 | 194 份文件中 178 份取得完整答覆；精確率仍未知 | `jev_document_routing` |
| OCR 表格分類 | 判斷表格類型和每一行的角色；失敗就回退固定解析 | 9 月 22 日實測把握值 0.97–1.00 | `jev_tables` |
| 審查草稿主張核對 | 核對草稿中的主張與證據是否相符，只作影子記錄 | 9 月 22 日研究 | `jev_claims` |
| **下一批：其他抽取事實** | 設計／試驗壓力、管道等級、材料牌號、簽字角色、「合格／無泄漏」等結論字，套用同一種模板核對 | 正則盤點中有 17 處語意正則影響裁決，都是事實抽取 | 待做：每一類都要做已知答案矩陣和真實快照回歸 |

**不用 Jev 的地方**

| 用途 | 為甚麼不用 | 由誰來做 |
|---|---|---|
| 任何原子項或節點的結論 | 沒有理由、引文和頁碼，監檢結論無法複核 | 規則、平台核驗、計算 |
| 91 項「確定性規則」原子項的第二意見 | 這些項目按凍結判據由工具判定，讓 Jev 再判等於重算門槛；實測中 2.3 對 2.4 的門檻被答成「通過」 | 改為核對它們所用的**事實** |
| 證書真偽、公示平台登記 | Jev 只讀 OCR，查不了平台 | 公示平台核驗 |
| 日期覆蓋、壓力倍數、單位換算、數量比例 | 數值題 4／6；到期當天是否有效答不出 | `check_date_covers` 等確定性工具 |
| 判「不適用」而刪掉一條要求 | 這屬於放寬結論 | 由已抽取的等級、壓力、溫度確定性判斷 |
| 從知識庫選條款 | 條款已由固定條款包綁定並經原文核對 | 固定條款包 |
| 自動掛載文件 | 歸屬只是候選 | 使用者保存 |
| 超過約 3 萬字的密集中文 | 約 1 token／字，3.5 萬字被拒 | 按文件和身份邊界拆分，不能截斷冒充全文 |
| 相近人名的身份辨認 | 「李衛」與「李衛伍」同表時以 0.41 答錯 | 完整姓名或證號匹配 |
| 每次運行臨時讓 Qwen 出題給 Jev | 同輸入重跑題目全變，還會漏寫要求（R09-01） | 事實核對用模板題；R19 用凍結題 |

## 3. 本次已改的程式碼

| 位置 | 改動 |
|---|---|
| [jev_primary.py](../../backend/libs/review_orchestrator/jev_primary.py) | `apply_decision` 改為 `attach_hints`：不改任何 `result`，只在原子項上附 `jevHint`（選項、把握值、是否與規則一致、逐人結果），記錄層附分歧清單。新增 `_RULE_OWNED_TOOLS`：綁有證書、許可、平台核驗或日期／數值／覆蓋計算工具的原子項不出題、不送 Jev。Jev 的節點整體看法改名 `opinionResult`，只供配對評測。出題失敗除例外類名外，另記錄固定錯誤碼 `reasonCode`（如 `qwen_question_count_mismatch`），供定位節點 1、6 失敗原因。 |
| [execution.py](../../backend/libs/review_orchestrator/execution.py) | `jev_decision` 步驟只附提示；節點建議的 `decisionSource` 一律為 `rule_engine`，另附 `jevHint`（狀態、分歧數、Jev 整體看法）。 |
| [output_contract.py](../../backend/libs/review_orchestrator/output_contract.py) | 逐項輸出的結果和理由一律取規則；有 Jev 答案時附 `jevHint`。已校準的 `secondOpinion` 通道不變。 |
| [run_jev_primary_lab_case.py](../../backend/scripts/run_jev_primary_lab_case.py)、[compare_jev_primary_live.py](../../backend/scripts/compare_jev_primary_live.py) | 報告版本升為 v2；配對比較改比 `jevOpinionResult`，因為有效結果已恆等於規則結果。 |
| 前端工作台 | 解析 `jevHint`；分歧項排在前面並顯示「Jev 提示不一致 · 請先核對原文」及「Jev 看法：…（無理由，僅作提示）」；節點摘要顯示「Jev 提示 N 項與規則不一致」。調整前的歷史運行仍按原樣顯示。 |
| [service.py](../../backend/apps/ocr_service/service.py)、[certificate_facts.py](../../backend/libs/review_orchestrator/certificate_facts.py) | 修正有效期區間被抽成起始日的問題：OCR 服務只看第一個有效期陳述，緊接標籤寫成區間時取截止日；規則側只在欄位值恰好等於原文某個區間的起始日時改回，因為已存的舊結果本身就是錯的。人工修正值不動，被替換的原值留痕。 |
| [jev_client.py](../../backend/libs/review_orchestrator/jev_client.py) | 429／529 退避重試兩次；400 `max_tokens_exceeded` 歸為「請求超長」，不重試，交人工。 |
| [evaluate_jev_boundaries.py](../../backend/scripts/evaluate_jev_boundaries.py)、[evaluate_jev_extraction_check.py](../../backend/scripts/evaluate_jev_extraction_check.py) | 以虛構文字、答案已知的能力邊界套件：15 個維度、93 道題，全套可重跑。 |
| [jev_usage_policy.py](../../backend/libs/review_orchestrator/jev_usage_policy.py) | 把第 2 節的「用／不用」寫成程式：允許的 5 種角色、8 種禁止用途、把握值低於 0.70 只作低優先。`jev_primary` 據此只讓業務包標為語意判斷的原子項取得 Jev 意見。 |
| [jev_fact_check.py](../../backend/libs/review_orchestrator/jev_fact_check.py) | 證書事實核對：由規則所用的起止日、證號、持證人按固定模板出題，只送證書所在的那一份文件；答「否」且把握值 ≥0.70 時列為可疑。結果掛在該證書檢查項上，節點摘要顯示可疑數，不改結論。新增 `FACT_CHECK` 開關。 |
| 前端工作台（本輪） | 證書檢查項顯示「Jev 核對：抽取值可疑 · 請先核對原文」和具體可疑的欄位與值，排在最前面；節點摘要顯示「Jev 核對：N 個抽取值可疑」。 |

**194 個原子項現在怎樣分配：**
- 67 項證據定位：由本地確定性檢查負責，不送 Jev。
- 119 項確定性規則，包括 28 項證書、平台和計算類，以及 91 項用凍結判據的專用評估工具：結論由工具給出，**不取得 Jev 意見**。其中證書類的事實改由 Jev 核對，其餘各類的事實核對排在下一批。
- 8 項 R19 語意判斷：可取得 Jev 第二意見。

上一版說「99 項仍可取得 Jev 提示」，是按工具名推算的。本輪查過業務包的定義，其中 91 項本來就標為「確定性規則」，而且工具都已實作，所以不應讓 Jev 重判。

**驗證（全部在本機執行，未使用伺服器）：**
- 後端完整測試（包含本節全部改動，最後一次在收尾時跑）：5,528 通過、81 跳過、0 失敗，退出碼 0；81 條跳過都需要 PostgreSQL 或 Temporal 實例。
- 前端：單測 107／107 個檔案通過，`vue-tsc` 通過，ESLint 通過。
- Ruff：改動檔案無新增告警。棘輪在本機 ruff 版本下為 325／286，但未改動的程式碼上也是 325，屬既有差異，不是本次新增。

## 4. 方案評分

評分是依目前程式碼和已知證據作的判斷，不是量測值。每項 0–10 分，按權重加總後換算為百分制。**「出境合規與後備」已按決定從評分中移除**，剩下六項權重合計 90，下表各欄均已按新口徑重算。

| 維度（權重） | A 原分支：Jev 覆寫結論 | B 調整當時 | B 邊界實測後 | B 基線（M0） | **B 現在（M1＋部分 M2、M4）** | **目標** | C 新提案作主線 | C′ 新提案作輔助 |
|---|---|---|---|---|---|---|---|---|
| Jev 用在它答得了的題（20） | 3 | 8 | 8 | 9 | 9 | 10 | 3 | 7 |
| 結論可追溯、有理由（15） | 3 | 8 | 8 | 8 | **9** | 9 | 3 | 6 |
| 不放寬、不漏審（20） | 3 | 8 | 8 | 8 | **8.5** | 9 | 2 | 7 |
| 同輸入可重現（10） | 3 | 6 | 6 | 8 | **9** | 9 | 2 | 5 |
| 效果有證據支持（15） | 2 | 3 | 5 | 5 | **5.5** | 8 | 1 | 3 |
| 工程成熟度與成本（10） | 7 | 7 | 8 | 8 | **8.5** | 9 | 3 | 5 |
| **百分制** | **33** | **68** | **73** | **77** | **83** | **91** | **23** | **57** |

**A 原分支（33）：**接線完整、測試齊全，但 Jev 覆寫了證書、平台和計算項，0.28 的「通過」也生效，失敗時還會蓋掉規則的「不符合」。

**B 調整當時（68）：**結論的來源、理由和證據都回到規則、平台核驗和計算，Jev 無論答甚麼都不會讓結論變寬鬆；Jev 不可用時規則照常運作，出境不批也不影響主流程。扣分在兩處：題目仍由 Qwen 每次運行帶著 OCR 生成，同輸入重跑題目全變；以及「Jev 分歧提示到底能抓到多少規則錯誤」沒有真值，只有 R02-02 一例。

**B 邊界實測後（73）：**
- 「效果有證據支持」3 → 5：R02-02 從個案證實為一類 9 種寫法的系統性錯誤，修正後在真實資料上只改動了該改的那一份；Jev 核對抽取值 32／32、合訂本 8／8，校準在 0.70 以上 80／80 全對。仍扣分，因為 Jev 的輸入全是虛構文字，監檢員標註仍是 0。
- 「工程成熟度」7 → 8：補上了過載重試和 token 超限的處理。
- 其餘不變。實測顯示同一套題目重跑時 Jev 很穩定（91／92），不穩定來自 Qwen 每次重新出題；但這部分程式碼還沒改，所以「可重現」仍是 6。
- 「Jev 用在答得了的題」也仍是 8：實測證明 Jev 最適合核對抽取值，但產品裡還沒有這個用法。

**B 現在（83）：**
- 「可追溯」8 → 9：每個「抽取可疑」提示都附上本地找到的原文頁碼和前後文字；原文裡找不到該值時會直接標明。R02-02 的提示會顯示「…=2024-09-07（原文第1頁：「…有效期：2024年9月7日至2028年9月6日」）」。
- 「不放寬」8 → 8.5：修正焊工證抽取。原本會把下一行的批准日期當成截止日、把表頭順序對調、把多人資料混在一起；修正後答案已知矩陣由 2/9 升到 9/9，真實快照中改動的 2 份都已對照原文核實。人名子串匹配也已修正。還差 0.5，是因為其他十幾類語意正則還沒逐類驗證。
- 「可重現」8 → 9：`jev_primary` 不再調用 Qwen 臨時出題，全部改用凍結模板；事實核對同樣用模板。
- 「效果」5 → 5.5：用真實快照回歸，又找到並修正了兩類真實抽取錯誤（焊工證日期、多人混合）；另外有一個泄漏試驗「合格標準」其實抽成了試驗步驟，正等 Jev 核對。Jev 本身在真實 OCR 上的效果仍未量測。
- 「工程」8 → 8.5：送出前按 token 估算攔截超長請求；在本機臨時 PostgreSQL 上跑完整後端測試，5,616 通過、0 失敗，原本跳過的 77 條資料庫整合測試全部實跑通過。還缺 Temporal、MinIO 和瀏覽器驗收。

**B 基線（77）：**
- 「Jev 用在答得了的題」8 → 9：用與不用已寫成 `jev_usage_policy` 並由程式強制執行；91 項確定性規則不再取得 Jev 意見；證書事實核對已接入產品，正好落在 Jev 實測最準的題型上。沒給 10，是因為事實核對只覆蓋證書類，壓力、等級、簽字等事實還沒做。
- 「同輸入可重現」6 → 8：事實核對的題目由抽取值按模板生成，同樣的事實永遠得到同樣的題目；確定性規則項也不再由 Qwen 每次運行臨時出題。沒給更高，是因為 `jev_primary` 的 R19 路徑仍由 Qwen 每次出題，而 R19 已有一條凍結題目的影子路徑，兩者尚未合併。
- 「效果有證據支持」維持 5：產品版事實核對模板在虛構許可證上的實測（31 次請求、93 個事實）已準備好但**未執行**；Jev 還沒在真實 OCR 上核對過；監檢員標註仍是 0。
- 「不放寬、不漏審」維持 8：Jev 無論答甚麼都不能放寬結論，有效期抽取錯誤也修了；但焊工證日期、相近人名子串匹配等已知風險還沒修。

**C 新提案作主線（23）：**見第 5 節。

**C′ 新提案作輔助（57）：**固定條款照審，RAG 只列出「可能遺漏、建議人工看」的條款，不刪任何必審項，也不產生結論。價值在補模板的覆蓋面，但 RAG 召回率未量測，額外清單也會增加人工負擔。

**目標 91 分的依據：**六項合計 81.5／90。
- 「效果」要到 8，需要**真實 OCR 上答案已知的證據**：在真實證書原文上注入舊抽取器的錯值和偏移值，看 Jev 能否否決，同時統計正確值被誤報的比例。這一步不必等監檢員。
- 「效果」要到 9–10，就需要監檢員的雙人背靠背標註；那時總分約 93–94。
- 真值出來後，如果 Jev 實際抓到的錯很少、誤報又多，「效果」會據實下調，Jev 的用途也要再縮小。分數不會為了湊到 90 而忽略這種結果。

## 5. 新提案評估：RAG 選條款 → Jev 判適用 → Qwen 出題 → Jev 判

新提案的流程是：節點審計 → 掛載的審計材料 → Qwen 從 RAG 選出候選條款 → Jev 判斷哪些條款適用 → Qwen 出題 → Jev 判斷。它想解決的問題是真的：R06-03 暴露出「適用性」判錯，固定模板也可能沒覆蓋材料中的特殊情況。**但作為主線不建議，原因如下：**

1. **與已定的固定條款綁定相反。**[節點與固定標準條款綁定方案](../业务节点与固定标准条款绑定方案.md)已把 69 個節點綁到經原文核對的條款，列明版本、頁碼、核驗狀態，並寫明「運行時不再讓 LLM 檢索、選擇或猜測條款」。這是因為 TSG 31-2025、NB/T 47014-2023 等剛換版，檢索很容易取到舊版條款。
2. **漏審是靜默的，而且誤差會相乘。**選條款、判適用、出題、判題四段都帶隨機性。假設每段有九成正確，連乘後只剩約三分之二；被 RAG 漏掉或被判成「不適用」的條款根本不會出現在介面上，監檢員看不到自己沒審到甚麼。
3. **Jev 判「不適用」等於用一個沒有理由的答案刪掉一條要求。**這正是會讓結論變寬鬆的決定，和「Jev 只作加強」矛盾。適用性多由 GC 等級、設計壓力、溫度、材料決定，這些已經抽取出來了，應由確定性規則判。
4. **Jev 又回到裁決位置。**最後一步仍是由 Jev 判題，而 Jev 沒有理由。
5. **更難評測。**除了題目的真值，還要條款選擇的真值（應審哪些條款），目前兩者都是零。

**建議改用 C′，分兩步做：**先離線把 RAG 跑在歷史工程上，找出「經常相關卻不在固定條款包內」的條款，由監檢員審核後補進條款包；線上只顯示「可能遺漏的條款」清單，不刪必審項、不產生結論。前提是先量測 RAG 對條款的召回率，並確保只檢索現行版本。

## 6. 開發計劃：從 77 分到 90 分以上

| 里程碑 | 內容 | 提升的維度 | 完成判準 | 狀態 |
|---|---|---|---|---|
| **M0 基線** | 方向調整、有效期抽取修正、Jev 使用政策、證書事實核對、邊界實測 | —（77 分） | 本機完整測試全部通過並提交 | **已完成**（`05acd291`） |
| **M1 修掉已知風險** | ① 焊工證日期：[welder_certificate_tool.py](../../backend/libs/ocr/welder_certificate_tool.py) 按出現順序取日期。② 多人身份改為完整姓名或證號匹配。③ R19 兩條路徑合併，只保留凍結題目。④ 長度上限改按 token 估算，超長請求在送出前攔下。 | 不放寬 8→8.5；可重現 8→9；工程 8→8.5 | 每項都經「答案已知矩陣＋真實快照前後對比＋節點重放」驗證，只改動該改的記錄 | **已完成**：①焊工證 `c39bd7d5`（矩陣 2/9→9/9，真實 24 份中改 2 份，均已核對原文）②人名 `e3a554eb` ③凍結題 `9f31fab1` ④token `15a91730` |
| **M2 事實核對擴展** | 把事實核對從證書擴到正則盤點中影響裁決的其他事實：設計／試驗壓力、管道等級、材料牌號、簽字角色、「合格／無泄漏」等結論字。每個「抽取可疑」提示都附上本地找到的原文引文和頁碼。 | 用在答得了的題 9→10；可追溯 8→9；不放寬 8.5→9 | 每類事實都有答案已知矩陣和真實快照回歸；提示可直接跳到原文 | **部分完成**：證書、設計試驗要求（`fca3b1b3`）；原文位置提示（`cd575234`）。焊工證合格項目、R13／R14 壓力等級、材料牌號、「合格」結論字待做 |
| **M3 真實 OCR 的已知答案實測** | ① 跑產品版事實核對模板的虛構實測。② 在七項目快照的真實證書和記錄上注入已知錯值（舊抽取器的起始日、年份偏移、他人證號），量 Jev 的否決率與正確值誤報率，並附不確定區間。 | 效果 5→8 | 報告附樣本選法、請求數和輸入哈希，可重跑 | 待做；②**送出真實 OCR 前會先請你確認** |
| **M4 工程驗收** | 瀏覽器登入流程的 Lab 驗收；在本機起 PostgreSQL／Temporal 跑整合測試；前後端全套測試。 | 工程 8.5→9 | 81 條跳過的整合測試轉為實跑並通過 | **部分完成**：本機 PostgreSQL 16＋pgvector，77 條資料庫整合測試實跑通過；Temporal（2 條）、MinIO（1 條）和瀏覽器驗收待做 |
| **M5 監檢員真值（可選）** | 不顯示模型答案的標註包，兩名監檢員背靠背標註，爭議再複核。 | 效果 8→9–10 | 查準率、誤報率、每百次複核抓到的規則錯誤數 | 需安排監檢員 |

M1、M2、M4 都在本機完成；M3 的第②步需要你同意送出資料；M5 需要監檢員。完成 M1–M4 預估 91 分。

## 7. 仍然有效的實測數據

以下數據取自調整前的同輸入配對評測，使用七項目唯讀快照（SHA-256 `5e883da5…42da`），詳見[配對評測](verification/2026-09-23-jev-primary-paired-judgment.md)和[文件歸屬實測](verification/2026-09-23-jev-live-routing-evaluation.md)。調整後 Jev 不再改結論，這些數字現在只說明 Jev 提示與規則的一致程度，不代表新方案的準確率。

- **節點配對：**21 個節點可在正式模式重跑舊規則；新路徑完成 20 個，節點 1 兩次出題失敗。20 個中 17 個兩邊都是「證據不足」，樣本嚴重偏向證據不足，沒有「符合」或「不適用」的真例。
- **原子題：**50 題中 43 題一致、7 題分歧；其中 4 題 Jev 選「通過」而規則是「證據不足」，把握值為 1.00、0.57、0.28、0.36，尚無真值。
- **個案：**R02-02 由 Jev 分歧暴露出規則的日期抽取錯誤；R06-03 兩邊都沒判對適用性；R09-01 的 Qwen 選項漏了泄漏性試驗；R25-01 同輸入重跑答案改變。
- **重跑：**5 個節點重跑，4 個完成，題目哈希全部改變，節點 13 的整體類別翻轉。
- **耗時與用量：**Qwen 出題加 Jev 判題中位數多 9.459 秒；20 個節點共用 Qwen 177,165／13,720 token、Jev 227,590／3,378 token，沒有供應商實付帳單。
- **文件歸屬：**194 份中 178 份取得完整答覆，共 546 次請求；獨立標註 0／1,904，精確率和召回率都無法計算。

## 8. 邊界

- 本文件記錄 2026-09-23 的分支狀態，本次改動尚未提交。
- 評分是判斷，不是量測；拿到監檢員真值後應重評。
- 本輪 Jev 的所有實測都只送了虛構文字；真實 OCR 只在本機用於規則抽取的回歸比較，沒有送出。
- 修了抽取、換了題目或 OCR 版本後，必須用新的輸入哈希重跑，不能沿用今天的數字。
