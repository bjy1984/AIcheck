# Jev 文件归属补齐第 69 节点（2026-09-23）

之前的影子分类题只取 `materialReviewPoints`，默认资产 168 项只覆盖 68 个节点。
第 69 节点是监检人员的质量体系人工评价，没有资料审查点，但已安装的工程模板
要求一份「施工单位质量保证体系实施状况评价工作流记录」。因此只用审查点出题会
让该文件没有对应的节点候选，并把 68 节点的结果误看成全量。

现在只为**没有配置任何审查点**、但工程树上存在且有必需资料的节点补题。
补题依据来自当前工程的 `tree_nodes` 和 `requirements`，核对工程、业务包与
已安装版本；管理员明确停用的审查点不会被补回。第 69 节点的题目只判断文件
能否作为该节点资料，Jev 不产生质量体系评价结论，也不写入节点挂载。

默认业务包的回归检查确认 69 个节点都有题目，且没有超长题目；跨工程、不同
业务包版本、停用点和人工拒绝路径均保留边界。文件归属／预检／离线对照的
27 项测试通过，Ruff 局部检查和全仓 287／287 棘轮通过。

`test2/` 的 20 份本地 OCR 容量预检：69 节点、19 份可发起、1 份因单文件
超 40,000 字跳过，预计 66 次完整原文请求。预检不调用 Jev，也不测准确率；
正式路由建议仍需监检确认的文件到节点真值集才能评价和启用。
