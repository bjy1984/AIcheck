# 自然語言草稿：原工作台登入驗收

## 環境與範圍

正常監檢測試帳號登入本機隔離服務：前端4400、API4183。使用原Vue工作台、節點16、工程規則編輯器。臨時服務腳本 /tmp/aicheck-rule-draft-ui-fixture.py 只替換模型客戶端回應，不替換生成接口、格式驗證、保存或試跑API。資料為合成界面驗收案例，未發布規則，未呼叫付費模型。

## 實際操作與結果

1. 工程規則→新建工程草稿→輸入厚度要求→生成。
2. 預覽列出文字要求、thickness≥10mm、未綁定原子項與待確認對象問題。未勾確認時「套入表單」不可用。
3. 勾確認→套入表單→保存，顯示本工程／草稿。版本inspection-rule-fd1381bf-draft-20260910-0408。
4. 示例9mm＋合成引用→不符合；10mm＋引用→符合。
5. 清空引用後10mm→證據不足，原因「缺少示例引用」。首次自動化fill空字符串未實際清空，讀取可見輸入值後以全選／刪除重試，確認並非業務工具誤判。
6. 結構化條件仍顯示發布驗收未完成，發布預覽按鈕被阻擋。
7. 保存後重新開啟仍可讀到草稿；截圖發現長表單讓標題與操作移出畫面，已增加對話框限高、內容區捲動，重開後截圖確認標題和保存操作同時可見。截圖由本次CUA工具直接記錄在對話中，未生成獨立圖片檔案。

## 未完成

真實模型對適用條件、數字和標準依據的忠實度；真實任務OCR試跑；所有條件正式發布資格；完整深色／鍵盤／真實案件UI驗收。這份記錄只證明合成模型輸出進入真實產品操作流程，不能當作模型品質提升證據。

## 回歸證據

後端完整收集4266項；修正部署報告兩項失敗後，第二輪4195通過、72環境選擇性跳過、0失敗（248.21秒）。兩輪間新增重放測試；完整執行開始後新增的權限映射測試，已由21條草稿專項另行通過。相關發布／部署回歸60條通過。前端vue-tsc、ESLint、Stylelint，後端Ruff289/289、monolith及git diff檢查通過。服務專項結果另記，不將環境跳過算成通過。

服務專項已補驗：以本輪跳過清單中的服務測試檔為範圍，開啟AICHECK_TEST_POSTGRES_URL、AICHECK_TEST_MINIO_ENDPOINT和AICHECK_TEST_TEMPORAL_LIVE，在獨立測試資料庫及本機服務上執行；255通過、0跳過、0失敗（53.93秒）。涵蓋PostgreSQL持久化／隔離、MinIO版本錨點、Temporal重試與工作程序重啟。測試與一般回歸有重疊，不累加；CNSE外部真人查詢未執行。可複核輸出保存在同目錄2026-09-09-rule-draft-regression/。
