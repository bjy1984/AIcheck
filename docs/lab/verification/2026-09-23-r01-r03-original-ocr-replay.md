# R01／节点 3 原生产 OCR 离线回放（2026-09-23）

从 `aicheck-prod-new` 的现有状态库**只读**提取 `P-2026-ECD202` 两个已完成的
历史任务快照：R01 `RRUN-6C5B72DB34`、节点 3 `RRUN-F8A006D339`。输入只含
本工程 4 份已选文件版本与 OCR 解析、两个任务冻结绑定和两条当时已落库的
CNSE 组织许可证查询缓存。原始 OCR 临时文件权限为 0600，未写入仓库或报告；
提取内容的 SHA-256 为
`bad9ac399ebf0a42b14cace9450956ef711af36ba5fffecf0bbd65b53aec3b2b`。
回放脚本为 `backend/scripts/replay_r01_r03_ocr.py`，强制禁止新的平台查询，
只输出状态、级别与判定，不输出原文、姓名或证号。**没有调用 Jev 或修改线上数据。**

| 核查 | 补设计文件级别前 | 补后（旧冻结绑定／当前新包） |
|---|---|---|
| AC-R01-02 工程 GC2 被设计许可覆盖 | 通过 | 通过／通过 |
| AC-R01-03 设计证有效及覆盖工程级别 | 通过 | 通过／通过 |
| AC-R01-04 设计文件级别被设计许可覆盖 | 证据不足 | 通过／通过 |
| 节点 3 OCR 错把评价句作单位名称 | 已拒用坏字段 | 坏字段继续拒用；平台状态 `unable_to_verify`，证照判 `evidence_insufficient` |

AC-R01-04 的原 OCR 两份设计文件都有结构化 `pressure_pipe_level=GC2`，但原事实
组装没有产出 `designDocument.pipelineGrades`，所以工具得到空级别。现从**本次
冻结输入中属于本工程的设计文件**逐份读取该结构化字段，并把版本、页码、
原文引文送进事实和人工核对列表。任何一份设计文件缺少可识别级别，或单个
字段混写多个级别而无法映射对象时，整组级别留空，继续返回证据不足；
不挑第一份，也不借用另一个工程的字段。

这次结果是**当前代码对旧 OCR 和旧任务绑定的反事实回放**：历史结论本身保持
原貌，不说明旧代码当年也会得到相同结果。平台记录是 2026-09-13 的缓存，
不代表今天重新核实的登记状态。只覆盖这个工程的两个节点，不能代替 69 条
规则、七项目或 Jev 阈值验收。正式数据出境与生产部署仍未放行。

验证：`pytest --collect-only -q` 收集 5,499 项；完整后端测试 **5,418 通过、
81 跳过、0 失败**（148.68 秒）；改动文件 Ruff 检查通过，全仓 Ruff 基线
**287／287**，`git diff --check` 通过。81 条跳过仍须按各自环境前提理解，
不能视为 PostgreSQL／MinIO／Temporal 的全部实机验收。
