# 實際檢測方案入口核查

## 結果與限制

這批完成完整後端回歸及真實PDF入口盤點，沒有宣稱R39整條業務驗收通過。

| 實際來源 | 頁數 | 原生文字頁 | 明確標籤抽取 |
|---|---:|---:|---|
| test2/5.2检测方案-盈德气体化工管道.pdf | 22 | 2–22 | 第3頁檢測單位 |
| test/10、无损检测委托单位资质/3.检测方案/射线检测施工方案(1).pdf | 28 | 2–28 | 無 |

兩份都是檢測方案，不是已確認配對的操作指導書及規程。此次明確指定ndt_procedure_v1做能力檢查；不能把強制指定profile當作自動分類成功。內容可能含相關技術章節，但原生抽取尚未提供本文件編號、版次、方法、標準的明確欄位，因此不能建立可靠引用版次核對。

每份的第1頁無文字層；無文字不等於空白或缺文件。另以既有scripts/vision_ocr.swift在本機實跑兩張首頁影像（PyMuPDF 180dpi），兩份均有文字觀測。盈德封面OCR讀到「广州声华科技股份有限公司」，與第3頁標籤「广州声华科技有限公司」不同。助手查看封面影像亦可見「股份」，但沒有執行機構身份核實；不可自動合併成同一機構。手寫簽名有漏讀／誤字，不能以OCR讀到名字當作有效簽核。

原生抽取和首頁視覺OCR分開留存；後者尚未接入本次業務事實或R39執行，不冒稱生產OCR已完成。這次未執行付費模型，也未變更發布門檻。

## 可重跑方式

在backend目錄執行：

```sh
.venv/bin/python -m scripts.probe_ndt_pdf_sources '../test2/5.2检测方案-盈德气体化工管道.pdf' '../test/10、无损检测委托单位资质/3.检测方案/射线检测施工方案(1).pdf' --output ../docs/lab/verification/2026-09-09-ndt-real-pdf-probe.json
```

工具只使用本機原生文字提取，不發起模型／網路請求。逐檔記錄SHA256、頁碼、bbox、可信度、缺少欄位和無文字頁。退出0僅代表probe執行成功；businessAcceptance始終not_evaluated。讀取失敗保留其他檔案報告並退出非零。

- 原生抽取：[機器報告](2026-09-09-ndt-real-pdf-probe.json)，來源PDF雜湊包含在內。
- 首頁OCR：[原始觀測](2026-09-09-ndt-cover-ocr.json)，影像名yingde對應首份、radiographic對應第二份；保留工具／影像雜湊和原生Vision左下原點xywh座標，不能直接當前端左上xyxy使用。
- 完整後端：[日誌](2026-09-09-full-backend-r39.txt)，4472 passed、72 skipped，134.55秒；在本批新增probe前執行。
- 新增probe與既有專用OCR測試：8 passed。覆蓋實體PDF無文字頁、檔名不作身份、來源雜湊、批次部分不可讀非零退出及既有欄位保護。Ruff289/289、monolith檢查通過。

下一步需要補複合方案與掃描頁的有效來源抽取、明確身份／引用關係，以及剩餘業務事實適配。這兩份文件不能替代全部69條及R39四態的真實案件驗收。
