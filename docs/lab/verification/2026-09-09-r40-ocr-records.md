# R40 原文欄位到事件對應

## 實作

既有 conditionObjectMapping 契約屬工程條件規則與規則快照，不能直接用來背書 R40 事件。本次沿用 R40 已有來源的事件清單，要求原文 weld_no、detection_method、detection_event_no 與清單 projectId/objectId/method/eventId 唯一且逐字一致。複數焊口不拆成任選一筆，不推測事件。

選定 RT／UT profile 的原文文件類型明示「檢測記錄」時取本記錄編號；明示「檢測報告」時取引用記錄編號。每個欄位都須唯一、置信度至少 .75、無衝突且能定位到同頁／座標的原文 fragment。來源讀取沿用既有可信欄位 helper，R40 引用使用獨立 ID 前綴。字段逐一進入 claimedFacts 與證據門檻，不只用記錄號的一個引用代表整份資料。

只有既有 records/reports 業務表都未提供時才走原文補入；不繞過部分或矛盾表。事件 inventory/members 仍必須有原文來源，絕不由檢測報告自行生成完整清單。頁碼與版本、租戶／工程、缺頁校驗沿用 R40 既有保護。

## 驗證

74 項相關測試通過，包括原文編號後處理到 R40 實際參數／dispatch、引用不一致、缺事件、複數焊口、事件不符、低置信度、原文不支持、重複解析、部分業務表不繞過及凍結頁碼範圍；R39 來源 helper 和資產生成回歸通過。Ruff 289／289，monolith 通過。輸出見 r40-ocr-records.txt。

## 未完成

使用合成 fragments 和既有 OCR field contract 驗收；焊口／方法欄位是測試輸入，未宣稱真實 PDF 引擎已成功抽取。成套真實記錄／報告驗收、清單原文適配、檢測參數／設計要求／報告結論仍未完成。沒有新增人工身份核驗背書；R40 的整條驗收及正式發布仍阻擋，整體未證明達 90%。
