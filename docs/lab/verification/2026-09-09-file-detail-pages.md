# 原文件詳情整合與最新完整回歸

在 b0c640b3 基礎上完成最新後端完整回歸和原 FileDetailDialog 桌面整合驗收；本階段沒有修改業務判定或發布設定。

## 原文件詳情與真實 PDF

Chrome headless、1600×1000，直接掛載原 `FileDetailDialog.vue`，沿用其子元件、定位池和原生 PDF iframe；注入測試文件元資料，HTTP 攔截回傳本地 `test2/9.1金辉焊接工艺评定20.pdf` 的真實內容。

1. 原結構化內容區出現逐頁辨識清單。
2. 點第 11 頁，iframe URL 更新到 `#page=11`，原定位提示同步。待原生閱讀器繪製後，已實際檢視截圖：工具列 11／16，原文是射線檢測報告主頁，可見相應表格。
3. 注入與目前版本不同的分類，分類清單消失，iframe 的舊 `#page=11` 定位參數移除。這確認版本隔離，不宣稱已測所有非同步 API 競態。
4. 無頁面 JS 錯誤。

證據：`browser/file-detail-page-11.png`、`2026-09-09-file-detail-pages.txt`。腳本從 frontend 執行 `node e2e/lab-rules/check-file-detail-pages.mjs`；4394 的獨立入口僅用來掛載原元件驗收。

這不是完整登入帳號的 API→文件詳情流程：元資料是種子資料，PDF 回應是本地攔截。它比此前子元件事件驗收多證明原父元件接線與真實原生 PDF 的第 11 頁渲染，不能代替權限、存儲服務、版本更新併發和完整業務案件驗收。

## 最新完整回歸

backend 下 `.venv/bin/python -m pytest -q`：**4684 passed、0 failed、75 skipped、6 warnings，158.16 秒，退出碼 0**。日誌：`2026-09-09-latest-full-backend.txt`。

涵蓋最近明確欄位、封閉單元格、逐頁分類／頁碼限制及文件詳情視圖修改。75 skipped 不計通過；此一般回歸没有取代先前 PG／MinIO／Temporal 的另行服務驗收。6 項為現有棄用警告。與先前 4634 的差異來自新增測試，本次未按失敗總數放行。

仍待完成：登入真實案件全流程、人工分類保存、續頁／區段歸屬、分段正式執行、事件映射，及原計畫全部規則／標準與發布驗收。不能據此宣稱整體達 90%。
