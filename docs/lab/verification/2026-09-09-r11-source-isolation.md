# R11 逐筆來源隔離（2026-09-09）

原問題：簽批資料中任何一筆低可信度／衝突都使 approval_arguments 整組返回空；即使改成逐筆判定，原子項的全域 grounding 還會掩蓋其他可信來源已證實的問題。

修復：先嚴格核對唯一且可信的上下文；其餘記錄逐筆驗證來源，失敗記錄保留身份及數量、清空可用引用並留下 r11_approval_record_source_untrusted。不可刪除低可信度重複記錄再拿剩餘首筆判定。簽名分角色核對，一個角色來源不足不抹除其他角色有據的缺簽。

AC-R11-01 的 construction_plan_approval profile 使用專屬 sourceJudgment，只重驗已通过來源檢查的記錄；其他 profile、整體證據原子項與既有 grounding 聚合優先順序不變。selectionIssues 阻止正面通過；可信的晚批復／缺簽仍可判不符合。上下文或批復本身不可靠時不推論時序，重複身份不選首筆。

233 項 R11／業務工具／executor 測試通過（2026-09-09-r11-source-isolation-tests.txt）。新增 12 個真實節點執行路徑的合成場景：低可信度、引用缺失、衝突、錯誤衝突型別、清單、上下文、業主回覆、不同角色及相同身份重複。驗證狀態不改寫、可信引用範圍、壞記錄不消失、獨立證據原子項仍為證據不足。

工具 v4，簽名權限及批復有效性兩項 pendingCapabilities 保留。這是合成資料的實際節點路徑驗收，非真實案件或人工簽章核驗。完整後端回歸：4779 通過、0 失敗、75 跳過、6 警告（138.32 秒）；日誌 2026-09-09-r11-source-isolation-full-backend.txt。跳過項不視為本輪服務／真人外部核驗通過。Ruff 289／289、monolith 通過。
