# Jev 七项目文件归属离线预检（2026-09-23）

在 `codex/jev-integration-v3` 上，从 `aicheck-prod-new` 当前状态库**只读**提取原影子研究
所用七个工程的工程元数据、当前文件版本、OCR 尝试、节点证据连结和已安装的节点模板。
本地私有压缩快照的 SHA-256 为
`577cbae7bda4fd74b3d84da0641f2c0f3dc38f1e31797095a3999c2f20f59d22`。
原始 OCR 只用于本机容量预检，未提交仓库、未调用 Jev，也未修改生产状态。

| 项目 | 文件 | 可发起 | 预计请求 | 不能发起的原因 |
|---|---:|---:|---:|---|
| P-2026-6B15AE | 21 | 18 | 54 | 全文超长 2，批次预算超限 1 |
| P-2026-7F7270 | 8 | 7 | 21 | 单题请求包超限 1 |
| P-2026-ECD202 | 44 | 38 | 114 | 全文超长 4，单题请求包超限 2 |
| P-2026-GDLNG-002 | 38 | 38 | 116 | — |
| P-2026-HDCP-001 | 39 | 36 | 109 | OCR 待人工确认 3 |
| P-TEST-OCR-001 | 24 | 22 | 66 | 全文超长 2 |
| P-TEST-OCR-002 | 20 | 19 | 66 | 全文超长 1 |
| **合计** | **194** | **178** | **546** | **16 份未进入请求队列** |

这里的 546 是当前整份 OCR、当前问题模板和 40,000 字请求限制下的**批次数估计**，
不是已发生的调用，也不能据此推算费用或准确率。九份全文超长、三份单题包超限、
一份超过每文件十批的预算、三份 OCR 待人工确认；没有截断文件或从片段猜归属。
两个使用 `2026.07.16` 旧业务包的工程只生成 68 个候选节点：它们没有节点 69 的
必需资料配置。其余五个工程为 69 节点。预检不会用现行配置替旧工程补题。

这批资料有 194 个文件版本、196 次 OCR 尝试。两个版本各有两次解析，其中
`DV-D1B62016-V1` 的两次均成功；旧代码会把两次全文拼接，形成重复输入。
`DV-D1FCFA52-V1` 是先失败后成功。现 Jev state、表格分类和支持页候选均只读取
**该版本最新一次解析**；最新尝试失败或仍待人工确认时，不回退到旧 OCR，也不发起
归属请求。影子状态新增 `ocr_not_ready` 以便与完全没有 OCR 的 `no_ocr_text` 区分。
这些变更不改原 OCR、既有挂载、确定性结论或历史任务快照。

按当前快照核对 434 条 `node_evidence_links`，没有发现缺失版本、缺失文件或跨工程
引用。旧影子研究曾出现的“其他工程焊工证被拿来问节点 29”不能由当前连结复现；
本检查只证明**这份当前快照的连结关系**，不证明历史影子脚本当时的输入已正确。
当前数据库也没有可复用的 `jevRoutingShadow`，所以仍需受控影子运行和监检真值集，
不能把旧挂载视为正确答案。

本批只统一 **Jev 输入**对重复 OCR 的读取。确定性材料事实构建器仍通过
`iter_requested_parse_results` 遍历选中版本的解析记录；它在重复解析版本上的
事实数是否重复，需要下一阶段单独做差异回放并修正，不能把这次 Jev 输入修复
当成整个审查链路已去重。

可复跑的离线入口是
[`preflight_jev_document_routing.py`](../../../backend/scripts/preflight_jev_document_routing.py)：

```bash
cd backend
.venv/bin/python -m scripts.preflight_jev_document_routing \
  --snapshot /path/to/private-seven-project.json.zlib \
  --expected-project-count 7 \
  --output /path/to/metadata-only-report.json
```

输入需包含明确的 `read_only_seven_project_ocr_snapshot` 来源标记，以及七项目的
`projects`、`documents`、`versions`、`ocr_parse_results`、`node_evidence_links`、
`routingPointsByProject`、`requirements`、`tree_nodes`、`configuredPoints`。
输出只有工程／文件版本 ID、状态和计数，不含 OCR 原文。无效证据连结会使命令
以非零状态退出。原始快照只放权限 0600 的临时文件，用完即删除。

本批针对性回归 **51 项通过**；最终收集 5,507 项，完整后端测试
**5,426 通过、81 跳过、0 失败**（146.82 秒）；改动文件 Ruff 通过，全仓基线
**287／287**。跳过项不当作外部服务实测。
正式资料出境、新密钥、33 题监检标注、文件归属真值与七项目实际 Jev 对照仍未完成。
