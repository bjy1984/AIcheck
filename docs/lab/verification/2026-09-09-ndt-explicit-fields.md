# NDT 明確標籤抽取與規程分流修正

## 完成內容

- `apply_profile_postprocessing` 恢復 ndt_procedure_v1 的專用返回，避免加入 RT／UT 分支後意外繼續執行一般表格處理。
- RT／UT 分支保留記錄／引用／事件身份抽取，新增 `libs/ocr/ndt_report_fields.py` 明確標籤抽取；保留原文、頁碼、位置、置信度和矛盾標記。不重新啟用舊的跨片段順序猜值、任意年份或「包含合格」結論推導。
- 報告號不當成記錄號或事件號；報告日期不當成檢測日期；合格／驗收級別不當成實際評定級別。結論保留完整原文，包括否定詞及待復核。
- 工程／機構名稱跨行需版面支持，本輪不由单行片段補成完整名稱；已有結構化字段不抹除。表格單元格、跨行與複合文件分類仍待接線。

## 真實來源回放

沿用上階段保存的 Apple Vision observations（不是重跑付費模型），實際執行原後處理入口。來源 `test2/9.1金辉焊接工艺评定20.pdf`：

- 第 9 頁沒有生成報告欄位，不把焊接／外觀記錄當 RT 報告。
- 第 10 頁新增完整 report_no=2023SHZH-022RTBG-01，保留實體頁碼 10 及原 bbox／confidence。工程名稱第一行截斷，不將其生成为完整 project_name。
- 第 11 頁只新增明確報告日期，保留原置信度 0.5；不是檢測日期，也不代表通過來源可信門檻。分散在相鄰儲存格的編號沒有盲目合併。

結果：`2026-09-09-pqr-rt-explicit-fields.json`；原始片段沿用 `2026-09-09-pqr-embedded-rt-probe.json`，不覆寫前次零欄位證據。真實封面 observations 固定為 `backend/tests/fixtures/ndt_real_cover_fields.json`，回歸重現原文位置與防截斷行為。

## 驗證

- 83 項：明確字段、規程、R40 記錄／參數／要求／結論；含 RT／UT 否定語義、合格與評定分離、日期分離、矛盾不覆蓋、跨格不拼接、規程不落入通用處理，以及真實封面回放。
- 44 項：OCR fast-first 與 NDT 記錄身份回歸。
- Ruff 289／289、monolith 與 diff check 通過。
- 本輪是 127 項針對性回歸。上一提交 4cc1d67b 的完整後端 4634 passed／75 skipped 證據仍保留，但不冒稱本輪再次完整回歸。

## 尚未完成

單元格邊界與同格跨行抽取、複合文件分類、對象／事件映射、真實 R40 成套案件均未完成。這次只讓已存在的明確欄位进入原處理路徑；仍不產生事件清單、判定或人工背書，R40 三類能力發布阻擋不變。整體未證明達 90%。
