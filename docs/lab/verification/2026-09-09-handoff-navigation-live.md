# 原工作台交接篩選實機驗收

使用更新後隔離後端127.0.0.1:4185、原Vue前端4402，保留原4395／4180服務。登入監檢測試帳號，工程為P-2026-HDCP-001种子資料。

## 實際操作

1. 登入後，工位區顯示「交接：1个节点需重验，1个尚未确认」，總列表69/69。
2. 選「交接需重验（1）」後只見R35；其原status仍待提交，並非改成業務不符合。工程總覽仍69節點／提交4。
3. 選「交接尚未确认（1）」後只見R36。該任務包含無權來源，不展示文件或任務細節。
4. 刷新狀態後仍正確顯示1／1；切回需重驗，進入R35並打開原AI審查頁。節點標題為R35，內部出現「本次交接需要重新核验」，與左側一致。
5. 截圖於本次對話實際查看。為區分作用範圍，左側按鈕改名「刷新工位交接状态」並重新點擊驗證；節點内仍為「重新核对交接状态」。

## 重跑

專用夾具：frontend/e2e/lab-rules/serve-handoff-navigation.py。使用backend虛擬環境啟動（需pytest等測試依賴）。夾具禁用SQLite／PostgreSQL、開啟demo與工位開關，復用既有交接路由測試，固定綁定本機4185；AI重查dispatch為本機測試替身。

前端在frontend目錄使用VITE_API_PROXY_TARGET=http://127.0.0.1:4185、VITE_AICHECK_WORKSTATIONS_ENABLED=true執行vite --mode live --host 127.0.0.1 --port 4402。這是原工作台，不是獨立原型頁。

本次不重新聲稱98個前端檔或58個後端測試已重跑；這些為上一批紀錄。本批新增的是登入後實際操作證據與按鈕文案核對，diff檢查通過。

尚待在保持登入的情況下變更來源並驗證數量改變、跨工程競態和PostgreSQL多程序讀取。種子交接的核驗文字不是對真實工程原文的人工背書；未發起付費模型或改写生產資料。
