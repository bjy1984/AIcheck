# Jev 测试外呼输入与七项目快照收口（2026-09-23）

测试用文件归属入口现在只发送选定版本的完整当前 OCR 正文（含页码）及冻结节点题目。
工程、文件、版本、租户 ID、文件名、现有挂载、规则结论及已核实检查不进 Jev 的
`state`。输入哈希取实际外呼的 OCR 与题目。版本、工程边界仍在本地先验证；最新
OCR 失败不回退，页码范围和该节点人工修正沿用审查读取规则。超长文件不截片。
正式文件归属路径未改，正式裁决、证据挂载与历史任务也未改。

新增 `backend/scripts/export_jev_seven_project_snapshot.py`：只取批准的七个工程、
当前文件版本的所有 OCR 尝试及本地对照需要的节点／连结数据，PostgreSQL 使用
`REPEATABLE READ READ ONLY`，不调用可能播种数据的 repository loader。输出为
新建的 0600 zlib 私有文件；只打印哈希、工程／文件数与预检次数。快照只在本机
供评估使用，绝不提交仓库或直接送 Jev。
快照还保留所选工程的 ReviewRun、规则结果与人工修正供**本地**原子题对照；
文件归属外呼会先剥离这些本地数据，原子题外呼只取修正后的选定 OCR 与固定题目。

首次尝试连接 `aicheck-prod-new` 时在 SSH banner 阶段超时；其后已成功只读取得
新七项目资料，并修正版本集合名称。新快照的预检恰好也是 178／194 份、
预计 546 次；这仍非 Jev 实际请求。单交易快照的来源与限制详见
[`新快照与中间报告`](2026-09-23-jev-fresh-snapshot-interim-report.md)。
`test2/` 按新 OCR-only 输入口径重新预检：20 份中 19 份可发起、1 份
超长，预计 66 次；指定 `test2-019`／`test2-020` 预计 8 次，实际外呼 0 次。

录制网关请求测试直接检查实际 JSON body，不含工程、文件名、版本和既有检查。
另覆盖最新失败、跨工程、页码、人工修正、全文超长、快照范围和 0600 输出。
