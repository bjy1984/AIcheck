# 壓力比較不得使用其他管線最大值（2026-09-09）

設計說明目前是合併文字投影，沒有逐次試驗到管線的明確綁定。原先取全工程最大設計壓力作分母或泄漏試驗基準，可能與實際試驗對象不同。

本次移除最大值回退：只有單一有效管線可提供絕對壓力比较基準；多條管線（包括數值相同、部分資料缺失）保留 multiple_pipeline_scope_unresolved，不取第一筆／最大值，也不從缺資料集合中挑一筆。原文直接陳述倍率時仍可檢查該倍率，沒有捏造分母，但不代表實際試驗對象驗收完成。

124 項相關測試通過，包括管線重新排序、同值、多值、缺值、單管線及明示倍率；實際業務工具在對象不明時保留 evidence_insufficient。Ruff 289／289、monolith、diff 通過。未重跑全量，最近全量仍4796通過／75跳過。

這是阻止錯配的修復，不是完成多管線逐事件映射；具來源的對象清單與試驗記錄綁定仍待開發。整體進度約60%為人工工程估算，不是驗收率。
