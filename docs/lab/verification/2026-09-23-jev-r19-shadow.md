# R19 八题 Jev 影子意见（2026-09-23）

R19 现有正式路径由 Qwen 语义审查、证据约束及固定聚合器决定结论。Jev 新增的
八题核对只复用 Temporal 图中既有的 `jev_second_opinion` 异步步骤，保存到
`reviewRun.jevSecondOpinions`；inline 路径不会同步调用 Jev，规则结果、
`r19SemanticReview` 和人工确认均不被改写。

每题分两问：适用／不适用／无法确定，以及符合／不符合／证据不足。组合后可记录
符合、不符合、证据不足、不适用四种最终情形；不适用必须有原文依据，适用性
不明归证据不足。Jev 接收所选版本的完整 OCR，不把 Qwen 的 R19 结论伪装为
「已核实事实」再喂给它。支持页仅为定位建议，不是正式 `evidenceRefs`。

影子记录逐题包含选择、把握值、适用性、建议支持页、来源版本及与当前 R19
结果是否一致。`agreesWithRuleEngine` 对 R19 保持空值，因为当前对照对象是
Qwen 语义结果；标记 `comparisonSource=r19_semantic_review`。通用第二意见 UI
即使打开，也不会把 R19 错标为「与确定性规则分歧」。没有整份可用 OCR、
最新解析失败、总请求超长或 Jev 不可达／响应不完整时仅记录状态，不产逐题意见。

固定回应用例覆盖八题、四种最终情形、页码建议、关闭开关、缺原文、失败 OCR、
超长、不可达和响应不完整；校验原规则结果及任务输入未变。测试使用伪造响应，
没有实际 Jev 调用，因此这批不能报告 R19 准确率。监检标注和轮换后的新测试
金钥仍是实际评估的前置条件。

本批最终完整后端 **5,442 通过、81 跳过、0 失败**，全仓 Ruff **286／286**。
81 条跳过不作为外部服务实测。
