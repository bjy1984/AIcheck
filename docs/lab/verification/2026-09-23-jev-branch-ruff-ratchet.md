# Jev 分支 Ruff 棘轮收口（2026-09-23）

在 `codex/jev-integration-v3` 清掉全仓相对基线的 29 条净增告警，并把基线从 289
收紧到 287 条、91 个文件。`backend/.venv/bin/python -m scripts.ruff_baseline`
现为通过；仓库仍有 287 条已登记存量告警，不能据此称 Ruff 全绿。

处理范围包括导入顺序、未使用导入、重复测试参数、测试构造、零宽空格字面量，
以及证书平台缓存和健康巡检的业务时区解析。R24 事实构建器没有进入离线重放
注册表，移除无用导入但保留注册排除理由。发现条款详情补全失败时维持原来的
降级行为，同时记录异常；显示名查不到业务包时仍回退到原子项 ID。

受影响后端测试：243 通过、1 条环境选择性跳过。Ruff 基线保护测试：5 通过。
`git diff --check` 通过。此前 R01 版本修复后跑过完整后端套件
5,409 通过、81 跳过、0 失败；本次 lint 清理后只跑受影响测试，没有把这次
局部回归冒充新一轮全套测试。81 条跳过也不是 PostgreSQL、MinIO、Temporal
全部实机通过的证明。
