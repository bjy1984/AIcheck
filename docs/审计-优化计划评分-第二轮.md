# 审计：《优化计划》第二轮评分（描述清晰性、无歧义、能否准确指导工作）

日期：2026-09-06　对象：`docs/优化计划-焊接节点.md`（P0–P11，430 行，提交 045a23b）
方法：逐行通读；用脚本检查含糊词、编号引用、估时一致性；以"一个没参加过本轮讨论的开发者拿到这份文档能否直接开工、不用回头问"为判据。

## 0. 评分

**总分 65 / 100**（第一轮 64）。内容层面明显补齐了：范围、迁移、判定口径默认分支、代码对齐、可修复性分类都有了。分数没涨多少是因为修订是叠加式写进去的，产生了新的不一致与三套并存的词表，这些恰恰是"能否准确指导工作"最敏感的地方。

| 维度 | 权重 | 得分 | 一句话理由 |
|---|---|---|---|
| A 描述清晰性 | 20% | 7 | 每项都落到文件、函数、字段与表格行号，读得懂；但 37 处 N-xx 编号的定义在另一份文档里，本文读不到它们是什么 |
| B 无歧义 | 25% | 5.5 | 结论词表三套并存、"证据不足 / 需人工确认 / 口径待确认"边界未定义、结论卡推导规则没有决策表；§0 验收第 1 条自相矛盾 |
| C 能准确指导工作 | 25% | 7 | P0–P3、P8、P9 可以直接开工；P4 的接口、P2/P3/P4 的测试、P0 2.3 平台工具的验收缺失，开发者要自己补 |
| D 内部一致性 | 15% | 5 | 估时在三处不一致；阶段顺序表与章节不一致；H5 估时两处不同；"待你决定"与"先按默认实现"并存 |
| E 完整性 | 15% | 8 | 目标、验收、迁移、灰度、风险、可修复性齐；缺责任人与里程碑日期 |

加权：7×0.20 + 5.5×0.25 + 7×0.25 + 5×0.15 + 8×0.15 = **6.48**。

## 1. 歧义与矛盾清单（按严重度）

### 1.1 会导致做错的（必须改）

| # | 位置 | 问题 | 改法 |
|---|---|---|---|
| 1 | §0 验收第 1 条 | 同一句里既写"按 WPS 无背面保护气无人覆盖"，又写"李卫伍 GTAW-FeⅣ-6G-5/57 可覆盖不锈钢"。李卫伍该项目带因素 02/10/12，因素 10 同样受充氩前提约束，两个结论不能同时成立 | 改为"李卫伍 3 项现行证与清单一致，FeⅣ 覆盖不锈钢母材；是否覆盖仍取决于 2.5 的充氩口径" |
| 2 | P6（8 章）、P9 12.3、2.5 | 三套结论词表并存：P6 用 不符合/预警/证据不足/需人工确认；P9 结论卡用 需处理/待确认/证据不足/未见问题；2.5 与 3.4 用"预警级发现"；工具输出用 severity critical/high/medium/low 与 groundingStatus。没有任何一处写它们之间的映射 | 增加一张"状态与词表映射表"：severity × groundingStatus × 确定性结果 → 结论卡四值 → 前端颜色 → 业务语义，放在 12.3 并在 P6 引用 |
| 3 | 12.3 | "结论四选一由确定性结果与通过守卫的最高严重度推导"没有规则。例如确定性结果 failed 但守卫全部降级，结论是需处理还是证据不足？ | 写成决策表（确定性 passed/failed/insufficient × 通过守卫的最高 severity × 是否全部降级） |
| 4 | 2.5 与 §10、3.4 | 2.5 明确"不再等待，先按默认实现"，§10 仍写"（待你确认）""待你决定"，3.4 写"（待你决定是否升级）" | §10 与 3.4 改为引用 2.5 的默认分支，去掉"待你决定" |
| 5 | 3.3 | "焊缝金属厚度缺失时按'打底 ≤3mm、其余为壁厚减打底'保守估算"是一条业务假设，没有来源，也没标待核 | 标"[待核]"并写来源或改为"缺失时判 evidence_insufficient" |

### 1.2 会导致返工或争论的（应改）

| # | 位置 | 问题 | 改法 |
|---|---|---|---|
| 6 | §1 表 vs §1 汇总 vs 14 章 | P11 表写 5–7 天，汇总写 5.5–7.5，14 章各行相加 8.6 天（0.1+1.5+2+0.5+1+1+1+1.5） | 三处统一为 8–9 天，总估时随之改 47–61 人日 |
| 7 | 11.2 vs 12.2 | H5 一处 2–3 天，一处"H5 合计 3 天" | 统一 3 天 |
| 8 | 4.2 vs §1 | 4.2 说 P2/P4 因 profile 已存在"各下调 0.5 天"，§1 表的 P2 4–5、P4 4–5 未改 | 改为 3.5–4.5 |
| 9 | §1 表行序 | 行序 P8、P10、P11、P9，章节序 P8、P9、P10、P11 | 按 P0–P11 排 |
| 10 | 全文 37 处 N-xx | N-01～N-44 的定义在《审计-规则覆盖》里，本文只给编号；P10 依赖栏写"横向前置 N-02～N-05"，其中 N-04、N-05 在本文没有任何解释 | 15 章前加一张 N-xx 索引：编号、一句话、落在哪个阶段 |
| 11 | 2.2 vs 2.4 | 2.2 写"改为必传"，2.4 写"先条件必传一周再改必传" | 2.2 表加"分两步"备注 |
| 12 | §0 目标 | 标题已含材料、设计与平台通用，目标句仍是"在焊接节点上" | 改目标句 |
| 13 | P4 6.2 | "焊工链（调用 P1）、工艺链（P2）"没有接口：输入什么、返回什么、谁负责把 P1–P3 的结果变成链状态 | 给 `weld_joint_ledger` 与三个工具之间的输入输出字段 |
| 14 | 2.3、P2、P3、P4 | 没有验收或测试：2.3 平台工具（用李卫伍/姜军两个身份证作固定样本即可）、P2 4 章无测试节、P3 5 章无测试节、P4 无测试节 | 各补一节"测试"，格式同 3.6 |
| 15 | 2.1 | 产出写"`docs/研究-…` 第 6 章逐条打钩"，路径省略 | 写全名 |

### 1.3 表述层面（可改）

- "预警级发现""预警"在 2.5、3.4、4.3、5.3、§10 出现 9 次，从未定义它对应哪个 severity。
- "证据不足""需人工确认""口径待确认""待核""待核对"五个状态词混用；建议只保留三个：证据不足（数据缺）、需人工确认（系统判不了）、口径待确认（业务未定），"待核"只用于法规数值。
- "2026-09-06 晚"这类相对时间改为版本号（v3）。
- 14.1 的正则放在 Markdown 反引号里，`\s` 与 `\d` 会被部分渲染器吃掉；改为代码块。

## 2. 做得好的（保持）

- 每个阶段都有"改动位置"列，落到文件、函数、YAML 键；开发者不需要再找。
- 2.4 存量迁移、2.5 默认分支、14.1 同步脚本三节是本轮新增里最能直接指导工作的部分：有输入、有输出、有失败处理、有验收。
- §0 的验收仍用真实项目、真实人名、真实编号，9 条里 7 条可以机器判对错。
- 15 章的可修复性分类与三条更正记录，让读者知道哪些结论曾经错过、为什么改。

## 3. 修订顺序

1. 先改 1.1 的 5 条（半小时）：验收第 1 条、词表映射表、结论卡决策表、去掉"待你决定"、3.3 标待核。
2. 再改 1.2 的估时与顺序（6、7、8、9）与 N-xx 索引（10）。
3. 最后补测试节与 P4 接口（13、14）。

改完 1.1 与 1.2 预期 80 分以上；补完 1.3 与测试节可到 85。
