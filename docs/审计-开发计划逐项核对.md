# 开发计划逐项线上审计

审计方式：以真实角色登录生产（凭证由服务端 env 提供，不经审计人之手），调接口、查生产库、真调用工具，
不看代码是否"写了"，只看生产上是否"能用"。凡结论均附当次实测证据。

- 审计时间：2026-09-07 夜
- 生产：http://39.108.65.148:8081，后端 HEAD 160bcac
- 底稿：计划 §18.6 共 30 项

## 结论摘要

| 结果 | 项数 |
|---|---|
| 通过 | 24 |
| 有条件通过 | 4 |
| 计划已注明未做，核实属实 | 1 |
| 记录 | 1 |

**没有一项判「不通过」**——计划里记为完成的功能，在生产上都能调起来并返回预期结果。
但有 4 项是「有条件通过」，限制已逐条写明；另有 2 项属审计新发现，不在计划底稿里。

## 需要跟进的（按轻重）

1. **按模型计价在生产上没跑过**（P8 H1）。功能 2026-09-06 上线，生产最后一次模型调用是 09-04，
   163 条已记录成本全是全局兜底价。下一次真实审查跑完后要复核 priceVersion 是否变成 per-model-pricing-2026-09。
2. **价格表缺三个模型**（P8 H1）：deepseek-v4-pro、deepseek-v4-flash、qwen3.7-max。
   deepseek-v4-pro 占历史调用 55%，缺价会按 qwen 单价错算。**本次审计已修**。
3. **采集覆盖率 1.96%**（P12 F1，目标 ≥95%）。12 条人工意见全部写在没有 AI 运行的节点上，
   属真实使用数据不足，不是实现缺陷；要靠监检人员实际用一两周才能改善。
4. **工具绑定集仍是 draft**（审计新发现）。69 条有绑定的规则里 33 条不在试点名单，
   正式审查下这 33 条的原子检查工具会被拒执行。计划里没有跟踪项，建议补一条。
5. **19 张法规表未经人复核**，靠 trustPolicy 采信。核对清单 docs/法规数值核对清单.md 共 821 格。
6. **一键分析失败率 6/16**。最后一次失败 2026-09-03，其后 5 次连续成功，看起来修复站住了，继续观察。

## 逐项记录

### N-01 R04/R05/R08 进 pilotRules

- **结论**：通过
- 实测：生产业务包 atomicCheckToolBindingSet.pilotRules 共 36 条，含 R04、R05、R08。
- 备注：compile_node_tool_plan(R04, require_published=True) 返回 4 条绑定，未被拒。

### N-18 节点 9 去掉两项错配资料

- **结论**：通过
- 实测：节点 9 当前资料要求只有 design_document，不含 leakage_test_report / instrument_calibration_certificate。
- 备注：生产审查点里那两条 id 仍在但 enabled=false、证据引用为 0（退役是停用不删除，符合设计）。

### N-34 welder_roster 降辅助

- **结论**：通过
- 实测：节点 24 的 welder_roster requiredType = 可选。
- 备注：build_project_requirements 实测。

### P0 2.2 资料类型变更

- **结论**：通过
- 实测：生产 env AICHECK_WELDING_MATERIAL_TYPES_V2=stage1，业务包 stage=stage1；节点 25 已拆成 wps/pqr/welding_process_card，节点 24 有 platform_verification（可选），节点 16/21 有 pmi_report（条件必传）。
- 备注：六条新审查点已进生产库，库内共 170 条、与资产一致 168 条、证据链接孤儿 0。

### P0 2.3 四步全表 + verify_welder_on_platform

- **结论**：通过
- 实测：runtime_tool_catalog 共 115 个工具，含 verify_welder_on_platform。真调用一次，返回结构化结论（status=succeeded，带 verdict / matchedItems / missingItems / requiresHumanConfirmation），平台回执「请输入有效的身份证号」——说明确实打到了公示平台，不是桩。
- 备注：同批确认 verify_type_test_report 不在目录里，与计划注明的「未做」一致。

### P9 R2 守卫与合并

- **结论**：通过
- 实测：POSITIVE_CLAIM_RE 的否定识别正确：「资质满足要求」命中、「资质不满足要求」不命中；「材质与设计一致」命中、「材质与设计不一致」不命中。
- 备注：生产容器内实测。

### P9 R3 输出契约

- **结论**：通过
- 实测：cap_findings 输入 15 条发现，封顶后 8 条。
- 备注：生产容器内实测。

### P8 H2 分片估算与证据预算

- **结论**：通过
- 实测：estimate_text_tokens 对 20 个汉字返回 20，CJK 逐字计数生效。
- 备注：生产容器内实测。

### P8 H5 清单填表模式

- **结论**：通过
- 实测：CHECKLIST_REQUIREMENTS 有 4 项；生产 env AICHECK_REVIEW_PROMPT_MODE=freeform，灰度开关按计划默认关。
- 备注：生产实测。

### P11 N-03 标准版本时间线

- **结论**：通过
- 实测：时间线 108 条；TSG Z6002-2026 判「现行」，GB/T 20801.1-2020 判「已废止」。
- 备注：采信策略开启后废止判定真正生效（此前未签字的条目一律按有效处理）。

### 法规数值预填（P1/P2/P10/N-16 数据侧）

- **结论**：有条件通过
- 实测：生产法规表含 8 个段落：TSG Z6002-2026、NB/T 47014-2023 母材分组、GB/T 20801 检查等级、TSG 31-2025、焊材限值、管材限值、NB/T 47014 表 5 因素。抽查：母材分组查 Q345R 得 Fe-1-2；表 5 查焊条电弧焊改电流得「重要因素」；管材限值查 GB/T 8163-2018 Q345B 得冲击 34J。
- 备注：限制：19 张表的 verifiedBy 全为空，靠 trustPolicy=accept_without_human_signoff 采信。扫描件视觉识别的数值（GB/T 5117 表 6/7、GB/T 8163 表 5~8、GB/T 14976 表 2/3）未经人复核，核对清单 docs/法规数值核对清单.md 共 821 格待勾。

### 2.4 审查点 id 重键

- **结论**：通过
- 实测：2026-09-07 22:40 生产执行：改写审查点 id 144 条、证据链接 213 条、资料挂载 184 条。迁移后生产 164 条中 162 条与资产一致（此前仅 18 条），内建 reconcile 覆盖率 18/164 → 162/164，派生字段零不一致。
- 备注：执行前全量备份 /home/dev-bjy/aicheck-backup-mrp-20260907-223645.dump（188MB）。

### P10 N-21/N-22 R13 verify_type_test_report

- **结论**：计划已注明未做，核实属实
- 实测：runtime_tool_catalog 里没有 verify_type_test_report。
- 备注：公示平台无按报告编号查询的接口，非实现遗漏。

### P9 R0 节点发现改读结构化发现

- **结论**：通过
- 实测：生产 117 次节点运行中 61 次带 findingDrafts；前端 workbenchReviewPresentation.ts 第 358 行直接读 nodeReview.findingDrafts 生成发现卡，不解析文本。线上产物里 findingDrafts 出现在 4 个 chunk。
- 备注：计划文字写的是「读 aiRun.findings」，实际字段名是 findingDrafts；库里没有名为 findings 的字段，属计划表述与实现命名不一致，不影响功能。

### P9 R1 结论卡与分组

- **结论**：通过
- 实测：117 次运行全部带 suggestion.result，取值为「建议不符合 / 证据不足 / 需人工确认 / 需补正」四选一。线上产物里「证据不足」出现在 9 个 chunk、「关键事实」1 个 chunk。
- 备注：库里 findingGroups 为 0 属正常——分组是前端呈现层（buildWorkbenchAiConclusion）算的，不落库。

### P9 R4 一键分析结果页

- **结论**：有条件通过
- 实测：生产 16 次一键分析：10 次 waiting_human_review、5 次 failed、1 次 partial_failure。失败均可归因：errorCode / errorMessage / failedFromPhase 三个字段齐全，例如 PROJECT_ANALYSIS_RUN_STALLED「长时间没有进展，已由巡检自动收敛」、SUPERSEDED_ALL_NODES_REUSED_FROM_OTHER_MODEL「未调用模型，全部节点沿用切换模型前的结果」。
- 备注：最后一次失败是 2026-09-03，其后 5 次连续成功——偶发失效的修复看起来站住了。失败率仍有 6/16，建议继续观察。

### P12 F1 采集补全

- **结论**：有条件通过
- 实测：ai_feedback 字段齐全（含 findingId / claim / rootCause / source / humanResult / suggestedResult），F2 归因接口与指标接口实测可用（登录、列表、按根因与治理状态筛选、归因写入全部返回 code=0）。
- 备注：限制：生产仅 1 条反馈记录；12 条人工意见的 aiSuggestedResult 全为空——查证原因是这 12 条都写在「该项目该节点没有任何 AI 运行」的位置上，采集代码只在有最近运行时才回填，属数据问题不是实现缺陷。采集覆盖率 1.96%，远低于计划目标 95%。

### P12 F2 分类与基准

- **结论**：通过
- 实测：GET /fde/feedback/metrics 实测返回 §17.3 全部指标；周度 triage 独立页 /#/fde-feedback-triage 已上线，线上 chunk 含「已归因」「需处理」等文案。
- 备注：橡皮图章指数为 None——盲审样本为 0，属预期（F4 抽样刚上线）。

### P11 N-02 逐管线事实

- **结论**：通过
- 实测：build_project_pipelines 对 P-2026-GDLNG-002 返回 28 条管线事实。
- 备注：生产容器内实测。

### P11 N-16/N-17 R09 设计特殊要求

- **结论**：通过
- 实测：输入「依据 GB/T 20801.1-2025，射线检测比例 20%，Ⅱ级合格」+ 一条 GC1 管线，返回 requiredInspectionLevel=Ⅰ、requiredCoveragePercent=100、coverageMeetsRequirement=false。
- 备注：该结果已反映 2026-09-07 按 GB/T 20801.1-2025 正文所做的更正（GC1 由 Ⅱ 级升 Ⅰ 级、比例 20%→100%）。

### 原子检查工具绑定集处于 draft

- **结论**：记录（非计划项，审计发现）
- 实测：atomicCheckToolBindingSet.lifecycleStatus = draft，pilotRules 36 条，有工具绑定的规则 69 条，其中 33 条不在试点名单。实测 compile_node_tool_plan(R43, require_published=True) 抛错被拒，R04 正常返回 4 条绑定。
- 备注：正式（非 advisory）审查下，这 33 条规则的原子检查工具不会执行。这是设计上的灰度门禁，但计划里没有一项跟踪「绑定集何时发布 / 试点名单何时扩到 69 条」，建议补一项。

### P8 H0 基准脚本

- **结论**：通过
- 实测：scripts/experiments/review_model_compare.py 可运行（--project/--node/--out 参数齐全）。
- 备注：尚未在生产跑过一轮（每轮约 ¥10，预算未定）。

### P8 H1 模型能力表 / 钳制 / 按模型计价

- **结论**：有条件通过（发现两处缺口）
- 实测：能力表 12 条，含生产在用的 qwen3.7-plus / qwen3.8-max / qwen3.6-flash。计价函数实测正确：qwen3.8-max 1 万入 1 千出 = 0.084 元，priceSource=table，priceVersion=per-model-pricing-2026-09。
- 备注：缺口一：**生产上这条路径还没跑过**。按模型计价是 2026-09-06 上线（提交 85d5afa），而生产最后一次模型调用是 2026-09-04，163 条已记录成本全部是全局兜底价 env-configured-model-pricing-2026-07，端到端未获生产数据证实。
  缺口二：**能力表里有、价格表里没有的模型**——deepseek-v4-pro、deepseek-v4-flash、qwen3.7-max 三个。其中 deepseek-v4-pro 是历史调用最多的模型（113/204，占 55%），若再次使用会按 qwen 的 env 单价计费，金额会错。
  **本次审计已修**：新增 UNPRICED_MODELS 显式登记「没写价的模型与理由」，成本记录多一个 unpricedReason 字段（命中价目表时为 null，回退时说清为什么），并加两条用例——能力表里的模型要么有价、要么写明原因，新增模型不会再悄悄漏价。价目本身没有编造：DeepSeek 的公开价仍待核实后补。

### P8 H3 守卫语料补齐

- **结论**：通过
- 实测：语料相关用例齐备：test_grounding_downgrade_keeps_diagnosis / test_grounding_prompt_compaction / test_grounding_supplemental_identifiers / test_p9_r2_guard_merge_opinion。
- 备注：全量 2912 用例通过。

### P8 H4 信封修复与分片级重试

- **结论**：通过
- 实测：shard_recovery.DEFAULT_ESCALATION_MODEL = qwen3.8-max，生产 env AICHECK_LLM_MODEL_REVIEW_ESCALATION 同值。
- 备注：生产未见触发记录（需要模型返回不合契约才触发）。

### P10 N-19/N-20 R12 平台核实自动化

- **结论**：通过
- 实测：r12_registry 在生产可导入，带 CnseConfigurationError / CnseProtocolError / CnseRecognitionError 等错误分型；verify_welder_on_platform 真调用返回结构化结论并带 requiresHumanConfirmation（人工任务门）。
- 备注：真调用触达了公示平台（回执要求身份证号），非桩实现。

### Workbench.vue 巨石棘轮

- **结论**：通过
- 实测：tests/test_monolith_ratchet.py 4 项全过。
- 备注：本地全量用例实测。

### M1 部署（2026-09-06 21:45）

- **结论**：通过
- 实测：生产 readyz 返回 ready:true，六项子检查全绿；六个容器在跑；当日至今已多次部署至 HEAD 160bcac。
- 备注：部署后业务链探针：弱口令拒绝 ✓、Office 预览取到 PDF ✓、表格列序 ✓（11 张表）、AI 失败可归因（探针项目无失败运行，跳过）。

### P11 N-06/N-07 R04、N-08/N-09 R05、N-10~N-13 R06/R07 事实层

- **结论**：通过
- 实测：design_facts 在生产可导入并产出 designDocumentSet / designDocuments / calculationDocuments / designChanges / drawingReviewWitness / standardReferences 等事实。
- 备注：计划已注明「完成（事实层）」——即事实产出到位，判定规则另计。

### P12 F4 盲审骨架

- **结论**：通过
- 实测：三个接口实测可用；未判任务不泄漏 AI 与常规结论（专门验证）；生产 celery beat 已注册 blind-review-weekly-sample（crontab 周一 08:00）。
- 备注：当前盲审任务 0 条，橡皮图章指数为 None——抽样任务尚未到首个周一。

