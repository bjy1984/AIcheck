# AICheck 壓力管道監檢審查系統 · 交接說明


> Lab 工具範圍修正：空任務文件清單不再讀取全部 OCR；工位 runtime 工具拒絕任務外工程、節點及文件版本。專用 planner 與標準來源鏈路仍待逐路驗收。

> Lab 全量後端回歸：3063 passed、66 skipped（本地環境），R69 明確掛載契約與巨石基線失敗均已修正；詳見 [測試紀錄](lab/verification/2026-09-08-backend-regression.txt)。跳過項不代替外部服務實機驗收。
> 2026-09-08 Lab 開發：`codex/lab-aicheck-workstations` 正在實作九工位、文件自由選取與工程規則編輯，完整範圍與驗收見 [Lab 計畫](lab/README.md)。目前未發布生產、未開放全部 69 條、未執行正式 ID 遷移。 Lab 已加入有效規則工程優先選擇及同工程／業務包發布、回滾邊界；Lab 新建任務已固定有效規則快照；規則編輯完整權限及可執行條件與工具一致性仍待完成。 執行器已支援原子項專屬參數與固定規則參數保護，編輯器及事實組裝器接入仍待辦。 Lab 已將完整 findings 與顯示摘要分開，品質驗證使用完整結果；摘要介面待接入。 已補工程規則寫入範圍、歸檔及成員檢查，規則列表、單筆、差異與預覽已加入工程讀取隔離；Lab 已接監檢工程草稿列表／建立／修改／複製 API，強制工程成員、節點與版本檢查；AIReviewB 已接工程草稿編輯介面（需前端 Lab 開關），結構化比較條件與輔助草稿試跑 API 已加入；試跑已支援單一適用條件及不適用／證據不足區分；試跑複合適用性已支援 all／any／not 三值邏輯；條件表單已接工程草稿（含複合適用條件）；草稿試跑介面已接入並強制規則版本一致；本地 Chrome 與真實 API 的草稿／試跑互動已驗證；正式工具整合、正式登入工作台验收及發布仍待辦。

更新：2026-09-08。本文件以本次實測取代先前「測試是綠的／固定八條失敗可放行／33 條工具只差名單」的描述。

## 當前狀態

- 修復分支：`codex/aicheck-repair`；審閱入口：[PR #25](https://github.com/bjy1984/AIcheck/pull/25)。各階段分開提交，具體提交以 `git log` 為準。
- 生產：本輪未部署新程式、未遷移 ID，交接記錄的版本為 `6d4c843`。本輪只讀 readyz 為 ready、六項 true；這不代表新功能已經完成生產驗收。
- 工具綁定仍為 `draft`，保留原有 36 條 pilotRules。負責人已選擇**驗收後一次開放全部 69 條**；發布節奏已確定，業務驗收尚未達標。
- stage2 未切換，生產預設保持 stage1；最早 2026-09-14 評估，另行確認。
- 付費模型跑批未執行，仍需頻率及預算上限。歷史約 ¥10／輪只可作過往估算，不能當成本保證。

## 本輪修復

| 項目 | 實際變更及限制 |
|---|---|
| Ruff／CI | 固定 Ruff 0.16.2；由 693 告警清至 289，原基線 337 收緊至 289。保留公共再匯出和字符串 monkeypatch 介面。`--update` 禁止新增告警；lint 與後端測試独立執行。 |
| 測試容器 | 測試專用衍生鏡像增加 git/zsh；`/app` 指向同步工作區，防止載入基底鏡像舊腳本。同步未忽略的新檔案、test2、標準及指定離線回放夾具；清理僅限已標記專用工作區。 |
| PostgreSQL 驗證 | 改為每項測試獨立 UTF-8 資料庫。既有不可變遷移 0003 指向 public，不能靠獨立 search_path schema 隔離。補齊測試的三版遷移預期及上傳整合測試初始化。 |
| 審查鏈路 | GC2 毒性／洩漏危害使用三態，資訊不全不得套寬鬆比例；驗收等級比較接入凍結規則。焊接方法查詢及 WPS／PQR／實際記錄別名歸一化；R14 正式綁定傳入完整產品規則；R16 傳入成分與未決條件，處理 Ti/Nb 動態下限與 Ni+Cu 合量；R26 按標準及焊材型號選限值；R23 無可讀試驗報告時為證據不足。 |
| 審查點 ID | 雜湊移除行序，保留 MRP 格式；重複業務鍵及衝突報錯。資產 168 條；ID 寫入工具使用只讀 dry-run、摘要核對及單一交易。 |
| 部署 | 預設主機 aicheck-prod-new；ID 前置檢查、API／worker 同鏡像同提交檢查；失敗非零退出。worker 溫停及日誌限制保留。 |
| 標準資料 | GB/T 14976 化學成分實際已錄 32／32，先前「6／32」為過期描述。GB/T 13296 表 3 成分已補 31／31（官方 PDF 頁 10–11，帶雜湊），硬度按合同選配，合格 ET 可依標準替代水壓；NB/T 47014 附錄 C–F 已錄原文、頁碼及 PDF 雜湊，特殊接頭圖示／公式及自動判定仍待驗收，未偽造人工核對簽名。 |

完整數字、執行結果及未完成項見 [本輪驗證記錄](verification/2026-09-08/README.md)。

## 如何測試

本機 `backend/.venv` 已能匯入 psycopg、temporalio、fitz；「任何 Python 都裝不全」不再適用。不要在正在服務的生產 API／worker 容器內跑 pytest。

```bash
cd backend
.venv/bin/python -m scripts.ruff_baseline
.venv/bin/python -m pytest --collect-only -q
.venv/bin/python -m pytest tests -q
```

整合測試需設定 `AICHECK_TEST_POSTGRES_URL` 指向專用測試 PostgreSQL，使用者必須能建立及刪除測試資料庫；不設定會有明確原因的環境跳過。不得填生產 DSN。本輪另外實跑 PostgreSQL，不把這些跳過當成整合驗收。

```bash
# 從倉庫根目錄，建立專用工作區及測試衍生鏡像
bash backend/scripts/run_tests_in_container.sh tests --collect-only
bash backend/scripts/run_tests_in_container.sh tests
```

預設主機 `aicheck-prod-new`，專用工作區 `/tmp/aicheck-tests-repair`。不要同時在同一工作區啟動兩輪同步／測試。權限、同步或測試失敗即停止，沒有「固定失敗數量可接受」模式。

GitHub `backend-quality.yml` 的 lint 和 test 是獨立工作，任何工作失敗都不能宣稱 CI 通過。test 工作包含 PostgreSQL、MinIO 保留版本及服務重啟、Temporal 重試與服務重啟驗證。

## 如何部署及遷移

```bash
bash backend/scripts/deploy_to_server.sh --backend
bash backend/scripts/deploy_to_server.sh --frontend
# 不帶參數為全量；可用 AICHECK_DEPLOY_HOST 覆蓋主機
```

本分支包含新的 ID 資產，普通後端部署會先執行只讀 ID 檢查；需要遷移即停止，不能跳過。正式操作見 [ID 維護窗口操作單](verification/2026-09-08/material-id-maintenance.md)。工具發布與 ID 遷移分開部署。

本輪生產只讀快照顯示：168 個 ID、304 條證據連結、262 筆掛載需改寫，沒有孤兒；兩個無引用的停用項已列明排除原因。窗口內必須重新 dry-run、停止相關寫入並處理在途任務、完成伺服器端備份及恢復驗證，才能正式遷移。歷史 AI 執行快照不改。

ID 現在能承受插行、刪除其他行及重排，但修改該條審查內容仍可能改 ID，須走映射遷移。回滾必須同步恢復資料庫、程式及鏡像，不能只回滾程式。

憑證僅放伺服器 `/home/dev-bjy/aicheck-secrets.env`，不得拷到本機、提交或貼入對話。runtime env 由 `backend/deploy/build_runtime_env.py` 生成，不手改。

## 發布及後續門檻

[69 條驗收矩陣](verification/2026-09-08/review-release-matrix.md) 已逐條列出必要事實、註冊、空輸入結果及通用工具缺口。194 個原子檢查都可編譯，但原受限規則中 28 條含未配置 ruleChecks 的通用解釋器；四情境與證據定位尚未逐條驗收。**禁止只改 lifecycleStatus 就聲稱完成全量業務發布。**修復缺口後更新綁定版本、發布狀態並保留試點回滾清單；不自動重跑歷史任務。

- GB/T 13296 化學成分已按官方表 3 補齊並接入 R16；沿用既有採信策略，`verifiedBy` 仍留空供人工覆核，沒有借用其他管材標準限值。
- 浏览器验收：Chrome 已開啟登入頁，等待登入監檢測試帳號；尚未驗收結論卡、工程結果、證據定位及整改流程。
- P4／P8：施焊記錄未到位，焊口聯合表及 H6 保留阻塞。
- 外部平台查不到資料仍為證據不足，保留查詢紀錄及人工比對，不推定不符合。
- 修改業務規則時同步本文件及 `docs/优化计划-焊接节点.md`。

### 任務文件範圍快照（Lab）

- 新工位任務保存 documentScopeSnapshot，固定工程、節點、業務包與文件版本 ID 清單，雜湊納入 inputHash。任務清單從上游 AI 輸入深複製，避免後續修改共享清單。
- 工位快照讀取與 OCR 選取會驗證文件範圍；清單增刪、身份改動或快照內容與雜湊不符時拒絕繼續。既有無快照任務維持歷史契約。
- 分片保留原任務，在 context 中縮小文件選取；測試確認不會擴張至清單外文件。此雜湊是完整性校驗，並非防管理者重算的數位簽章。
- 文件範圍、工位、規則快照及分片執行回歸 41 passed；Ruff 289 / 289。本批未重跑完整後端。
- 尚待完成檔案內容／OCR 與人工修正版本固定、專用 planner 全路徑驗收及文件選取介面，不代表 W2/W4 已完成。

### OCR／人工修正變動檢查（Lab）

- 新工位任務在文件範圍快照加入 sourceFingerprint，涵蓋已選文件的 OCR 記錄及本工程／節點有效人工修正，並經快照雜湊納入 inputHash。
- 通用 OCR 選取前校驗來源。OCR／修正變動、停用修正或新增解析結果時，以 review_document_sources_changed_recreate_run 阻擋舊任務讀取；重取既有任務不更新指紋，新任務使用當前資料。
- 未選文件及其他工程的修正不影響指紋。當前按完整記錄及既有順序計算，相關記錄僅元資料變動也可能要求重建任務，屬保守處理。
- 單元／工位／快照／分片／巨石基線 51 passed，任務建立契約 2 passed；Ruff 289 / 289，diff check 通過。未重跑完整後端。
- 此批提供變動偵測，尚非歷史輸入重放：完整 OCR／修正版本儲存、物件內容核驗、專用 planner 路徑及對使用者的重建提示仍待完成。無生產部署。

### 輸入變動的正式流程失敗處理（Lab）

- 來源檢查接入正式執行：專用前置審查之前、每個 graph step 之前及 graph 完成後檢查；來源或文件快照不一致轉為 REVIEW_INPUT_CHANGED_RECREATE_RUN，非自動重試錯誤。
- R12／材料等前置審查納入既有統一失敗處理，使失敗狀態、AI 任務錯誤碼、事件及原節點狀態恢復沿用同一流程。已完成／待人工任務仍保留既有返回契約。
- R12／R14／R24 的變動輸入契約測試確認前置審查未呼叫、無新發現、任務失敗與節點狀態恢復；合計相關契約 5 passed。工位／來源／分片／重試／巨石 52 passed；另前置人工核驗及 inline 持久化與重試批次 15 passed（與前批有重疊）。Ruff 289 / 289，diff check 通過。
- 巨石檢查首輪超出 1 行，已抽出 failure_policy.py 並保留 execution.py 相容再匯出，未提高基線。
- 尚未完成使用者介面的專用提示、可重放的完整版本存儲與全後端回歸；中途已保存的草稿保持失敗任務留痕，不作正式通過結論。本批無生產部署。

### 工作台輸入變動提示

- AIReviewB 取得失敗任務的 REVIEW_INPUT_CHANGED_RECREATE_RUN 時，在目前上下文顯示不可關閉的原因提示與「按當前資料重新發起」按鈕文案；目前任務狀態不再被聊天完成覆蓋。
- 沿用既有 requestAiRecheckApi 與每次新 idempotency key，建立使用目前資料的新任務；不使用保留舊快照的 replay。按鈕仍遵守工程／節點 canStartReview 權限及原有正式／缺項預審選擇。
- 成功、執行中任務即使帶舊錯誤碼也不顯示重新發起提示；普通失敗維持原有流程。聊天執行動態仍獨立呈現。
- 新狀態判斷、既有停滯及任務綁定測試通過，全前端 vue-tsc、修改檔 ESLint、diff check 通過。Stylelint 12 errors / 3 warnings，與 HEAD 修改前 stdin 檢查完全相同，皆為未修改的 CSS；未宣稱 Stylelint 通過。
- 本批未做登入工作台的瀏覽器驗收；整體工位介面、自由選文件及完整輸入重放仍待完成。

### 最新完整回歸與契約修正

- 本次以 4cac73d1 為起點，收集 3196 項。首輪 3127 passed / 3 failed / 66 skipped：新增工程規則 API 未更新 OpenAPI 工件，以及部署報告無法追蹤函式內匯入的幂等委派、未列明只讀草稿試跑。
- 工程規則路由改用模組匯入，保留實際既有幂等委派；只讀 trial 列入精確路徑豁免，建立／編輯／複製仍須通過幂等檢查。更新生成 OpenAPI，確認無既有路徑／schema 名稱被刪除。部署與契約 40 passed。
- 第二輪在 fault-proxy 單元測試的真實網路轉發等待，人工中止（1703 passed / 20 skipped，非完整通過）。該測試改用 MockTransport 驗證一次 429 後一次成功轉發及上游呼叫次數，避免使用真實 socket／環境代理。相關 15 passed，不替代實機故障注入。
- 最終完整後端：3130 passed / 66 skipped / 6 warnings，168.40 秒，退出碼 0。原先三項失敗逐項消除，未按失敗總數放行。Ruff 289 / 289。
- 前端單元測試 84 個檔案全部通過。清理工作台既有 CSS 次序／註解告警，Stylelint 與 ESLint 通過。減少動畫及響應式覆寫保留必要 cascade 順序、精確註明 order/order 例外；Chrome 1200px／1600px 及兩種減少動畫設定與修改前結果一致。重跑腳本 frontend/e2e/lab-rules/check-workbench-css.mjs。
- 完整原始紀錄在 docs/lab/verification/regression-4cac73d1/。選擇性跳過仍不算 PostgreSQL／MinIO／Temporal 的實機驗收；沒有付費模型評估、正式 69 條發布、ID 遷移或生產部署。

### 以任務 OCR 資料試跑工程規則

- 工程規則 trial API 可傳 reviewRunId，與手填 facts 互斥。伺服器檢查工程、節點及業務包；只接受有來源指紋的文件快照，沿用範圍與來源變動驗證讀取 OCR／人工修正。
- 按條件欄位名稱精確匹配，保留零值、單位、文件版本、欄位 ID、頁碼、bbox 與 correctionId；不隱式把字串轉數字或轉換單位。缺欄位／定位或多筆同名欄位均輸出 factDiagnostics 並維持證據不足，多筆相同值也不假設同一焊口或材料批次。
- 回應帶 sourceMode=run_ocr、sourceReviewRunId、sourceSnapshotHash，仍是 advisoryOnly=true / evidenceVerified=false：来源定位不是對真實性或法規適用性的人工背書，不保存正式結論，不發布規則。
- 新取證、條件、文件快照、部署及 OpenAPI 回歸 98 passed；試跑 API 3 passed；Ruff 289 / 289，diff check 通過。本批未重跑完整後端，上一批 3130 passed 為修改前紀錄。
- 前端任務來源選擇、對象／欄位映射、正式工具整合仍待完成。此入口可供後續真實案例離線比對，不代表 W3/W4/W10 已完成。

### 任務 OCR 試跑介面

- 工作台將目前 reviewRunId 傳入工程規則編輯器；試跑可選手填示例或目前任務 OCR，沒有任務時停用該選項。請求型別互斥，OCR 模式不送出手填 facts。
- 切換來源／任務清除舊結果，來源任務與欄位診斷以文字顯示；未保存或執行中不重複試跑。缺快照、來源變動提供重新發起審查提示。
- 本地 Chrome＋真實 API 通過原有手填案例、鍵盤開啟來源選單、OCR 成功判定、來源快照回應、來源切換清除結果、缺欄位診斷及未保存保護，無瀏覽器 runtime error。截圖 docs/lab/verification/browser/rule-trial-run-ocr.png。
- 測試初版因夾具缺 tenantId，在請求正規化後觸發來源變動阻擋；已在建立測試快照前補齊夾具，未放寬生產來源校驗。Element Plus 選單內 input 被 placeholder 覆蓋，驗收改用正常鍵盤 Enter 開啟，未強制點擊。
- vue-tsc、修改檔 ESLint／Stylelint、diff check 通過。獨立測試 API／Vite 已停止。此為元件與 API 驗證，完整登入工作台及任意歷史任務選取尚未驗收；正式條件工具整合仍待完成。

### 結構化條件的全入口驗證與發布邊界

- 確認目前 compile_business_rule_execution 仍編譯文字流程，正式 atomic 工具沒有採用 executionConditions。新增條件不能只因試跑成功就宣稱已成為正式判定。
- 抽出 rule_mutation_validation.py，保留 routes.rule_project_mutation_error 再匯出介面；平台與工程建立／修改／複製均驗證結構化條件。非法運算／schema 不寫入規則。
- 發布與回滾目標有 executionConditions 時，在任何規則狀態改動前阻擋，明示正式工具尚未接入。這是開發中的必要發布門檻，不是永久取消自由修改規則；正式條件適配、原子項映射與四態驗收完成後必須替換為實際能力驗證。
- 新增平台建立／編輯／複製及發布／回滾測試，確認拒絕後整份規則清單不變。相關契約 18 passed；条件／部署報告／巨石基線 75 passed；Ruff 289 / 289，diff check 通過。未重跑完整後端，無生產發布。

### 條件與原子項綁定編譯

- 条件 checks 可指定 atomicCheckId；開始綁定時，所有判定條件都須明確對應同一節點的有效原子項。多條條件可共同取代一個原子項，未取代項以 retainedAtomicCheckIds 保留，不能因自訂門檻漏掉其他審查。
- 編譯核對業務包、單一節點、原子項唯一性及基礎工具綁定完整性。跨節點、缺失、重複基礎綁定拒絕；保存入口使用同一驗證。
- 帶映射規則的 trial 回傳 bindingPlan，含 replacements、條件／基礎綁定雜湊與 planHash；副本不修改草稿或配置。既有無映射草稿仍可試跑。
- 明確 formalExecutable=false，發布門檻維持：正式執行器、前端原子項選擇、完整版本驗證及四態驗收仍待接入。此批不宣稱門檻修改已作用於正式審查。
- 條件／映射／試跑 API 56 passed；部署與巨石基線 38 passed；Ruff 289 / 289、diff check 通過。未重跑完整後端，無生產操作。

### 條件替代執行器接入

- 新 condition_execution 從 effectiveRuleSnapshot 編譯原子項替代，使用固定文件範圍／來源校驗後的 OCR 與人工修正，四態結果轉為現有工具契約並保存完整條件結果、引用、規則／來源／計畫雜湊。
- 通用 run_rule_engine 接入替代執行：被替代項不呼叫舊工具，其餘項依原順序執行；整批核對原子項集合與參數，缺任何保留項即拒絕，不允許部分計畫偽裝完整。既有無條件快照路徑保持原執行契約。
- 工程草稿 ID 與 R24 等基礎規則 ID 分離，由節點原子目錄確定唯一 sourceRuleId，避免新草稿 ID 找不到基礎工具。R19 專用語義結果若遇條件替代，明確拒絕 pending adapter，避免静默忽略條件。
- 四態、凍結門檻、資料變動、未知／缺失原子項、舊流程及真實 run_rule_engine 分支測試通過；R24 整合測試保留五項結果並使用條件替代第一項。條件執行／既有工具／巨石 37 passed，映射／工位／規則及文件快照 51 passed；最後局部重驗 14 passed，Ruff 289 / 289。
- 發布仍阻擋：專用語義路由整合、完整 raw capture／工具註冊契約、業務適用性驗收及前端映射尚未完成；不能把程式中的替代執行等同全部規則已正式可用。本批未做全後端回歸或生產部署。

### 條件替代工具的原始留痕

- prepare_condition_results 使用既有 Raw Vault 保存 evaluate_saved_conditions 的完整條件、實際 facts、診斷、計畫雜湊與完整結果；請求／結果共用 toolCallId，原子結果帶 requestEventId／resultEventId。
- 判定例外保存 tool.call.failed 後繼續拋出。已配置留痕儲存而請求或結果保存失敗時，拋出 CONDITION_TOOL_CAPTURE_FAILED 系統錯誤，不返回成功條件結果；沿用既有服務錯誤重試策略。
- 未配置資料庫留痕的本地模式明示 rawCapture.status=not_configured，不聲稱已完成原始留痕；不因此解除條件規則發布保護。
- 條件與工具留痕 15 passed，Raw Vault／既有工具／巨石及重試 38 passed；包括原始資料核對、事件鏈驗證、保存失敗及判定例外。Ruff 289 / 289、diff check 通過。
- 尚未驗收 PostgreSQL／MinIO 實機留痕、工具註冊及專用語義路由；未重跑完整後端或部署生產。

### 原子項綁定選擇介面

- 工程規則列表 API 附帶依工程／節點權限過濾的 atomicChecks（ID、名稱、節點）；前端再限目前節點。受限 R24 成員僅取得該節點五項。
- 規則編輯器每個條件可選取要取代的原子項，支援搜尋／清除，清除會移除欄位而非保存空 ID。開始綁定後所有條件需明確對應的要求已提示；切換工程／節點會清除選項。
- 試跑結果顯示替代項數及保留項數，保留尚未發布提示。前端型別支援 atomicCheckId 與 bindingPlan。
- 本地 Chrome＋真實 API 驗證選取 R24-01、無 R25 選項、保存 ID、試跑取代 1 項／保留 4 項，以及原有 OCR／缺欄位／未保存流程；截圖已更新。權限與工程規則 API 11 passed，vue-tsc／ESLint／Stylelint／Ruff／diff check 通過。
- 本次獨立 API／Vite 測試服務已停止。完整登入工作台、專用語義路由及正式發布驗收仍待完成，未部署生產。

### R19 語義結果與條件替代合併

- R19 run_rule_engine 不再遇條件就拒絕：指定項使用凍結條件結果，未取代項保留原語義判定、證據引用與解釋，再用固定聚合器重算節點結果；訊息與 executionMode 明示混合來源。
- 合併驗證完整預期原子項集合；保留項缺失、重複或混入其他項目直接拒絕。原 r19SemanticReview 快照維持原貌，最終合併結果另存 rule_check_results。
- R19 八項結果的符合／不符合／證據不足／不適用整合測試通過，包含舊節點總結不再覆蓋新結果、來源引用保留及歷史不改寫。條件／巨石 25 passed，既有 R19／工具／原始留痕 32 passed；Ruff 289 / 289、diff check 通過。
- 前置 R19 語義規劃仍產生全量候選，需要繼續按保留項縮小判定／人工提問範圍；本批只完成結果層整合，不宣稱已避免前置重複工作。完整流程、實機留痕和發布驗收尚未完成，條件發布保護仍保留。


### R19 前置語義與人工提問範圍

- 前置模型上下文、提交要求、停用／失敗／未完成時的人工提問，統一從任務凍結的有效規則編譯保留項。模型提交驗證由伺服器重新計算集合，不接受模型自行縮小範圍；缺項、額外已取代項仍拒絕。
- 全部八項由條件取代時不呼叫語義模型、不建立人工提問；正式引擎仍須執行八項條件並聚合，不能因語義項為空跳過判定。無條件快照的舊任務仍要求完整八項。
- R19／條件執行／巨石基線 38 passed，含部分取代後的模型提交、人工回退、漏項拒絕及全部取代後的實際規則引擎；Ruff 289 / 289，無新增告警。
- 專用 planner 的文件讀取與 runtime context 仍須補齊：已發現 R19 本地上下文在空文件清單時可能讀取全庫，且 runtime 呼叫未傳 reviewRun，列為下一批修復。完整來源重放、實機留痕、業務驗收與發布門檻仍未完成；條件發布保護保持。
- 本批完整後端回歸：3179 passed / 66 skipped / 6 warnings，264.42 秒，退出碼 0；原始紀錄 docs/lab/verification/r19-semantic-scope-backend.txt。環境跳過不代替 PostgreSQL／MinIO／Temporal 實機驗收。


### 2026-09-09 R19 專用資料讀取邊界

- R19 前置上下文使用共用 selected_parse_results，只讀任務 inputDocumentVersionIds；空清單返回空資料，不回退全庫。讀取同時驗證固定文件／來源，套用本工程本節點有效人工修正，證據保留 correctionId，原始 OCR 不改寫。
- R19 runtime 讀取及語義驗證呼叫傳入 reviewRun，沿用執行端文件邊界。模型呼叫前後、每個工具之前檢查來源；中途變動轉為 REVIEW_INPUT_CHANGED_RECREATE_RUN 並記錄模型嘗試失敗，向既有流程失敗處理拋出，不降級為一般人工補答。
- R19／runtime／文件快照／條件執行／巨石基線 72 passed，涵蓋空清單、範圍外資料、人工修正留痕、來源變動及實際 runtime 讀取。Ruff 289 / 289、diff check 通過。本批未重跑完整後端；前批 3179 passed 為修改前紀錄。
- 本批修復 R19 專用路徑，未宣稱所有專用 planner 已驗收。完整來源重放、其他工位資料鏈路、服務實機驗收與發布門檻仍待完成；無付費模型呼叫或生產操作。


### 2026-09-09 材料專用取證共用文件範圍

- 將 OCR 選取／欄位修正抽出為 libs/review_input_data.py，避免專用判定工具與 runtime 註冊互相匯入。runtime_tools 保留 selected_parse_results 及 apply_field_corrections_to_parse_results 相容匯出。
- R12 許可證、元件表與質保書，以及 R13–R18 設計／材料／檢驗事實組裝，統一使用任務文件範圍與來源校驗；空清單不再讀取全庫。R19 使用同一模組，移除循環依賴所需的局部匯入。
- 人工欄位修正依既有工程／節點／文件限制套用，不修改原 OCR；此批未新增表格儲存格修正或完整歷史重放能力。
- 相關回歸 144 passed，包含七個節點的空清單、指定文件排除外部資料、固定來源變动，以及元件質保書使用人工修正。Ruff 289 / 289、diff check 通過。
- 尚未宣稱所有模型規劃／外部查詢路徑均完成版本驗收；全量規則、工位交接、文件選取介面與正式發布仍待完成，無生產操作。
- 本批完整後端回歸：3191 passed / 66 skipped / 6 warnings，334.03 秒，退出碼 0；原始紀錄 docs/lab/verification/material-input-scope-backend.txt。環境選擇性跳過不代替服務實機驗收。
- 接續核對 design_facts 的任務文件選取與 certificate_facts 的證書選取；兩者仍有空清單擴張讀取的路徑。工程管線總覽是工程範圍入口，須與任務取證分別處理。


### 2026-09-09 設計／證書與管線取證範圍

- 設計文件與證書取證使用共用讀取器，空文件清單不擴張。證書還須屬於指定工程；正式 merge 傳入 reviewRun，校驗來源並套用人工欄位修正，保留既有 build_certificate_facts 呼叫介面。
- 帶 documentScopeSnapshot 的任務，設計計算與合併管線事實只使用選定文件；既有非空管線／級別不再跳過範圍校驗，空輸入產生空管線與零數量。工程總覽及無快照歷史任務維持工程管線查詢契約。
- Lab 證書合併以本次取證覆寫對應欄位，包含空證書結果，避免舊證書覆蓋已移除的文件；深複製保護傳入的既有結果。歷史模式保留原合併順序。
- 證書／設計／管線／工位／文件快照／巨石回歸 63 passed，涵蓋跨工程證書、空清單、人工修正、來源變動、未選管線變動不影響結果，以及舊事實不污染新的固定輸入。Ruff 289 / 289、diff check 通過；本批未重跑完整後端，前批 3191 passed 為修改前紀錄。
- 仍需建立跨工位事實交接與固定上游版本，使用者需將本次需要的管線資料納入輸入，不能依賴未固定的工程全庫補讀。完整版本重放、全量業務與服務驗收仍待完成；無生產部署。


### 2026-09-09 清單題目採用凍結規則

- 發現清單模式原先只從業務包原始原子項產生題目，可能在條件工具採用新門檻後仍要求模型按舊門檻審查。build_review_prompt_parts 現傳入 effective_rule_snapshot。
- 清單編譯使用同一條件綁定驗證器，被替代項改為解釋凍結條件工具結果；保留項題目與資料要求完整保留，原子項引用使用本次有效規則 ID／版本／嚴重度，無快照時維持既有行為。範圍錯誤拒絕，深複製保護原規則。
- 實際工位 prompt、清單、條件執行及巨石基線 57 passed；Ruff 289 / 289、diff check 通過。本批未重跑完整後端。
- 尚未完成清單輸出的不適用狀態及模型與確定性工具衝突的強制檢查；目前指令一致性不代表模型一定遵守。條件規則發布保護仍維持，不能視為自由修改規則已正式驗收。


### 2026-09-09 清單不適用與條件工具一致性

- 清單輸入／輸出新增不適用與 not_applicable 別名，草稿保存 checklist_not_applicable，提示要求引用適用性證據；沒有引用時降為證據不足，不發出整改要求。需人工確認仍作為獨立狀態保留。
- 新 checklist_consistency 從凍結規則編譯替代項，要求每項只有一份成功條件工具結果，核對原子結果、工具名、計畫／規則／來源雜湊及文件快照。缺失或版本不符拒絕使用。
- 替代项必須在模型清單中恰好出現一次，判定與工具一致；缺證據、證據守衛降級或移除結果時拒絕生成此批草稿。原工具結果不改寫。無條件替代的既有清單維持原流程。
- 清單／工位／條件執行／巨石回歸 71 passed，含四態、錯誤判定、缺失／重複、舊雜湊、證據缺失／降級／移除及不適用別名；Ruff 289 / 289、diff check 通過。本批未重跑完整後端。
- 此批是條件替代項的結構化清單一致性，未宣稱自然語言說明／extraFindings 無矛盾，或自由模式、所有專用工具已具備同等強制檢查。發布保護仍維持，完整業務／服務驗收未完成。


### 2026-09-09 條件清單引用與工具證據對齊

- 清單替代項除判定／版本一致外，逐筆核對模型引用與該條件工具實際 evidenceRefs 的文件版本、頁碼、bbox；模型提供 fieldId／correctionId 時也須一致。
- evidenceLinkId 可從本次 groundingInput 解析；未知連結、連結與明示位置衝突、引用其他文件／頁面／欄位／修正均拒絕。模型至少提供必要引用後，草稿使用工具完整證據副本，保留所有來源與人工修正編號，再經既有 grounding guard 驗證。
- 清單／工位／條件執行／巨石回歸 80 passed，新增六類錯誤引用、連結解析、衝突、完整引用保留及真實 grounding guard 整合。Ruff 289 / 289、diff check 通過；本批未重跑完整後端。
- 這是已判定條件的引用對齊，不代表自然語言說明或其他自由模式發現已經與工具完全一致。發布保護維持，完整規則／服務驗收及其他工作包仍待完成。


### 2026-09-09 本次審查文件選取接口

- Lab ai-recheck 可明確傳入 inputDocumentVersionIds（1–500 個不同版本）；使用既有帳號／工程／節點／租戶文件可見性檢查，無權或不存在的版本以相同錯誤拒絕。未提供欄位時沿用原選取方式，未啟用 Lab 時拒絕明確選取，不靜默忽略。
- 在脫離持久資料的可見證據集合中依所選版本重新計算 readiness。補充文件可以缺項預審，仍須由既有已確認證據滿足正式審查；不把選取動作當作掛載、人工確認或必傳滿足。
- 明確選取一路傳入證據 snapshot／manifest／shards，避免建立證據包時再次用累積節點掛載覆蓋輸入；保存 selectionMode=run_only，保留 OCR／文件雜湊，無 OCR 時保留空解析資訊，不捏造證據。
- 實際 API 測試通過補充文件入任務與證據快照、永久掛載不變、無原掛載證據混入、正式審查阻擋、跨工程／租戶／未知／重複／空清單拒絕，以及 Lab 關閉時拒絕。API 相關 3 passed，證據包／分片／文件範圍／巨石 41 passed；Ruff 289 / 289、diff check 通過。
- 測試曾誤把監檢可見的未提交文件當作不可見，已按現有權限契約改為跨租戶案例；未收緊使用者原有文件權限。前端請求型別加入可選清單，選取視窗、搜尋預覽、頁碼與保存掛載操作仍待接入；尚未宣稱 W2 介面完成。本批未重跑完整後端或部署生產。
- 前端全量 vue-tsc 型別檢查通過；此批未修改視覺介面，未宣稱已完成文件選取瀏覽器驗收。


### 2026-09-09 工作台本次文件選取介面

- Lab 工作台新增本次文件選取入口（含嵌入模式），使用既有工程文件可見性 API，搜尋、分頁、跨頁批選、移除、取消保留及恢复節點預設；明示保存後只用所選版本，不修改永久掛載。無本體版本不可選，待識別狀態明確顯示。
- 可選缺項預審／正式復核；明確選取時以 canManageEvidence 身份權限開啟發起操作，避免被舊節點 readiness 擋住補充文件預審，正式 readiness 仍由後端依本次選取驗證。確認後傳入固定版本 ID；確認期間若切換工程／節點則中止舊發起。
- 切換上下文清除選取並關閉對話框，搜尋／預覽忽略舊回應。預覽讀取詳情後核對版本；PDF／圖片有內嵌入口，其餘格式顯示資料庫查看提示。
- 獨立 Chrome＋真實 API 完成搜尋／跨頁保留／缺本體禁止／取消／載入失敗保留／批選／模式／節點重設／預設恢復與不支援預覽提示。已檢視截圖 docs/lab/verification/browser/document-picker.png，無瀏覽器 runtime error。測試初版缺 isCurrent 導致本體檢查正確拒絕，已補夾具，未放寬產品檢查。
- 全前端 vue-tsc、84 個單元測試檔案、修改檔 ESLint／Stylelint、diff check 通過。此為元件與 API 聯合驗收，完整登入工作台發起操作、實際 PDF／圖片、手機、頁碼範圍、歷史版本選擇及保存永久掛載仍待驗收或開發，W2 尚未完成。無生產操作。


### 2026-09-09 掛載版本號與提交繼承

- 保存掛載的 versionNo 現取實際 documentVersionId，避免指定舊版卻顯示最新版號。保留原 API 的歷史版本掛載能力，不自動改成目前版本。
- 掛載提交繼承按指定版本核對：其他版本的已提交掛載不授予本版提交資格；資料池既有已提交狀態只適用目前版本。未指定版本的資料級查詢維持原契約。
- 舊版掛載仍不自動建立目前版本的正式證據連結；保存補充文件不等於滿足必傳要求。資料池提交狀態仍沿用既有資料級模型，本批未新增逐版本提交歷史。
- 人工掛載／組織權限／巨石回歸 147 passed；既有掛載及 R69 API 回歸 8 passed；Ruff 289 / 289、diff check 通過。含舊／新版與已／未提交交叉案例、版本號及不產生錯版證據檢查；未重跑完整後端。
- 這是 W2 永久掛載介面的後端前置修復；保存介面、重複掛載處理、頁碼及歷史版本選取仍待完成。無生產部署或歷史資料改寫。


### 2026-09-09 文件選取保存補充掛載介面

- Lab 文件選取預設仍僅本次使用；明確勾選「同時保存為本節點補充資料」才呼叫既有監檢掛載 API，傳所選版本、工程 etag 與冪等編號。沿用監檢資料 usage，不自動指定 requirementId、替換原掛載或提交未提交版本。
- 保存成功才更新本次選取並要求工作台刷新；失敗保留選取與提示。保存中停用取消、關閉及選取修改；主畫面切換工程／節點後忽略舊保存回應，不把選取帶入新節點。已送出的原節點寫入不因切換而撤銷。
- 同一元件生命週期內，相同工程／節點／版本集合沿用操作編號；真實 API 模擬保存後遺失回應，再重試只新增一筆掛載。尚未解決重新載入頁面後另一次操作與既有掛載的業務去重；嚴格 If-Match 衝突仍須刷新，不盲目覆寫。
- 本地 Chrome＋真實 API 驗證明確勾選、固定版本、補充要求、錯誤保留、回應遺失重試、重新開啟恢復預設及保存中切換節點；原文件選取瀏覽器回歸通過。截圖 document-binding-retry.png 已檢視。全量 vue-tsc 與修改檔 ESLint／Stylelint 通過。
- 此為測試帳號傳輸、記憶體 API 的元件驗收，未啟用嚴格 If-Match；完整登入工作台刷新、頁碼、歷史版本選取及跨頁面去重仍待驗收／開發。無生產部署。
- 本批前端單元測試 84 個檔案通過、0 失敗，diff check 通過；獨立 API／Vite 測試服務已停止。


### 2026-09-09 重複保存掛載去重

- 保存 API 按工程、節點、文件、固定版本、requirementId 與 usage 比對既有活躍掛載；完全相同時返回既有 ID，同批重複項亦只返回一次。重新載入頁面使用新冪等編號仍可沿用掛載。
- 沿用掛載不改提交／需補正／已通過狀態，不補建或重新確認已駁回證據，也不重設節點狀態。不同版本、要求或用途保持獨立；撤回／作廢／刪除／解除及未知狀態不作為活躍掛載沿用。不清理歷史重複資料。
- 人工掛載／權限／巨石回歸 152 passed，既有掛載／R69 API 回歸 8 passed；Ruff 289 / 289，diff check 通過。Chrome＋真實 API 驗證保存、回應遺失重試、整頁重載後重選保存及上下文切換，重载後仍為相同單筆 ID。
- 本批未執行完整後端回歸；去重為既有 repository 寫入路徑的業務比對，不宣稱已有資料庫唯一約束或多程序同時寫入驗收。完整登入工作台、嚴格 If-Match 並行操作、頁碼／歷史版本選取及其餘 W1–W13 工作仍待完成。測試服務已停止，無生產操作。


### 2026-09-09 文件選取與掛載整合後完整回歸

- 驗證程式版本 4756d97c，測試期間未修改程式或測試檔。先 collect-only 收集 3298 項成功，再完整執行 backend/tests：3232 passed / 66 skipped / 6 warnings，134.71 秒，退出碼 0。原始紀錄 docs/lab/verification/document-bindings-backend.txt。
- 此結果涵蓋最近的設計／證書資料範圍、凍結規則清單、條件工具與引用一致性、本次文件選取 API、掛載版本及去重修改。Ruff 289 / 289，無新增告警；不把測試通過等同 69 條業務驗收或品質提升已量測。
- 66 項環境選擇性跳過仍不作為 PostgreSQL／MinIO／Temporal 實機驗收。前端沿用上一批實際結果，本批未重跑前端或付費模型，也未部署生產。
- 接續 W2 歷史版本預覽已核對：document_detail 列出各版本但 preview／download 仍使用目前版本；original 端點亦讀 current_version，本機原文會按檔名尋找 Scan 備用檔。下一批需提供指定版本讀取、工程／文件／版本權限核對、版本專屬 storageKey 與檔案型別，禁止舊版缺失時回退到最新版或同名檔；驗收至少含新舊內容不同、未知／跨文件版本、舊版儲存缺失及非目前版本讀取。此項尚未實作。


### 2026-09-09 指定版本原文讀取

- 原有工程文件 original API 新增可選 versionId；指定時核對工程文件讀權、版本所屬文件及租戶，再只讀該版本 storageKey。未指定時維持既有目前版本入口。
- 新讀取模組不使用目前版本簽名網址，也不按 Scan 同名檔回退。所選本機或物件檔案缺失時返回 NOT_FOUND；版內 fileName／fileType 優先，沒有 MIME 時按版內檔名／儲存名稱推定，未知用二進位類型。支援 inline／attachment，本機沿用既有圖片轉換能力。
- 組織／版本權限與巨石回歸 147 passed，既有原文／預覽 API 回歸 15 passed；Ruff 289 / 289、diff check 通過。新測試實際讀取不同新舊本機內容，驗證檔名與 MIME、缺檔不回退、未知／跨文件／跨租戶拒絕；模擬物件串流核對所選 bucket/key 及無權時不呼叫儲存。
- 尚未接上歷史版本選擇視窗，PDF／圖片瀏覽器與 MinIO 實機仍待驗收。僅有外部 HTTP 或 mock 位址、沒有可讀版內本機／物件位置時明確不可用，不回退到最新版。未增加完整歷史重放或變更既有檔案儲存政策。
- 本批未重跑完整後端；前批 3232 passed 為修改前紀錄。無生產部署，其他工作包繼續保留。


### 2026-09-09 歷史版本選擇與預覽介面

- Lab 文件列新增版本入口，列出目前／歷史版本、版號及版本檔名；可分別勾選不同版本，缺本體 hash 的版本不可選。已選標籤顯示版號，沒有版號時顯示固定版本 ID；保存／重新開啟仍保留相同版本。
- 歷史版本預覽用帶既有身份資訊的 Blob 請求呼叫 original?versionId=...，支援圖片／PDF 內嵌。JSON 錯誤不當作檔案顯示，缺原文明確提示、不改讀其他版本；關閉／切換／卸載釋放 Blob URL，忽略過期非同步回應。
- Chrome＋真實 API 驗證選取舊版、實際 PNG 位元組解碼、缺本體停用、原文不可用提示，以及保存後固定 ID／版號；原文件選取及掛載／重試／重載去重瀏覽器測試也通過。截圖 document-versions.png 已檢視。測試傳輸加入 Blob 支援，橋接保留實際 Content-Type，本機原文使用獨立臨時目錄夾具。
- 全量 vue-tsc、84 個前端單元測試檔案、修改檔 ESLint／Stylelint、diff check 通過。此批未改後端產品程式或重跑完整後端。
- 尚未驗收真實登入工作台、PDF 內容呈現、手機／大型檔案、頁碼範圍與完整歷史輸入重放；不代表 W2 已完成。獨立測試服务已停止，無生產部署。


### 2026-09-09 審查输入版本本體與租戶核對

- 明確 inputDocumentVersionIds 不再只憑文件 currentVersionId 通過；每項必須有可見、同租戶的實際版本記錄，並以所選版本通過既有上傳本體 hash 檢查，不借用目前版本的 hash。
- 未完成上傳、版本記錄缺失或租戶不符時返回驗證錯誤，不建立 AI 審查／證據快照，不改掛載。已上傳但沒有 OCR 的歷史版本仍可做缺項預審，保持真正缺解析證據的語義。
- 實際 API 回歸 5 passed，包括目前版本已上傳但所選舊版未上傳、dangling currentVersionId、版本租戶不符，以及合法舊版從請求進入審查和證據快照。原明確選取夾具補上傳完成 hash，未放寬產品校驗。
- 文件範圍／證據快照／manifest／分片／組織隔離／巨石回歸 184 passed；Ruff 289 / 289、diff check 通過。本批未重跑完整後端或瀏覽器。
- hash 是沿用系統既有上傳完成標記，不代表本批逐次讀取物件儲存驗證檔案仍存在；完整歷史重放、PDF／手機／登入工作台及其他工作包仍待完成。無生產部署。


### 2026-09-09 歷史 PDF 與窄螢幕驗收

- 本地夾具新增具兩頁識別文字的實際歷史 PDF。Chrome 真實 API／Blob 請求取得 PDF，內建閱讀器載入 docLength=2；已檢視 1280px 與 390px 的頁面內容截圖，確實顯示歷史版本兩頁，不只檢查 iframe 存在。
- 新腳本 check-version-pdf-mobile.mjs 在兩種寬度驗證版本選取、PDF 原文格式、兩頁載入、預覽對話框界限、返回與保存固定版本；版本列無水平溢出。既有歷史圖片／原文不可用／選取保留腳本亦通過。
- 窄螢幕原先狀態文字擠壓檔名，現將版本標籤獨立一列、狀態與預覽動作放下一列，勾選和預覽操作最小高度 44px。原文請求增加載入指示，過期回應不清除新請求的載入狀態。
- 全量 vue-tsc、修改檔 ESLint／Stylelint、diff check 通過。截圖 version-list-390.png、version-pdf-390.png、version-pdf-1280.png 已檢視；測試橋接沿用隔離記憶體資料與临時原文，服務已停止。
- 此為桌面 Chrome 的 390px 視窗模擬，不代表實體手機、Safari 或完整登入工作台驗收；大型檔案、頁碼範圍、歷史重放與其餘工作包仍待完成。本批未重跑完整後端或前端全部單元測試，無生產操作。


### 2026-09-09 發起審查與會話綁定上下文隔離

- 發起時複製所選文件版本，固定工程／節點／會話及上下文世代。確認後、AI 回應後、會話綁定重試前後均核對；離開再返回同節點也不接納舊回應。切換節點不取消已送出的後端任務，但不把其結果綁到新會話。
- 會話綁定固定原 sessionId，etag 過期時透過統一 fetchWorkspace 讀取原工程節點的新會話資料；只在會話仍相同時重試。統一入口保留伺服器時鐘校正，過期上下文不更新時鐘。
- 上下文切換時清理原發起動態；舊請求 finally 不停止新上下文的提示或重設其 loading。卸載時使舊請求失效，同時避免重複點擊發起。沒有改寫歷史結果。
- 新測試抽取並轉譯實際 SFC handler，以受控延遲 API 執行成功、遲到回應、離開再返回、etag 重試、重試途中切換及确认期间输入变动；不是僅做字串存在斷言。既有綁定測試改為檢查統一讀取與刷新 etag，服務端時鐘契約保留。
- 全前端 85 個測試檔案通過、0 失敗，全量 vue-tsc、修改檔 ESLint、diff check 通過。沒有瀏覽器完整登入重跑或後端程式變更。
- 本批保障發起及綁定流程；背景工作區／訊息／審計視圖讀取的所有非同步回應仍需另外逐路驗證，不能宣稱完整工作台切換一致性已驗收。其餘工作包保持未完成，無生產部署。


### 2026-09-09 背景工作區與取證視圖上下文隔離

- 工作區載入／背景刷新在 await 後核對工程節點上下文世代，過期資料、錯誤與 finally 不覆寫新節點工作區、提示或 loading。切換時重設輪詢旗標並停止舊 SSE；節點初始載入期間不另起背景刷新。
- 訊息與事件輪詢同時核對世代／會話 ID；審計視圖核對世代／run ID，遲到失敗也不清空新審計結果。SSE 回調檢查 abort／世代／會話，拒絕切換後殘留事件。
- 會話建立固定工程、節點、任務及 run ID，授權恢復重試不改用當下工程；建立完成後如上下文已變，不讀取或覆寫新工作區。工程樹回應過期時不改節點選擇。
- 新動態測試執行實際 SFC 函式，涵蓋工作區／刷新遲到成功與失敗、新 loading／錯誤保留、訊息／事件隔離、run 切換後審計回應、舊工程樹、會話重試参数固定，以及正常訊息／捲動仍執行。游標契約測試更新為固定 sessionId，保留 reset 全量與輪詢重疊窗口。
- 全前端 86 個測試檔案通過、0 失敗，全量 vue-tsc、修改檔 ESLint、diff check 通過。未做完整登入瀏覽器重跑，未改後端。
- 本批不是所有非同步操作的完成聲明：同節點內重疊請求的先後仲裁、聊天／整改寫入與完整工作台整合仍需驗證；其他 W1–W13 工作保持未完成。無生產部署。


### 2026-09-09 同節點重疊讀取的先後順序

- 工作區手動載入與背景刷新共用 workspaceReadSequence；只有最新請求可保存工作區、清除錯誤及結束 loading。新節點載入接手時清理舊背景 polling 旗標；會話建立後的補讀也核對原請求序號。
- 審計視圖新增獨立 auditReadSequence，即使工程、節點及 run 都相同，較早發出的成功／失敗回應也不能覆蓋後發請求的結果。
- 動態測試執行實際函式，以後發先完成的順序驗證兩次工作區載入、背景刷新與手動載入交錯，以及同 run 的審計請求；均涵蓋舊成功／舊失敗，保留最新資料與提示。
- 全前端 86 個測試檔案通過、0 失敗；全量 vue-tsc、修改檔 ESLint、diff check 通過。首輪型別檢查與單測同時執行時捕捉到已刪除的 __unit 暫存檔，已在單測完成後單獨重跑通過；未修改 tsconfig 迴避產品程式檢查。
- 未宣稱聊天／整改寫入、所有同節點事件合併與完整登入工作台已驗收；頁碼範圍、工位交接、規則补齊及其他工作包仍待完成。未改後端，無生產部署。


### 2026-09-09 工位交接草稿契約

- 新 review_handoffs 模組建立事實／判定／協作三類草稿，固定來源與接收任務、工位、工程／租戶／業務包、輸入與輸出版本指紋、工程對象及返修輪次。使用真實工位快照校驗，拒絕跨範圍與同節點自交接。
- 事實／判定草稿必須提供來源輸入版本中的證據及正整數頁碼；協作请求可無證據。對象限明確類型／ID／非負整數輪次，深複製內容；相同草稿產生相同 ID。這些是範圍與格式檢查，不是證據原文核驗。
- 讀取驗證來源／接收任務版本、文件清單、輸出與對象輪次；變動時拒絕使用，不改寫舊草稿。所有草稿 authoritative=false、objectMatchStatus=unverified、evidenceVerificationStatus=unverified，不能冒充已核驗或自動變成下游正式判定。
- 交接／工位／文件範圍測試 53 passed，涵蓋三類型、真實 A→E 歸屬、跨工程、缺證據、來源外版本、內容深複製、版本變動、目標輸入變動、錯輪次與篡改；Ruff 289 / 289、diff check 通過。
- 此為 W5 的內部草稿契約，尚無持久化／API／介面／正式接收方调用；規範對象登錄與人工核驗、可信事實組裝、依賴圖循環拒絕、影響範圍及下游待重驗仍待實作。不把草稿建構函式當成工位交接完成；無生產部署。


### 2026-09-09 工位交接草稿保存與讀取 API

- Lab 新增 POST /projects/{projectId}/review-handoffs 與 GET /projects/{projectId}/review-handoffs/{id}（亦有 /api 前綴）。保存只接受來源／接收 run ID、類型、對象、內容與引用；伺服器讀取任務快照產生草稿，客戶端不可指定權威狀態或身份欄位。
- 需有效監檢工程成員、兩端節點範圍及兩端輸入文件讀權；POST 另接 review:save 動作權限和既有工程 mutation guard，歸檔工程不可保存。讀取時重新檢查當前權限，不因過去能建立就永久取得文件存取。
- 新 review_handoffs 獨立持久集合保存建立者／時間與原始草稿，不寫入或改動歷史 review_runs。相同內容 ID 去重；讀取來源／接收版本改動的草稿返回 stale_or_invalid，原草稿不變，所有結果 authoritative=false。
- API／交接契約／巨石測試 31 passed，涵蓋保存去重、失效讀取、原審查不變、跨工程／租戶／未知任務、來源外證據、越權欄位、身份與 Lab 開關、兩端節點／文件權限，以及實際 API 保存後 SQLite 新 repository 重載。權限／租戶／動作／持久对象回歸 36 passed；Ruff 289 / 289、diff check 通過。
- SQLite 重載不代表 PostgreSQL 跨程序持久性驗收。列表／介面、標準對象配對、證據核驗、可信事實取用、正式接收與依賴圖失效仍待實作；目前只保存可追溯草稿，不作下游判定依據。本批未重跑完整後端，無生產部署。


### 2026-09-09 交接列表與固定文件讀取權限

- 新增 GET /projects/{projectId}/review-handoffs（亦支援 /api），可按 sourceRunId／targetRunId 篩選，支援 page／pageSize（1–100）。按建立時間及 ID 穩定排序，在權限過濾後計算 total 並分頁，空頁與無權任務不洩漏草稿筆數。
- 單筆與列表共用草稿讀取驗證；列表每次請求共用一份可見文件版本集合。每項仍須有兩端節點、目前任務輸入與原交接輸入的讀權，保留版本失效標記及 authoritative=false。
- 交接身份快照新增 documentVersionIds，避免來源任務移除文件後，舊草稿失去原文件的權限檢查。缺少固定清單的早期 Lab 草稿拒絕返回內容，需從可核驗來源重新建立；不自動回填、改寫舊草稿或取得額外權限。
- API／交接／巨石測試 33 passed，包含過濾後分頁、來源／目標篩選、未知／無權篩選不洩漏筆數、pageSize 驗證，以及原來源清單清空後仍拒絕讀取已不可見文件的草稿。文件組織／範圍回歸 157 passed；Ruff 289 / 289、diff check 通過。
- 參數錯誤測試按既有 HTTP 200＋VALIDATION_ERROR 契約檢查，未改錯誤中介層。本批未重跑完整後端；工位交接介面、對象及證據核驗、依賴失效與正式使用仍待完成，無生產部署。


### 2026-09-09 交接草稿的當前 OCR 頁面定位

- 單筆／列表讀取有效交接草稿時新增 validation.evidenceLocationCheck，按來源固定文件版本及租戶檢查 OCR pages；區分已定位、缺解析、缺頁、解析重複與頁面重複，不借用其他版本或租戶資料，不把布林頁碼當成整數頁碼。
- 返回 current_ocr 基準與解析內容指紋，內容變更可辨識；不修改原交接草稿。失效草稿不提供此檢查。此為目前 OCR 的頁面定位，不是原始 PDF 實體頁核驗、歷史輸入重放或內容支持性核驗；authoritative=false、contentSupportStatus=unverified，仍不得用作已核驗的下游判定。
- API／交接契約 36 passed，涵蓋八種定位情境、列表與單筆一致、解析內容變動、草稿不變及失效時不提供定位；Ruff 289 / 289，diff check 通過。本批未執行完整後端回歸，未部署生產。工位交接的對象匹配、證據內容核驗、依賴失效與正式取用仍待完成。


### 2026-09-09 工位管線事實衝突阻擋

- 核對交接對象來源時發現管線事實按同號去重、優先選較完整資料行，會掩蓋不同來源的矛盾參數。帶固定輸入的新任務現在檢查同號所有候選行的非空判定參數；矛盾時拋出 REVIEW_PIPELINE_FACTS_CONFLICT（409，不自動重試），不返回供下游判定的管線集合。
- 比對使用既有抽取／數值正規化結果，涵蓋等級、壓力、溫度、材質、規格、介質、毒性及其他已抽取參數；不以欄位完整度或行序消除衝突。例外內保留衝突欄位、值和來源定位。未選取／其他工程資料不參與；無快照歷史讀取保留原行為，不改寫歷史結果。
- 工作台新增對應失敗提示，要求核對原文及版本後重新發起；不是把衝突判為不符合。詳細衝突清單尚未接入持久錯誤報告或介面；也不代表已有規範對象登錄、返修輪次匹配或證據內容核驗。
- 管線／設計測試 14 passed，含正反行序、較完整行未含衝突欄位時仍檢查其他候選、正規化相同值、工程範圍、歷史相容及實際 load_context 中斷／不重試。前端 86 個測試檔案通過；Ruff 289 / 289。未執行完整後端或正式瀏覽器验收，無生產部署。
- 本批 vue-tsc 全量型別檢查通過；修改檔 ESLint 格式修正後通過，diff check 通過。


### 2026-09-09 管線衝突報告保存與授權讀取

- 任務實際失敗處理現在保存 pipelineConflictReport，包含失敗時工程／租戶／節點／任務、inputHash、固定文件版本、衝突欄位／值／來源及建立時間；深複製例外資料，不因後續解析或例外物件修改而改變。
- Lab 新增 GET /projects/{projectId}/review-runs/{runId}/pipeline-conflicts（亦支援 /api）。限定監檢工程／節點範圍，核對報告身份、目前任務及原報告全部固定文件的讀權；移除任務目前輸入不能繞過歷史文件權限。返回 historical=true、authoritative=false，不把資料矛盾當作業務不符合。
- 一般 review_run_view 隱藏詳細報告，避免既有任務／工作台入口新增原文參數暴露；既有 include_sensitive 受控診斷模式沿用原用途。詳細衝突介面與定位跳轉仍待接入，目前工作台仍顯示上批的一般衝突提示。
- API／管線／設計／巨石測試 47 passed，含真實任務失敗處理、非重試狀態、資料深複製、SQLite 保存後新 repository 重載、一般視圖遮蔽、授權讀取及原文件失去權限後拒絕；Ruff 289 / 289、diff check 通過。SQLite 不代表 PostgreSQL 實機持久性驗收，本批未執行完整後端、付費模型或生產部署。其餘工位交接與全量規則驗收仍未完成。


### 2026-09-09 管線衝突明細工作台入口

- 工作台管線衝突失敗提示下新增按需讀取明細元件，顯示管線號、欄位名稱、各來源數值、文件名、固定版本、頁碼及表 ID；明確標註失敗時的歷史資料、尚未裁定何者正確。讀取走上批專用授權 API，不從一般任務視圖取原文參數。
- 元件在工程／任務切換與卸載時清除報告並作廢未完成請求；每次讀取清除舊內容，身份不符或失敗顯示可重試提示。工作台另按工程與任務 key 重建元件；不自動反覆輪詢報告。
- 新獨立瀏覽器測試使用實際 Vue 元件及受控 API 回應，通過數值／版本／頁碼、390px 無水平溢出、讀取拒絕、回應身份不符、舊任務延遲回應不覆蓋新資料。已檢視 docs/lab/verification/browser/pipeline-conflicts-390.png。此測試不是實際登入工作台與後端聯合驗收，證據原文跳轉尚未接入。
- vue-tsc 全量檢查、修改檔 ESLint／Stylelint 通過。無生產部署或付費模型呼叫，完整工位交接與規則發布驗收仍未完成。
- 全前端 86 個單元測試檔案通過；diff check 通過，獨立測試服務已停止。


### 2026-09-09 完整後端回歸與交接冪等修復

- 在 5b8b76e2 收集 3352 項測試成功；首輪完整執行 3283 passed／66 skipped／3 failed。逐項定位：生成 OpenAPI 未包含新交接／衝突報告端點，部署報告兩項因交接 POST 未接統一冪等契約而失敗。
- 更新 openapi/generated/openapi.json；交接保存新增可選 Idempotency-Key 與統一 idempotent producer，保留按內容 ID 去重。同鍵不同內容或任務快照變動拒絕重播。
- 針對性測試發現共用中介層會早於路由返回快取，另加入交接重播檢查：重新核對 Lab 開關、節點範圍、目前與原固定文件權限及兩端快照，防止快取繞過授權／失效檢查。未修改其他 API 的重播政策。
- 新增兩項回歸後，完整後端 3288 passed／66 skipped／6 warnings，130.49 秒，退出 0；Ruff 289 / 289、diff check 通過。保留完整輸出及跳過原因於 docs/lab/verification/handoffs-backend.txt。原三項失敗已全部消除，未以失敗總數容忍放行。
- 跳過包括未配置 PostgreSQL、MinIO、Temporal 等實機條件；本機無 Docker／Temporal 指令。此結果不代表服務持久性／重啟驗收、69 條业务验收或品質提升量測完成。未部署生產、未發布工具、未執行 ID 遷移。


### 2026-09-09 R35 現場質量體系專用判定核心

- 核對 rules.yaml／atomic_checks.yaml：R35 關注質量手冊、受控表格、任命、實施記錄及設備檢定，原 evaluate_ndt_quality_system 仍落到通用規則檔案判定。新增專用 r35_tools，註冊同名 handler 與明確參數契約，通用「文件存在」參數不能再讓它通過。
- 專用輸入須有帶引用的適用性、項目／機構身份、檢測活動日期、各類實施狀態記錄與實際設備清單。逐記錄核對身份和引用；模板／未知狀態、缺證據、錯配、重複設備檢定匹配皆返回證據不足。明確 nonconforming 或具完整證據的檢定有效期未覆蓋活動日期返回不符合；日期含首尾日。帶引用的明確不適用可返回不適用，不使用當天日期替代活動日期。
- 返回逐項結果與引用，保留原輸入。引用目前僅檢查版本／正整數頁碼格式，不等於原文支持性或人工見證已核驗；conforming/nonconforming 為輸入事實契約，尚待可信抽取與見證記錄接入，不能由文件存在自動產生。
- R35 核心 22 項測試；連同業務工具／執行器／範圍／巨石回歸共 130 passed，Ruff 289 / 289、diff check 通過。通用 profile 的成功測試不再套用 R35，已由專用四狀態／缺項／錯配／模板／日期邊界及不可繞過測試替代。
- 此批僅完成專用核心，R35 真實文件事實組裝、原子參數傳遞、原文證據核驗和端到端業務驗收仍未完成；維持 binding_only 及既有發布狀態，不從 28 條未完成清單移除 R35。不涉及生產部署、全量工具發布或外部標準採信變更。


### 2026-09-09 R35 固定輸入資料與工具接線

- 新 r35_facts 從本次固定版本 OCR 的明確 businessSchema 表格讀取：ndt_site_activity、ndt_quality_manual、ndt_controlled_forms、ndt_appointments、ndt_implementation_records、ndt_equipment_inventory、ndt_calibration_reports。核對版本記錄、工程及租戶；空選取、未識別表格或多筆活動身份不任意選第一筆。
- 保留記錄原有實施狀態，不從文件存在產生 conforming。來源引用由解析版本／頁碼／表 ID／行序重建，覆蓋嵌入記錄的外部引用；生成引用 ID、quotedText、已有的置信度與 claimedFacts，送既有證據門禁。缺置信度、低置信度、衝突皆不能通過門禁；不偽造人工見證或置信度。
- 節點35 load_context 接入資料組裝；工具參數從 r35 事實傳入專用 evaluator。R35-01 綁定改用 r35 必需事實路徑，移除無專用參數的重複 check_required／check_date_covers，由專用工具判斷缺項及日期，保留獨立 validate_evidence_grounding。
- 資料鏈路／核心／執行器／工位／巨石測試 85 passed，包含工程租戶與版本範圍、模板、活動歧義、引用不可跨版本、来源变动阻擋、參數傳遞，以及置信度／衝突門禁；69 節點／194 原子綁定編譯回歸通過。
- 本批是明確結構化 OCR 表格的接線，尚無真實掃描件識別上述 schema 與欄位的端到端驗收，不能聲稱任意上傳文件已可可靠抽取；表格單元格人工修正、現場見證可信來源及適用性依據仍需驗收。R35 維持 binding_only，未納入試點、未發布全量工具、未部署生產。


### 2026-09-09 R35 完整綁定執行與不適用聚合修復

- 使用真實業務包的 R35 兩條原子綁定、runtime 工具派發、固定 OCR 事實及證據門禁執行測試，發現專用工具 not_applicable 會被輔助證據工具 passed 聚合成 passed，已修復。
- R35-01 參數明確 decisionTool=evaluate_ndt_quality_system；R35-02 明確 resultRole=evidence_gate。執行器在其他工具僅為取證／門禁且指定判定工具唯一時保留不適用；證據門禁通過不算另一項業務通過。證據不足、執行錯誤照常阻擋；僅有證據門禁、沒有業務判定不能產生通過。
- 門禁角色僅接受具 validate_evidence_grounding 且不含業務判定工具的計畫；無效或混合 decisionTool 聲明不任意跳過其他工具。未聲明角色的原有計畫保持原聚合行為，未擴大變更至其他規則。
- 完整綁定涵蓋符合、不符合、缺實施記錄、不適用、僅不適用活動資料及低置信度；連同專用核心／聚合／執行器／工位／巨石回歸共 95 passed。此為結構化資料到真實工具計畫的整合測試，不是掃描件、模型或人工見證的實機驗收。R35 仍維持 binding_only、尚未發布，無生產部署。


### 2026-09-09 R35 原文定位與任務身份修正

- 移除將 normalizedRows 的 Python 字串表示用作 quotedText 的錯誤做法；正規化欄位不是原文摘錄。引用只保留 OCR 已記錄的非空 contentMarkdown，或有效的表格座標；沒有兩者時保持缺定位，交由證據門禁返回證據不足。
- 座標限四個有限數字、非負起點及正向非零矩形；空值、布林值、無限值、倒置／零面積不當成有效 bbox。頁碼限正整數，其他值轉為缺失，不以預設第一頁補齊。這些是定位格式驗證，不是 PDF 頁面邊界或摘錄支持性的實機核驗。
- R35 資料組裝要求非空工程／租戶身份及節點35；避免任務和來源同時缺少 tenantId 時因 None 相等而被當成同租戶資料。錯節點或缺身份明確拒絕，不回退讀取其他資料。
- 資料／專用核心／判定聚合／執行器／工位／巨石回歸 112 passed，涵蓋無效座標、實際已記錄表格原文保留、不偽造摘錄、非法頁碼及缺租戶身份；Ruff 289 / 289、diff check 通過。未執行完整後端或實機掃描件驗收，R35 仍未發布，無生產部署。


### 2026-09-09 工具綁定生成來源恢復一致

- 開始核對 R36 時發現生成腳本落後於已審核 YAML：113 項 implementationStatus 不一致、10 條原子綁定的工具／事實／參數有差異，且缺少既有4條試點名單。直接重新生成會覆蓋近期 R35 接線及其他既有修復。
- 增加獨立維護的 atomic_binding_overrides.py 保存10條明確修訂；生成器不讀生成結果來決定輸出。同步既有36條試點名單、2026.08.07版本及已審核能力聲明，區分試點政策與是否有專用實作，未新增發布或能力宣稱。
- 全量194條綁定重新生成後與目前 YAML 語義完全一致，連續兩次輸出位元組一致；R35 的 decisionTool／evidence_gate、必需事實與未發布狀態保留。更新 tools规划.md，使生成文件與實際綁定一致。
- 生成／工具執行器／工位／巨石回歸53 passed，涵蓋獨立臨時目錄以腳本路徑直接執行、不依賴當前工作目錄、所有元資料及綁定一致、覆寫資料不被呼叫端修改。Ruff 289 / 289、diff check 通過。
- 本批解決生成覆蓋風險，R36 專用判定尚未實作，R35 實機與業務驗收仍未完成；未改生產、未發布工具、未執行ID遷移。


### 2026-09-09 R36 無損檢測方案專用判定核心

- 新增 evaluate_r36_ndt_plan 專用工具與參數契約，保留 R39／R40 等既有 evaluate_ndt_process 行為。依既有 R36 審查要求，核對方案工程身份、審批、人員／設備準備，以及逐 objectId＋method 的明確檢測要求；資料須帶版本／正整數頁碼引用，單有文件不能通過。
- 檢測比例使用明確 ratioPercent（0–100），不猜測比例單位；低於具證據的要求為不符合，缺值、NaN／Infinity、布林、超界或含百分號而未正規化為證據不足。重复要求或方案匹配、缺項、錯工程／對象／方法保持證據不足。
- 時機及驗收級別相同可匹配；不同標籤不猜測嚴格程度或語義等價，無允許集合時返回證據不足。有明確來源的 allowedTiming／allowedAcceptanceLevels 時依集合判定，集合與要求矛盾時保留證據不足。選配要求不強迫方案提供相應項目；整體不適用須有明確引用依據。
- 24項核心測試；連同業務工具、執行器、工位及巨石回歸146 passed。新工具註冊數由89增至90，註冊／允許名單及所有工具空輸入均不通過的檢查仍執行。Ruff 289 / 289、diff check 通過。
- 這是明確結構化要求的判定核心，尚未接入R36真實文件事實組裝、標準查詢與原子綁定；引用格式不等於原文支持性，審批及準備布林值也不能由文件存在推定。下一步需可信來源與證據門禁接線，R36仍未完成／未發布，無生產部署。


### 2026-09-09 R36 固定資料、專用綁定與節點執行

- 提取 R35 已驗證的同工程／租戶／版本及原文定位讀取至 ndt_table_facts，共供 R35/R36 使用。R36 讀取明確 ndt_plan_context、ndt_plan_approval、ndt_plan_items、ndt_design_requirements 表格；活動或方案主記錄有歧義時不任選第一筆，審批記錄內嵌 items 由獨立來源表格覆蓋。
- R36 load_context、專用工具參數及兩條原子綁定已接通；主判定指定 evaluate_r36_ndt_plan，第二項為 evidence_gate，保留不適用語義。來源生成覆寫與 tools规划.md 同步，194條綁定重建一致，未變更發布名單或能力狀態。
- 增加明確 planId 關聯：方案主記錄必須有ID，檢測項目須匹配同一ID；不以同工程或相同焊口代替方案身份，避免用另一方案的審批。此ID須來自可信資料映射，不能由文件存在自動產生。
- 來源到真實runtime工具／節點計畫四狀態整合、低置信度、錯工程租戶、缺版本、未選文件、方案歧義、偽造嵌入引用、來源變動及跨方案防混用，連同R35、生成、執行器、工位與巨石回歸128 passed。Ruff 289 / 289、diff check通過。
- 本批支援已形成明確schema的結構化OCR資料，尚未驗收任意掃描件自動識別、方案ID可靠映射、設計／標準要求合併與標準查詢、審批及人員設備準備的可信見證來源。不得把這些測試當成R36業務全量驗收；仍保持binding_only，未發布、未部署生產。


### 2026-09-09 NDT 驗收要求查詢方法識別修復

- 核對 R36 可复用標準查詢時發現 ndt_acceptance_level 按字串包含匹配，PAUT 會先命中 UT 而讀到一般超聲表項。改為英文方法代碼邊界識別；相控陣／TOFD 的中文名稱含「超聲」時使用專項表，不被通用超聲覆蓋。
- 明確 RT/UT、UT/PAUT、PAUT/TOFD 等混合方法不任選第一項，返回 None 供呼叫方逐方法處理。OUTPUT／SMART 等普通字串不再誤命中 UT／RT。
- 比例若提供，須為有限數值且大於0、不超過100；布林、字串、NaN／Infinity、負值、零及超界不選取局部檢測要求。PAUT／TOFD 不依賴比例的既有專項查詢仍可不傳比例，RT／UT 比例缺失仍返回 None。
- 法規表、原有審查鏈路、設計事實及R36回歸95 passed，Ruff 289 / 289、diff check通過。未修改標準原數值、人工核驗紀錄或採信政策。本批修復既有查詢入口，R36設計與標準要求的可靠合併仍待實作，無生產部署或工具發布。


### 2026-09-09 R36 設計與明確標準要求共同判定

- 增加 r36_requirements，保留設計及明確提供標準要求的獨立記錄與引用，逐項核對；任何一份要求較嚴格都不會被另一份覆蓋。選配項不取消其他來源的必做要求，不根據級別名稱猜測嚴格程度。
- 標準記錄必須有非空 standardRef／clauseRef；同來源重複要求、來源不明、時機或允許級別集合無交集時返回證據不足。不同標準條款可共同約束同一對象／方法；來源類型由輸入集合指定，不能用記錄自報欄位繞過重複檢查。衝突項保留提供的依據引用。已獨立證實的審批／準備不符合仍按原有結果優先序保留。
- R36 固定文件事實讀取新增 ndt_standard_requirements，經 standardRequirements 傳入專用工具；仍通過原來源／置信度門禁。判定版本更新為 r36-explicit-ndt-plan-requirements-v2。
- 相關回歸141 passed；完整後端成功收集3471項，最終3405 passed／66 skipped／6 warnings，484.77秒，退出0。原始結果及跳過原因保存於 docs/lab/verification/r36-combined-requirements-backend.txt。Ruff 289 / 289、diff check通過。PostgreSQL／MinIO／Temporal等環境選擇性跳過不計入實機驗收。
- 這是明確結構化要求的共同判定，不代表已自動查齊全部適用標準或完成真實掃描件抽取。標準適用性與版本核驗、可靠對象映射、現場見證和全量業務驗收仍待完成；R36維持未發布，未改生產、未進行ID遷移或付費模型評估。
- 接續已定位：design_special_requirements 仍以 next(...) 取第一個可查到的檢測方法驗收要求。後续须保留逐方法結果，處理比例／級別與方法的可靠關聯，涵蓋方法順序互換、不完整方法查詢與多方法不同要求，避免首項結果代表全體。本批尚未修改該路徑。


### 2026-09-09 設計 NDT 多方法要求逐項核對

- 新 design_ndt_requirements 依明確分句保存每種檢測方法的比例、級別、既有標準查詢結果及原句；R09 不再用第一個查到的方法代表全部，也不把全文第一組數值套給其他方法。同方法中英文別名去重，PAUT不誤讀UT，英文方法碼增加邊界。
- 明確並列方法可共同使用同句唯一數值；替代、或、斜線選項與多組數值歸屬不清時保留未知。分句中缺值、查不到要求的方法不借用其他方法資料。已有明確不符合仍保留；其餘只有全部方法核對通過才產生整體通過布林值。
- 不同方法的有效比例／級別以逐方法文字摘要送既有凍結規則，保留 methodRequirements 明細；共同數值欄位僅在全部一致時提供。既有工程級比例要求逐項比較，不以第一項代替其他項。
- 新測試透過實際R09綁定／參數組裝／凍結規則驗證順序互換、其中一種不符合、不同有效比例／級別同時通過、缺資料／不支援方法／替代方式不能通過；連同設計、法規鏈路、業務工具、執行器、巨石回歸169 passed。Ruff 289 / 289、diff check通過。本批未重跑完整後端；前批3405 passed為修改前的完整紀錄。
- 這是明確文字分句的解析與判定修復，尚未支援任意跨行表格、複雜自然語言條件及逐管線／焊口適用性映射。仍沿用既有工程級最嚴比例口徑，後續須以對象關聯細化；不宣稱R09/R36全部真實文件驗收或全量標準查詢完成。未修改標準表值／採信策略、未部署生產或發布工具。


### 2026-09-09 驗收級別格式與含糊文字處理

- acceptance_level_meets 由羅馬數字子字串搜尋改成完整格式核對，NFKC統一Unicode／ASCII／全形及明確數字級別；II、Ⅱ、2級按既有同一序位比較，大小寫不影響。要求端保留既有「不低于」格式；非文字、複合級別、否定、待核實附註及無效級別返回未知，不從中挑一個有效字元。
- 設計NDT解析保留級別附近的明確限定／否定及複合表述，防止VIII或12級被截成有效尾碼、普通英文詞尾II被識別成級別。「Ⅰ/Ⅱ」「Ⅲ或者Ⅱ」「不符合Ⅱ級」「Ⅱ級不合格」均不得產生通過；沒有完整語義支持的限定表述仍待核對。
- 首輪收集因新增import位置及測試import錯誤失敗，已修正後重跑；最終設計／法規表／真實R09規則鏈／業務工具／巨石回歸176 passed，42.25秒。Ruff 289 / 289、diff check通過。本批未執行完整後端或真實掃描件驗收。
- 沿用既有級別序位與標準表值，不新增法規口徑；明確格式歸一化不代表任意自然語言都能可靠解讀。R36專用要求集合的級別等價映射及對象適用性仍待驗收。三份文件同步，未部署生產或發布全量工具。


### 2026-09-09 R37 不合格處理見證專用核心

- 核對R37三項原子要求後，將既有 evaluate_ndt_nonconformance 從通用規則檔案回退路徑接到專用r37_tools；單有文件或通用requiredFields／ruleChecks不能判通過。工具名稱及註冊總數90維持，能力說明及參數契約更新。
- 核對带來源的適用性、工程／機構、控制程序、委託單，以及有完整性聲明的問題清單；通知與反饋須按caseId／objectId／commissionId／repairRound唯一匹配，委託單須覆蓋相應對象。缺件、錯工程／機構、錯輪次、模板、重複／孤兒見證、缺頁碼引用保持證據不足，明確nonconforming保留不符合。
- 明確且有引用的完整空問題清單可以免去不存在問題的通知與反饋，但仍核對程序與委託單；不能由沒有找到問題記錄推定清單完整。輸入狀態與完整性聲明均須由可信抽取／見證來源提供，本核心不從文件存在生成conforming或complete。
- 23項專用測試；連同工具派發／空輸入／工位／執行器／巨石回歸144 passed，2.28秒。首次新測試引用了不存在的派發函式導致收集失敗，已改用實際dispatch_business_tool後完整重跑本批套件。Ruff 289 / 289、diff check通過。
- 這是結構化工作見證判定核心，引用格式不等於原文支持性。R37真實文件抽取、固定來源事實組裝、正式原子參數傳遞與標準條款適用性核對仍未完成；不從28條待完成清單移除，維持binding_only，不發布、不部署生產。本批未重跑完整後端或付費模型。


### 2026-09-09 R37 固定文件資料與工具參數接線

- 新r37_facts從本次固定版本的明確businessSchema表格讀取活動、程序、問題清單標頭、獨立問題明細、委託單、通知、反饋。沿用read_ndt_tables的工程／租戶／版本核對與引用重建，來源既有置信度送judgment證據門禁，不保留記錄自報的外部引用。
- 活動、程序及清單標頭有歧義時不任選一筆；標頭內嵌cases不能代替獨立問題表。專用核心新增inventoryId與明示caseCount，明細數不符時保持證據不足，防止漏讀問題表被當成零問題；通知／反饋也須匹配清單ID。判定版本更新r37-nonconformance-witness-chain-v2，未自行生成清單ID或完整性聲明。
- execution的節點37資料入口及executor專用參數已接通。首次回歸115 passed／1 failed，失敗為execution.py新增分支超過巨石行數基線；將R35–R37集中至NDT_FACT_BUILDERS登錄後重驗，沒有調高基線。
- 最終資料／專用核心／R35／R36／工位／工具執行器／巨石回歸139 passed，3.07秒；涵蓋真實runtime派發、錯工程租戶／版本缺失／未選、清單歧義、缺明細、錯清單與返修輪次、嵌入引用覆寫、來源變動及實際證據門禁低置信度阻擋。Ruff 289 / 289、diff check通過。
- 本批是明確結構化OCR來源到專用工具與證據門禁的接線，未更改R37原子綁定，也不宣稱整節點已通過；AC-R37-02標準適用性與實際條款核對、原子綁定分工及真實掃描件仍待實作／驗收。沒有發布或生產部署，本批未執行完整後端。


### 2026-09-09 NDT 驗收與不合格處理引用修正

- 接續R37標準原子項核對，直接讀取倉庫GB/T 20801.1-2025 PDF第118、123、124頁，發現acceptance區段多處引用少一層：射線／超聲／組合應為8.3.3.2.2／8.3.3.2.3／8.3.3.2.4，表面範圍、延遲裂紋、再熱裂紋、返修重檢與位置批准亦已按原文更正。
- 同步R09凍結規則sourceClause。ndt_acceptance_level新增requirementStandard／sourceClause／sourcePage，區分GB/T驗收要求來源與原有NB/T方法標準；RT／UT／PAUT／TOFD查詢均保留來源。未改任何檢測比例、技術級別或驗收限值。
- 原文頁碼與檔案SHA-256核對紀錄：docs/lab/verification/ndt-clause-locators.md。這是本批原文定位核對，不新增或偽造人工verifiedBy。
- 法規表／R09鏈路／設計多方法／R37來源及核心／生成／巨石回歸151 passed，3.08秒；Ruff 289 / 289、diff check通過。本批未重跑完整後端。
- R37原子項分工尚未修改；標準判定還須實作返修／更換後檢測及按批次、焊工、階段的累進檢查，不能重用工作見證的通過代替。未發布全量工具、未部署生產，其餘工作包保持未完成。


### 2026-09-09 R37 返修／更換後重檢核心

- 依已核對倉庫PDF第118／124頁的GB/T 20801.1-2025 8.1.3及8.3.3.4，新增evaluate_r37_reinspection；單一缺陷按工程／機構、清單、問題、對象及返修輪次核對原檢查、處理記錄與重檢報告。更換件使用明確replacementObjectId，不以原件報告冒充新件。
- 原檢測要求的每種方法須有唯一重檢報告，保存原方法、範圍與驗收準則ID；範圍或準則不一致時保持待核對，不猜測等價或用另一套準則下的不合格推定本要求不合格。引用格式與身份缺失、方法缺項／重複、舊輪次、錯清單均不得通過。
- 修復完成與重檢時間須為帶時區的明確時間，檢查早於完成為不符合，時間不明保持證據不足；在匹配範圍與準則下的明確unqualified返回不符合。明確且有來源的非超標缺陷返回不適用。輸入時間、缺陷性質與修復事實均須由可信資料提供，沒有由文件存在自動生成。
- 工具註冊及允許名單數由90增至91，空輸入不得通過的全工具檢查維持。19項專用測試，連同R37來源／工作見證、業務工具、工位、執行器及巨石回歸180 passed，2.05秒；Ruff 289 / 289、diff check通過。
- 此為單一缺陷重檢核心，尚未接入獨立OCR來源表與R37正式原子綁定；批次累進檢查、相同件／同焊工／同批配對及各階段升級仍待實作。未宣稱整節點或整批已合格，未發布、未部署生產，本批未重跑完整後端。


### 2026-09-09 R37 重檢固定來源與完整問題清單執行

- r37_facts新增ndt_original_inspections／ndt_defect_dispositions／ndt_reinspection_reports獨立來源表，沿用固定文件範圍、工程／租戶／版本核對及原引用重建；新增資料保留既有置信度並進入judgment證據門禁，沒有由程序文件推定重檢已執行。
- evaluate_r37_reinspection新增整份清單執行：確認清單ID、完整性、明示筆數及問題身份後，逐問題唯一匹配原檢查及處理記錄，按清單／問題／返修輪次選取重檢報告並调用既有單一缺陷核心。重複問題、原報告歧義、孤兒／跨範圍記錄均阻擋通過；每個問題的完整结果保留於caseResults。
- 完整空清單返回重檢不適用，缺清單不能當作零問題。單筆明確輸入仍按原契約執行；同時提供單筆與清單的含糊模式拒絕，不以第一筆結果代表全清單。清單路徑版本為r37-reinspection-inventory-v1。
- 來源／R37核心與見證／業務工具／工位／執行器／巨石回歸196 passed，2.08秒；包括真實runtime、嵌入引用被實際來源替代、來源變動拒絕、未選文件、缺項、舊輪次、整份清單與獨立證據門禁低置信度。Ruff 289 / 289、diff check通過。
- 這是明確結構化OCR到重檢工具及證據門禁的接線，仍未更改R37正式原子綁定。批次累進檢查、同焊工／相同件及升級階段、全部標準要求與真實掃描件驗收仍待完成；未發布、未部署生產，本批未執行完整後端。


### 2026-09-09 R37 累進檢查覆蓋核心

- 依已核對倉庫GB/T 20801.1-2025 PDF第118頁8.1.4，新增evaluate_r37_progressive_inspection。輸入須有明確初始事件、帶完整性／筆數的批次成員清單及分階段報告；追加件核對同批、相同件分組及焊接接頭的同焊工身份，並保留每份來源引用。
- 首階段至少兩個不同追加件；發現超標缺陷後按每個明確缺陷項要求再追加兩件，各組不得重用已檢件。第二階段任何有效報告出現超標缺陷即要求全批覆蓋，不等待其他組湊齊；原件及先前有效階段的檢查可計入覆蓋，避免重複要求已檢件。
- 完整初檢、無超標缺陷、返修後重檢再次不合格不觸發新的累進分支；後者仍列入待修復對象。修復重檢報告不得混入追加檢查階段。報告須保持原方法、範圍與驗收準則，使用帶時區時間核對其不早於對應觸發事件。
- 輸出stage、缺少對象／每缺陷需追加數及repairRequiredObjectIds；batchAcceptance固定為not_evaluated，覆蓋passed不代表缺陷已修復或整批驗收通過，須另接既有重檢與完整處理清單核對。未知範圍、缺證據、未明確缺陷項、錯身份／時間保持證據不足。
- 新增23項專用測試；連同重檢、業務工具、工位、執行器及巨石回歸163 passed，2.67秒。註冊工具由91增至92，所有工具空輸入不能通過的檢查仍執行。Ruff 289 / 289、diff check通過。
- 本批完成明確結構化事件的覆蓋核心，批次／焊工／缺陷及時間可靠抽取、固定來源組裝、完整多事件關聯、R37正式原子綁定與真實掃描件驗收仍待完成；未發布、未部署生產，本批未重跑完整後端。


### 2026-09-09 R37 累進檢查固定來源與多事件執行

- r37_facts新增明確事件清單、事件、批次標頭、獨立成員及階段報告來源表。按本次固定版本讀取，保留原文定位與置信度，不以批次標頭內嵌members代替獨立成員資料。
- evaluate_r37_progressive_inspection新增整份事件清單執行：核對完整性與明示事件數、唯一事件／批次，拒絕孤兒成員及未知階段／跨事件清單報告；按eventId、stage選取資料，逐事件調用既有累進覆蓋核心。整合保存eventResults和全部待修復對象，不以第一事件代表後續事件。
- 清單路徑版本r37-progressive-inventory-v1；完整空事件清單為累進檢查不適用，缺清單保持證據不足。batchAcceptance維持not_evaluated，來源與覆蓋通過仍不代表整批結案。
- 相關回歸191 passed，2.19秒；包含真實runtime、來源引用重建與變动拒絕、缺獨立成員、事件／批次歧義、事件隔離、舊清單、孤兒成員／報告及獨立證據門禁低置信度。完整後端收集3628項，最終3562 passed／66 skipped／6 warnings，131.99秒，退出0；原始結果及跳過原因保存於docs/lab/verification/r37-progressive-backend.txt。環境跳過不計入PostgreSQL／MinIO／Temporal實機驗收。Ruff 289 / 289、diff check通過。
- 仍需將累進檢查产生的每個待修復對象與重檢結案清單按事件、對象及有效輪次完整對齊，才能組合標準判定；R37正式原子綁定及真實掃描件驗收尚未完成。未發布全量工具、未部署生產。


### 2026-09-09 R37 待修復對象與有效重檢結案對齊

- 新evaluate_r37_defect_closure重新計算完整累進檢查與重檢清單，按每個eventId／objectId逐一要求帶來源的結案連結；連結必須匹配兩份清單ID、caseId及目前清單中該事件／對象的最高返修輪次，拒絕缺連結、重複、孤兒及舊輪次結案。
- 進一步核對原檢查要求與事件的方法／範圍／驗收準則一致，修復完成不得早於事件及先前返修輪次再次不合格的已知時間。不能用較弱準則下的合格、事件發生前的修復或虛構輪次順序關閉缺陷。
- 較新的有效合格輪次可結案；舊輪次失敗及完整原始組件結果保留於輸出，沒有改寫歷史。缺陷對齊通過仍不宣稱整批驗收，batchAcceptance保持not_evaluated；呼叫方傳入的已通過結果及全域不適用標記不能跳過已知修復義務。
- r37_facts新增ndt_defect_closure_links來源表，executor傳遞兩條原始資料鏈及連結；真實runtime已驗證引用頁碼、未選文件、跨工程、来源变动與獨立證據門禁低置信度。工具註冊由92增至93，空輸入不通過及允許名單檢查維持。
- 結案／來源／累進／重檢／業務工具／工位／執行器／巨石回歸168 passed，2.50秒；Ruff 289 / 289、diff check通過。本批未重跑完整後端，上一批3562 passed是修改前全量紀錄。
- R37正式原子綁定、程序控制與標準要求的完整覆蓋及真實掃描件驗收仍待完成；未發布、未部署生產。結案連結、事件、對象與輪次仍須由可靠來源提供，不由文件存在推定。


### 2026-09-09 R37 原子綁定分工與真實節點計畫

- AC-R37-01改用r37固定事實及evaluate_ndt_nonconformance工作見證判定；AC-R37-02改用evaluate_r37_defect_closure，重新計算累進覆蓋、重檢及缺陷結案，不再重複使用工作見證結果充當標準判定。兩者指定各自decisionTool，AC-R37-03明確為evidence_gate。
- atomic_binding_overrides生成來源、YAML及tools规划.md同步，194條綁定重新生成語義一致、重複生成及獨立目錄啟動測試通過。保留R37的binding_only，不加入既有36條試點、不更改發布狀態。
- 結案工具新增適用性衝突檢查：明確不適用與已計算出的待修復義務同時存在時返回證據不足，避免工作見證不適用被另一項通過掩蓋。結案版本更新r37-progression-reinspection-closure-v2。
- 使用實際業務包compile_node_tool_plan、三項原子綁定、runtime工具及證據門禁驗證符合、不符合、缺通知、缺累進報告、低置信度、不適用與適用性矛盾；保留三項完整結果且不改來源。連同生成、結案、R35／R36、工位、工具執行器與巨石回歸147 passed，2.78秒；Ruff 289 / 289、diff check通過。
- 這是結構化來源下的Lab節點計畫驗證，不代表R37全部標準要求或真實掃描件已驗收。NB/T通用要求與檢測單位控制程序的完整語義覆蓋、人工見證可信來源及發布矩陣仍待核對；本批未部署、未發布、未重跑完整後端。


### 2026-09-09 R37 見證與重檢事件身份一致性

- 多輪返修檢查時發現，部分工作見證／處理記錄／重檢配對僅核對清單、問題、對象與輪次，沒有核對eventId；即使其餘ID相同，也可能接受另一事件的記錄。新增共用matches_case_record：任一側出現事件欄位時，兩側必須有相同非空事件ID，缺欄位／空值不視為匹配，輪次保持嚴格型別。
- 工作見證配對及孤兒掃描、單筆處理／重檢、整份重檢清單皆套用。正確記錄旁多出跨事件記錄仍返回證據不足，不能因已找到一份正確資料而忽略錯誤資料。兩側皆無事件欄位的既有單筆契約保留，但完整結案路徑仍要求問題事件身份。
- 版本更新：工作見證r37-nonconformance-witness-chain-v3、單筆重檢r37-original-reinspection-v2、重檢清單r37-reinspection-inventory-v2。測試資料明確補上來源事件身份，沒有在生產事實組裝時自動猜填。
- 共用身份／真實R37節點計畫／見證／重檢／結案來源／業務工具／執行器／巨石回歸222 passed，2.15秒；包括同問題同輪次但事件錯誤／缺失／空白、多出的跨事件記錄及缺基本身份。Ruff 289 / 289、diff check通過。
- 本批沒有自動忽略舊輪次的程序或見證不符合，也未改寫歷史結論；新重檢合格不等同歷史程序問題已經結案。完整標準覆蓋、可信見證與真實文件驗收仍待完成，未發布、未部署生產，本批未重跑完整後端。


### 2026-09-09 綁定發布驗收記錄檢查

- 發布腳本新增必填驗收清單與新版本；在原有業務包、實作狀態及工具註冊檢查之外，要求每條規則四情境留存、全部原子結果覆蓋、凍結輸入及程式／綁定雜湊一致。依當前綁定推導證據門禁角色，使用正式聚合函式核對整體結果，不信任報告自報角色。
- 引用限制於凍結文件版本與有效頁碼，接受原文摘錄或有效 bbox；缺證據案例可沒有文件／引用，但不能帶越界引用。檔案解析後不得超出驗收目錄。發布更新版本並保存原試點清單及驗收雜湊，dry-run 不寫入；替換前再次核對來源，變動則中止。
- 發布檢查／執行器／盤點／業務包／巨石相關回歸 63 passed，1 條相依套件棄用警告；Ruff 289／289，未提高基線。本批未重跑完整後端。
- 契約詳見 docs/lab/verification/release-acceptance-contract.md。這是留存記錄一致性檢查，尚非案例重放器或可信人工審核；真實文件、逐原子證據支持、全部69條業務驗收與原36條差異仍待完成。W9/W11保持未完成，未發布綁定、未部署生產、未執行ID遷移。


### 2026-09-09 NDT 凍結來源離線重放

- 新增 replay_review_acceptance.py，從留存 state／reviewRun／documentScopeSnapshot 重建 R35–R37 事實，再執行當前實際原子綁定與 runtime 工具。執行前核對輸入雜湊、節點／規則及文件範圍；來源變動不可藉由重新雜湊驗收檔案繞過既有快照檢查。
- 明確本地工具允許清單阻擋外部查詢與未知工具，未支援節點直接報錯；無付費模型或平台調用。保存原始 fixture、完整原子／工具 output 與 report 雜湊，新目錄輸出不覆寫舊驗收資料。結果比對只影響 matchesExpected／CLI 退出碼，不改觀測結果；businessAcceptance 始終 not_reviewed。
- 合成結構化來源涵蓋 R37 四態、缺證據、來源變動、跨節點／文件範圍、外部工具預檢及重跑一致性，R35／R36 真實綁定亦執行。相關回歸 87 passed；CLI 實跑驗證匹配退出0、不匹配退出1，兩者均保存結果。Ruff 289／289，diff check通過。
- 完整後端先收集3712項，再執行3646 passed／66 skipped／6 warnings，134.88秒，退出0；原始結果與跳過原因見 docs/lab/verification/acceptance-replay-backend.txt。環境跳過不計入PostgreSQL／MinIO／Temporal或外部平台實機驗收。
- 契約及命令見 docs/lab/verification/release-acceptance-contract.md。目前是R35–R37工具與結構化OCR重放，不是69條全量、真實掃描或模型端到端驗收；其餘節點、外部平台留存回放、可信人工審核及三組品質評估仍待完成。未發布、未部署生產、未執行ID遷移。


### 2026-09-09 工位三組品質對照報告

- 新workstation_quality_report.py離線比較現況／僅工位／完整方案，要求雙人金標宣告、相同凍結輸入與模型設定、各組實作版本。缺组或不匹配案例從三組共同排除並列明原因；同節點同輸入不可重複計數。
- 分開統計金標不符合被判通過、未檢出不符合、誤報不符合、資料足夠卻返回不足及缺證據誤通過，提供分子／分母與逐節點結果。執行錯誤及轉人工不從分母消失，無樣本返回null。
- 比較保留改正／退步筆數、一致率差與相對錯誤減少。成本、人工及等待時間缺值不當零；收益比較只使用雙方都有量測的同案例並顯示配對數。
- 相關報告／既有評估／發布驗收／離線重放／巨石回歸60 passed；Ruff289／289、diff check通過。本批未重跑完整後端，上一批3646 passed／66 skipped仍為修改前紀錄。
- 契約見docs/lab/verification/workstation-quality-contract.md。這是描述統計工具，不自動核驗審核人或來源雜湊、不宣稱顯著性與因果提升、不批准發布。真實金標收集、三組結果匯出、引用支持性及完整品質評估仍待完成；W10保持未完成，未付費跑批或部署生產。


### 2026-09-09 交接雙方凍結來源新鮮度

- 修復交接讀取只比較任務快照、未核對當前OCR／人工修正來源的缺口。新增inspect_handoff_sources，重用正式validate_document_sources檢查來源與接收任務，先載入ocr_parse_results／fact_corrections；不更新任務或交接快照。
- 保存交接前拒絕已失效來源。讀取／列表新增inputSourceCheck，逐端點標示current、unverified或stale_or_invalid；已變動時交接整體stale_or_invalid，返回requiresRevalidation與affectedTargetRunId，停止顯示當前證據位置檢查。冪等中介層沿用讀取檢查，不能重送已失效快取。
- 無來源指紋的歷史草稿維持原讀取契約，但明確unverified，不能冒充來源已驗證。未選文件與不相關修正不觸發失效；來源變動後也不改寫已保存記錄、既有任務狀態或歷史結論。
- 交接API／契約／巨石基線57 passed，1條相依套件棄用警告；Ruff289／289，diff check通過。案例覆蓋source／target的OCR及人工修正變動、單筆／列表、保存／冪等重送、歷史無指紋與未選資料隔離。本批未重跑完整後端。
- 此批提供交接草稿讀取時的來源失效判定；可信人工核驗、正式下游使用、依賴圖與傳遞失效、工位介面及選擇性重跑仍待完成。不能把此批描述為已完成自動下游重驗；未發布、未部署生產。


### 2026-09-09 交接事件身份與v1存量相容

- 新create_handoff_draft統一生成review-handoff-draft-v2；subject除objectType／objectId／repairRound外必須有明確非空eventId，拒絕缺欄位、非字串、空白及帶首尾空白的事件ID。不從文件存在、焊口ID或返修輪次猜填事件。
- 事件納入完整快照與交接ID；同對象同輪次的不同事件產生不同記錄，使用另一事件subject驗證時拒絕。同事件同內容保持冪等。列表新增eventId精確過濾，沿用雙節點與文件權限檢查後分頁。
- validate_handoff_draft仍支援既有v1，依原欄位重建並核對完整雜湊，不改原ID、內容或補造事件；新建API不提供降級v1入口。v1沒有事件範圍，不能套用到帶事件subject的交接驗證；後續正式使用與核驗仍需明確處理此限制。
- 交接契約／API／巨石基線66 passed，1條相依套件棄用警告；Ruff289／289，diff check通過。本批未重跑完整後端。
- 此為Lab新建交接契約的必要欄位變更，呼叫方需提供實際工作事件身份；事件ID存在不代表事件已核實，objectMatchStatus與evidenceVerificationStatus維持unverified、authoritative=false。完整工作台事件選取、可信核驗及下游使用／失效傳遞仍待完成，未部署生產或遷移歷史資料。


### 2026-09-09 工位交接人工核驗API與留存

- 新增POST /projects/{projectId}/review-handoffs/{handoffId}/verifications，限定Lab、監檢角色、review:save、兩端節點及當前／凍結文件權限；要求v2事件身份、有效來源指紋、草稿雜湊及expectedPreviousId。人工明示對象配對與證據支持後才可verified，亦可追加rejected與說明。
- 核驗人與時間由伺服器記錄，逐筆追加verifications並鏈接前次ID，不改draft或原結論；單筆／列表顯示最新核驗摘要，來源／任務變動時為stale，歷史保留。冪等重送重新核對權限、來源及最新決定，不返回被後續退回取代的舊確認。
- 交接API／契約／巨石相關78 passed，1條相依套件棄用警告；包含內容／角色偽造拒絕、同版本單程序並行僅一次成功、SQLite重載留存。Ruff289／289、diff check通過；本批未重跑完整後端。
- 契約詳見docs/lab/verification/handoff-human-review-contract.md。authoritative仍false；此批為人工核驗留存入口，尚未供正式下游工具使用，亦未為真實資料生成核驗背書。人工介面、依賴失效傳遞及重跑仍待完成；多API程序的資料庫級CAS／鎖尚待實作與實機驗收，未部署生產。


### 2026-09-09 工位交接人工核驗介面

- 監檢AIReviewB工作台新增Lab「工位交接」入口，依當前接收任務讀取並分頁列出交接；選取時重新取詳情，核對工程／任務身份，顯示事件、對象、返修輪次、業務內容、證據引用與核驗歷史。原文按固定版本／頁碼交由既有預覽入口處理。
- 確認須勾選對象／事件配對與原文支持且填寫說明；退回可不勾選但仍需說明。來源失效、未凍結、v1舊事件或核驗歷史异常停用操作；證據頁未定位時只能退回。提交帶草稿雜湊及前次核驗ID，核驗人由伺服器記錄。
- 工程／任務切換與卸載清除狀態並忽略舊回應；未提交意見切換或關閉前提示。提交結果不明時保留意見，停用再次提交並要求重新載入核對，避免沿用過期修訂。
- 前端87個單元測試檔全部通過；vue-tsc全量、修改檔ESLint／Stylelint與diff check通過。Chrome元件驗收涵蓋確認／退回／歷史／過期停用／失敗保留及任務切換隔離；390px與1280px無水平溢出，已檢視截圖。首次腳本使用錯誤輸出目錄未執行，修正後又因Element Plus隱藏原生checkbox的定位方式失敗，改為點擊可見標籤並核對checked後完整重跑通過。
- 截圖見docs/lab/verification/browser/handoff-review-390.png、handoff-review-form-390.png及handoff-review-1280.png；重跑方式見frontend/e2e/lab-rules/README.md。本批使用模擬API回應，未替代真實登入帳號、完整工作台／API聯合、原文預覽或深色模式驗收。未部署生產，交接建立介面、下游執行／依賴失效與多程序併發仍待完成。


### 2026-09-09 工位交接真實API瀏覽器聯調與完整回歸

- 使用隔離loopback橋接、實際FastAPI路由與seed監檢身份，連續驗證建立facts交接、固定版本PDF預覽、確認、追加退回、來源變更失效及來源節點權限撤回後列表／詳情拒絕。沒有模擬API回應；PDF／OCR與核驗說明均明確為合成資料，不作真實業務背書。
- 聯調修復兩項缺口：交接引用沒有documentId而既有預覽元件需要它，且預設文件預覽可能採用新版；現由伺服器新增evidenceDocuments解析有權存取的版本／文件身份，前端唯一匹配並帶明確versionId開啟，不回退新版。空工作區首次保存則在延遲載入後初始化缺失交接集合，不覆寫已有資料。
- 瀏覽器實際驗證原文PDF內容與對話框所用Blob的PDF標頭、iframe第1頁定位；來源失效後原核驗保留、操作停用，但仍可讀取有權存取的固定原文。截圖docs/lab/verification/browser/handoff-live-stale.png，重跑方式frontend/e2e/lab-rules/README.md。
- 首輪完整後端3 failed／3687 passed／66 skipped：兩項部署報告暴露核驗路由缺集中式review:save映射與端點api.idempotent，另一項為生成OpenAPI未同步。已接入既有安全／冪等流程並重新匯出openapi/generated/openapi.json，沒有提高基線或增加豁免；首輪原始結果保存handoff-integration-backend-initial.txt。
- 修正後部署／交接相關82 passed、OpenAPI7 passed；完整收集3756項，最終3690 passed／66 skipped／6 warnings，209.16秒、退出0，原始結果及跳過原因見docs/lab/verification/handoff-integration-backend.txt。環境跳過不計入PostgreSQL／MinIO／Temporal／外部平台實機驗收。
- 前端87個測試檔全部通過，vue-tsc、修改檔ESLint／Stylelint、Ruff289／289與diff check通過。最終程式再次完成真實API瀏覽器重跑；測試橋接與Vite已停止。本批仍未完成真實登入帳號／完整工作台、真實監檢文件、交接建立介面、多程序資料庫併發與下游執行，未部署生產。


### 2026-09-09 交接接收端進度與輸入版本分離

- 新建交接升為review-handoff-draft-v3。來源仍綁定完整狀態與輸出；接收端身份僅綁定既有inputHash、工程／租戶／業務包／節點／任務、工位與有效規則、文件版本及來源快照，排除接收任務自己的status／outputHash／findingDrafts，避免接收端執行後令既有交接自我失效。
- 接收端輸入、規則、文件或身份變更仍阻擋使用；來源的狀態或結果變更仍使交接過期。v1／v2按原身份契約驗證，不重寫歷史ID；人工核驗與前端接受事件明確的v2／v3，v1限制保持。
- 相關交接API／契約／巨石回歸90 passed，1條相依套件棄用警告；前端87個測試檔、vue-tsc、修改檔ESLint、Ruff289／289及diff check通過。真實API瀏覽器流程在人工確認後更新接收任務進度與自身結果，確認核驗仍有效，後續退回、來源失效及撤回權限仍通過。
- 初次新增測試以損壞快照觸發了更早的文檔契約拒絕，已改成合法但不同的來源快照後完整重跑本批；沒有放寬拒絕條件。本批未重跑完整後端，上一批3690 passed／66 skipped為修改前全量紀錄。
- 這僅解決接收任務自身進度造成的錯誤失效；完整依賴圖、交接資料加入正式執行快照、選擇性重跑與多程序併發仍待完成。未部署生產，未自動採用交接內容作正式判定。


### 2026-09-09 R39首次應用工藝驗證核心

- 新增evaluate_r39_first_use_validation，依本庫NB/T47013.1-2015第4.3.2.3轉錄，核對首次應用時的工藝驗證實施事實。要求工程、檢測單位、操作指導書與版本、檢測方法、對象及事件完整匹配，依據／應用／驗證各自保留版本、頁碼與摘錄。
- 同事件明確非首次應用返回子檢查不適用；首次應用尚未完成或未知時返回不足，避免提前判不符合。已完成且有來源明確未驗證、或驗證不在首次應用時進行才判不符合；缺記錄不推定未執行。不新增開始前完成或排他驗證方式要求。
- 工具註冊增至94項；輸出明確wholeRuleAcceptance=not_evaluated、evidenceVerified=false。R39原子綁定與發布狀態未改；此核心不判定工藝技術符合性、批准程序、驗證效果或整條R39，正式凍結資料接線與真實文件驗收仍待完成。
- 首次測試命令誤用後端工作目錄下的backend相對路徑，未產生測試檔或執行案例；修正為絕對檔案路徑後，針對性163 passed，完整收集3860項，Ruff289／289無新增告警。完整後端3794 passed／66 skipped／6 warnings，250.53秒，退出0；原始結果與環境跳過原因見docs/lab/verification/r39-first-use-backend.txt，跳過不計入PostgreSQL／MinIO／Temporal實機驗收。
- 契約見docs/lab/verification/r39-first-use-contract.md。未部署生產、未發布69條、未執行ID遷移；W7/W9保持未完成，本批不產生實測品質提升宣稱。
