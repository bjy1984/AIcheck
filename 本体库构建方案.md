# 压力管道监督检验垂直 Agent 本体库构建方案

> 版本：v1.0  
> 日期：2026-07-15  
> 适用业务包：`engineering_inspection_v1`  
> 方案定位：在现有知识库、确定性 Tool、ReviewRun 和人工审查体系之间增加可版本化、可计算、可追溯的领域语义层。

## 1. 结论

本项目不宜从“采购一个图数据库、把文档关系导进去”开始建设本体库。当前最需要补齐的是统一的**领域事实层**：把 OCR 字段、表格、印章、项目数据和固定标准条款，转换为有类型、有主语、有单位、有时间有效性、有证据来源的事实，再以现有 `requiredFacts` 契约提供给确定性 Tool。

推荐目标形态为：

```text
标准规范 / 项目文件 / 业务数据
              ↓
      OCR、表格、签章、元数据
              ↓
     本体映射 + 实体消歧 + 单位归一
              ↓
   版本化 Fact Graph + EvidenceRef
       ↙             ↓             ↘
确定性 Tool       Graph-RAG       Agent 上下文
       ↓             ↓             ↓
原子检查结果 ── 条款依据 ── 可解释审查草稿
              ↓
          人工确认/签发
```

技术上采用“**本体即代码 + PostgreSQL 事实图谱 + pgvector 混合检索**”：

- YAML 作为本体、事实定义、映射和约束的维护发布源，与现有 Business Pack 模式一致。
- PostgreSQL 作为权威运行库，新增类型化实体、关系、事实、证据和快照表。
- 现有 pgvector、Hybrid RAG、PageIndex 和固定标准条款包继续使用，本体图作为过滤、扩展和解释层。
- 第一阶段不新增 Neo4j；当实例边超过千万、4 跳以上路径查询成为主要负载或需要独立图分析时，再增加只读图投影。
- 对外有互操作需求时导出 JSON-LD/Turtle；内部运行不依赖大型 OWL 推理器。

## 2. 项目现状与核心断点

### 2.1 已有资产

当前工程已经具备较好的本体建设基础：

| 资产 | 当前规模/实现 | 本体库中的定位 |
| --- | --- | --- |
| 工业压力管道业务包 | 69 个监督检验节点/规则 | 应用本体的业务边界 |
| 资料类型 | 50 类 | 文档/证据类概念和抽取 Profile |
| 节点资料要求 | 151 项 | `InspectionNode requires MaterialType` 关系 |
| 原子检查 | 173 条 | 规则约束和事实消费方 |
| 唯一 `requiredFacts` | 331 个 | 事实属性注册表的直接输入 |
| 原子检查 Tool 绑定 | 173 条；140 implemented、33 pilot | 规则执行计划 |
| 标准目录 | 29 份标准 | 标准与版本实体 |
| 固定条款绑定/条款包 | 69/69 | `Rule governedBy Clause` 关系和冻结快照 |
| 知识检索 | Hybrid BM25+dense、PageIndex、固定条款 | 文本知识与条文定位 |
| Agent 编排 | Temporal + LangGraph + ReviewRun | 本体事实的消费和审计入口 |
| 数据基础设施 | PostgreSQL JSONB、pgvector、MinIO、Redis/Celery | 本体实例、向量和原件存储基础 |

### 2.2 需要优先解决的问题

1. **Fact Builder 尚未闭环。** `run_rule_engine` 会读取 `context.businessFacts`，但当前 `load_ocr_result` 只写入 `fields/tables/seals/fragments/evidenceLinks`，没有把它们构造成 `businessFacts`。正常运行时，大量确定性 Tool 因收到空事实而只能 fail-closed。
2. **331 个 requiredFacts 只是字符串路径。** 目前没有统一定义其数据类型、所属实体、单位、值域、来源优先级、冲突策略和证据要求。
3. **实体没有统一身份。** 机构名称、人员、管线、焊口、材料批次、证书和报告主要按字段或文本存在，跨文件一致性依赖临时比较，容易误合并或漏关联。
4. **文档知识与业务事实脱节。** 标准条款、项目文件、OCR 字段、规则结果均可追溯，但缺少“条款约束哪个对象属性、文件证明哪个事实、结果消费哪些事实”的显式关系。
5. **通用 JSONB 不适合作为全部图查询的长期载体。** `aicheck_state` 适合兼容状态持久化，但难以对实体类型、关系基数、时间有效性和证据完整性实施数据库约束。
6. **版本冻结还缺本体维度。** ReviewRun 已冻结条款包、规则、Prompt 等，但还应冻结本体版本、映射版本、实体解析版本和 FactSnapshot 哈希。

## 3. 建设边界

### 3.1 本体库负责

- 定义领域对象、属性、关系、枚举、单位、标识和约束。
- 把项目数据与文档解析结果映射为标准事实。
- 对机构、人员、设备、管线、焊口、材料批次等实体进行受控链接。
- 为每条事实保存原始证据、置信度、抽取版本和人工修正记录。
- 为确定性 Tool 输出稳定的 `FactSnapshot` 和兼容的 `businessFacts` 视图。
- 支持跨文件、跨实体、时态和适用性查询。
- 为 Graph-RAG 提供结构化过滤、邻域扩展和条款适用范围。

### 3.2 本体库不负责

- 不替代原始 PDF、图片、OCR 结果、向量索引或固定条款快照。
- 不把标准条文文本直接等同于可执行规则；公式、阈值和覆盖关系仍由版本化 Rule Profile 和确定性 Tool 执行。
- 不让 LLM 自动修改本体、合并关键实体、发布规则或签发监检结论。
- 不把 LLM 草稿当作领域事实；模型输出只能成为待确认的 `Claim` 或审查结果。
- 不以“图中存在一条路径”直接推导合规。合规结论必须由固定原子检查、证据门禁和人工流程产生。

## 4. 四层本体模型

### 4.1 L0：核心本体

跨业务包复用的基础概念：

- `Tenant`、`BusinessPack`、`Project`、`Organization`、`Person`、`Role`
- `Document`、`DocumentVersion`、`EvidenceFragment`、`ImageRegion`
- `Activity`、`Asset`、`Location`、`TimeInterval`
- `Standard`、`StandardVersion`、`Clause`
- `Rule`、`AtomicCheck`、`Tool`、`ReviewRun`、`Finding`、`HumanDecision`
- `FactAssertion`、`Claim`、`Unit`、`CodeItem`、`VersionedRelease`

### 4.2 L1：压力管道领域本体

按稳定业务对象拆分为七个模块：

| 模块 | 核心类 |
| --- | --- |
| 工程与资产 | `PipelineSystem`、`PipelineLine`、`PipeSegment`、`WeldJoint`、`Component`、`Valve`、`SafetyAccessory`、`Instrument` |
| 组织与资质 | `DesignOrganization`、`ConstructionOrganization`、`NdtOrganization`、`OrganizationLicense`、`LicenseScope` |
| 人员与资格 | `Welder`、`NdtPerson`、`PersonnelCertificate`、`QualificationItem`、`Registration` |
| 材料与追溯 | `Material`、`MaterialBatch`、`QualityCertificate`、`RetestReport`、`MarkTransfer`、`MaterialSubstitution` |
| 工艺与施工 | `Wps`、`Pqr`、`WeldingActivity`、`RepairActivity`、`HeatTreatmentActivity`、`InstallationActivity` |
| 检测与试验 | `NdtPlan`、`NdtProcedure`、`NdtActivity`、`NdtReport`、`RadiographicFilm`、`PressureTest`、`LeakTest` |
| 防腐及其他 | `CoatingActivity`、`CathodicProtectionSystem`、`GroundingTest`、`PurgingActivity`、`CrossingWork` |

### 4.3 L2：监督检验应用本体

把现有业务包编排资产语义化：

- `InspectionNode requiresMaterial MaterialType`
- `InspectionNode evaluatedBy Rule`
- `Rule decomposedInto AtomicCheck`
- `AtomicCheck requiresFact FactDefinition`
- `AtomicCheck executedBy ToolPlan`
- `Rule governedBy ClausePackage`
- `ClausePackage contains ClauseReference`
- `ReviewRun reviews Project/InspectionNode`
- `ReviewRun consumes FactSnapshot`
- `CheckResult basedOn FactAssertion/ClauseReference`
- `Finding supportedBy EvidenceFragment`

### 4.4 L3：项目实例与事实图

只存具体项目运行数据，例如：

```text
项目 P-2026-HDCP-001
  ├─ 由设计单位 ORG-001 设计
  ├─ 包含管线 LINE-101，级别 GC1
  ├─ 设计许可证 LIC-001 由 ORG-001 持有
  ├─ 许可证范围包含 GC1
  ├─ 许可证有效至 2027-12-31
  ├─ 施工图 DOCV-101 标题栏单位指向 ORG-001
  └─ 每条事实均指向页码、bbox/原文和 OCR/人工来源
```

L0-L2 可跨项目复用；L3 必须按 `tenant_id + project_id` 隔离。

## 5. 核心关系设计

本体关系按五组治理，避免关系无限增长。

### 5.1 结构关系

- `Project contains PipelineSystem/PipelineLine/WeldJoint`
- `PipelineLine contains PipeSegment/WeldJoint/Component`
- `Document hasVersion DocumentVersion`
- `Standard hasVersion StandardVersion`
- `Rule decomposedInto AtomicCheck`

### 5.2 主体与责任关系

- `Organization participatesIn Project`
- `Organization performs Activity`
- `Person employedBy Organization`
- `Person performs WeldingActivity/NdtActivity`
- `Certificate issuedTo Person/Organization`

### 5.3 证明与追溯关系

- `Document describes Asset/Activity`
- `Certificate certifies Person/Organization/MaterialBatch/Instrument`
- `Report reports Activity`
- `EvidenceFragment supports FactAssertion`
- `FactAssertion derivedFrom DocumentVersion`
- `Finding supportedBy FactAssertion`

### 5.4 规则与标准关系

- `Clause defines Requirement`
- `StandardVersion supersedes StandardVersion`
- `Requirement appliesTo EntityType/Condition`
- `AtomicCheck operationalizes Requirement`
- `ToolResult basedOn RuleProfile/ClauseReference`

### 5.5 时间与状态关系

- 证照、标准、资格、仪表校准和规则均保存 `validFrom/validTo`。
- 事实同时保存业务有效时间与系统记录时间，支持“当时有效、后来更正”的双时态查询。
- 删除只用于错误草稿；已发布概念、事实和映射使用 `deprecated/superseded`，保留历史 ReviewRun 可复现性。

## 6. 事实定义协议

为 331 个 `requiredFacts` 建立 `fact_definitions.yaml`。每个事实至少包含：

| 字段 | 说明 |
| --- | --- |
| `factCode` | 与现有路径兼容，如 `designLicense.validUntil` |
| `subjectClass` | 事实所属实体类型 |
| `predicate` | 本体属性/关系 |
| `valueType` | string、decimal、date、code、entityRef、objectList 等 |
| `cardinality` | `0..1`、`1..1`、`0..n`、`1..n` |
| `unit` | MPa、℃、mm、min 等；无单位时为空 |
| `codeSet` | GC1/GC2/GCD、NDT 方法、结论状态等值域 |
| `sourceProfiles` | 允许的资料类型、表格 Schema 或业务接口 |
| `normalizer` | 机构名、标准号、日期、单位、材料牌号等归一策略 |
| `identityPolicy` | 该字段是否可参与实体主键/匹配 |
| `evidencePolicy` | 页码、bbox/原文、最低置信度、是否允许人工录入 |
| `conflictPolicy` | 拒绝、取权威源、保留多值或转人工 |
| `missingPolicy` | 证据不足、不符合、不适用或人工确认 |
| `sensitivity` | 普通、个人信息、敏感业务数据 |

示例：

```yaml
- factCode: designLicense.validUntil
  subjectClass: OrganizationLicense
  predicate: validUntil
  valueType: date
  cardinality: 1..1
  sourceProfiles: [design_license]
  normalizer: iso_date
  evidencePolicy:
    requireDocumentVersion: true
    requirePage: true
    requireBboxOrQuotedText: true
    minConfidence: 0.75
  conflictPolicy: human_review
  missingPolicy: evidence_insufficient

- factCode: project.pipelineGrades
  subjectClass: Project
  predicate: hasPipelineGrade
  valueType: code[]
  cardinality: 1..n
  codeSet: pipeline-grade-cn-v1
  sourceProfiles: [project_master_data, design_document, pipeline_schedule]
  normalizer: pipeline_grade
  conflictPolicy: authoritative_project_then_human_review
```

## 7. 实体身份与消歧

### 7.1 稳定 ID

建议统一使用不可变业务 ID，显示名称不得作为主键：

- `ORG-{tenant}-{hash}`：机构，优先统一社会信用代码或权威证照主体号。
- `PER-{tenant}-{hash}`：人员，优先证件/资格档案号；前端和日志脱敏。
- `PIPE-{project}-{lineNo}`、`WELD-{project}-{weldNo}`。
- `MATB-{project}-{batchNo}-{manufacturer}`。
- `LIC-{issuer}-{certificateNo}`、`CERT-{issuer}-{certificateNo}`。
- `DOCV-*` 继续复用现有 `documentVersionId`。

### 7.2 合并策略

实体解析按以下顺序执行：

1. 权威唯一标识完全匹配。
2. 已审核别名映射命中。
3. 组合键匹配，例如“机构统一代码 + 许可证号”。
4. 归一名称 + 地址/发证机关/项目关系等多特征候选。
5. 关键实体仅有模糊名称时不自动合并，生成 `EntityResolutionTask` 供人工确认。

任何实体合并必须保存旧 ID、候选分值、规则/模型版本、操作者和撤销记录。不得用单一字符串相似度自动合并机构或人员。

## 8. 存储设计

### 8.1 选型

第一阶段继续使用 PostgreSQL，原因是：

- 当前持久化、租户隔离、事务、审计和 ReviewRun 都以 PostgreSQL 为中心。
- 本项目主要是 1～3 跳的证据、规则和工程对象查询，递归 CTE 与类型化索引足够。
- 合规事实和人工修正需要强事务、唯一约束和版本冻结，关系库更易治理。
- 避免在本体尚未稳定时同时维护 PostgreSQL 与图数据库双写一致性。

现有 `aicheck_state` 继续保存兼容业务状态；本体核心新增规范化表。不要修改已经冻结的 `0001`，通过新的版本化 migration 增加。

### 8.2 建议表

| 表 | 关键字段 | 用途 |
| --- | --- | --- |
| `ontology_releases` | tenant、pack、version、status、checksum | 本体发布与回滚 |
| `ontology_terms` | release、code、kind、label、definition | 类、属性、关系、枚举注册表 |
| `ontology_constraints` | release、target、constraint_json | 基数、值域、条件和证据约束 |
| `ontology_entities` | tenant、project、entity_id、class_code、canonical_key、payload | 项目实例 |
| `ontology_entity_aliases` | entity_id、alias、normalizer、status | 别名与主数据映射 |
| `ontology_edges` | subject_id、predicate、object_id、valid_time、status | 实体关系 |
| `fact_assertions` | subject_id、predicate、value_json、type、unit、confidence、valid_time | 标准化事实 |
| `fact_evidence_refs` | fact_id、document_version_id、page、bbox、quote、source_version | 事实证据 |
| `fact_conflicts` | predicate、candidate_fact_ids、resolution | 冲突治理 |
| `fact_snapshots` | review_run_id、ontology/mapping/profile versions、hash | ReviewRun 冻结事实集 |
| `fact_snapshot_items` | snapshot_id、fact_id、value_hash | 不可变快照项 |
| `ontology_validation_runs` | release/snapshot、result、errors、metrics | 编译和质量门禁 |
| `entity_resolution_tasks` | candidates、scores、decision、audit | 人工消歧 |

所有实例表必须包含 `tenant_id`；项目事实还必须包含 `project_id`。关键唯一索引和查询索引至少覆盖：

- `(tenant_id, project_id, entity_id)`
- `(tenant_id, project_id, class_code)`
- `(tenant_id, subject_id, predicate)`
- `(tenant_id, predicate, object_id)`
- `(tenant_id, document_version_id)`
- `(tenant_id, review_run_id)`
- `payload/value_json` 的 GIN 索引只作辅助，不替代类型化列。

## 9. 本体维护目录

建议直接并入现有工程业务包：

```text
backend/business_packs/engineering_inspection_v1/ontology/
  manifest.yaml
  classes.yaml
  predicates.yaml
  code_sets.yaml
  units.yaml
  fact_definitions.yaml
  entity_resolution.yaml
  constraints/
    core.yaml
    licensing.yaml
    welding.yaml
    ndt.yaml
    pressure_test.yaml
  mappings/
    project_master_data.yaml
    ocr_field_mappings.yaml
    table_schema_mappings.yaml
    material_profiles/
      design_license.yaml
      welder_certificate.yaml
      pressure_test_report.yaml
  fixtures/
    r01.yaml
    r03.yaml
    r24.yaml
    r61.yaml
```

新增运行模块：

```text
backend/libs/ontology/
  registry.py             # 加载与查询本体注册表
  compiler.py             # 编译、引用完整性和发布校验
  fact_builder.py         # OCR/业务数据 → 候选事实
  normalizers.py          # 单位、名称、编号、日期归一
  entity_resolver.py      # 实体链接和人工消歧任务
  validator.py            # 类型、基数、值域、证据和冲突校验
  repository.py           # PostgreSQL 读写
  snapshot.py             # ReviewRun 不可变 FactSnapshot
  compatibility_view.py   # 输出现有 businessFacts 点路径结构
  query_service.py        # 图查询和 Graph-RAG 邻域查询
```

现有 `business_pack/loader.py` 应把 ontology manifest 作为可选但可发布校验的业务包组成部分，校验：

- `requiredFacts` 是否全部在注册表中存在。
- `materialTypeCode` 与映射 Profile 是否存在。
- predicate 的 domain/range、单位和值域是否合法。
- Tool Binding 引用的事实、Rule Profile 和条款包是否同版本可用。
- 发布版本中不存在未处理的 breaking change。

## 10. 构建流水线

### 10.1 Schema 编译

```text
本体 YAML
  → 语法校验
  → 跨文件引用校验
  → requiredFacts 覆盖校验
  → 约束冲突/循环/废弃项校验
  → 生成 checksum 和发布包
  → 写入 ontology_releases/terms/constraints
```

每个发布包应生成：

- `ontologyVersion`
- `mappingVersion`
- `constraintVersion`
- `checksum`
- 与上一版本的 breaking/additive/metadata diff
- 受影响的规则、原子检查、资料类型和评测用例清单

### 10.2 实例构建

```text
项目主数据 + DocumentVersion + OCR ParseResult
  → 按 MaterialType 选择映射 Profile
  → 生成候选实体和候选事实
  → 字段/日期/单位/标准号归一
  → 实体链接
  → 类型、基数、值域和证据校验
  → 冲突检测
  → 写入实体/关系/事实/证据
  → 生成面向当前节点的 FactSnapshot
```

事实来源优先级建议：

```text
人工已确认值
  > 权威业务主数据
  > 结构化表格且通过证据门禁的值
  > 普通 OCR 字段
  > LLM/VLM 候选值
```

优先级只决定推荐候选，不得静默覆盖冲突值；冲突事实必须保留并按策略转人工。

## 11. 与 ReviewRun/Agent 的接入

### 11.1 调整执行图

在现有图中增加事实构建与校验步骤：

```text
load_context
→ load_ocr_result
→ build_fact_snapshot       # 新增
→ validate_fact_snapshot    # 新增
→ run_rule_engine
→ retrieve_knowledge
→ build_prompt
→ llm_generate_findings
→ schema/evidence/reference/critic/quality gates
→ persist_drafts
```

其中：

- `build_fact_snapshot` 读取项目、节点、资料类型、OCR、表格、签章与条款快照。
- `validate_fact_snapshot` 对当前节点的 requiredFacts 做类型、缺失、冲突和证据检查。
- `compatibility_view.py` 将图事实输出为现有 Tool 可直接读取的结构：

```json
{
  "designLicense": {
    "holderName": "某设计院",
    "scopeCodes": ["GC1"],
    "validFrom": "2024-01-01",
    "validUntil": "2027-12-31"
  },
  "designDocument": {
    "titleBlockOrganization": "某设计院",
    "designSealOrganization": "某设计院",
    "pipelineGrades": ["GC1"]
  },
  "project": {
    "constructionStart": "2026-06-01",
    "plannedConstructionEnd": "2027-03-31",
    "pipelineGrades": ["GC1"]
  }
}
```

这样 `compile_node_tool_plan`、`execute_node_tool_plan` 和现有确定性 Tool 可先保持接口不变，降低改造风险。

### 11.2 ReviewRun 冻结项

每次运行新增冻结：

- `ontologyVersion`、`ontologyChecksum`
- `factDefinitionVersion`
- `mappingVersion`
- `entityResolutionVersion`
- `unitCatalogVersion`
- `factSnapshotId`、`factSnapshotHash`
- `factValidationRunId`

Tool 结果应记录实际消费的 `factIds/evidenceRefIds`，不能只记录 `ocr_fields count`。

### 11.3 Agent 使用边界

Agent 获得三类受控查询能力：

1. `get_fact_snapshot`：读取当前节点必需事实及状态。
2. `query_project_graph`：只读查询当前租户/项目授权范围内的实体邻域。
3. `explain_fact_lineage`：取得事实到原件、页码、坐标、映射、人工修正的完整血缘。

Agent 不获得本体发布、实体合并、事实覆盖或自动放行权限。

## 12. 与知识检索的协同

检索顺序建议调整为：

```text
固定条款包（节点精确依据）
  → 本体过滤（对象类型、工艺、管道级别、标准有效时间）
  → 图邻域扩展（相关证书、报告、活动、上下游记录）
  → Hybrid BM25 + dense
  → PageIndex 长文档定位
```

本体图不替代向量检索，而是解决三类问题：

- **检索过滤：** 只检索对当前管道等级、材料、工艺和日期有效的条款/文件。
- **跨文件连接：** 从焊口找到焊工证、WPS/PQR、焊接记录、NDT 报告和返修复检。
- **依据解释：** 展示“原子检查 → 所需事实 → 实体 → 证据 → 固定条款 → Tool 结果”的完整链。

不要把整个项目图谱塞入 Prompt；只发送当前节点子图、确定性结果和必要证据摘要。

## 13. 管理端能力与接口

### 13.1 管理页面

可在现有“AI 知识库管理”中增加：

- 本体版本与发布：版本差异、影响范围、审批、回滚。
- 类/关系浏览器：概念层次、domain/range、使用位置。
- Fact 字典：331 个 requiredFacts 的类型、单位、来源和覆盖状态。
- 映射管理：资料类型、OCR 字段、表格列到事实定义的映射。
- 实体消歧任务：候选机构/人员/设备合并确认。
- 项目事实图：按项目、节点、实体查看事实与证据。
- 冲突与低置信度队列：冲突值、缺失事实、无证据事实。
- 影响分析：本体/映射变化影响哪些规则、Tool、ReviewRun 和评测集。
- 质量看板：覆盖率、准确率、冲突率、人工修正率和版本漂移。

### 13.2 建议 API

| 接口 | 用途 |
| --- | --- |
| `GET /api/ontology/releases` | 版本列表与发布状态 |
| `GET /api/ontology/terms` | 类、属性、关系和枚举查询 |
| `GET /api/ontology/fact-definitions` | requiredFacts 注册表 |
| `POST /api/ontology/releases/{id}/validate` | 发布前编译与门禁 |
| `POST /api/ontology/releases/{id}/publish` | 经审批后发布 |
| `GET /api/projects/{projectId}/knowledge-graph` | 项目授权子图 |
| `GET /api/entities/{entityId}` | 实体、关系、事实和证据详情 |
| `GET /api/review-runs/{reviewRunId}/fact-snapshot` | 运行冻结事实 |
| `POST /api/documents/{versionId}/fact-builds` | 重建文档事实 |
| `GET /api/entity-resolution/tasks` | 实体消歧队列 |
| `POST /api/entity-resolution/tasks/{id}/decide` | 人工确认/拒绝合并 |
| `GET /api/fact-conflicts` | 事实冲突队列 |
| `POST /api/fact-conflicts/{id}/resolve` | 人工解决冲突 |

所有变更接口继续使用现有 JWT、action/node scope、`Idempotency-Key`、租户隔离和审计链机制。

## 14. MVP 范围

不要一开始为 69 个节点全部建模。首批选择三类具有代表性的闭环：

### 14.1 R01 设计单位许可资质

验证：机构实体归一、三处名称一致性、许可范围覆盖、时间区间覆盖、印章证据、固定条款关联。

核心实体：`Project`、`Organization`、`OrganizationLicense`、`DesignDocument`、`PipelineLine`。

### 14.2 R24 焊工资格

验证：人员身份、资格项目解码、实际焊接范围、证书有效期、焊口/焊接活动关联。

核心实体：`Welder`、`PersonnelCertificate`、`QualificationItem`、`WeldJoint`、`WeldingActivity`。

该节点现有专业规则仍处于受限试点时，本体先支持证据和事实闭环，不扩大自动放行范围。

### 14.3 R61 耐压试验参数

验证：单位归一、数值/公式、设计条件与试验记录跨文件一致性、压力表校准关系和时态有效性。

核心实体：`PressureTest`、`PipelineSystem`、`Instrument`、`CalibrationCertificate`、`PressureTestReport`。

该节点继续遵守当前压力试验规则门禁，未明确或缺少正式依据的分支转人工。

三个场景分别覆盖“主体/资质”“人员/作业范围”“数值/设备/公式”，可以验证本体底座能否支撑后续 69 节点扩展。

## 15. 分阶段实施计划

以下工期按 1 名后端、1 名知识工程/算法、0.5 名领域专家和现有前端协作估算。

| 阶段 | 建议周期 | 交付物 | 退出条件 |
| --- | --- | --- | --- |
| P0 盘点与冻结 | 1～2 周 | 331 Fact 清单、实体清单、命名/单位/ID 规范、MVP 金标样本 | requiredFacts 无孤儿；MVP 业务口径签字确认 |
| P1 本体基础 | 2～3 周 | ontology 目录、编译器、版本/校验表、loader 校验、发布 diff | 本体可重复编译；checksum 稳定；错误引用 fail-fast |
| P2 Fact Builder MVP | 3～4 周 | R01/R24/R61 映射、归一、实体解析、事实/证据表、冲突队列 | 金标事实准确率和证据完整率达标 |
| P3 Agent 接入 | 2～3 周 | 新图步骤、FactSnapshot、兼容 businessFacts、Tool 输入/结果引用 | 同输入同版本结果可复现；缺证 fail-closed |
| P4 规模化 | 6～10 周 | 按模块扩展至 69 节点、管理页面、Graph-RAG | 每个发布节点通过覆盖、评测和领域审批 |
| P5 持续治理 | 持续 | 变更影响分析、回归集、人工反馈闭环、质量看板 | 发布门禁自动化，质量无回退 |

扩展顺序建议：资质/设计 → 材料 → 焊接/热处理 → NDT → 防腐/安装 → 压力/泄漏试验 → 综合评价。

## 16. 验收指标

### 16.1 Schema 与治理

- 331 个唯一 `requiredFacts` 注册覆盖率：100%。
- 已发布 FactDefinition 的 domain/range、类型、单位和值域完整率：100%。
- 每次发布均有 checksum、版本 diff、影响清单和审批记录：100%。
- 已发布概念被删除或改变语义而未升级 major 版本：0。

### 16.2 事实与证据

- 关键事实的 `documentVersionId + pageNo + bbox/quotedText` 证据完整率：100%。
- 机构/人员关键实体自动链接精确率：≥99%；低于阈值必须转人工。
- MVP 金标集字段 precision/recall：关键字段均 ≥95%。
- 压力、温度、长度、时间等关键数值单位归一准确率：100%。
- 未解决冲突或低置信度事实进入自动“符合”判断：0。

### 16.3 Agent 与规则

- ReviewRun 的 ontology/mapping/fact snapshot 版本冻结率：100%。
- Tool 结果可追溯到具体 factIds、evidenceRefIds 和 clauseRefs：100%。
- 同输入、同版本、同规则的 FactSnapshot 和确定性结果哈希一致率：100%。
- `evidence_insufficient` 被 LLM 改写为自动符合：0。
- 现有业务包、Tool、ReviewRun、权限和审计回归测试无新增失败。

### 16.4 性能

- 已完成 OCR 后，单节点 FactSnapshot 构建 P95：目标 <3 秒。
- 常用 1～3 跳项目子图查询 P95：目标 <300 毫秒。
- FactSnapshot 读取 P95：目标 <100 毫秒。
- 大批量重建采用 Celery 分片、幂等 checkpoint 和可恢复任务，不阻塞 API。

## 17. 测试体系

新增测试分层：

1. **Schema 测试：** 重复 ID、未知类/属性、domain/range、基数、单位、枚举、循环和废弃引用。
2. **映射测试：** 每种资料 Profile 的字段/表格/签章输入到期望事实输出。
3. **实体解析测试：** 同名不同机构、名称变体、换证、人员重名、跨项目隔离。
4. **时态测试：** 标准替代、许可证续期、证书到期、校准周期和 ReviewRun 历史重放。
5. **证据测试：** 缺页码、缺 bbox/原文、OCR 低置信度、冲突来源、人工修正。
6. **规则契约测试：** 331 FactDefinition 与 173 Tool Binding 的静态一致性。
7. **端到端测试：** 上传 → OCR → Fact Builder → Tool → 条款 → Finding → 人工决定。
8. **租户与权限测试：** 跨租户实体、事实、图查询和证据链接全部拒绝。
9. **回放测试：** 历史 ReviewRun 必须使用原本体和 FactSnapshot，不受新版本污染。

现有 `fixtures.yaml`、OCR 评测集、evaluation cases 和 R01-R69 正反用例可以作为本体回归集来源。

## 18. 发布与治理机制

### 18.1 版本拆分

不要用一个版本号覆盖所有变化：

- `ontologySchemaVersion`：类、关系和事实语义。
- `mappingVersion`：OCR/表格/业务字段映射。
- `entityResolutionVersion`：主数据、别名和匹配策略。
- `unitCatalogVersion`：单位和换算。
- `ruleProfileVersion`：公式、阈值、覆盖表和适用性。

版本状态统一为：`draft → in_review → published → deprecated`。生产 ReviewRun 只能使用 published 版本。

### 18.2 职责

| 角色 | 责任 |
| --- | --- |
| 领域专家/监检人员 | 概念定义、事实口径、规则适用性和发布审批 |
| 知识工程 | 本体、映射、实体解析和质量规则 |
| 后端工程 | 编译、存储、Fact Builder、API 和运行可靠性 |
| FDE/测试 | 金标集、回归、漂移、错误分析和发布门禁 |
| 系统管理员 | 租户配置、任务重建和权限，不审批专业语义 |

关键语义和规则至少需要“领域专家 + 非开发审批人”双人审批，延续现有高风险发布治理方式。

## 19. 主要风险与控制

| 风险 | 控制措施 |
| --- | --- |
| 过度建模，半年仍不能运行 | 以 R01/R24/R61 和 331 requiredFacts 为边界，先闭环再扩展 |
| 把文档目录当文件本体 | `DocumentVersion bodyAvailable/parseable` 建成独立事实和强约束 |
| 机构/人员误合并 | 权威 ID 优先；模糊匹配只产候选；人工决策可撤销 |
| 标准条文与执行规则混淆 | Clause、Requirement、RuleProfile、ToolResult 分层并分别版本化 |
| 新本体污染历史审查 | ReviewRun 冻结完整版本与 FactSnapshot，不读取最新事实重算历史 |
| 图谱成为新的数据孤岛 | 由 Fact Builder 消费现有项目/OCR/条款数据，并输出现有 Tool 契约 |
| 图查询泄露项目数据 | 所有表带 tenant/project scope，API 复用现有 action/node scope |
| LLM 生成虚假图事实 | LLM/VLM 仅生成候选；未通过证据和类型门禁不得进入正式快照 |
| 双写和运维复杂 | PostgreSQL 为唯一权威库；外部图数据库仅做可重建只读投影 |

## 20. 首轮可执行任务清单

1. 从 `atomic_check_tool_bindings.yaml` 自动生成 331 个 requiredFacts 基线清单。
2. 将 331 个事实按 91 个左右的上层对象路径聚类，确认实体、类型、单位和证据策略。
3. 建立 `ontology/manifest.yaml`、`classes.yaml`、`predicates.yaml`、`fact_definitions.yaml` 初版。
4. 给 Business Pack Loader 增加事实引用完整性校验，先只在 CI 报告，不改变生产运行。
5. 增加新的 migration 和本体规范化表，保留现有 `aicheck_state` 兼容层。
6. 实现 R01 Fact Builder，打通 `OCR → FactAssertion → EvidenceRef → businessFacts → Tool`。
7. 将 `build_fact_snapshot` 和 `validate_fact_snapshot` 加入 ReviewRun 图。
8. 为 ToolResult 增加 `factIds/evidenceRefIds/ontologyVersion/factSnapshotHash`。
9. 用 R01 正常、缺证、过期、范围不覆盖、名称冲突五组用例跑通端到端回归。
10. R01 通过后再分别实施 R24、R61，最后按业务模块扩展。

## 21. 最终判断

这个项目已经有“文档知识库、固定条款、规则、Tool 和 Agent 编排”，缺的并不是另一个检索库，而是把这些资产连接起来的**可计算语义骨架**。本体库建设成功的标志，也不是能画出一张很大的知识图，而是：

- OCR 的每个关键值能变成有类型的事实；
- 每个事实能找到唯一或明确待消歧的业务对象；
- 每个 Tool 都能获得完整、版本固定、证据可追溯的输入；
- 每个 AI 结论都能沿“结论 → Tool → Fact → Evidence → Clause”反向复核；
- 新标准、新资料类型和新业务包可以通过配置和发布机制扩展，而不是持续在 Prompt 和单文件代码中堆条件。

因此，建议把“Fact Builder + 本体注册表 + FactSnapshot”列为当前基础设施 P0；图数据库和高级语义推理列为规模验证后的可选增强项。
