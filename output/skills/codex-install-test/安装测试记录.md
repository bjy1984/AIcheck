# Codex 安装与测试记录

日期：2026-09-22。Codex CLI：0.144.6。

## 已完成安装

- 用户级 skill：`/Users/mac/.agents/skills/aicheck-inspection/SKILL.md`。
- 全局 MCP：`aicheck-inspection`，以 `/opt/homebrew/bin/python3` 启动安装目录中的 `scripts/mcp_server.py`。
- 安装发布包的八个文件，逐一核对源文件与 SHA-256；未覆盖已有技能，不包含凭据或工程测试资料。
- 使用 skill-installer 的校验、复制逻辑安装本地发布包；用户级目录依据当前 [Codex 官方技能说明](https://learn.chatgpt.com/docs/build-skills)。MCP 通过官方 `codex mcp add` 注册，见 [官方 MCP 说明](https://learn.chatgpt.com/docs/extend/mcp?surface=cli)。

## 已验证项目

| 检查 | 实际结果 | 证据 |
| --- | --- | --- |
| 安装内容一致性 | 八个文件与已验证发布包一致 | `installation.json` |
| Skill 格式 | quick_validate 通过 | 本次命令输出 |
| Codex 实际发现 | `skills/list` 返回唯一 `aicheck-inspection`，scope=user、enabled=true，无错误 | `codex-discovery.json` |
| MCP 直接协议 | initialize、tools/list、R24 和 R25 本地规则调用成功 | `installed-mcp-transcript.json` |
| Codex 实际 MCP 发现 | `mcpServerStatus/list` 已握手并发现七个工具 | `codex-mcp-discovery.json` |
| 工具边界 | 七个工具，无 OCR、工程资料上传或后台审查任务工具 | `installed-mcp-transcript.json` |
| 后台连接诊断 | 返回 notConfigured；尚无服务地址和个人令牌 | `installed-mcp-transcript.json` |

新后台接口尚未部署，本次没有伪装远程规则、标准或证件核验成功。

## Scan 项目资料实际测试

用户已明确授权“允许，在工程中找到scan中的文件进行分析”，本次资料范围据此改为本项目 `Scan` 目录。此前针对 `test2` 图像的自动审批拒绝已不再是本次测试的阻碍；`test2` 准备材料保留，但不计入本轮正式审查。

独立 Codex CLI 试跑经批准后失败，原因是本机 CLI 0.144.6 不支持当前 `gpt-6-astra` 模型，错误日志保存在 `../codex-scan-test/codex-exec.jsonl`。没有把失败运行标为通过，也没有替换模型或擅自升级 CLI。

实际审查由**当前 Codex 会话**完成：已安装 skill 已出现在本轮可用技能列表；宿主读取扫描原页，按设计、材料、焊接三组执行 v3 节点审查，本地汇总证据和补件建议。审查结果及覆盖范围见 [Scan 资料审查报告](../codex-scan-test/Scan资料审查报告.md)。

本地 MCP 使用安装目录的脚本实际完成 stdio 握手、七个工具发现和全部十二个节点规则读取；协议响应记录见 `../codex-scan-test/mcp-review-transcript.json`。本轮传给 MCP 的业务参数仅为节点编号，未发送工程原件或 OCR 全文。当前宿主工具列表未直接暴露该连接器，因此不把直接 stdio 协议验证表述为本轮界面原生工具调用。

后端连接仍返回 `notConfigured`：标准查询、证件有效性服务及官方登记核验尚未端到端验证。报告中的证载日期比较是本地字段比对；公开标准核对使用官方网页/文件，不冒充 AIcheck 后台返回。

工程原件未改动；报告、结构化证据、文件 SHA-256 与本地渲染页保存在 `output/skills/codex-scan-test/`。没有上传至 AIcheck 后台，没有调用后端 OCR 或创建工程审查任务。

## 使用方法

下一轮可直接输入：

> 使用 $aicheck-inspection 审查我提供的本地资料，先完成必要的识别和字段整理，再按选定节点列出证据、缺项及人工复核事项。

MCP 本地规则无需后台账号。其余后台能力仍需部署新审查服务并配置实际服务地址和个人令牌；本次未写入生产账号或令牌。
