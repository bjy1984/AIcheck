# AI 工程监检复核业务节点分析（R01-R69）

## 1. 文档目的

本文记录 R01-R69 业务节点的审查目标、固定标准条款、证据要求、Tool 调用链、结论规则以及待确认事项。编号唯一依据为 `files/checklist.docx` 的 1-69 项；完整旧新编号关系见 `docs/业务规则编号迁移表.md`。条款级绑定关系应固化在标准条款包和数据库中。默认节点中 LLM 只组织 Tool 和解释结果；R19 允许 LLM 基于固定条款与已登记 EvidenceRef 形成原子项语义判断，但不得自行发现或改写条款绑定关系，节点 result 仍由固定聚合器生成。

## 2. 通用执行原则

每个业务节点按以下顺序执行：

```text
业务节点适用性判断
→ 固定条款加载
→ 证据文件定位与完整性校验
→ 结构化事实抽取
→ 确定性 Tool 判断
→ 原子项结果聚合
→ 证据、条款和结果关联
→ 生成 AI 复核结论
```

> Tool 名称口径：各节点“Tool 调用链”已经按 atomicCheck 替换为当前工程实际注册和正式绑定的运行时 Tool，并直接给出中文 description、代码实现类型和绑定状态。专业分析中的 Fact Builder、实体归一化、规则解析、结果聚合和人工确认仍是编排能力，不再伪装成可调用 Tool。正式绑定源为 `atomic_check_tool_bindings.yaml`，`tools规划.md` 和本文调用链均由该口径核对。

实现状态说明：`已实现（证据读取）`只负责取得证据；`已实现（专用确定性判断）/（专用业务规则）`表示存在独立 handler；`已实现（通用确定性判断）`表示复用通用比较器；`已实现（通用规则解释器；仍需规则参数装配）`表示只有通用执行框架，Fact Builder 和 Tool Argument Assembler 未提供完整参数时仍会返回 `evidence_insufficient`。绑定状态 `pilot_implemented` 表示仍受试点范围或专业规则版本限制，不能按生产放行能力理解。

统一结论状态：

- `COMPLIANT`：证据充分且满足规则。
- `NON_COMPLIANT`：证据充分且明确不满足规则，或应提供文件但文件本体缺失。
- `INSUFFICIENT_EVIDENCE`：资料不完整，无法作出可靠判断。
- `NOT_APPLICABLE`：经业务规则确认该节点或原子项不适用。
- `REQUIRES_HUMAN_REVIEW`：形式检查完成，但需要专业人员作实质判断。

“目录中列出文件”不能等同于“文件本体已上传”。文件本体缺失时，应按业务规则判定为文件缺失，不得返回资料齐全。

## 3. R01 设计单位许可资质

### 审查目标

核对设计单位是否具有适用的设计许可，许可证范围是否覆盖当前工程和设计活动，许可证有效期是否覆盖整个施工计划工期。

### 固定规则

- 许可证类型、许可级别、许可范围与项目要求匹配。
- 覆盖日期采用计划结束、实际结束、技术变更澄清结束日期中的最晚者作为设计许可覆盖结束日。
- 许可证在工程结束日前失效，判定不符合。
- 设计单位名称必须做实体归一化，不能用简单字符串包含判断。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R01-01` | 1 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R01-01` | 2 | `recognize_signatures_and_seals` | 读取签字角色、签章、印章文字、位置和置信度；不判断签字是否齐全。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R01-01` | 3 | `check_all_equal` | 对多个来源的值按指定归一化方式比较一致性；有效值不足时返回证据不足。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R01-01` | 4 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R01-02` | 1 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R01-02` | 2 | `extract_table_records` | 读取指定文档版本的表格和标准化行记录，可按业务表 Schema 过滤。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R01-02` | 3 | `check_design_license_scope` | 按 GC1、GC2、GCD 设计许可覆盖规则核验许可范围。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R01-02` | 4 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R01-03` | 1 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R01-03` | 2 | `check_date_covers` | 判断证照或报告有效期是否覆盖闭区间业务周期。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R01-03` | 3 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R01-04` | 1 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R01-04` | 2 | `extract_table_records` | 读取指定文档版本的表格和标准化行记录，可按业务表 Schema 过滤。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R01-04` | 3 | `check_design_license_scope` | 按 GC1、GC2、GCD 设计许可覆盖规则核验许可范围。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R01-04` | 4 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R01-05` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R01-05` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |

### 已落地实现和输入契约

- Tool Argument Assembler 已把 `designLicense`、`designDocument` 和 `project` 事实装配为名称一致性、范围覆盖和日期覆盖 Tool 的顶层参数。
- R01 日期结束点固定取 `plannedConstructionEnd`、`actualConstructionEnd`、`changeClarificationEnd` 中可解析日期的最晚者；缺少必要日期时返回 `evidence_insufficient`。
- 上游 Fact Builder 仍须从证书本体、标题栏、设计印章、施工说明和管道特性表生成这些结构化事实及 EvidenceRef；Tool 不从自然语言自行猜值。

### 关键事实

许可证编号、单位统一社会信用代码、许可类别、许可级别、许可范围、有效起止日期、计划结束日期、实际结束日期、项目名称。

### 结论规则

- 许可证过期或范围不覆盖：`NON_COMPLIANT`。
- 只上传证书目录、未上传证书本体：`NON_COMPLIANT / 文件缺失`。
- 单位名称无法可靠匹配：`INSUFFICIENT_EVIDENCE`，转人工确认。

### 待确认

许可证变更、换证、延续期间的过渡有效规则，需要补充到证照版本和过渡政策表中。

## 4. R02 施工单位许可资质

### 审查目标

确认受检单位具有本项目所需的安装资质，并检查资质级别、许可范围、有效期和单位主体是否匹配。

### 已确认业务口径

- 当前项目要求明确为“**A级锅炉安装资质**”。
- 固定覆盖关系为：GC1 只能由 GC1 覆盖；GC2 可由 GC2、GC1 或 GCD 覆盖；GCD 可由 GCD 或 A级锅炉安装资质覆盖。A级锅炉安装资质不能单独覆盖 GC2。
- 单位名称使用实体匹配，不采用模糊子串匹配。
- 联络单暂不实现；有效期不覆盖时只输出 `CONTACT_NOTICE_REQUIRED` 建议，不创建、不发送外部单据。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R02-01` | 1 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R02-01` | 2 | `extract_table_records` | 读取指定文档版本的表格和标准化行记录，可按业务表 Schema 过滤。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R02-01` | 3 | `check_installation_license_scope` | 按 GC1、GC2、GCD 和 A级锅炉安装资质的固定覆盖关系核验安装许可范围。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R02-01` | 4 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R02-02` | 1 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R02-02` | 2 | `check_date_covers` | 判断安装许可证有效期是否覆盖施工计划闭区间。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R02-02` | 3 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R02-03` | 1 | `check_date_covers` | 复用有效期覆盖结果；失败时返回联络单提醒建议但不创建外部单据。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R02-03` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R02-04` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R02-04` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |

### 已落地实现和输入契约

- `check_installation_license_scope` 接收 `installationLicense.scopeCodes` 和 `project.pipelineGrades`，使用版本化固定覆盖表，不再同时调用语义冲突的通用 `check_scope_coverage`。
- `check_date_covers` 接收证书有效期和施工计划起止日期；R02 不采用实际结束日，也不因“覆盖”一词触发任何签字、印章 Tool。
- Fact Builder 必须从安装许可证本体和设计文件生成范围代码、项目管道等级及计划工期；缺少事实时禁止判定符合。

### 结论规则

- 未满足项目管道等级对应的固定覆盖关系：`NON_COMPLIANT`。
- 许可证有效期不能覆盖施工计划工期：`NON_COMPLIANT`，并返回 `CONTACT_NOTICE_REQUIRED` 建议。
- 证照信息不完整：`INSUFFICIENT_EVIDENCE`。

### 待实现

联络单生成与发送作为后续业务节点或补证流程实现；当前仅保存结构化建议，`externalDocumentCreated=false`。

## 5. R03 无损检测机构核准资质

### 审查目标

检查承担无损检测工作的机构是否具有适用资质，资质范围是否覆盖实际检测活动，证书有效期是否覆盖整个施工计划工期。

### 已确认业务口径

- 每家无损检测机构分别生成一次 R03 判断，不把多家机构合并成一个结论。
- 有效期覆盖整个施工计划工期。
- 核准项目代码按 TSG Z7002—2022 附件 A 表 A-1 解码，其中 `CG` 表示常规检测并覆盖 `RT/UT/MT/PT`；未知代码返回证据不足，不由 LLM 猜测。
- “覆盖”不表示签字或盖章要求，不能因为文本包含“盖”字而误绑定签字、印章 Tool。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R03-01` | 1 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R03-01` | 2 | `evaluate_ndt_agencies` | 按机构分别归一化比较核准证机构名称和检测方案机构名称。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R03-01` | 3 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R03-02` | 1 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R03-02` | 2 | `extract_table_records` | 读取指定文档版本的表格和标准化行记录，可按业务表 Schema 过滤。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R03-02` | 3 | `decode_ndt_approval_item_codes` | 按 TSG Z7002—2022 附件 A 表 A-1 解码核准项目代码。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R03-02` | 4 | `evaluate_ndt_agencies` | 按机构分别比较解码后的许可方法与设计要求的检测方法。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R03-02` | 5 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R03-03` | 1 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R03-03` | 2 | `evaluate_ndt_agencies` | 按机构分别核验核准证有效期是否覆盖施工计划工期；失败只输出联络单建议。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R03-03` | 3 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R03-04` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R03-04` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |

### 已落地实现和输入契约

- Fact Builder 输出 `ndtAgencies.agencies[]`；每项至少包含稳定 `agencyId`，并按原子项提供机构名称、核准项目代码、设计要求方法、有效期和计划工期。
- `evaluate_ndt_agencies` 对数组逐机构 fan-out，返回 `agencyResults[]`，不会把一家机构的资质或有效期借给另一家机构。
- `decode_ndt_approval_item_codes` 使用冻结的 TSG Z7002—2022 代码表；未知代码和必要事实缺失均 fail closed。

### 结论规则

- 任一机构资质不覆盖其实际检测范围：该机构 `NON_COMPLIANT`。
- 证书在工程结束前失效：`NON_COMPLIANT`。
- 机构信息或证书本体缺失：`INSUFFICIENT_EVIDENCE` 或文件缺失规则。

## 6. R04 设计文件的批准程序

### 审查目标

检查设计图纸及相关设计文件是否齐全，图纸目录与文件本体是否一致，项目名称、图号、版本、日期、签章、签字和审查结论是否完整。

### 已确认业务口径

- 图纸目录中有文件，但文件本体未上传：判定“文件缺失”。
- AC-R04-03 的四级签字条件已经固化为结构化规则：GC1，或者 GCD 且 `P≥16.7MPa`，或者 GCD 且 `P≥4.0MPa、T≥570℃`。
- 需要判断当前项目要求三级还是四级签字。
- 三级、四级签字必须逐文件判断，禁止把不同文件识别出的签字角色合并后判断齐全。
- 多管线项目必须建立设计文件与覆盖管线的关联；无法关联时返回 `INSUFFICIENT_EVIDENCE`，不得默认采用三级签字。
- 设计图纸变更后，必须对新版图纸重新审查。
- 印章名称采用精确识别，不允许使用“名称包含”方式判定。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R04-01` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R04-01` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R04-01` | 3 | `extract_table_records` | 读取指定文档版本的表格和标准化行记录，可按业务表 Schema 过滤。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R04-01` | 4 | `check_document_set_completeness` | 核验所需文件本体是否已上传且可解析；目录列名不能代替文件本体。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R04-01` | 5 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R04-02` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R04-02` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R04-02` | 3 | `extract_table_records` | 读取指定文档版本的表格和标准化行记录，可按业务表 Schema 过滤。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R04-02` | 4 | `recognize_signatures_and_seals` | 读取签字角色、签章、印章文字、位置和置信度；不判断签字是否齐全。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R04-02` | 5 | `evaluate_design_document_approval` | 逐份核验主要设计文件本体和签字角色，并根据文件覆盖的管道级别、设计压力和设计温度确定三级或四级批准程序。 | 已实现（专用业务规则）；绑定：`implemented` |
| `AC-R04-02` | 6 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R04-03` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R04-03` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R04-03` | 3 | `extract_table_records` | 读取指定文档版本的表格和标准化行记录，可按业务表 Schema 过滤。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R04-03` | 4 | `recognize_signatures_and_seals` | 读取签字角色、签章、印章文字、位置和置信度；不判断签字是否齐全。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R04-03` | 5 | `evaluate_design_document_approval` | 逐份核验主要设计文件本体和签字角色，并根据文件覆盖的管道级别、设计压力和设计温度确定三级或四级批准程序。 | 已实现（专用业务规则）；绑定：`implemented` |
| `AC-R04-03` | 6 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R04-04` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R04-04` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |

### 已落地实现和输入契约

- AC-R04-01 已改用 `check_document_set_completeness`，固定检查六类文件的 `uploaded` 和 `parseable` 状态；图纸目录中的文件名不再代替文件本体。
- AC-R04-02、AC-R04-03 已改用专用确定性业务 Tool `evaluate_design_document_approval`，逐文件输出要求等级、触发条件、实际角色和缺失角色。
- 四级签字布尔规则固定为 `GC1 OR (GCD AND (P>=16.7 OR (P>=4.0 AND T>=570)))`。
- GC1 不要求先取得压力、温度即可触发；GCD 在 `P>=16.7MPa` 时不要求温度；GCD 在 `4.0MPa<=P<16.7MPa` 时必须取得温度才能判断。
- 多管线项目中的每份设计文件必须提供 `coveredPipelineIds`；缺少关联时返回 `evidence_insufficient`。

Fact Builder 必须生成以下业务事实；当前 Tool Argument Assembler 已负责转换为 Tool 顶层参数：

```json
{
  "designDocumentSet": {
    "catalogListedDocumentTypes": [],
    "uploadedDocumentTypes": [],
    "parseableDocumentTypes": []
  },
  "designDocuments": {
    "documents": [
      {
        "documentId": "string",
        "documentType": "pipeline_layout_drawing",
        "bodyUploaded": true,
        "signatureRoles": ["设计", "校核", "审核", "审定"],
        "coveredPipelineIds": ["PIPE-001"],
        "evidenceRefs": []
      }
    ]
  },
  "project": {
    "pipelines": [
      {
        "pipelineId": "PIPE-001",
        "pipelineGrade": "GCD",
        "designPressureMPa": 4.0,
        "designTemperatureC": 570
      }
    ]
  }
}
```

如果上游尚未形成这些结构化事实，R04 固定返回 `evidence_insufficient`，不得由 LLM 从自然语言中补造参数。

### 结论规则

- 目录有记录、文件本体缺失：`NON_COMPLIANT / 文件缺失`。
- 关键字段缺失但文件可读：`NON_COMPLIANT`。
- 文件上传不完整导致无法判断：`INSUFFICIENT_EVIDENCE`。
- 新版图纸未重新审查：`NON_COMPLIANT`。
- 主要设计文件缺设计、校核或审核签字：`NON_COMPLIANT`。
- 触发四级条件的文件缺审定签字：`NON_COMPLIANT`。
- 未触发四级条件：四级签字原子项为 `NOT_APPLICABLE`，但三级签字原子项仍须执行。

## 7. R05 施工图审查手续

### 审查目标

确认施工图审查是否有可认定的见证材料，材料是否能关联项目、图纸版本、审查日期、审查结论和必需的签字角色。

### 当前业务边界

- R05 的见证文件需要监检人员进行业务确认。
- 不能仅凭文件名称判断为“施工图审查见证材料”。
- 新版图纸必须重新审查，并能关联新版图号或版本。
- 证据缺失与证据不完整分开处理。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R05-01` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R05-01` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R05-01` | 3 | `check_document_set_completeness` | 核验所需文件本体是否已上传且可解析；目录列名不能代替文件本体。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R05-01` | 4 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R05-02` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R05-02` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |

### 结论规则

- 找不到标准认可的见证材料：`INSUFFICIENT_EVIDENCE`，提交监检人员确认。
- 文件明确缺失：按业务规则返回 `NON_COMPLIANT`。
- 见证材料与新版图纸版本不匹配：`NON_COMPLIANT`。
- 文件形式满足但材料是否属于标准认可类型存在争议：`REQUIRES_HUMAN_REVIEW`。

### 待确认问题

需要基于适用标准进一步确认：

- 哪些文件可以认定为施工图审查见证材料。
- 必须有哪些签字角色。
- 是否允许审查记录、审查报告、审查意见单互相替代。

## 8. R06 强度计算书、管道应力分析计算书的审批手续

### 审查目标

检查强度计算书和应力分析计算书的适用性、覆盖范围、参数一致性和审批手续。

### 标准及关键修正

- TSG D7006-2020 D2.2（3）。
- TSG 31-2025 3.1.3.1、3.1.3.3、3.1.4.2。
- GB/T 20801.1-2025 6.7.5.5。

经核对项目内 TSG 31-2025 PDF 第 19 页（印刷第 15 页）3.1.3.3：强度计算书和管道应力计算书均需设计、校核、审核三级签字；四级文件清单只包含管道应力计算书，不包含强度计算书。四级触发条件为 `GC1 OR (GCD AND (P>=16.7 OR (P>=4.0 AND T>=570)))`。

### 原子项建议

- 强度计算书是否应提供及是否齐全。
- 强度计算书参数与设计文件是否一致。
- 强度计算书三级签字是否完整。
- 是否需要应力分析。
- 应力分析计算书覆盖范围和参数是否一致。
- 应力分析是否触发四级签字。
- 应力分析签字是否完整。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R06-01` | 1 | `extract_document_fields` | 读取计算书逐文件结构化字段及证据定位。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R06-01` | 2 | `extract_table_records` | 读取计算书中的管线、设计参数和比较记录。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R06-01` | 3 | `evaluate_calculation_document_consistency` | 逐份核验计算书本体、覆盖管线以及与设计文件的结构化参数比较。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R06-01` | 4 | `validate_evidence_grounding` | 校验证据页码、坐标、原文、置信度和冲突状态。 | 已实现（证据门禁）；绑定：`pilot_implemented` |
| `AC-R06-02` | 1 | `extract_document_fields` | 读取计算书逐文件结构化字段及证据定位。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R06-02` | 2 | `recognize_signatures_and_seals` | 逐份读取签字角色和签章证据，不合并不同文件的角色。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R06-02` | 3 | `evaluate_design_document_approval` | 对强度计算书和应力计算书逐份核验设计、校核、审核三级签字。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R06-02` | 4 | `validate_evidence_grounding` | 校验证据页码、坐标、原文、置信度和冲突状态。 | 已实现（证据门禁）；绑定：`pilot_implemented` |
| `AC-R06-03` | 1 | `extract_document_fields` | 读取应力计算书和受影响管道参数。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R06-03` | 2 | `recognize_signatures_and_seals` | 逐份读取应力计算书签字角色。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R06-03` | 3 | `evaluate_design_document_approval` | 仅对应力计算书执行四级条件和审定签字核验。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R06-03` | 4 | `validate_evidence_grounding` | 校验证据页码、坐标、原文、置信度和冲突状态。 | 已实现（证据门禁）；绑定：`pilot_implemented` |
| `AC-R06-04` | 1 | `locate_evidence_fragment` | 生成计算书文件版本、页码、坐标和原文证据引用。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R06-04` | 2 | `validate_evidence_grounding` | 校验证据页码、坐标、原文、置信度和冲突状态。 | 已实现（证据门禁）；绑定：`pilot_implemented` |

### 已落地输入契约

Fact Builder 输出 `calculationDocuments.documents[]`。每份计算书应包含 `documentId`、`documentType`、`bodyUploaded`、`coveredPipelineIds`、`signatureRoles`、`parameterComparisons` 和 EvidenceRef；项目管道通过 `project.pipelines[]` 提供等级、设计压力和设计温度。Tool Argument Assembler 已将这些事实装配给一致性和批准程序 Tool。

### 结论规则

- 应提供但文件本体缺失：`NON_COMPLIANT`。
- 证据不完整：`INSUFFICIENT_EVIDENCE`。
- 参数冲突或审批层级不足：`NON_COMPLIANT`。
- 应力分析不适用且理由成立：`NOT_APPLICABLE`。

### 待确认

R06 是否只做文件、覆盖范围、参数和审批手续审查；应力计算正确性是否由后续专业计算复核节点处理。

## 9. R07 设计变更的书面批准文件

### 审查目标

检查设计变更、材料代用或设计条件调整是否取得原设计单位书面批准，并检查变更前后图纸、版本和实施时点的连续性。

### 标准

- TSG D7006-2020 D2.2（4）。
- TSG 31-2025 2.1.4、3.1.2、3.1.3.3。
- TSG 07-2019 E1.4.1（8）。

### 业务口径

- 无设计变更或材料代用：`NOT_APPLICABLE`。
- 有变更但无原设计单位书面批准：`NON_COMPLIANT`。
- TSG 31-2025 PDF 第 19 页（印刷第 15 页）3.1.2 明确：管道图纸目录和管道布置图应加盖压力管道设计许可印章，复印章无效；已加盖竣工图章的图样不得用于压力管道安装。
- D7006 D2.2（4）只要求设计变更（含材料代用）具有设计单位书面批准文件，并未要求所有变更通知单一律加盖许可印章。
- 因此仅对实际重新出具的 `drawing_catalog`、`pipeline_layout_drawing` 核验原始许可印章；普通变更通知单不适用该印章检查。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R07-01` | 1 | `extract_document_fields` | 读取设计变更及被变更文件的结构化字段和证据定位。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R07-01` | 2 | `recognize_signatures_and_seals` | 逐份读取签字角色、印章文字、印章主体和原件/复印状态。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R07-01` | 3 | `evaluate_design_change_approval` | 核验变更适用性、原设计单位书面批准，并按被变更文件类型和管道条件逐份确定三级或四级签字。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R07-01` | 4 | `verify_design_license_seals` | 仅对重新出具的管道图纸目录和管道布置图核验原始压力管道设计许可印章、主体和许可编号。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R07-01` | 5 | `validate_evidence_grounding` | 校验证据页码、坐标、原文、置信度和冲突状态。 | 已实现（证据门禁）；绑定：`pilot_implemented` |
| `AC-R07-02` | 1 | `locate_evidence_fragment` | 生成设计变更文件版本、页码、坐标和原文证据引用。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R07-02` | 2 | `validate_evidence_grounding` | 校验证据页码、坐标、原文、置信度和冲突状态。 | 已实现（证据门禁）；绑定：`pilot_implemented` |

### 已落地输入契约

Fact Builder 输出 `designChanges.hasDesignChanges` 和 `designChanges.documents[]`。每份文件提供实际 `documentType`、被变更 `changedDocumentType`、原设计单位和批准单位、书面批准状态、签字角色、覆盖管道以及许可印章结构。无变更时两个业务 Tool 均返回 `not_applicable`；印章识别不确定时返回证据不足，确认无章或使用复印章时判定不符合。

## 10. R08 设计采用的安全技术规范以及相关标准、压力管道元件的材料标准的版本

### 审查目标

检查设计引用的标准编号、年份、有效期、替代关系，以及设计批准日和施工监检日的版本适用性。

### 标准

- TSG D7006-2020 D2.2（5）。
- TSG 31-2025 1.7、3.1.3.2（1）—（2）。
- TSG 07-2019 M3.1.1（3）。

TSG 07-2019 M3.1.1（3）的当前 PDF 定位需修正：专业定位为 PDF 第 168 页、印刷第 165 页，不是当前配置的第 167 页。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R08-01` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R08-01` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R08-01` | 3 | `check_date_covers` | 判断证照或报告有效期是否覆盖闭区间业务周期。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R08-01` | 4 | `check_standard_version_active` | 按评审日期核验标准的实施日期、废止日期和状态。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R08-01` | 5 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R08-02` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R08-02` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |

### 判断原则

采用双时间判断：

1. 设计批准日期：设计采用的版本当时是否有效。
2. 施工或监检日期：是否发生强制替代，是否存在过渡期或合同锁定版本。

缺少标准年份不能让 LLM 猜测，应返回 `INSUFFICIENT_EVIDENCE`。

## 11. R09 设计文件上注明的无损检测、防腐、耐压试验和泄漏试验要求

### 审查目标

仅审查设计说明是否分别对无损检测、防腐、耐压试验和泄漏试验规定了具体要求，以及这些要求是否通过已冻结的安全技术规范和标准规则校验。

本节点不审查无损检测、防腐施工、耐压试验或泄漏试验的实际实施情况，不审查报告结果、记录或仪器校准证书。

### 标准

- TSG D7006-2020 D2.2（6）。
- TSG 31-2025 3.1.3.2（4）—（5）、3.1.8、3.1.9。
- GB/T 20801.1-2025 8.3、8.6。

### 原子项与输出粒度

R09 保留一个业务原子项 `AC-R09-01`，由一个专用确定性 Tool 在同一次调用中返回四个 `domainResults`：`ndt`、`corrosion`、`pressureTest`、`leakTest`。这样既保持业务节点结论的原子性，又能逐领域展示“是否具体规定”和“标准符合性”。`AC-R09-02` 仅负责证据落地性门禁。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R09-01` | 1 | `extract_document_fields` | 从设计说明及相关设计文件提取四类专项要求的结构化字段，保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R09-01` | 2 | `extract_table_records` | 提取设计文件表格中的无损检测、防腐、耐压和泄漏试验要求，补充正文字段事实。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R09-01` | 3 | `evaluate_design_special_requirements` | 逐领域检查设计说明是否给出必需的具体要求，并仅使用冻结标准规则判断符合性；返回四个领域子结果和节点结果。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R09-01` | 4 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R09-02` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R09-02` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |

### 事实和冻结规则输入

- Fact Builder 生成 `designSpecialRequirements.domains.<domain>`，每个领域至少包含 `specified`、`requirements` 和 `standardRefs`。
- 规则库生成 `fixedClauses.designSpecialRequirementRules.<domain>.checks[]`，每条检查包含 `code`、`actualPath`、`operator`、`expected` 和 `standardRef`。
- `requiredPathsByDomain` 由 atomicCheck 冻结配置提供；`standardRules` 只能来自已发布的固定条款规则，不得由 LLM 临时生成或修改。

### 结论规则

- 任一领域事实缺失、`specified` 不是明确布尔值，或该领域缺少冻结标准规则：`INSUFFICIENT_EVIDENCE`。
- 任一领域 `specified=false`，或必需的具体要求字段缺失：`NON_COMPLIANT`。
- 任一冻结标准规则校验不通过，或设计事实未引用该规则的 `standardRef`：`NON_COMPLIANT`。
- 四个领域的具体性和标准符合性均通过：`COMPLIANT`。
- 上述结论只表示设计文件要求审查结果，不得推导实际施工或试验已合格。

## 12. R10 采用其他标准时，设计文件或工程规定中应包括符合《工业管道安全技术规程》基本安全的符合性申明及比照表

### 审查目标

当设计采用其他标准，尤其是境外标准或替代标准时，检查符合性声明、标准对照表和差异处理资料。

### 标准

- TSG 31-2025 1.9（3）。
- TSG 31-2025 3.1.3.1。
- TSG 31-2025 附录 B，PDF 第 74—77 页、印刷第 70—73 页。

当前标准包只绑定正文，必须补充附录 B 条款绑定。R10 在 `rules.yaml` 中的 `criteria` 和 `checkMethod` 也需要补齐。

### 适用规则

- 没有采用其他标准：`NOT_APPLICABLE`。
- 仅补充引用其他标准、未替代基本安全依据：通常不触发 R10。
- 使用其他标准替代规定标准或采用境外标准：触发 R10。

### 附录 B 对照项

至少逐项检查：材料和组成件、采用标准、设计载荷和安全系数、设计准则和应力分析、材料检验、热处理、无损检测、其他检验、压力试验、泄漏试验及其他差异说明。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R10-01` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R10-01` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R10-01` | 3 | `check_required` | 检查事实对象中的必需字段是否存在；字段缺失返回 `failed`，规则未配置返回证据不足。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R10-01` | 4 | `check_conditional_requirement` | 条件为真时检查必需事实，条件为假返回不适用。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R10-01` | 5 | `evaluate_alternative_standard` | 按冻结的替代标准符合性声明、对照表和附录 B 检查规则执行判断。 | 已实现（通用规则解释器；仍需规则参数装配）；绑定：`implemented` |
| `AC-R10-01` | 6 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R10-02` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R10-02` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R10-02` | 3 | `extract_table_records` | 读取指定文档版本的表格和标准化行记录，可按业务表 Schema 过滤。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R10-02` | 4 | `recognize_signatures_and_seals` | 读取签字角色、签章、印章文字、位置和置信度；不判断签字是否齐全。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R10-02` | 5 | `check_signature_completeness` | 核验识别出的签字角色是否覆盖规则要求角色。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R10-02` | 6 | `check_scope_coverage` | 按显式覆盖映射核验已授予范围是否覆盖要求范围。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R10-02` | 7 | `evaluate_alternative_standard` | 按冻结的替代标准符合性声明、对照表和附录 B 检查规则执行判断。 | 已实现（通用规则解释器；仍需规则参数装配）；绑定：`implemented` |
| `AC-R10-02` | 8 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R10-03` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R10-03` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R10-03` | 3 | `extract_table_records` | 读取指定文档版本的表格和标准化行记录，可按业务表 Schema 过滤。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R10-03` | 4 | `check_conditional_requirement` | 条件为真时检查必需事实，条件为假返回不适用。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R10-03` | 5 | `evaluate_alternative_standard` | 按冻结的替代标准符合性声明、对照表和附录 B 检查规则执行判断。 | 已实现（通用规则解释器；仍需规则参数装配）；绑定：`implemented` |
| `AC-R10-03` | 6 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R10-04` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R10-04` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |

### 业务边界

Tool 只做声明和对照表的形式完整性检查，不由 LLM 判定替代标准与国内标准是否实质等效。涉及实质等效性的结果应为 `REQUIRES_HUMAN_REVIEW`。

当前 AC-R10-02 中的通用签字、印章检查应删除；只有其他明确规则规定签字或印章时才附加检查。

## 13. R01-R10 共用数据和 Tool 模型

### 证据对象

每个证据片段至少保存：

```json
{
  "evidenceId": "E-001",
  "documentId": "DOC-001",
  "fileName": "...",
  "page": 1,
  "region": {"x": 0, "y": 0, "width": 0, "height": 0},
  "text": "...",
  "documentVersion": "...",
  "sourceHash": "..."
}
```

### 统一 Tool 分层

- 文件和证据层：文件存在性、页面定位、OCR、表格抽取、证据完整性。
- 事实层：日期、单位、证照、图号、版本、签字、印章、标准编号抽取。
- 规则层：日期覆盖、范围匹配、版本有效、审批层级、对照表完整性。
- 聚合层：原子项汇总、节点结论、人工复核转办。

### LLM 的职责

LLM 可以：

- 将用户问题映射到已配置的业务节点。
- 选择已绑定的 Tool 并组织参数。
- 根据 Tool 返回值和证据片段生成可读的判断摘要。
- 输出缺失项、冲突项和下一步建议。

LLM 不可以：

- 临时新增标准条款绑定。
- 根据常识补齐缺失证据。
- 把目录记录当作文件本体。
- 自行判定标准实质等效性。
- 用隐式推理替代确定性 Tool 结果。

## 14. 后续落地顺序

1. 修订 R06 的三级/四级审批规则，补入 TSG 31-2025 3.1.3.3。
2. 修订 R07 的适用性和原设计单位批准规则。
3. 修正 R08 的标准页码定位并建立标准版本元数据。
4. 按 R09 一个业务原子项、四个领域子结果的结构维护 Fact Builder 和冻结标准规则。
5. 补齐 R10 的 `criteria`、`checkMethod` 和附录 B 绑定。
6. 将 R01-R10 的原子项、条款、Tool、输入参数和输出结构固化到数据库。
7. 使用固定测试样例验证 `COMPLIANT`、`NON_COMPLIANT`、`INSUFFICIENT_EVIDENCE`、`NOT_APPLICABLE` 和 `REQUIRES_HUMAN_REVIEW` 五类结果。

---

# R11-R69 细致分析记录

## 15. 分析范围、编号基准和阶段状态

本部分以 `files/checklist.docx` 的序号作为唯一业务规则编号，`sourceRuleId`、`nodeId` 和 `sourceSequence` 均与附件 1-69 项一致。当前工程已覆盖 R11-R69；R69 为施工单位质量保证体系实施状况的人工评价节点。

R11-R69 的实际业务分组如下：

- R11：施工组织设计。
- R12-R23：材料、压力管道元件、安全附件和阀门。
- R24-R34：焊接与热处理。
- R35-R42：无损检测。
- R43-R47：防腐、保温、阴极保护和静电接地。
- R48-R55：穿跨越、预制和管道安装。
- R56-R58：安全附件。
- R59-R68：耐压试验、替代性试验、泄漏试验、吹扫清洗。
- R69：施工单位质量保证体系实施状况的评价，由监检人员确认和签发。

阶段状态：R11-R69 已完成第一轮标准原文核验和业务分析，并已按附件新编号完成第二次结构检查；专业语义风险仍按后文 P0/P1 队列治理。

## 16. R11 施工组织设计

### 业务审查描述

审查施工组织设计或适用的专项施工方案是否已经批准，项目范围是否与设计文件一致，施工方法是否覆盖焊接、热处理、无损检测、耐压试验、泄漏试验、防腐、穿跨越等实际施工活动，并符合适用标准和施工单位质量保证体系。

### 固定标准条款

- TSG D7006-2020 D2.3，PDF 第 27 页：施工组织设计应符合安全技术规范和设计文件，批准程序应符合质量保证体系文件。
- TSG 07-2019 E3.1.4-E3.1.5，PDF 第 89 页：安装单位应建立并有效实施质量保证体系，具备相应工艺文件和安装质量保证能力。
- GB 50235-2010 3.1.4(2)，PDF 第 18 页（印刷页 7）：“施工组织设计或施工方案已经批准，并已进行技术和安全交底。”已通过 PDF 页面可视化复核，条款包已由错误的 3.0.2、3.0.3 更正为 3.1.4(2)。
- GB 50235-2010 3.1.2，同一 PDF 页：施工单位应建立管道施工现场的质量管理体系，并具有健全的质量管理制度和相应的施工技术标准；该条可作为“施工单位质量体系”的补充依据，但不直接规定统一的签字角色名称。

### 业务边界

已核对的固定条款要求“批准程序符合质量体系”，但没有直接规定所有项目必须由建设单位批复，也没有统一规定固定的“编制、审核、审批”三个角色名称。建设单位批复和具体签字角色应来自施工单位质量体系、合同、项目管理制度或专项方案管理规则，不能标成统一法定条款要求。

### 建议 atomicCheck

- AC-R11-01：施工组织设计或适用专项方案是否存在且为有效版本。
- AC-R11-02：批准流程和角色是否符合该施工单位已固化的质量体系审批矩阵。
- AC-R11-03：工程名称、装置、管线、规格、材质、长度、压力温度和施工范围是否与设计文件一致。
- AC-R11-04：方案是否覆盖本工程实际发生的关键工序。
- AC-R11-05：各关键工序采用的标准版本、技术参数和验收要求是否完整。
- AC-R11-06：证据可追溯性。

### requiredFacts

方案编号、版本、编制日期、适用工程、批准状态、签字角色、建设单位批复要求来源、工程范围、管线清单、施工工序、采用标准、专项方案引用关系。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R11-01` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R11-01` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R11-01` | 3 | `recognize_signatures_and_seals` | 读取签字角色、签章、印章文字、位置和置信度；不判断签字是否齐全。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R11-01` | 4 | `check_signature_completeness` | 核验识别出的签字角色是否覆盖规则要求角色。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R11-01` | 5 | `evaluate_construction_plan` | 按施工组织设计的审批、项目匹配、工序覆盖和标准版本规则执行判断。 | 已实现（通用规则解释器；仍需规则参数装配）；绑定：`implemented` |
| `AC-R11-01` | 6 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R11-02` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R11-02` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R11-02` | 3 | `check_required` | 检查事实对象中的必需字段是否存在；字段缺失返回 `failed`，规则未配置返回证据不足。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R11-02` | 4 | `check_cross_document_match` | 按文本/机构名归一化或数值容差比较跨文档字段。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R11-02` | 5 | `evaluate_construction_plan` | 按施工组织设计的审批、项目匹配、工序覆盖和标准版本规则执行判断。 | 已实现（通用规则解释器；仍需规则参数装配）；绑定：`implemented` |
| `AC-R11-02` | 6 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R11-03` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R11-03` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R11-03` | 3 | `evaluate_construction_plan` | 按施工组织设计的审批、项目匹配、工序覆盖和标准版本规则执行判断。 | 已实现（通用规则解释器；仍需规则参数装配）；绑定：`implemented` |
| `AC-R11-03` | 4 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R11-04` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R11-04` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |

### 结论规则

- 应有施工组织设计但文件本体缺失：`NON_COMPLIANT`。
- 项目范围与批准设计文件冲突：`NON_COMPLIANT`。
- 缺少某专项工序方案：相应原子项 `NON_COMPLIANT`。
- 质量体系审批矩阵未提供，无法确认角色要求：`INSUFFICIENT_EVIDENCE`。

### 现有 Tool 实现能力审计

结论：当前 R11 正式绑定虽标记为 `implemented`，但只完成 Tool 注册和通用 handler，没有完成 R11 专用 Fact Builder、参数装配和专业规则，因此不能实现本节点的完整业务目标。

| 业务目标 | 当前 Tool | 实际能力 | 审计结论 |
| --- | --- | --- | --- |
| 编制、审核、审批签字 | `recognize_signatures_and_seals` + `check_signature_completeness` | 可读取签字识别结果，但 R11 绑定没有提供 `actualRoles` 和 `requiredRoles`，装配器也未从 `constructionPlan.signatureRoles` 和质量体系审批矩阵组装参数 | 当前返回 `evidence_insufficient`，不能判断 |
| 建设单位批复 | `evaluate_construction_plan` | 没有专用 handler，也没有“是否必须批复的依据来源”、批复文件本体、单位和日期校验 | 不能判断；且建设单位批复不是 GB 50235-2010 3.1.4(2) 统一明文要求，必须绑定质量体系、合同或项目制度来源 |
| 装置名称、管道规格、材质、长度等与设计文件一致 | `check_required` + `check_cross_document_match` | `check_required` 只能检查顶层 requiredFacts 存在；`check_cross_document_match` 需要 `comparisons[]`，但 R11 没有组装逐字段比较对、归一化方式和数值容差 | 当前跨文档比较返回 `evidence_insufficient`，不能判断 |
| 焊接、无损检测、耐压/泄漏试验等内容符合施工标准 | `evaluate_construction_plan` | 当前落入通用 `evaluate_rule_profile()`，必须提供 `requiredFields` 和 `ruleChecks`；现有绑定仅有 `profile/clauseSource/failurePolicy`，也没有将 TSG 31、GB/T 20801.1、NB/T 47014、GB 50235、GB 50184、GB 50236、NB/T 47013 按工序编译为可执行规则 | 当前返回 `evidence_insufficient`，不能作为专业放行结论 |
| 符合施工单位质量体系 | `evaluate_construction_plan` | 条款包已绑定 TSG 07-2019 E3.1.4-E3.1.5，但没有读取该施工单位有效质量体系文件、审批矩阵和工艺文件目录 | 只有依据定位，没有业务判断能力 |

特别注意：`implementationStatus: implemented` 目前只表示调用链中的 Tool 名称已注册，不表示 R11 参数和专业规则已实现。按当前运行时，`AC-R11-01`、`AC-R11-02`和 `AC-R11-03` 都不具备自动判定“符合”的条件。

## 17. R12 压力管道元件及安全附件制造单位的许可资质

### 业务审查描述

按“制造单位 × 产品类别/品种 × 制造日期”分别判断该产品是否依法需要制造许可；需要时核查制造单位主体、许可证号、许可范围和有效期是否覆盖实际产品。

### 固定标准条款

- TSG D7006-2020 D2.4.1（1），PDF 第 27-28 页。
- TSG 31-2025 1.10、2.2.1.1，PDF 第 7、9 页。
- 市场监管总局 2021 年第 41 号公告附件 1 的特种设备生产单位许可目录及后续有效版本。
- 安全附件还应按 TSG 92-2026 和许可目录分类。

### 当前适用性问题

不是所有管道组成件都需要制造许可。R12 可对项目始终执行产品分类，但每个产品的许可证原子项应按许可目录条件适用。制造单位应与产品质量证明书上的制造主体匹配，不能用经销商许可证替代。

### 半自动 Agent 流程（已落地）

1. Fact Builder 从制造许可证 OCR 中提取许可证号、制造单位、许可范围、有效期和页码定位；焊工资格证、人员证和仅含安装许可的页面不会进入 R12 候选。
2. 正式复核发现许可证候选后，R12 Agent 通过 `request_official_registry_verification` 发起人工输入任务，ReviewRun 进入 `waiting_human_input`。LLM 未正确调用时由工作流守门器强制创建同一任务，禁止绕过官网核验。
3. Fact Builder 同时从材料表/特性表提取元件名称、规格、材质，从产品质量证明书提取制造单位和规格；按规格优先、产品名称与材质辅助关联，不能关联时保持证据不足，禁止猜测制造单位。
4. 监检人员逐证填写官网查询结果、登记状态、单位、许可范围、有效期、查询地址和人工确认声明；原 OCR 值与人工官网值分开保存，发生更正时必须填写原因。
5. Temporal 收到 `submit_human_input` 后恢复同一 ReviewRun。`check_license_registry_match` 先判断官网登记一致性和证照状态；`evaluate_component_manufacturer_scope` 再生成“工程元件—制造单位—许可证—许可范围”逐项覆盖矩阵。
6. 官网未查到、登记不一致、证照非有效状态或许可范围不覆盖时判定 `failed`；人工无法核验、元件类型或制造单位无法映射时返回 `evidence_insufficient`，不得自动判定符合。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R12-01` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R12-01` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R12-01` | 3 | `extract_table_records` | 读取指定文档版本的表格和标准化行记录，可按业务表 Schema 过滤。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R12-01` | 4 | `check_license_registry_match` | 对每张许可证核对人工官网查询结果、许可证号、单位名称和登记状态；未查到、不一致、过期、吊销或暂停时输出不符合，未完成人工查询时输出证据不足。 | 已实现（R12 专用确定性判断与人工输入装配）；绑定：`implemented` |
| `AC-R12-01` | 5 | `evaluate_component_manufacturer_scope` | 将特性表、材料表中的每个工程元件关联到制造单位和已核验许可证，按产品类别判断许可范围是否覆盖，并返回逐项矩阵。 | 已实现（R12 专用 handler、参数装配和覆盖矩阵）；绑定：`implemented` |
| `AC-R12-02` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R12-02` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |

## 18. R13 需制造监检或有型式试验要求的压力管道元件的监检证书、型式试验报告

### 业务审查描述

对每类压力管道元件先判断是否触发制造监督检验、型式试验或两者，再检查监检证书齐全性和型式试验报告的产品覆盖范围、机构、版本及有效状态。

### 固定标准条款

- TSG D7006-2020 D2.4.1（2），PDF 第 28 页。
- TSG D7006-2020 1.2.1，PDF 第 5 页：制造监检适用于埋弧焊钢管、聚乙烯管（不包括带金属骨架和纤维增强聚乙烯管），以及燃气调压装置、减温减压装置、工厂化预制管段、流量计壳体。
- TSG D7006-2020 A1.2-A1.3，PDF 第 12 页：埋弧焊钢管、聚乙烯管逐批监检；元件组合装置通常逐台监检，只有相同设计文件、工艺文件和质量计划时才可组批。
- 市场监管总局2021年第41号公告附件1注三，PDF 第 13-15 页：固化需制造许可的压力管道管子、阀门、管件、补偿器、法兰和元件组合装置分类。
- 市场监管总局2021年第41号公告附件1注四，PDF 第 15 页：固化只需型式试验的热扩无缝钢管、有色金属管、球墨铸铁管、复合管、复合/非金属管件、低压金属阀门、非金属阀门、旋转补偿器、非金属膨胀节、密封元件、防腐管道元件及指定元件组合装置。
- TSG 31-2025 2.2.1.2-2.2.1.3，PDF 第 9 页：需制造监检或型式试验的组成件取得相应合格证明后方可出厂。

### 条款绑定修正结果

原专业条款包中的 TSG 31-2025 1.8、2.1.3 已删除。当前条款包已改绑 TSG D7006-2020 1.2.1、A1.2-A1.3，2021年第41号公告附件1注三、注四，以及 TSG 31-2025 2.2.1.2-2.2.1.3；每项均带标准文件版本和 PDF 页级定位，可从预览跳转原文。

### 已落地 atomicCheck

- `AC-R13-01`：对设计材料表逐项分类，检查型式试验证书/报告的产品类别、制造单位、材料、结构、制造工艺、规格/直径/压力范围、试验机构、结论和状态是否覆盖实际元件。
- `AC-R13-02`：对需制造监检的元件逐项确定监检粒度，按批号核对埋弧焊钢管和聚乙烯管，按产品编号核对元件组合装置；缺证书或证书不覆盖判定不符合，关联字段不足判定证据不足。
- `AC-R13-03`：核验所有事实的文件版本、页码、坐标/原文和 OCR 置信度，证据不完整时禁止输出符合。

### Fact Builder 与确定性结果

- `build_r13_business_facts` 从材料表构造 `r13.designItems`，从制造监督检验证书构造 `r13.supervisionCertificates`，从型式试验证书/报告及其产品范围表构造 `r13.typeTestReports`。
- 分类与覆盖均由固定 Tool 返回结论，不交给 LLM 二次判断。LLM 只负责调用 Tool、解释覆盖矩阵和组织证据。
- 未知元件类别不会默认“不需要”；缺少制造工艺、产品子类或关键覆盖字段时返回 `evidence_insufficient`。
- 已新增 `manufacturing_supervision_certificate_v1`、`type_test_report_v1` OCR Profile，支持批号、产品编号、材料、结构、制造工艺及直径/压力上下限抽取。

### Agent 交互架构

- R13 已接入与 R12 同类的受控 LLM Tool Loop：正式复核先调用 `inspect_r13_review_facts`，再由 LLM 选择并调用分类、监检证书齐全性和型式试验覆盖 Tool；模型返回的 `reasoning_content`、Tool Call 和 Tool Result 均写入 Agent Trace。
- R13 不包含 R12 的官网强制人工核验，因此正常情况下不进入 `waiting_human_input`。如果模型漏调 Tool、调用失败或模型服务不可用，工作流仍执行完整 AtomicCheck Tool Plan，不能因 LLM 行为而跳过确定性判断。
- R12 与 R13 的共同架构是“LLM 调度 + Fact Builder + 确定性 Tool + Workflow Guard”；差异只是 R12 存在必须持久暂停的外部官网人工任务，R13 当前没有该业务触发条件。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R13-01` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R13-01` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R13-01` | 3 | `extract_table_records` | 读取指定文档版本的表格和标准化行记录，可按业务表 Schema 过滤。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R13-01` | 4 | `classify_r13_component_requirements` | 依据 TSG D7006-2020 1.2.1、A1.2 和2021年第41号公告附件1注三、注四，逐项判定是否触发制造监检、型式试验及监检粒度；未知类别返回证据不足。 | 已实现（R13 专用法规目录分类）；绑定：`pilot_implemented` |
| `AC-R13-01` | 5 | `evaluate_r13_type_test_coverage` | 将设计元件与型式试验证书/报告逐项匹配，确定产品类别、制造单位、材料、结构、制造工艺及规格/直径/压力范围是否覆盖。 | 已实现（R13 专用覆盖矩阵）；绑定：`pilot_implemented` |
| `AC-R13-01` | 6 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R13-02` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R13-02` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R13-02` | 3 | `classify_r13_component_requirements` | 依据固定法规目录确定制造监检适用性，并返回逐批或逐台监检粒度。 | 已实现（R13 专用法规目录分类）；绑定：`pilot_implemented` |
| `AC-R13-02` | 4 | `evaluate_r13_supervision_certificate_completeness` | 对埋弧焊钢管、聚乙烯管按批号，对指定元件组合装置按产品编号（满足组批条件时按批号）核对监检证书编号、制造单位、产品类别和合格结论。 | 已实现（R13 专用齐全性与追溯矩阵）；绑定：`pilot_implemented` |
| `AC-R13-02` | 5 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R13-03` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R13-03` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |

## 19. R14 不需制造许可、监检、型式试验的管道组成件的出厂检验报告，必要时进行现场抽查复验

### 业务审查描述

对确认不需要制造许可、监检和型式试验的管道组成件，按具体产品标准和设计要求检查出厂检验项目，并在必要时核对现场复验。

### 固定标准条款

- TSG D7006-2020 D2.4.1（3），PDF 第 28 页。
- 具体管子、管件、紧固件、密封件等产品标准。

当前专业条款包仅绑定 GB/T 8163-2018 和 GB/T 12771-2019，无法覆盖规则示例中的螺栓、螺母及其他管道组成件。

### 业务规则修正

光谱、硬度、金相、无损检测和耐压试验不是所有产品均必须全部提供。D2.4.1（3）的判断是这些报告在相应产品标准或设计要求触发时是否合格。必须先分类产品和必检项目，再判断文件缺失。

### 已落地实现

- `build_r14_business_facts` 分别构造 `r14.designItems`、`r14.pipelineCharacteristics`、`r14.factoryInspectionReports` 和 `r14.specialInspectionReports`，保留元件、规格、等级、材质、管线号、批号、压力等级、报告类别和页码证据。
- `factory_inspection_report_v1` 和 `material_retest_report_v1` OCR Profile 已接入自动路由；光谱、硬度、金相、无损检测和耐压试验报告按报告类型与元件/批号关联。
- R14 正式复核已接入受控 LLM Tool Loop，并由完整 AtomicCheck Tool Plan 兜底。LLM 只编排和解释结果，不自行判断等级材质、必检项目或压力等级。
- 当前冻结产品规则明确配置 GB/T 12771—2019 6.9 的无损检测要求；其他产品标准未形成可靠规则矩阵时返回 `evidence_insufficient`，不得把五类专项报告全部强制或默认不需要。
- R14 条款适用性已从 `always` 更正为条件适用：只有确认不需要制造许可、制造监检和型式试验的元件才能进入 R14；其余分别转 R12/R13，无法分类时返回证据不足。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R14-01` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R14-01` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R14-01` | 3 | `extract_table_records` | 读取指定文档版本的表格和标准化行记录，可按业务表 Schema 过滤。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R14-01` | 4 | `classify_r14_component_applicability` | 逐项确认元件是否不需要制造许可、制造监检和型式试验；需许可转 R12，需监检或型式试验转 R13，无法分类返回证据不足。 | 已实现（R14 专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R14-01` | 5 | `evaluate_r14_component_design_match` | 按元件类型、规格、管线号和批号关联出厂检验报告，核对等级、材质及报告结论与设计材料表是否一致。 | 已实现（R14 专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R14-01` | 6 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R14-02` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R14-02` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R14-02` | 3 | `classify_r14_component_applicability` | 逐项确认元件是否进入 R14，防止把 R12/R13 对象按普通出厂检验报告审查。 | 已实现（R14 专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R14-02` | 4 | `resolve_r14_required_inspection_items` | 根据设计明确要求和冻结产品标准规则确定光谱、硬度、金相、无损检测和耐压试验等必检项目；规则未覆盖时返回证据不足。 | 已实现（R14 专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R14-02` | 5 | `evaluate_r14_special_report_coverage` | 按元件、规格和批号检查必需专项报告是否齐全、关联正确且结论合格，同时检查已提交报告不得存在明确不合格结论。 | 已实现（R14 专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R14-02` | 6 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R14-03` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R14-03` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R14-03` | 3 | `extract_table_records` | 读取指定文档版本的表格和标准化行记录，可按业务表 Schema 过滤。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R14-03` | 4 | `classify_r14_component_applicability` | 逐项确认元件是否进入 R14。 | 已实现（R14 专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R14-03` | 5 | `evaluate_r14_pressure_compatibility` | 按管线号关联材料表、管道特性表、出厂检验报告和专项报告，判断额定压力等级是否覆盖管线要求；PN 可确定性换算，无法可靠换算的 Class 返回证据不足。 | 已实现（R14 专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R14-03` | 6 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R14-04` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R14-04` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |

## 20. R15 境外制造的压力管道元件、安全附件的型式试验证书及其制造单位的制造许可证资质

### 业务审查描述

当产品制造地点和制造主体位于境外时，检查其制造许可、型式试验，以及不能在境外实施制造监检时的到岸监检或随锅炉/压力容器整机检验安排。

### 固定标准条款

- TSG 31-2025 1.10，PDF 第 7 页：管道组成件制造、管道设计安装以及检验检测机构应严格执行规程，并按特种设备信息化管理规定提交数据。
- TSG 31-2025 2.2.1.5，PDF 第 9-10 页：境外制造组成件不能在境外完成制造监检时，到达口岸或使用地后由监检机构依据 TSG D7006 实施到岸检验；随锅炉、压力容器整机配套的随整机进行产品安全性能检验。
- TSG D7006-2020 附件 D D2.4.1，PDF 第 27-28 页，其中第（1）项规定制造许可资质检查，第（2）项规定制造监检证书和有型式试验要求时的型式试验报告/证书检查，第（3）项规定无许可、型式试验、制造监检要求产品的替代检验路径，第（4）项直接规定境外制造元件及安全附件的型式试验和制造许可检查。

### 已固化业务边界

- R15 只以制造国家、制造地点、制造主体境外属性或明确的结构化业务事实判断“境外制造”。
- R19 只处理境外材料牌号，不得用境外材料牌号证明替代境外制造事实。
- “相应制造许可”和“按照安全技术规范要求取得型式试验证书”先经过冻结目录分类，不能默认所有境外产品都需要相同证书。
- 需要制造监检的产品必须进一步分流为境外制造监检、到岸检验或随整机安全性能检验。
- 没有制造许可、型式试验和制造监检要求的其他组成件路由到 D2.4.1（3）/R14，不在 R15 中直接判定不符合。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R15-01` | 1 | `get_document_ocr_result` | 读取设计材料表和境外制造产品清单 OCR 结果。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R15-01` | 2 | `extract_table_records` | 抽取产品、制造单位、制造国家、规格和监管要求结构化行。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R15-01` | 3 | `classify_r15_foreign_manufacturing_applicability` | 仅依据制造国家、制造地点或明确业务事实判断境外制造；禁止把境外材料牌号当作境外制造。 | 已实现（R15 专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R15-02` | 1 | `classify_r15_regulatory_requirements` | 逐项分类制造许可、型式试验、制造监检要求以及 D2.4.1（3）替代检查路由。 | 已实现（R15 专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R15-03` | 1 | `get_document_ocr_result` | 读取制造许可证 OCR 证据。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R15-03` | 2 | `extract_document_fields` | 抽取许可证号、持证单位、许可范围和有效期。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R15-03` | 3 | `evaluate_r15_manufacturing_license_coverage` | 对需要许可的境外产品核对制造单位、官网人工核验状态和许可范围覆盖。 | 已实现（R15 专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R15-04` | 1 | `get_document_ocr_result` | 读取型式试验证书或报告 OCR 证据。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R15-04` | 2 | `extract_document_fields` | 抽取报告编号、制造单位、产品、材料、工艺、规格和压力范围。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R15-04` | 3 | `extract_table_records` | 抽取型式试验产品覆盖范围表。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R15-04` | 4 | `evaluate_r15_type_test_coverage` | 逐项判断型式试验证书是否覆盖工程实际产品。 | 已实现（复用 R13 覆盖内核的 R15 专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R15-05` | 1 | `get_document_ocr_result` | 读取制造监检证书、到岸检验或随整机检验记录。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R15-05` | 2 | `extract_document_fields` | 抽取检验路径、制造单位、产品、批号/产品编号、检验机构和结论。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R15-05` | 3 | `evaluate_r15_manufacturing_inspection_route` | 根据是否在境外完成制造监检、是否随整机配套，核验对应制造监检、到岸或随整机检验资料。 | 已实现（R15 专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R15-06` | 1 | `locate_evidence_fragment` | 定位 R15 结论所引用的页码、坐标和原文片段。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R15-06` | 2 | `validate_evidence_grounding` | 校验声明事实和证照、检验路径结论均有完整 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |

## 21. R16 压力管道元件以及安全附件产品质量证明文件

### 业务审查描述

按产品和炉批/批号核查压力管道元件及安全附件的质量证明文件原件或合规复印件，确认制造主体、材料、规格、交货状态、检验试验结果和标识可追溯，并满足设计及产品标准的特殊要求。

### 固定标准条款

- TSG D7006-2020 D2.4.1（5），PDF 第 28 页。
- TSG 31-2025 2.2.1.4，PDF 第 9 页。
- GB 50235-2010 4.1.1-4.1.2，PDF 第 20 页。
- GB/T 20801.1-2025 7.2.1。
- 按实际产品动态绑定 GB/T 12459、GB/T 13401、GB/T 8163、GB/T 3087、GB/T 5310、GB/T 9948、GB/T 14976、GB/T 12771 等标准。

当前已补入上述通用条款及 8 类产品标准的专业条款与 PDF 页级定位，并在代码中冻结对应标准路由。各牌号的具体数值限值仍必须作为结构化 `acceptanceLimits` 固化；未固化时 Tool 返回 `evidence_insufficient`，不得由 LLM 查表猜测。

### 已落地 atomicCheck

- 产品与质量证明书一一或按批关联。
- 制造单位、标准、牌号、规格、炉批号、交货状态和日期完整。
- 检验、试验和特殊要求结果符合产品标准及设计。
- 实物标识可追溯到质量证明文件。
- 复印件有经营单位公章和经办负责人章。
- 电子质量证明文件的真实性和签章方式仍属于待确认业务口径，当前不默认等同原件或合规复印件。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R16-01` | 1 | `resolve_r16_product_standard_profile` | 按设计执行标准将元件路由至已冻结的 8 类产品标准规则；未知标准返回证据不足。 | 已实现（R16 专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R16-02` | 1 | `evaluate_r16_quality_certificate_batch_coverage` | 按产品、规格、炉批号或产品编号逐项核验质量证明文件的唯一覆盖。 | 已实现（R16 专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R16-03` | 1 | `evaluate_r16_quality_certificate_form_and_seals` | 原件核验制造单位质量检验章；复印件分别核验经营单位公章和经办负责人章。 | 已实现（R16 专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R16-04` | 1 | `evaluate_r16_quality_certificate_design_match` | 逐批核对制造单位、产品、规格、材质、执行标准和交货状态与设计要求。 | 已实现（R16 专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R16-05` | 1 | `evaluate_r16_quality_certificate_content` | 按产品标准和设计特殊要求核验必需字段、出厂检验项目及合格结论。 | 已实现（R16 专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R16-06` | 1 | `evaluate_r16_quality_certificate_results` | 仅使用已固化的结构化验收限值核验化学、力学和专项检验数值。 | 已实现（R16 专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R16-06` | 2 | `evaluate_r16_batch_traceability` | 核验设计材料表、质量证明文件和实物标识的炉批号或产品编号追溯链。 | 已实现（R16 专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R16-07` | 1 | `locate_evidence_fragment` | 定位 R16 结论所引用的页码、坐标和原文片段。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R16-07` | 2 | `validate_evidence_grounding` | 校验声明事实和质量证明判断均具有完整 EvidenceRef。 | 已实现（证据门禁）；绑定：`pilot_implemented` |

印章识别必须按“经营单位公章”“经办负责人章”的语义角色精确匹配，不能用名称包含关系。

## 22. R17 压力管道元件以及安全附件产品验收的见证资料、抽样复验

### 业务审查描述

检查施工单位依据质量保证体系对每批到货产品实施的文件验收、标识核对、外观尺寸检查、抽样复验、不合格隔离和放行记录。

### 固定标准条款

- TSG D7006-2020 D2.4.1（6），PDF 第 28 页。
- TSG 07-2019 M3.4，PDF 第 169 页。
- GB/T 20801.1-2025 7.2.1-7.2.7，PDF 第 87-88 页。

### 与 R18 的分工

R17 主审到货验收过程是否按质量体系实施；R18 主审被触发的材料复验和 NDT 报告的技术项目、审批和结果。R17 可以引用 R18 的复验结果，但不重复计算。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R17-01` | 1 | `evaluate_r17_arrival_acceptance_batch_coverage` | 按产品、规格、炉批号或产品编号核验每批到货元件的唯一验收记录。 | 已实现（R17 专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R17-02` | 1 | `evaluate_r17_acceptance_procedure` | 核验质量证明、身份标识、外观、尺寸、结论记录和验收签字等质量体系步骤。 | 已实现（R17 专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R17-03` | 1 | `resolve_r17_sampling_retest_requirement` | 依据设计事实或冻结抽样规则逐批确定是否需要抽样复验。 | 已实现（R17 专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R17-04` | 1 | `evaluate_r17_sampling_witness_chain` | 仅对适用批次核验取样见证、见证角色、样品号与复验报告链。 | 已实现（R17 专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R17-05` | 1 | `evaluate_r17_nonconformance_control` | 对验收不合格批次核验隔离、处置及必要的放行批准。 | 已实现（R17 专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R17-06` | 1 | `locate_evidence_fragment` | 定位 R17 结论所引用的页码、坐标和原文片段。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R17-06` | 2 | `validate_evidence_grounding` | 校验验收、抽样见证和处置事实均有完整 EvidenceRef。 | 已实现（证据门禁）；绑定：`pilot_implemented` |

## 23. R18 材料复验报告、无损检测报告

### 业务审查描述

先判断设计、产品标准、质量证明异常、境外牌号、材料类别或质量体系是否触发复验/NDT，再检查取样、项目、方法、审批、结果和材料批次追溯。

### 固定标准条款

- TSG D7006-2020 D2.4.1（7），PDF 第 28 页。
- GB/T 20801.1-2025 7.2.1（b）、7.2.3及具体产品标准。
- 境外牌号材料分支适用 TSG 31-2025 2.1.2（3）。
- NDT 适用相应 NB/T 47013 分册。

### 已修正的配置问题

R18 已由 `always` 改为条件适用，先由 `classify_r18_material_test_applicability` 识别复验和材料本体 NDT 触发事实。TSG 31-2025 2.1.2（3）的境外牌号验证性复验已保留在 R19，不再误绑为 R18 的通用前提。

AC-R18-02 已改成“材料试验要求规则绑定”；产品标准、复验项目、NDT 方法或数值限值未固化时返回 `evidence_insufficient`。材料本体 NDT 使用独立的 `material_ndt_report_v1`，不再复用焊缝 NDT Profile。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R18-01` | 1 | `classify_r18_material_test_applicability` | 逐批识别材料复验和材料本体 NDT 是否适用；不作无条件必传判断。 | 已实现（R18 专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R18-02` | 1 | `resolve_r18_material_test_requirement_profile` | 绑定产品标准、复验项目、材料 NDT 方法和结构化验收限值。 | 已实现（R18 专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R18-03` | 1 | `evaluate_r18_material_retest_report_completeness` | 对需要复验的批次核验报告及要求复验项目的覆盖。 | 已实现（R18 专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R18-04` | 1 | `evaluate_r18_material_ndt_report_completeness` | 对需要材料本体 NDT 的批次核验专用报告和检测方法。 | 已实现（R18 专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R18-05` | 1 | `evaluate_r18_material_report_approval_procedure` | 核验复验/NDT 报告批准程序及试验、审核、批准签字角色。 | 已实现（R18 专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R18-06` | 1 | `evaluate_r18_material_test_results_and_traceability` | 核验报告结论、结构化数值限值及材料批号—样品号—报告号追溯链。 | 已实现（R18 专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R18-07` | 1 | `locate_evidence_fragment` | 定位 R18 结论所引用的页码、坐标和原文片段。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R18-07` | 2 | `validate_evidence_grounding` | 校验适用性、报告和结果事实均有完整 EvidenceRef。 | 已实现（证据门禁）；绑定：`pilot_implemented` |

## 24. R19 使用境外牌号材料制造的压力管道元件以及安全附件，验证性复验结果

### 业务审查描述

当产品使用境外材料牌号时，核查该材料在境外标准中的允许使用和相似工况业绩、与国内同类材料的化学成分和性能相当性、验证性复验、首次使用焊接工艺评定及资料归档。

### 固定标准条款

- TSG D7006-2020 D2.4.1（8），PDF 第 28 页。
- TSG 31-2025 2.1.2（1）-（6），PDF 第 8 页。

### 条件规则

“制定企业标准”只适用于**境内制造单位使用境外牌号材料**情形，不是所有境外制造产品或进口境外牌号材料的统一要求。首次使用状态不明确时不得豁免焊接工艺评定；境外牌号材料不适用也必须引用设计材料表或质量证明文件中的材料牌号和执行标准作为证据。

### 已固化 atomicCheck

- `AC-R19-01`：境外牌号材料适用性、元件/附件、制造单位、牌号、批次和使用范围。
- `AC-R19-02`：境外压力管道现行材料标准与类似工况使用经历。
- `AC-R19-03`：化学成分、力学/物理/工艺性能与国内相近材料的等同性和不降低要求。
- `AC-R19-04`：化学成分和力学性能验证性复验及批次追溯。
- `AC-R19-05`：首次使用判断及焊接工艺评定覆盖。
- `AC-R19-06`：复验和适用工艺评定结果纳入产品质量证明文件。
- `AC-R19-07`：境内制造单位企业标准条件分支及内容覆盖。
- `AC-R19-08`：全部判断的 EvidenceRef 可追溯性。

### Agent 执行方式

R19 使用 `llm_semantic_primary`，不同于 R13-R18 的 `tool_result_primary`：文件版式和标准比较字段尚不能完全预建模，LLM 负责基于固定条款和原文证据形成八个 atomicCheck 判断。Tool 不替 LLM 作材料专业语义判断，而负责读取 OCR/表格、定位 EvidenceRef，以及校验模型提交的 Schema、ClauseRef 和 EvidenceRef。节点 `result` 仍由服务端按 `failed → evidence_insufficient → human_review_required → passed → not_applicable` 固定聚合。

模型不能可靠确认材料等同性、类似工况经历、首次使用状态或企业标准内容时，应调用 `request_r19_human_input`。人工响应以结构化证据写入 ReviewRun，恢复后重新进入 Agent，而不是由人工直接填写 AI 节点结论。

### Tool 调用链

> 以下为当前工程正式绑定的运行时能力。R19 的 Tool 由 Agent 按证据需要动态调用，不要求每个 atomicCheck 机械执行全部读取 Tool；`validate_r19_semantic_judgment` 是强制提交校验边界。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R19-01` 至 `AC-R19-08` | 按需 | `get_document_ocr_result` | 读取输入文件的 OCR 文本、字段、表格和原文片段；只提供事实，不作符合性判断。 | 已实现（通用证据读取）；绑定：`pilot_implemented` |
| `AC-R19-01`、`03`、`04`、`05`、`07` | 按需 | `extract_document_fields` | 读取材料牌号、标准号、制造单位、批号、报告号等已解析字段并保留定位。 | 已实现（通用证据读取）；绑定：`pilot_implemented` |
| `AC-R19-01`、`03`、`04`、`05` | 按需 | `extract_table_records` | 读取设计材料表、化学成分、力学性能、复验和 WPS/PQR 表格记录。 | 已实现（通用证据读取）；绑定：`pilot_implemented` |
| `AC-R19-01` 至 `AC-R19-08` | 按需 | `locate_evidence_fragment` | 按模型查询词生成带文件版本、页码、坐标、原文和置信度的 EvidenceRef。 | 已实现（证据定位）；绑定：`pilot_implemented` |
| `AC-R19-01` 至 `AC-R19-08` | 提交前/提交时 | `validate_r19_semantic_judgment` | 校验 LLM 逐原子项判断的结果 Schema、固定 ClauseRef 和已登记 EvidenceRef；不生成或改写业务结果。 | 已实现（R19 专用判断校验）；绑定：`pilot_implemented` |
| `AC-R19-08` | 提交时 | `validate_evidence_grounding` | 对 R19 全部判断执行页码、坐标/原文、置信度和冲突证据门禁。 | 已实现（证据门禁）；绑定：`pilot_implemented` |
| 证据不足的任一原子项 | 按需 | `request_r19_human_input` | 创建 `r19_semantic_evidence_confirmation` 结构化人工任务并暂停 Workflow；模型遗漏或不可用时由 Guard 补建。 | 已实现（R19 人工交互）；运行时 Agent Tool |
| 全部八项 | 完成 | `submit_r19_semantic_review` | 一次提交八项语义判断；服务端拒绝遗漏、重复、未知证据及无证据的通过/不符合/不适用判断。 | 已实现（R19 Agent 提交边界）；运行时 Agent Tool |

境外材料与国内材料的实质相当性仍属于专业语义判断。当前试点允许 LLM 基于文件和固定条款分析；证据不能支持时必须返回 `evidence_insufficient` 或 `human_review_required`，不得因尚未建设材料数据库而自动放行。

## 25. R20 新材料制造的压力管道元件以及安全附件的型式试验报告、技术评审、批准手续

### 业务审查描述

判断材料是否属于 TSG 31-2025 定义的新材料分支，再检查材料数据、元件型式试验、技术评审和批准手续。

### 固定标准条款

- TSG D7006-2020 D2.4.1（9），PDF 第 28 页。
- TSG 31-2025 1.8、2.1.3.1-2.1.3.2，PDF 第 7-8 页。

### 新材料分类

- 未列入 GB/T 20801、GB/T 32270，也未列入压力管道或承压设备专用材料国家/行业标准。
- 未列入 GB/T 20801、GB/T 32270，但已列入其他压力管道或承压设备专用材料国家/行业标准。

两类所需材料数据、型式试验和技术评审程序不同，不能以“材料名称较新”判断。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R20-01` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R20-01` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R20-01` | 3 | `check_required` | 检查事实对象中的必需字段是否存在；字段缺失返回 `failed`，规则未配置返回证据不足。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R20-01` | 4 | `evaluate_material_component` | 按元件/材料类型、批次、质量证明、复验、型式试验或新材料 profile 执行判断。 | 已实现（通用规则解释器；仍需规则参数装配）；绑定：`implemented` |
| `AC-R20-01` | 5 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R20-02` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R20-02` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |

当前 AC-R20-01 缺少批准手续和两类新材料分支，应补齐。

## 26. R21 材料标志移植

### 业务审查描述

当切割、加工或安装使原始材料标记无法保留时，检查标记移植前确认、移植方法、材料身份链和抽查记录，确保不混料、不损伤、不污染材料。

### 固定标准条款

- TSG D7006-2020 D2.4.2，PDF 第 28 页：现场抽查管子、管件标志移植；包含特殊材料时，对所有种类特殊材料抽查。
- GB/T 20801.1-2025 7.3.2.1-7.3.2.3，PDF 第 88-89 页。
- TSG 07-2019 M3.4。

### 适用性

项目可始终检查材料追溯体系，但只有发生标记无法保留和实际移植时才检查移植事件；无移植事件时对应原子项为 `NOT_APPLICABLE`。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R21-01` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R21-01` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R21-01` | 3 | `extract_table_records` | 读取指定文档版本的表格和标准化行记录，可按业务表 Schema 过滤。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R21-01` | 4 | `check_required` | 检查事实对象中的必需字段是否存在；字段缺失返回 `failed`，规则未配置返回证据不足。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R21-01` | 5 | `check_sampling_requirement` | 按总体数、比例和最小数量计算抽样数，并校验实际样本。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R21-01` | 6 | `check_traceability` | 核验原标志、移植标志、批号及移植记录之间的追溯关系。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R21-01` | 7 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R21-02` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R21-02` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |

低温钢、不锈钢和有色金属不得使用硬印标记；色码材料也应检查有害成分限制。

## 27. R22 材料代用

### 业务审查描述

在材料表、采购、质量证明和施工记录之间识别实际代用，核查原材料、代用材料、适用范围和原设计单位书面批准，并验证竣工图和相关工艺文件已同步更新。

### 固定标准条款

- TSG D7006-2020 D2.4.3，PDF 第 28 页。
- TSG 31-2025 2.1.4，PDF 第 8-9 页：制造、安装阶段材料代用应取得原设计单位书面批准。
- GB/T 20801.1-2025 7.1.2、7.1.4。

### 当前适用性问题

当前配置为 `always`。应先判断是否存在材料代用；没有代用时为 `NOT_APPLICABLE`。仅有采购替代建议而未实际使用，不应判为已发生代用。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R22-01` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R22-01` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R22-01` | 3 | `recognize_signatures_and_seals` | 读取签字角色、签章、印章文字、位置和置信度；不判断签字是否齐全。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R22-01` | 4 | `check_signature_completeness` | 核验识别出的签字角色是否覆盖规则要求角色。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R22-01` | 5 | `check_required` | 检查事实对象中的必需字段是否存在；字段缺失返回 `failed`，规则未配置返回证据不足。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R22-01` | 6 | `evaluate_design_approval_level` | 逐文件核验文件本体及其三级/四级签字角色。 | 已实现（专用业务规则）；绑定：`implemented` |
| `AC-R22-01` | 7 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R22-02` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R22-02` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |

安装阶段必须是原设计单位书面批准；不能用施工单位、供应商或监理签字替代。

## 28. R23 阀门的施工资料和耐压试验记录（报告）

### 业务审查描述

按阀门检验批和单台阀门检查施工记录、产品资料、壳体试验和密封试验，核对抽样数量、试验方法、介质、压力、持续时间、泄漏量和结果。

### 固定标准条款

- TSG D7006-2020 D2.5，PDF 第 28 页。
- GB/T 20801.1-2025 7.2.4.1-7.2.4.5，PDF 第 87-88 页。
- GB/T 13927-2022 5.4、5.6-5.9、6.1-6.3、7.1-7.4。
- 当设计或供货合同明确采用时，适用 GB/T 26480-2011 第 5-8 章。

GB/T 13927-2022 和 GB/T 26480-2011 当前均为现行版本；GB/T 26480-2011 已有修订计划，但新版本尚未发布。

### 当前条款缺口和标准优先级

当前专业条款包漏掉决定试验数量的 GB/T 20801.1-2025 7.2.4：

- GC1：逐个进行壳体和密封试验。
- GC2：每检验批抽查 10%，且不少于 1 个。
- GC3：每检验批抽查 5%，且不少于 1 个。
- 经建设/使用单位同意，制造厂逐件见证并有记录的可免现场压力试验。

试验标准的优先级应为：设计文件和供货合同优先；无规定时按 GB/T 13927。不能无条件要求同一阀门同时满足 GB/T 13927 和 GB/T 26480 的所有不同条款。

### 建议 atomicCheck

- 阀门清单、检验批和管道级别。
- 现场试验数量或制造厂逐件见证免试条件。
- 阀门编号、类型、DN、PN/Class、材料和产品标准。
- 壳体试验和密封试验是否适用。
- 试验介质、压力、持续时间和泄漏验收值。
- 夹套阀、超大口径闸阀等特殊分支。
- 不合格检验批的逐个检查或隔离处理。
- 记录、报告和实际安装阀门可追溯。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R23-01` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R23-01` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R23-01` | 3 | `extract_table_records` | 读取指定文档版本的表格和标准化行记录，可按业务表 Schema 过滤。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R23-01` | 4 | `check_required` | 检查事实对象中的必需字段是否存在；字段缺失返回 `failed`，规则未配置返回证据不足。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R23-01` | 5 | `evaluate_valve_test` | 按管道等级计算阀门抽检比例，或核验工厂逐台见证豁免条件，并检查试验记录和结果。 | 已实现（专用业务规则）；绑定：`implemented` |
| `AC-R23-01` | 6 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R23-02` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R23-02` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R23-02` | 3 | `check_required` | 检查事实对象中的必需字段是否存在；字段缺失返回 `failed`，规则未配置返回证据不足。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R23-02` | 4 | `check_cross_document_match` | 按文本/机构名归一化或数值容差比较跨文档字段。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R23-02` | 5 | `check_numeric_range` | 对一个或多个数值执行开闭区间上下限检查。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R23-02` | 6 | `evaluate_valve_test` | 按管道等级计算阀门抽检比例，或核验工厂逐台见证豁免条件，并检查试验记录和结果。 | 已实现（专用业务规则）；绑定：`implemented` |
| `AC-R23-02` | 7 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R23-03` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R23-03` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |

## 29. R12-R23 第一轮共性问题清单

1. R12-R14 必须先由产品目录分类许可、监检、型式试验和出厂检验要求。
2. R13 条款误绑定已关闭：已补绑 TSG D7006-2020 1.2.1、A1.2-A1.3，2021年第41号公告附件1注三、注四和 TSG 31-2025 2.2.1.2-2.2.1.3。
3. R14 的光谱、硬度、金相、NDT、耐压试验不能对所有产品全量强制。
4. R15 条款误绑已关闭：现已绑定 TSG 31—2025 第 1.10、2.2.1.5 和 TSG D7006—2020 附件 D D2.4.1，并与 R19 的“境外牌号材料”明确分离。
5. R16 必须按实际产品动态绑定完整的管子、管件和安全附件标准。
6. R17 与 R18 应分别负责到货验收过程和条件触发的技术复验。
7. R18 应由 `always` 改为条件适用，AC-R18-02 需要重写成可执行判断。
8. R19 的企业标准只在境内制造境外牌号材料时触发。
9. R20 必须区分两类新材料并补充批准手续。
10. R21、R22 应按是否发生标志移植/材料代用条件适用。
11. R23 条款包漏绑 GB/T 20801.1-2025 7.2.4，且需明确 GB/T 13927 与 GB/T 26480 的适用优先级。

## 30. R24 焊工资格证及持证合格项目

> **2026-07-17 实施状态更新：** R24-R34 已完成专用 Fact Builder、OCR 资料路由、确定性业务 Tool 和显式 atomicCheck 参数绑定。以下章节中标注“通用规则解释器；仍需规则参数装配”或 `implemented` 的旧 Tool 表属于改造前差距分析，不再代表当前运行状态；当前权威绑定以生成文件 `tools规划.md` 和 `backend/business_packs/engineering_inspection_v1/atomic_check_tool_bindings.yaml` 为准。能力边界和验收结果见 `docs/R24-R34_Tools完善实施报告.md`。

### 业务审查描述

以“每名焊工 × 每类实际焊接作业”为判断单元，核查人员身份、证书有效状态以及焊接方法、母材类别、焊件形式、焊接位置、厚度、管径、填充金属和工艺因素是否被持证合格项目覆盖。

### 固定标准条款

- TSG D7006-2020 D2.6.1，PDF 第 28 页。
- GB/T 20801.1-2025 7.4.1.1、7.4.1.5，PDF 第 92-93 页。
- TSG Z6002-2010 附件 A 的焊接方法、材料类别、位置、厚度、直径、填充材料、工艺因素和项目代号适用范围表。

### 标准版本切换风险

市场监管总局已发布 TSG Z6002-2026，并规定自 2026 年 8 月 1 日起施行。当前工程标准库只有 TSG Z6002-2010。系统必须按作业日期和新规过渡条款选择版本：2026 年 8 月 1 日前不能提前用新规否定旧证，施行日后也不能继续无条件使用 2010 版覆盖表。新规及证书过渡规则应在上线前补入标准版本库。

官方发布信息：[市场监管总局关于发布《特种设备焊接操作人员考核细则》的公告](https://scjgj.fujian.gov.cn/zt/tzsbxzxkggzl/zzwj/202605/t20260507_7144987.htm)。

### 当前 atomicCheck 问题

当前 AC-R24-02 至 AC-R24-04 将一个示例中的 FeII、6G、3/57 拆成固定规则，这是错误的。示例只能用于测试，正式 atomicCheck 应按资格覆盖维度拆分，不能把示例数值写成所有项目的判据。

### 建议 atomicCheck

- 焊工身份与人员名册、证书是否一致。
- 证书在实际施焊日期是否有效。
- 焊接方法覆盖。
- 母材类别及异种材料组合覆盖。
- 板/管焊件形式和焊接位置覆盖。
- 焊缝金属厚度、管外径覆盖。
- 填充金属类别和附加工艺因素覆盖。
- 每条抽查焊缝是否能关联到具体焊工和合格项目。
- 证据可追溯性。

### requiredFacts

人员姓名、身份证明、证书编号、签发/复审/有效信息、项目代号、实际作业日期、焊缝编号、焊接方法、材料牌号及类别、焊件形式、位置、母材厚度、焊缝金属厚度、管外径、填充金属、工艺因素。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R24-01` | 1 | `extract_welder_certificate` | 从焊工资格证 OCR 结果抽取证件号、档案号、发证机关和作业项目代码。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R24-01` | 2 | `decode_welder_qualification` | 解析焊工资格作业项目代码，形成方法、材料、位置、厚度和管径覆盖事实。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R24-01` | 3 | `check_date_covers` | 判断证照或报告有效期是否覆盖闭区间业务周期。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R24-01` | 4 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R24-02` | 1 | `extract_welder_certificate` | 从焊工资格证 OCR 结果抽取证件号、档案号、发证机关和作业项目代码。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R24-02` | 2 | `decode_welder_qualification` | 解析焊工资格作业项目代码，形成方法、材料、位置、厚度和管径覆盖事实。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R24-02` | 3 | `check_all_equal` | 对多个来源的值按指定归一化方式比较一致性；有效值不足时返回证据不足。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R24-02` | 4 | `check_welder_work_coverage` | 核验焊工资格项目是否覆盖实际方法、材料、位置、壁厚和管径。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R24-02` | 5 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R24-03` | 1 | `decode_welder_qualification` | 解析焊工资格作业项目代码，形成方法、材料、位置、厚度和管径覆盖事实。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R24-03` | 2 | `check_welder_work_coverage` | 核验焊工资格项目是否覆盖实际方法、材料、位置、壁厚和管径。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R24-03` | 3 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R24-04` | 1 | `decode_welder_qualification` | 解析焊工资格作业项目代码，形成方法、材料、位置、厚度和管径覆盖事实。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R24-04` | 2 | `check_welder_work_coverage` | 核验焊工资格项目是否覆盖实际方法、材料、位置、壁厚和管径。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R24-04` | 3 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R24-05` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R24-05` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |

### 业务边界和结论

“人证相符”属于现场确认。仅凭证书扫描件不能自动判定现场人员就是证书本人；没有可信现场身份见证时，该原子项应为 `REQUIRES_HUMAN_REVIEW`。任一实际作业超出证书覆盖范围，应对该焊工该作业返回 `NON_COMPLIANT`。

## 31. R25 焊接（粘接）工艺文件

### 业务审查描述

核查 WPS、PQR 及其批准状态，确认每种实际焊接组合都有经评定合格的工艺覆盖，WPS 参数未超出 PQR 和适用标准允许范围，并可关联到实际焊缝。

### 固定标准条款

- TSG D7006-2020 D2.6.2，PDF 第 29 页。
- NB/T 47014-2023 4.2-4.4、第 6 章及相关附件。
- GB/T 20801.1-2025 7.4.1.1-7.4.1.4，PDF 第 92-93 页。

### 业务边界

节点名称包含“粘接”，但现有固定条款和 Tool 只覆盖金属焊接。若工程存在非金属管道粘接，应进入独立分支并绑定相应非金属材料和粘接工艺标准；不能用 NB/T 47014 判断粘接工艺。

WPS/PQR 覆盖不是简单比较电流、电压、速度和厚度。应按焊接方法和材料类别解析重要因素、补加因素、厚度和直径覆盖范围、热处理状态、冲击试验要求等。

### 建议 atomicCheck

- 实际连接方式属于焊接还是粘接。
- 每份 WPS、PQR 是否存在、有效、审批完整且相互关联。
- WPS 规定字段是否完整。
- WPS 变量是否处于对应 PQR 的评定覆盖范围。
- 项目全部实际焊接组合是否都有 WPS/PQR 覆盖。
- 现场焊接记录引用的 WPS 版本是否正确。
- 证据可追溯性。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R25-01` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R25-01` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R25-01` | 3 | `recognize_signatures_and_seals` | 读取签字角色、签章、印章文字、位置和置信度；不判断签字是否齐全。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R25-01` | 4 | `check_signature_completeness` | 核验识别出的签字角色是否覆盖规则要求角色。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R25-01` | 5 | `check_required` | 检查事实对象中的必需字段是否存在；字段缺失返回 `failed`，规则未配置返回证据不足。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R25-01` | 6 | `check_cross_document_match` | 按文本/机构名归一化或数值容差比较跨文档字段。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R25-01` | 7 | `check_wps_pqr_coverage` | 按方法、材料、位置、填充金属、厚度和管径核验 PQR/WPS 覆盖实际焊接任务。 | 已实现（专用业务规则）；绑定：`implemented` |
| `AC-R25-01` | 8 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R25-02` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R25-02` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R25-02` | 3 | `extract_table_records` | 读取指定文档版本的表格和标准化行记录，可按业务表 Schema 过滤。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R25-02` | 4 | `recognize_signatures_and_seals` | 读取签字角色、签章、印章文字、位置和置信度；不判断签字是否齐全。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R25-02` | 5 | `check_signature_completeness` | 核验识别出的签字角色是否覆盖规则要求角色。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R25-02` | 6 | `check_cross_document_match` | 按文本/机构名归一化或数值容差比较跨文档字段。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R25-02` | 7 | `check_scope_coverage` | 按显式覆盖映射核验已授予范围是否覆盖要求范围。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R25-02` | 8 | `check_numeric_range` | 对一个或多个数值执行开闭区间上下限检查。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R25-02` | 9 | `check_wps_pqr_coverage` | 按方法、材料、位置、填充金属、厚度和管径核验 PQR/WPS 覆盖实际焊接任务。 | 已实现（专用业务规则）；绑定：`implemented` |
| `AC-R25-02` | 10 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R25-03` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R25-03` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |

### 结论规则

- 实际焊接组合没有覆盖它的合格 PQR/WPS：`NON_COMPLIANT`。
- WPS/PQR 文件不全或关键变量无法读取：`INSUFFICIENT_EVIDENCE`。
- 涉及项目当前未配置的粘接标准：`REQUIRES_HUMAN_REVIEW`，不能判符合。

## 32. R26 焊接材料质量证明文件

### 业务审查描述

以焊材牌号和批号为单位，核查质量证明文件、包装标记、采购验收和实际领用记录是否一致，焊材是否满足设计文件、WPS 和相应产品标准。

### 固定标准条款

- TSG D7006-2020 D2.6.3（1），PDF 第 29 页。
- GB/T 20801.1-2025 7.4.2.1-7.4.2.2，PDF 第 93 页。
- 按焊材类型条件适用 NB/T 47018、GB/T 5117、GB/T 8110 等产品标准。

当前条款包只绑定 GB/T 20801.1 和 JB/T 3223，没有把规则中列出的 NB/T 47018、GB/T 5117、GB/T 8110 等产品标准绑定到具体焊材类型，无法完成化学成分和力学性能的确定性判断。

### 规则修正

焊材不存在统一的“证书有效期”。GB/T 20801.1-2025 7.4.2.4 要求的是：超过质量证明文件或说明书注明的库存期限时，应复验合格后使用。因此当前 `check_date_covers` 不能直接用“当前日期是否超过证书日期”作否决。

### 建议 atomicCheck

- 质量证明文件和包装标记是否存在。
- 牌号、规格、批号、制造商是否与实物/领用记录一致。
- 焊材与设计、WPS 和母材组合是否匹配。
- 化学成分、力学性能及其他必检项目是否符合对应产品标准。
- 是否超过制造商规定库存期限；超过时是否完成复验。
- 证据可追溯性。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R26-01` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R26-01` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R26-01` | 3 | `check_required` | 检查事实对象中的必需字段是否存在；字段缺失返回 `failed`，规则未配置返回证据不足。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R26-01` | 4 | `check_cross_document_match` | 按文本/机构名归一化或数值容差比较跨文档字段。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R26-01` | 5 | `evaluate_welding_consumable` | 按焊材批次、牌号、规格、质量证明、产品标准和库存期限规则执行判断。 | 已实现（通用规则解释器；仍需规则参数装配）；绑定：`implemented` |
| `AC-R26-01` | 6 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R26-02` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R26-02` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R26-02` | 3 | `check_cross_document_match` | 按文本/机构名归一化或数值容差比较跨文档字段。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R26-02` | 4 | `check_date_covers` | 判断证照或报告有效期是否覆盖闭区间业务周期。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R26-02` | 5 | `evaluate_welding_consumable` | 按焊材批次、牌号、规格、质量证明、产品标准和库存期限规则执行判断。 | 已实现（通用规则解释器；仍需规则参数装配）；绑定：`implemented` |
| `AC-R26-02` | 6 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R26-03` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R26-03` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |

## 33. R27 焊接材料的验收、保管、发放、使用和回收的管理

### 业务审查描述

检查焊材从入库验收到焊缝使用的全链条管理，确保不同牌号、批号和状态可识别、未混用，烘干和保温按说明书或 WPS 执行，超期焊材经复验，发放和回收可追溯。

### 固定标准条款

- TSG D7006-2020 D2.6.3（2），PDF 第 29 页。
- GB/T 20801.1-2025 7.4.2.3-7.4.2.6，PDF 第 93 页。
- JB/T 3223-2017 第 6 章、7.1-7.3。

### 建议 atomicCheck

- 入库验收和批号建账。
- 分类、隔离、标识和库房环境管理。
- 库存期限和超期复验。
- 按说明书/WPS 的烘干、保温和允许重复烘干次数。
- 按焊工、焊缝、批号发放。
- 退回焊材状态识别、隔离和再使用条件。
- MTC、库存、烘干、领用、焊接记录的批号链完整性。
- 证据可追溯性。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R27-01` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R27-01` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R27-01` | 3 | `extract_table_records` | 读取指定文档版本的表格和标准化行记录，可按业务表 Schema 过滤。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R27-01` | 4 | `check_required` | 检查事实对象中的必需字段是否存在；字段缺失返回 `failed`，规则未配置返回证据不足。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R27-01` | 5 | `evaluate_welding_consumable_control` | 按焊材验收、保管、烘干、发放、回收和焊口追溯规则执行判断。 | 已实现（通用规则解释器；仍需规则参数装配）；绑定：`implemented` |
| `AC-R27-01` | 6 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R27-02` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R27-02` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R27-02` | 3 | `evaluate_welding_consumable_control` | 按焊材验收、保管、烘干、发放、回收和焊口追溯规则执行判断。 | 已实现（通用规则解释器；仍需规则参数装配）；绑定：`implemented` |
| `AC-R27-02` | 4 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R27-03` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R27-03` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |

烘干、保温不能设一个通用固定温度和时间，应从焊材说明书、质量证明文件、WPS 或施工单位受控规程中取得参数。

## 34. R28 管道组对

### 业务审查描述

按焊接接头逐项核对坡口、清理、内壁错边、组对间隙、不等壁厚削薄、支管组对、强力组对和附加应力控制是否满足设计、WPS 和标准要求。

### 固定标准条款

- TSG D7006-2020 D2.6.4，PDF 第 29 页。
- GB/T 20801.1-2025 7.4.4.1-7.4.4.4，其中 7.4.4.3.1-7.4.4.3.5 位于 PDF 第 94-95 页。

### 当前 atomicCheck 问题

当前只有一个业务原子项，把错边量、间隙、坡口角度放在一起，且没有覆盖不等壁厚削薄、支管组对、禁止强力组对、定位焊和清理要求。

### 建议 atomicCheck

- 坡口形式、尺寸和表面状态。
- 焊前清理范围和结果。
- 对接接头内壁错边量。
- 不等壁厚端部削薄修整。
- 支管连接组对偏差。
- 组对间隙是否在 WPS 范围内。
- 是否存在违规强力组对或附加应力。
- 定位焊材料、工艺、焊工资格和缺陷处理。
- 证据可追溯性。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R28-01` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R28-01` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R28-01` | 3 | `extract_table_records` | 读取指定文档版本的表格和标准化行记录，可按业务表 Schema 过滤。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R28-01` | 4 | `check_required` | 检查事实对象中的必需字段是否存在；字段缺失返回 `failed`，规则未配置返回证据不足。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R28-01` | 5 | `check_numeric_range` | 对一个或多个数值执行开闭区间上下限检查。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R28-01` | 6 | `evaluate_pipe_fit_up` | 按接头形式、错边、壁厚过渡、组对间隙和禁止强力组对规则执行判断。 | 已实现（通用规则解释器；仍需规则参数装配）；绑定：`implemented` |
| `AC-R28-01` | 7 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R28-02` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R28-02` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |

## 35. R29 施焊参数、施焊记录、焊缝标识

### 业务审查描述

以焊缝编号为主键，将实际施焊参数、焊工、WPS、焊材批号、预热/层间温度、施工日期和焊缝标识关联，判断实际施工是否符合当时有效的 WPS。

### 固定标准条款

- TSG D7006-2020 D2.6.5（1），PDF 第 29 页。
- GB/T 20801.1-2025 7.4.1.4、7.4.5.1-7.4.5.13、8.3.4，PDF 第 92、96-97、124 页。

### 建议 atomicCheck

- 每条抽查焊缝是否有唯一编号和可识别标记。
- 焊缝记录能否关联焊工、WPS、材料和焊材批号。
- 电流、电压、焊接速度/热输入、保护气体、层间温度等实际参数是否在 WPS 范围内。
- 要求的背面保护、预热、后热、焊道间检查是否执行。
- 焊缝标识方法是否符合项目规定且不损伤材料。
- 证据可追溯性。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R29-01` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R29-01` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R29-01` | 3 | `extract_table_records` | 读取指定文档版本的表格和标准化行记录，可按业务表 Schema 过滤。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R29-01` | 4 | `check_required` | 检查事实对象中的必需字段是否存在；字段缺失返回 `failed`，规则未配置返回证据不足。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R29-01` | 5 | `check_numeric_range` | 对一个或多个数值执行开闭区间上下限检查。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R29-01` | 6 | `evaluate_welding_process` | 按焊口、焊工、WPS、焊材和实际施焊参数关联规则执行判断。 | 已实现（通用规则解释器；仍需规则参数装配）；绑定：`implemented` |
| `AC-R29-01` | 7 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R29-02` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R29-02` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |

## 36. R30 焊接接头外观质量

### 业务审查描述

检查焊接接头目视检查覆盖率和具体外观缺陷，按管道检查等级、接头类型和设计要求应用对应验收限值，同时核对监检抽查数量。

### 固定标准条款

- TSG D7006-2020 D2.6.5（2），PDF 第 29 页：监检抽查数量，GC1 每个施工机组不少于焊接接头总数 5%，其他管道不少于 2%。
- GB/T 20801.1-2025 8.2.2、8.3.2、表 42、表 43，PDF 第 118-122 页。

### 必须区分的两个比例

- 施工单位按 GB/T 20801.1 表 42 实施的目视检查比例。
- 监检机构按 TSG D7006 D2.6.5（2）实施的监督抽查比例。

这两个比例的责任主体、分母和目的不同，不能由一个 `check_numeric_range` 混合判断。

### 建议 atomicCheck

- 确定管道级别和检查等级。
- 施工单位目视检查比例是否达到表 42 要求。
- 监检抽查是否按每个施工机组达到 5%/2% 要求。
- 裂纹、未熔合、未焊透、气孔、夹渣、咬边、根部凹陷、余高等是否满足表 43 和设计要求。
- 每名焊工是否被纳入要求的目视检查范围。
- 证据可追溯性。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R30-01` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R30-01` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R30-01` | 3 | `extract_table_records` | 读取指定文档版本的表格和标准化行记录，可按业务表 Schema 过滤。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R30-01` | 4 | `check_required` | 检查事实对象中的必需字段是否存在；字段缺失返回 `failed`，规则未配置返回证据不足。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R30-01` | 5 | `check_conditional_requirement` | 条件为真时检查必需事实，条件为假返回不适用。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R30-01` | 6 | `evaluate_weld_appearance` | 按管道等级、接头类型、外观缺陷和相应验收限值规则执行判断。 | 已实现（通用规则解释器；仍需规则参数装配）；绑定：`implemented` |
| `AC-R30-01` | 7 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R30-02` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R30-02` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R30-02` | 3 | `extract_table_records` | 读取指定文档版本的表格和标准化行记录，可按业务表 Schema 过滤。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R30-02` | 4 | `check_required` | 检查事实对象中的必需字段是否存在；字段缺失返回 `failed`，规则未配置返回证据不足。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R30-02` | 5 | `evaluate_weld_appearance` | 按管道等级、接头类型、外观缺陷和相应验收限值规则执行判断。 | 已实现（通用规则解释器；仍需规则参数装配）；绑定：`implemented` |
| `AC-R30-02` | 6 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R30-03` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R30-03` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |

## 37. R31 焊缝返修

### 业务审查描述

以缺陷和返修部位为单位，检查原因分析、缺陷清除、返修措施、合格工艺、焊工资格、审批、返修后原方法复检、返修次数和必要的重新热处理。

### 固定标准条款

- TSG D7006-2020 D2.6.6，PDF 第 29 页。
- GB/T 20801.1-2025 7.4.11.1-7.4.11.5，PDF 第 102 页。

标准的准确口径是：同一部位返修次数**超过两次**时，应重新制定返修措施，并经施焊单位技术负责人批准后返修。当前文本中的“通常不得超过2次”容易被误解为第三次绝对禁止，应修正。

### 建议 atomicCheck

- 缺陷部位、类型、发现方法和原因分析。
- 缺陷是否清除并在必要时经检测确认。
- 返修使用的工艺是否评定合格，焊工是否覆盖。
- 超过两次时专项返修措施和技术负责人批准是否齐全。
- 返修后是否按原方法重新检验并合格。
- 热处理后返修是否重新热处理。
- 返修和复检记录是否进入交工文件。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R31-01` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R31-01` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R31-01` | 3 | `recognize_signatures_and_seals` | 读取签字角色、签章、印章文字、位置和置信度；不判断签字是否齐全。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R31-01` | 4 | `check_signature_completeness` | 核验识别出的签字角色是否覆盖规则要求角色。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R31-01` | 5 | `check_required` | 检查事实对象中的必需字段是否存在；字段缺失返回 `failed`，规则未配置返回证据不足。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R31-01` | 6 | `check_conditional_requirement` | 条件为真时检查必需事实，条件为假返回不适用。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R31-01` | 7 | `evaluate_weld_repair` | 按同一物理位置的返修次数、审批、工艺、复检和闭环规则执行判断。 | 已实现（通用规则解释器；仍需规则参数装配）；绑定：`implemented` |
| `AC-R31-01` | 8 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R31-02` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R31-02` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |

## 38. R32 焊接接头焊后热处理工艺文件

### 业务审查描述

先判断每个焊接接头是否需要焊后热处理，再检查工艺文件中的温度、保温时间、升降温速度、加热范围、测温方式和特殊材料控制是否符合设计、WPS/PQR 和标准。

### 固定标准条款

- TSG D7006-2020 D2.7（1），PDF 第 29 页。
- GB/T 20801.1-2025 7.6.1-7.6.4，重点为 7.6.3、表 36、表 38，PDF 第 104-109 页。

### 当前配置问题

当前适用性配置为 `always`。实际是否需要热处理取决于材料类别、厚度、强度、接头形式、工况和设计要求；不要求热处理的焊口应为 `NOT_APPLICABLE`，不能因没有热处理工艺卡判不符合。

### 建议 atomicCheck

- 单个焊口是否触发焊后热处理。
- 工艺是否与设计、WPS/PQR 一致并经批准。
- 热处理温度和保温时间。
- 400℃以上的升温、降温速度。
- 热处理厚度计算和加热范围。
- 特殊材料及时热处理、后热或缓冷要求。
- 热电偶布置和自动记录要求。
- 证据可追溯性。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R32-01` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R32-01` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R32-01` | 3 | `recognize_signatures_and_seals` | 读取签字角色、签章、印章文字、位置和置信度；不判断签字是否齐全。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R32-01` | 4 | `check_signature_completeness` | 核验识别出的签字角色是否覆盖规则要求角色。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R32-01` | 5 | `check_numeric_range` | 对一个或多个数值执行开闭区间上下限检查。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R32-01` | 6 | `evaluate_heat_treatment` | 按焊口级适用性、热处理参数、曲线和硬度规则执行判断。 | 已实现（通用规则解释器；仍需规则参数装配）；绑定：`implemented` |
| `AC-R32-01` | 7 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R32-02` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R32-02` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |

## 39. R33 热处理设备用测温记录仪表

### 业务审查描述

检查实际用于热处理的测温记录仪、热电偶及相关测量链是否满足校验要求，仪表身份、校验有效期和量程精度是否覆盖实际热处理日期及温度范围。

### 固定标准条款

- TSG D7006-2020 D2.7（2），PDF 第 29 页。
- GB/T 20801.1-2025 7.6.5.1-7.6.5.2，PDF 第 109 页。

### 适用性和事实关联

R33 应继承 R32 的适用结果。项目无任何需要热处理的焊口时，R33 可为 `NOT_APPLICABLE`。仪表证书必须与实际曲线/报告中的设备编号一致，且校验有效期覆盖热处理执行日期；只存在一张未关联设备的校验证书不能判符合。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R33-01` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R33-01` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R33-01` | 3 | `check_required` | 检查事实对象中的必需字段是否存在；字段缺失返回 `failed`，规则未配置返回证据不足。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R33-01` | 4 | `evaluate_heat_treatment_instruments` | 按热处理仪表身份、量程、精度和检定有效期规则执行判断。 | 已实现（通用规则解释器；仍需规则参数装配）；绑定：`implemented` |
| `AC-R33-01` | 5 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R33-02` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R33-02` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |

## 40. R34 热处理记录、报告曲线、硬度检测报告

### 业务审查描述

将热处理工艺、自动温度曲线、热处理报告和硬度报告按焊口关联，检查实际温度、保温时间、升降温速度、连续记录、测点和硬度结果。

### 固定标准条款

- TSG D7006-2020 D2.7（3），PDF 第 29 页。
- GB/T 20801.1-2025 7.6.5.2、7.6.6及表36，PDF 第 105-109 页。

### 当前规则错误风险

当前 AC-R34-02 写成“一般碳钢≤200HB、合金钢≤225HB”，该规则过度简化。表 36 对不同铬含量和材料规定 200、225、241、250 等不同限值；部分材料仅在设计规定或特定工况下检查。表中未给限值的材料还适用“焊缝和热影响区不大于母材硬度 125%”的规则。必须按材料类别、厚度、工况和设计条件查表，不能固定两个阈值。

GB/T 20801.1-2025 7.6.6.2 还规定：炉内热处理每炉次至少抽查 10%，局部热处理 100% 测定硬度。

### 建议 atomicCheck

- 每个应热处理焊口是否有记录、曲线和报告。
- 曲线与工艺卡、焊口、设备编号是否关联。
- 实际温度、保温时间和升降温速度是否合格。
- 自动记录是否覆盖整个热处理过程，是否存在不可解释中断。
- 硬度检测比例是否符合炉内 10%/局部 100% 及设计要求。
- 焊缝、热影响区和要求的母材测点是否完整。
- 按材料规则计算的硬度限值是否满足。
- 证据可追溯性。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R34-01` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R34-01` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R34-01` | 3 | `extract_table_records` | 读取指定文档版本的表格和标准化行记录，可按业务表 Schema 过滤。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R34-01` | 4 | `check_required` | 检查事实对象中的必需字段是否存在；字段缺失返回 `failed`，规则未配置返回证据不足。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R34-01` | 5 | `check_numeric_range` | 对一个或多个数值执行开闭区间上下限检查。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R34-01` | 6 | `evaluate_heat_treatment` | 按焊口级适用性、热处理参数、曲线和硬度规则执行判断。 | 已实现（通用规则解释器；仍需规则参数装配）；绑定：`implemented` |
| `AC-R34-01` | 7 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R34-02` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R34-02` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R34-02` | 3 | `extract_table_records` | 读取指定文档版本的表格和标准化行记录，可按业务表 Schema 过滤。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R34-02` | 4 | `check_cross_document_match` | 按文本/机构名归一化或数值容差比较跨文档字段。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R34-02` | 5 | `check_numeric_range` | 对一个或多个数值执行开闭区间上下限检查。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R34-02` | 6 | `evaluate_heat_treatment` | 按焊口级适用性、热处理参数、曲线和硬度规则执行判断。 | 已实现（通用规则解释器；仍需规则参数装配）；绑定：`implemented` |
| `AC-R34-02` | 7 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R34-03` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R34-03` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |

## 41. R35 无损检测机构施工现场质量保证体系的实施

### 业务审查描述

按每家在场无损检测机构分别检查人员、设备器材、工艺文件、检测环境、实施过程、资料档案和安全防护等质量控制要素是否在项目现场有效运行。

### 固定标准条款

- TSG D7006-2020 D2.8.1（1），PDF 第 29 页。
- NB/T 47013.1-2015 6.1-6.3、7.1，PDF 第 13-14 页。

### 业务边界

“现场有效实施”不是仅凭质量保证手册即可得出的文件结论。AI 可以预审手册、任命、人员、设备校验、工艺文件和记录是否齐全，但现场人员是否按体系执行、环境和安全措施是否有效，需要监检人员的现场见证或结构化现场记录。

### 建议 atomicCheck

- 每家机构的项目组织、责任人员和授权是否明确。
- 人员资格和实际岗位是否匹配。
- 设备器材、灵敏度器材的检定/校准/核查是否有效。
- 工艺文件和受控表单是否为有效版本。
- 检测场所、环境和辐射/作业安全措施是否满足要求。
- 委托单、记录、报告、底片/数据档案链是否完整。
- 现场实施见证是否支持“有效运行”的结论。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R35-01` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R35-01` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R35-01` | 3 | `extract_table_records` | 读取指定文档版本的表格和标准化行记录，可按业务表 Schema 过滤。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R35-01` | 4 | `check_required` | 检查事实对象中的必需字段是否存在；字段缺失返回 `failed`，规则未配置返回证据不足。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R35-01` | 5 | `check_date_covers` | 判断证照或报告有效期是否覆盖闭区间业务周期。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R35-01` | 6 | `evaluate_ndt_quality_system` | 按每家 NDT 机构的现场质量体系、任命、设备和记录追溯规则执行判断。 | 已实现（通用规则解释器；仍需规则参数装配）；绑定：`implemented` |
| `AC-R35-01` | 7 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R35-02` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R35-02` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |

缺少现场见证时不得自动返回 `COMPLIANT`，应为 `REQUIRES_HUMAN_REVIEW`。

## 42. R36 无损检测方案

### 业务审查描述

检查项目级无损检测方案是否与设计文件和管线清单一致，是否完整规定检测对象、方法、比例、位置、时机、技术等级、合格级别、执行标准、人员设备、环境安全、记录报告和不合格处理。

### 固定标准条款

- TSG D7006-2020 D2.8.1（2），PDF 第 29 页。
- NB/T 47013.1-2015 4.3.1、4.3.2、7.2，PDF 第 8、14 页。
- GB/T 20801.1-2025 8.3.1、8.3.3.1，PDF 第 119-122 页。
- 根据实际方法条件绑定 NB/T 47013.2、.3、.4、.5、.10、.11、.14、.15 等具体分册。

### 当前 atomicCheck 问题

当前 AC-R36-01 只有“需提供无损检测方案”，仅能检查文件存在性，不能证明方案符合标准。应拆分为方案完整性、设计一致性、方法适用性、比例和级别、审批与版本等原子项。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R36-01` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R36-01` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R36-01` | 3 | `check_required` | 检查事实对象中的必需字段是否存在；字段缺失返回 `failed`，规则未配置返回证据不足。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R36-01` | 4 | `evaluate_ndt_process` | 按 NDT 方案、工艺或记录报告 profile 的方法、比例、级别、时机和版本规则执行判断。 | 已实现（通用规则解释器；仍需规则参数装配）；绑定：`implemented` |
| `AC-R36-01` | 5 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R36-02` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R36-02` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |

## 43. R37 检测过程中发现问题的处理

### 业务审查描述

检查无损检测发现超标缺陷或其他不符合时，是否形成从缺陷通知、处置、返修/更换、复检、累进检查到关闭的完整链条。

### 固定标准条款

- TSG D7006-2020 D2.8.1（3），PDF 第 29 页。
- NB/T 47013.1-2015 4.5、6.1-6.2。
- GB/T 20801.1-2025 8.1.3-8.1.4、8.3.3.4，PDF 第 118、124 页。
- 缺陷评定和复检方法还应条件绑定实际采用的 NB/T 47013 分册。

### 适用性拆分

不论项目是否已出现不合格，机构都应有受控的不符合处理程序；但具体事件闭环检查只有在发现问题时适用。因此应拆成：

- 程序文件检查：始终适用。
- 不合格事件闭环：存在事件时适用，否则 `NOT_APPLICABLE`。

### 建议 atomicCheck

- 不符合控制程序是否存在且受控。
- 每个超标缺陷是否有唯一事件和焊缝/部位标识。
- 是否及时通知施工和相关责任方。
- 返修或更换是否有批准和实际记录。
- 是否按原方法、范围和验收准则重新检查。
- 抽样/局部检查发现不合格时是否执行累进检查。
- 返修、复检、扩检结果是否完整关闭。
- 证据可追溯性。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R37-01` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R37-01` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R37-01` | 3 | `check_required` | 检查事实对象中的必需字段是否存在；字段缺失返回 `failed`，规则未配置返回证据不足。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R37-01` | 4 | `evaluate_ndt_nonconformance` | 按 NDT 问题通知、处置、扩检、复检和闭环规则执行判断。 | 已实现（通用规则解释器；仍需规则参数装配）；绑定：`implemented` |
| `AC-R37-01` | 5 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R37-02` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R37-02` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R37-02` | 3 | `check_required` | 检查事实对象中的必需字段是否存在；字段缺失返回 `failed`，规则未配置返回证据不足。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R37-02` | 4 | `evaluate_ndt_nonconformance` | 按 NDT 问题通知、处置、扩检、复检和闭环规则执行判断。 | 已实现（通用规则解释器；仍需规则参数装配）；绑定：`implemented` |
| `AC-R37-02` | 5 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R37-03` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R37-03` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |

### 当前项目边界

系统当前可以审查已有联络单或意见书，但不实现联络单自动生成流程。没有发现问题时，不能因为没有联络单判不符合；发现问题却没有任何通知和闭环证据时，应判 `NON_COMPLIANT`。

## 44. R11、R24-R37 第一轮共性问题清单

1. R11 的 GB 50235-2010 专业条款号和页码需要重新定位。
2. R24 必须增加 TSG Z6002-2026 和 2026 年 8 月 1 日的版本切换逻辑。
3. R24 的示例参数不能作为固定 atomicCheck。
4. R25 必须把焊接与粘接分支拆开。
5. R26 不应使用通用“证书有效期”，应使用库存期限和复验规则。
6. R26 缺少按焊材类型动态绑定产品标准的机制。
7. R30 必须分开施工单位目视检查比例和监检抽查比例。
8. R32-R34 必须共享同一个焊口级热处理适用性结果。
9. R34 的 200HB/225HB 固定阈值必须改成材料和条件驱动的表格规则。
10. R35 的现场有效实施必须保留监检人员现场确认。
11. R36 不能只判断方案是否存在。
12. R37 必须区分“不符合控制程序”与“实际不符合事件闭环”。

## 45. R38 无损检测人员资格证、执业注册证及持证合格项目

### 业务审查描述

以“每名现场无损检测人员 × 实际检测方法 × 承担职责”为判断单元，核查资格证、执业注册、证书有效期、注册单位、方法和级别是否覆盖其实际检测、评定、审核工作，并与现场公示及检测记录一致。

### 固定标准条款

- TSG D7006-2020 D2.8.2，PDF 第 29 页。
- NB/T 47013.1-2015 4.1.1-4.1.3，PDF 第 7 页。
- TSG Z8001-2019 及适用修改单。
- 实际方法对应的 NB/T 47013 分册。

当前专业条款包仅绑定 NB/T 47013.1 的人员通用要求，没有绑定 TSG Z8001-2019 的证书、项目、级别和执业要求，无法独立完成资格合法性判断。

### 建议 atomicCheck

- 现场人员与人员明细、现场公示、证书身份一致。
- 资格证和执业注册在实际检测日期有效。
- 注册单位与实际执业机构一致。
- 检测方法覆盖实际 RT/UT/MT/PT 等作业。
- 级别覆盖实际操作、评定、审核或批准职责。
- 检测记录、报告签字人与系统人员事实一致。
- 证据可追溯性。

### requiredFacts

人员姓名、身份证明、资格证号、注册证号、方法、级别、有效期、注册单位、实际作业日期、检测方法、承担角色、记录和报告签字。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R38-01` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R38-01` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R38-01` | 3 | `extract_table_records` | 读取指定文档版本的表格和标准化行记录，可按业务表 Schema 过滤。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R38-01` | 4 | `check_required` | 检查事实对象中的必需字段是否存在；字段缺失返回 `failed`，规则未配置返回证据不足。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R38-01` | 5 | `check_conditional_requirement` | 条件为真时检查必需事实，条件为假返回不适用。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R38-01` | 6 | `check_ndt_personnel_coverage` | 按工作日期、注册状态、检测方法和等级核验每项 NDT 任务的人员覆盖。 | 已实现（专用业务规则）；绑定：`implemented` |
| `AC-R38-01` | 7 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R38-02` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R38-02` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |

“人证与现场人员相符”仍需现场见证；只有扫描件时该原子项不得自动判符合。

## 46. R39 无损检测工艺文件

### 业务审查描述

检查每种实际使用的无损检测方法是否有受控的工艺规程和针对具体检测对象的操作指导书，内容、验证、审批和版本是否满足 NB/T 47013 通用及相应分册要求。

### 固定标准条款

- TSG D7006-2020 D2.8.3，PDF 第 30 页。
- NB/T 47013.1-2015 4.3.2.1-4.3.2.4、7.2，PDF 第 8-10、14 页。
- 条件绑定 NB/T 47013.2、.3、.4、.5、.10、.11、.14、.15 等实际检测方法分册。

### 建议 atomicCheck

- 实际采用的每种检测方法均有工艺规程。
- 每类具体对象有操作指导书，首次应用已完成工艺验证。
- 工艺规程包含版本、范围、标准、人员、设备器材、工艺因素、时机、表面准备、标记、评定等级、记录报告要求和审批。
- 操作指导书包含对象、编号、规格材质、热处理状态、部位比例、设备器材、参数、程序和示意图。
- 文件版本在实际检测日期有效并与检测记录一致。
- 证据可追溯性。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R39-01` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R39-01` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R39-01` | 3 | `check_required` | 检查事实对象中的必需字段是否存在；字段缺失返回 `failed`，规则未配置返回证据不足。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R39-01` | 4 | `evaluate_ndt_process` | 按 NDT 方案、工艺或记录报告 profile 的方法、比例、级别、时机和版本规则执行判断。 | 已实现（通用规则解释器；仍需规则参数装配）；绑定：`implemented` |
| `AC-R39-01` | 5 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R39-02` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R39-02` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |

当前 `evaluate_ndt_process` 过于宽泛，应将方法适用性、字段完整性、工艺验证和版本有效性拆成确定性结果。

## 47. R40 无损检测记录、报告

### 业务审查描述

将委托单、检测记录、检测报告、焊口清单和设计要求逐项关联，核查检测时机、方法、比例、位置、执行标准、技术等级、合格级别、结果、人员和设备是否符合要求。

### 固定标准条款

- TSG D7006-2020 D2.8.4，PDF 第 30 页。
- NB/T 47013.1-2015 7.3.1-7.4.4，PDF 第 14-16 页。
- GB/T 20801.1-2025 8.3.3 及实际方法对应的 NB/T 47013 分册。

### 建议 atomicCheck

- 项目要求的每个检测对象是否有记录和报告。
- 记录、报告与委托单、焊口编号、检测部位一致。
- 检测时机满足焊后、热处理后或延迟检测条件。
- 检测方法、比例和位置满足设计、方案和标准。
- 技术等级和合格级别正确。
- 人员、设备、工艺文件版本可追溯且有效。
- 记录与报告结果一致，签字权限正确。
- 不合格结果已进入 R37 闭环。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R40-01` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R40-01` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R40-01` | 3 | `extract_table_records` | 读取指定文档版本的表格和标准化行记录，可按业务表 Schema 过滤。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R40-01` | 4 | `check_required` | 检查事实对象中的必需字段是否存在；字段缺失返回 `failed`，规则未配置返回证据不足。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R40-01` | 5 | `evaluate_ndt_process` | 按 NDT 方案、工艺或记录报告 profile 的方法、比例、级别、时机和版本规则执行判断。 | 已实现（通用规则解释器；仍需规则参数装配）；绑定：`implemented` |
| `AC-R40-01` | 6 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R40-02` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R40-02` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |

## 48. R41 射线检测底片抽查

### 业务审查描述

先确定射线检测底片或数字图像总体，再按 TSG D7006 要求生成监检抽查样本，检查样本是否覆盖各施工单位、固定焊口和返修部位，并由有资格人员审查底片/图像质量及缺陷评定质量。

### 固定标准条款

- TSG D7006-2020 D2.8.5，PDF 第 30 页：抽查数量不少于底片总数 5%，覆盖每个施工单位，包括固定焊和返修部位；评片质量不符合时增加抽查比例。
- NB/T 47013.2-2015 第 4-8 章。
- 数字射线检测时适用 NB/T 47013.11-2023。
- GB/T 20801.1-2025 8.3.3.2.1-8.3.3.2.4，PDF 第 123 页。

当前专业条款包没有在 R41 中绑定 NB/T 47013.11-2023，导致数字图像分支缺失。

### 建议 atomicCheck

- 底片/数字图像总量和唯一编号是否可确定。
- 抽查数量是否达到总数 5%。
- 是否覆盖每个施工单位、固定焊口和返修部位。
- 底片/图像的工件、焊口、日期和报告映射是否正确。
- 像质计、标记、黑度/图像质量、灵敏度和有效评定区是否满足方法标准。
- 缺陷评定是否与报告一致。
- 发现评片质量问题时是否增加抽查比例并完成。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R41-01` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R41-01` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R41-01` | 3 | `check_required` | 检查事实对象中的必需字段是否存在；字段缺失返回 `failed`，规则未配置返回证据不足。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R41-01` | 4 | `evaluate_rt_film` | 核验底片/数字影像与焊口、报告、影像质量和抽样要求的关联。 | 已实现（专用业务规则）；绑定：`implemented` |
| `AC-R41-01` | 5 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R41-02` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R41-02` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |

未经验证的通用视觉模型不能独立作出射线缺陷评级和放行结论。Tool 可完成抽样、映射和质量参数核对，专业评片结论应由有资格人员确认。

## 49. R42 射线检测现场抽查

### 业务审查描述

监检机构在现场独立对焊接接头实施射线检测抽查，系统用于计算抽查数量和覆盖范围、记录抽查过程并核对结果及加倍抽查闭环，不能用施工单位原有底片替代现场抽查。

### 固定标准条款

- TSG D7006-2020 D2.8.6，PDF 第 30 页：不少于焊接接头总数 1‰，且不少于 2 个，覆盖每个施工单位和检测机构；不合格时对同一施工单位及检测机构责任范围加倍抽查。
- NB/T 47013.2-2015；使用数字成像时适用 NB/T 47013.11-2023。

### 建议 atomicCheck

- 焊接接头总体数量和参与施工/检测机构是否确定。
- 初次现场抽查数量达到 1‰ 且不少于 2 个。
- 抽样覆盖每个施工单位和检测机构。
- 抽查为监检机构现场实施并有过程见证。
- 检测工艺、人员、设备和结果合格。
- 出现不合格时加倍抽查范围和数量正确并完成。
- 抽查结果与原检测结果差异已处理。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R42-01` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R42-01` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R42-01` | 3 | `extract_table_records` | 读取指定文档版本的表格和标准化行记录，可按业务表 Schema 过滤。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R42-01` | 4 | `check_required` | 检查事实对象中的必需字段是否存在；字段缺失返回 `failed`，规则未配置返回证据不足。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R42-01` | 5 | `check_sampling_requirement` | 按总体数、比例和最小数量计算抽样数，并校验实际样本。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R42-01` | 6 | `evaluate_rt_film` | 核验底片/数字影像与焊口、报告、影像质量和抽样要求的关联。 | 已实现（专用业务规则）；绑定：`implemented` |
| `AC-R42-01` | 7 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R42-02` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R42-02` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |

现场实施和射线安全控制必须由监检人员见证，AI 只做计划计算和记录一致性审查。

## 50. R43 防腐及保温材料质量证明文件

### 业务审查描述

按材料类型和批次检查防腐层、补口、补伤、保温、保护层及相关辅材的质量证明、产品说明书、性能报告、标识和进场验收，并判断工厂预制防腐管道元件是否触发许可、型式试验或制造监检要求。

### 固定标准条款

- TSG D7006-2020 D2.9（1），PDF 第 30 页。
- GB/T 19285-2026 5.3.1.1、5.3.4，PDF 第 12-13 页，适用于埋地钢质管道腐蚀防护材料和资料。
- GB/T 20801.1-2025 8.5。
- 具体材料应条件绑定设计采用的产品技术标准。

GB/T 19285-2026 已于 2026 年 5 月 1 日实施并代替 GB/T 19285-2014。当前项目采用 2026 版在时间上有效。官方状态：[全国标准信息公共服务平台](https://std.samr.gov.cn/gb/search/gbDetailedCNF?id=49FD1852CB2DC31FE06397BE0A0A1304)。

### 当前条款定位错误

当前专业条款包将 GB/T 19285-2026 的 4.2、5.1 标记为“防腐材料及进场检验”。原文 4.2 是腐蚀防护检验的管段划分，5.1 是建设期检验一般规定；真正涉及材料质量证明的是 5.3.1.1，资料验收和追溯是 5.3.4。应修正绑定。

### 建议 atomicCheck

- 每类材料是否有合格证、说明书和性能测试报告。
- 产品名称、型号、批号、生产日期和制造商与进场/使用记录一致。
- 性能项目符合设计和相应产品标准。
- 工厂预制防腐管件的出厂资料完整。
- 产品类别是否依法触发制造许可、型式试验或制造监检，触发时证书齐全。
- 材料批次能追溯到具体施工区域。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R43-01` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R43-01` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R43-01` | 3 | `check_required` | 检查事实对象中的必需字段是否存在；字段缺失返回 `failed`，规则未配置返回证据不足。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R43-01` | 4 | `check_numeric_range` | 对一个或多个数值执行开闭区间上下限检查。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R43-01` | 5 | `check_conditional_requirement` | 条件为真时检查必需事实，条件为假返回不适用。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R43-01` | 6 | `evaluate_corrosion_protection` | 按具体防腐、保温、电火花、阴极保护或静电接地 profile 执行判断；不同 profile 不得混用。 | 已实现（通用规则解释器；仍需规则参数装配）；绑定：`implemented` |
| `AC-R43-01` | 7 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R43-02` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R43-02` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |

## 51. R44 防腐、补口、补伤及保温

### 业务审查描述

按防腐体系和保温结构分别审查施工过程、环境条件、表面处理、材料批次、厚度、搭接/粘结、漏点、修补、保护层及验收记录。

### 固定标准条款

- TSG D7006-2020 D2.9（2），PDF 第 30 页。
- 埋地钢质管道外防腐适用 GB/T 19285-2026 5.3.1-5.3.4，PDF 第 12-13 页。
- 具体防腐层、补口材料和保温体系按设计文件引用的专业标准动态绑定。

当前固定条款只能覆盖埋地钢质管道的部分防腐检验，不能覆盖所有地上涂层、非钢管道、保温层和保护层。R44 应先分类，不能由一个 `evaluate_corrosion_protection` 统一判断。

### 建议 atomicCheck

- 防腐/保温体系和适用标准识别。
- 表面处理等级、清洁度和粗糙度。
- 施工环境温湿度、露点和基材温度。
- 材料型号、批次、配比和可使用时间。
- 防腐层外观、厚度、搭接、粘结和固化。
- 补口补伤位置、100% 漏点检测及修复复检。
- 保温材料、厚度、接缝、防潮层和外保护层。
- 施工区域、检验记录和材料批次追溯。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R44-01` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R44-01` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R44-01` | 3 | `extract_table_records` | 读取指定文档版本的表格和标准化行记录，可按业务表 Schema 过滤。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R44-01` | 4 | `check_required` | 检查事实对象中的必需字段是否存在；字段缺失返回 `failed`，规则未配置返回证据不足。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R44-01` | 5 | `evaluate_corrosion_protection` | 按具体防腐、保温、电火花、阴极保护或静电接地 profile 执行判断；不同 profile 不得混用。 | 已实现（通用规则解释器；仍需规则参数装配）；绑定：`implemented` |
| `AC-R44-01` | 6 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R44-02` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R44-02` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |

## 52. R45 防腐层电火花检测

### 业务审查描述

对埋地防腐层、补口和补伤区域，核查检测仪器、检测电压、检测时点、覆盖范围、漏点位置、修复和复检结果。

### 固定标准条款

- TSG D7006-2020 D2.9（3），PDF 第 30 页。
- GB/T 19285-2026 5.3.2.2（c），PDF 第 13 页：补口、补伤区 100% 漏点检测，无漏点；发现漏点应修复并重新检测合格。
- SY/T 4113.11-2023 第 4-7 章。

### 建议 atomicCheck

- 是否存在触发电火花/漏点检测的防腐层区域。
- 仪器型号、编号、校准或核查状态在检测日期有效。
- 检测电压按防腐层类型和厚度正确计算或选择。
- 检测在下沟/覆盖前的正确时点完成。
- 补口补伤区覆盖率 100%，其他区域按设计和标准覆盖。
- 漏点位置完整记录，修复后复检合格。
- 报告与施工区域、材料、厚度记录一致。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R45-01` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R45-01` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R45-01` | 3 | `extract_table_records` | 读取指定文档版本的表格和标准化行记录，可按业务表 Schema 过滤。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R45-01` | 4 | `check_required` | 检查事实对象中的必需字段是否存在；字段缺失返回 `failed`，规则未配置返回证据不足。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R45-01` | 5 | `check_date_covers` | 判断证照或报告有效期是否覆盖闭区间业务周期。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R45-01` | 6 | `evaluate_corrosion_protection` | 按具体防腐、保温、电火花、阴极保护或静电接地 profile 执行判断；不同 profile 不得混用。 | 已实现（通用规则解释器；仍需规则参数装配）；绑定：`implemented` |
| `AC-R45-01` | 7 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R45-02` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R45-02` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |

## 53. R46 牺牲阳极、外加电流阴极保护、杂散电流排流装置

### 业务审查描述

先按设计识别牺牲阳极、外加电流、联合保护或杂散电流排流分支，再分别检查产品、安装、接线、绝缘、调试、保护电位/电流和验收结果。

### 固定标准条款

- TSG D7006-2020 D2.9（3），PDF 第 30 页。
- GB/T 33378-2025 6.3.2、6.4-6.5、6.9、7.4。
- GB/T 21448-2017 第 5-7、9 章。
- 测量方法还应绑定 GB/T 21246 等设计采用标准。

GB/T 33378-2025 已于 2026 年 5 月 1 日实施并代替 2016 版；GB/T 21448-2017 经 2025 年复审继续有效。版本状态应写入标准元数据，而不是由 LLM 推断。

### 建议 atomicCheck

- 阴极保护和排流装置适用性及类型。
- 产品型号、数量、性能和质量证明。
- 阳极、参比电极、测试桩、电缆和绝缘装置安装位置。
- 接线极性、连接质量和绝缘隔离。
- 系统投运时点和临时保护要求。
- 保护电位、电流、IR 降及干扰测试方法正确。
- 不满足保护准则时的调整和复测闭环。
- 施工、调试和验收资料完整。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R46-01` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R46-01` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R46-01` | 3 | `extract_table_records` | 读取指定文档版本的表格和标准化行记录，可按业务表 Schema 过滤。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R46-01` | 4 | `check_required` | 检查事实对象中的必需字段是否存在；字段缺失返回 `failed`，规则未配置返回证据不足。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R46-01` | 5 | `evaluate_corrosion_protection` | 按具体防腐、保温、电火花、阴极保护或静电接地 profile 执行判断；不同 profile 不得混用。 | 已实现（通用规则解释器；仍需规则参数装配）；绑定：`implemented` |
| `AC-R46-01` | 6 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R46-02` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R46-02` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |

保护电位不能脱离参比电极、测量方法、IR 降和环境条件使用一个固定阈值判断。

## 54. R47 静电接地

### 业务审查描述

先判断设计和介质是否要求静电接地，再检查接地点、跨接、材料连接方式、接地电阻和跨接电阻测试结果。

### 固定标准条款

- TSG D7006-2020 D2.9（4），PDF 第 30 页。
- GB/T 20801.1-2025 7.7.13.1-7.7.13.4，PDF 第 115 页。
- GB/T 20801.1-2025 附录 G G.9，PDF 第 236-237 页。

### 当前适用性错误

当前条款包配置为 `always`，但 7.7.13 明确以“设计有静电接地要求”为前提；附录 G 主要针对爆炸、火灾危险场所以及易燃介质等情形。应改为条件适用。

### 建议 atomicCheck

- 设计是否要求静电接地，危险场所/易燃介质是否触发。
- 接地点位置和数量符合设计。
- 法兰、螺纹等接头跨接电阻不大于 0.03Ω，否则是否设置跨接线。
- 除设计另有规定外，对地电阻不大于 100Ω；山区特殊规则按适用条款处理。
- 不锈钢或有色金属采用同材质连接板过渡。
- 安装完成后测试、超限调整和复测闭环。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R47-01` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R47-01` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R47-01` | 3 | `extract_table_records` | 读取指定文档版本的表格和标准化行记录，可按业务表 Schema 过滤。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R47-01` | 4 | `check_required` | 检查事实对象中的必需字段是否存在；字段缺失返回 `failed`，规则未配置返回证据不足。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R47-01` | 5 | `evaluate_corrosion_protection` | 按具体防腐、保温、电火花、阴极保护或静电接地 profile 执行判断；不同 profile 不得混用。 | 已实现（通用规则解释器；仍需规则参数装配）；绑定：`implemented` |
| `AC-R47-01` | 6 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R47-02` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R47-02` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |

## 55. R48 穿跨越工程的管道结构、焊缝布置

### 业务审查描述

按穿越/跨越类型核对设计结构、套管或保护结构、管段组成、焊缝位置、焊缝间距和无损检测要求，并与施工记录、竣工图和现场见证一致。

### 固定标准条款

- TSG D7006-2020 D2.10（1），PDF 第 30-31 页。
- GB/T 20801.1-2025 7.4.6，PDF 第 97 页，适用于焊缝一般布置和间距。
- 穿越道路、铁路、河流、墙体、定向钻或架空跨越的具体结构要求，应按设计采用的专项标准条件绑定。

### 当前条款误绑定和规则风险

当前专业条款包把 GB/T 20801.1-2025 G.6.7.2 解释为“焊缝布置和套管内管段”。原文 G.6.7.2 实际规定直埋管道阴极保护及钢套管内绝缘支撑和密封，不包含套管内环焊缝或 100% 无损检测要求。

当前 atomicCheck 中“穿墙、道路应设套管”“套管内有环焊缝应 100% 无损检测”尚未绑定到已核实的具体固定条款，不能直接作为自动否决条件。需要根据穿越类型查找并补齐专项标准原文，或明确这是项目设计规则。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R48-01` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R48-01` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R48-01` | 3 | `extract_table_records` | 读取指定文档版本的表格和标准化行记录，可按业务表 Schema 过滤。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R48-01` | 4 | `check_required` | 检查事实对象中的必需字段是否存在；字段缺失返回 `failed`，规则未配置返回证据不足。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R48-01` | 5 | `check_sampling_requirement` | 按总体数、比例和最小数量计算抽样数，并校验实际样本。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R48-01` | 6 | `evaluate_pipeline_installation` | 按穿跨越、套管、绝缘支撑、预制、连接、补偿器或支吊架 profile 执行判断。 | 已实现（通用规则解释器；仍需规则参数装配）；绑定：`implemented` |
| `AC-R48-01` | 7 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R48-02` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R48-02` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |

## 56. R49 穿跨越工程施工

### 业务审查描述

按穿越或跨越类型、施工方法和设计要求检查专项方案、测量定位、管段制作、焊接检测、防腐保护、牵引/顶进/吊装、回填固定、竣工测量和验收记录。

### 固定标准条款

- TSG D7006-2020 D2.10（2），PDF 第 31 页。
- GB 50235-2010 第 7 章的适用安装条款。
- GB 50424-2015、GB 50460-2015 仅在其适用范围内作为油气输送管道穿越/跨越专项标准，不能自动适用于所有工业管道。

### 建议 atomicCheck

- 穿跨越类型和专项标准适用范围。
- 专项施工方案和审批。
- 测量、放线和障碍物/地质条件确认。
- 预制、焊接、热处理和 NDT 结果。
- 防腐层保护及施工后复检。
- 套管、支撑、密封、固定和排水等结构施工。
- 施工过程参数及异常处理。
- 竣工坐标、标高、结构和隐蔽验收资料。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R49-01` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R49-01` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R49-01` | 3 | `extract_table_records` | 读取指定文档版本的表格和标准化行记录，可按业务表 Schema 过滤。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R49-01` | 4 | `check_required` | 检查事实对象中的必需字段是否存在；字段缺失返回 `failed`，规则未配置返回证据不足。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R49-01` | 5 | `check_sampling_requirement` | 按总体数、比例和最小数量计算抽样数，并校验实际样本。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R49-01` | 6 | `evaluate_pipeline_installation` | 按穿跨越、套管、绝缘支撑、预制、连接、补偿器或支吊架 profile 执行判断。 | 已实现（通用规则解释器；仍需规则参数装配）；绑定：`implemented` |
| `AC-R49-01` | 7 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R49-02` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R49-02` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |

现场工序质量和隐蔽部位必须保留监检见证，AI 不替代现场确认。

## 57. R50 套管防腐绝缘

### 业务审查描述

在设计采用钢套管且需要防腐、电气隔离或阴极保护时，检查套管外防腐、管道与套管绝缘隔离、端部非导电密封和绝缘测试。

### 固定标准条款

- TSG D7006-2020 D2.10（3），PDF 第 31 页。
- GB/T 20801.1-2025 附录 G G.6.7.2（d），PDF 第 235 页：局部采用钢套管时，套管间设置绝缘支撑，套管两端采用牢固的非导电材料密封。
- 外防腐层和阴极保护要求按设计及相应专业标准。

### 建议 atomicCheck

- 是否设置钢套管及是否触发防腐绝缘要求。
- 套管外防腐体系、厚度和检验。
- 管道与套管之间绝缘支撑的位置和完整性。
- 套管端部非导电密封。
- 套管与工作管不存在金属短接。
- 绝缘/电连续性测试及异常整改。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R50-01` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R50-01` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R50-01` | 3 | `extract_table_records` | 读取指定文档版本的表格和标准化行记录，可按业务表 Schema 过滤。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R50-01` | 4 | `check_required` | 检查事实对象中的必需字段是否存在；字段缺失返回 `failed`，规则未配置返回证据不足。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R50-01` | 5 | `check_sampling_requirement` | 按总体数、比例和最小数量计算抽样数，并校验实际样本。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R50-01` | 6 | `evaluate_corrosion_protection` | 按具体防腐、保温、电火花、阴极保护或静电接地 profile 执行判断；不同 profile 不得混用。 | 已实现（通用规则解释器；仍需规则参数装配）；绑定：`implemented` |
| `AC-R50-01` | 7 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R50-02` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R50-02` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |

## 58. R51 绝缘支撑

### 业务审查描述

按设计识别“套管内绝缘支撑”或“管道与普通支撑绝缘”两类对象，分别检查位置、材质、结构、安装和绝缘测试。

### 当前业务歧义和条款问题

R51 名称和规则描述的是“设计要求管道与支撑绝缘”，而 R50 已覆盖套管内绝缘支撑。当前专业条款却只绑定 GB/T 20801.1-2025 7.7.12 的普通支吊架安装要求，该条款没有规定电气绝缘性能。

因此需要业务确认 R51 的独立对象：

- 如果是套管内绝缘支撑，应与 R50 合并或明确分工。
- 如果是地上管道与支架之间的防腐/电气绝缘，应补充设计依据和专业条款。

在边界确认前，Tool 只能检查设计一致性和证据完整性，不能建立统一电阻阈值。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R51-01` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R51-01` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R51-01` | 3 | `extract_table_records` | 读取指定文档版本的表格和标准化行记录，可按业务表 Schema 过滤。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R51-01` | 4 | `check_required` | 检查事实对象中的必需字段是否存在；字段缺失返回 `failed`，规则未配置返回证据不足。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R51-01` | 5 | `check_cross_document_match` | 按文本/机构名归一化或数值容差比较跨文档字段。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R51-01` | 6 | `check_sampling_requirement` | 按总体数、比例和最小数量计算抽样数，并校验实际样本。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R51-01` | 7 | `check_conditional_requirement` | 条件为真时检查必需事实，条件为假返回不适用。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R51-01` | 8 | `evaluate_pipeline_installation` | 按穿跨越、套管、绝缘支撑、预制、连接、补偿器或支吊架 profile 执行判断。 | 已实现（通用规则解释器；仍需规则参数装配）；绑定：`implemented` |
| `AC-R51-01` | 9 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R51-02` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R51-02` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |

## 59. R52 管道现场制作（预制）

### 业务审查描述

对施工现场完成的管段预制，按预制管段和焊口聚合检查焊接、焊后热处理及相关检查检测质量，并确保预制件标识和现场安装位置可追溯。

### 固定标准条款

- TSG D7006-2020 D2.11（1），PDF 第 31 页：抽查现场预制管道的焊接、焊后热处理质量。
- GB/T 20801.1-2025 7.3、7.4、7.6及第 8 章。

### 当前适用性和范围问题

当前配置为 `always`，但项目没有现场预制时应为 `NOT_APPLICABLE`。D2.11（1）的核心是现场预制焊接和热处理；当前 atomicCheck 扩展到加工、检查、检测、试验，应明确哪些属于本节点聚合证据，避免与 R28-R42 重复判定。

R52 不需要重新实现一套通用 `evaluate_pipeline_installation`，应复用 R28-R34 的焊接热处理结果和 R36-R40 的检测结果，按现场预制管段聚合。

### 建议 atomicCheck

- 是否存在现场预制及预制管段清单。
- 预制管段、焊口、材料、图纸版本和安装位置可追溯。
- 组对、焊接和外观检查结果。
- 需要热处理时的工艺、曲线和硬度结果。
- 需要 NDT 时的方案、人员、工艺和报告结果。
- 预制完成后的尺寸、封口保护和标识。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R52-01` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R52-01` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R52-01` | 3 | `check_sampling_requirement` | 按总体数、比例和最小数量计算抽样数，并校验实际样本。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R52-01` | 4 | `evaluate_pipeline_installation` | 按穿跨越、套管、绝缘支撑、预制、连接、补偿器或支吊架 profile 执行判断。 | 已实现（通用规则解释器；仍需规则参数装配）；绑定：`implemented` |
| `AC-R52-01` | 5 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R52-02` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R52-02` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |

## 60. R38-R52 第一轮共性问题清单

1. R38 缺少 TSG Z8001-2019 的固定条款绑定，现场身份仍需人工见证。
2. R39-R40 当前 atomicCheck 只检查文件存在，未检查方法、比例、等级、时机、字段和版本。
3. R41 缺少数字射线 NB/T 47013.11-2023 分支及 5% 分层抽样逻辑。
4. R42 必须实现 1‰、不少于 2 个、覆盖施工和检测机构、不合格加倍抽查的确定性算法。
5. R43 的 GB/T 19285-2026 条款定位错误，应由 4.2/5.1 改为 5.3.1.1/5.3.4。
6. R43-R46 的专业判断不能共用一个笼统 `evaluate_corrosion_protection`。
7. R44 必须按具体防腐和保温体系动态绑定标准。
8. R47 应由 `always` 改为设计和危险介质条件触发。
9. R48 将 G.6.7.2 错绑为焊缝布置条款，套管内焊缝及 100% NDT 规则尚缺可靠条款来源。
10. R49 必须先分类穿越/跨越类型，GB 50424/GB 50460 不得无条件套用。
11. R51 与 R50 的绝缘支撑边界不清，当前 7.7.12 条款不能支持电气绝缘判定。
12. R52 应按现场预制条件适用，并复用焊接、热处理和 NDT 节点结果。

## 61. R53 管道布管与连接方式、穿跨越

### 业务审查描述

以管线和连接接头为单位，核对轴测图、布管位置、焊缝和预制段、法兰/螺纹/卡套/粘接等连接方式、设备接口和穿墙/道路/铁路保护是否与设计一致并满足安装要求。

### 固定标准条款

- TSG D7006-2020 D2.11（2），PDF 第 31 页。
- GB/T 20801.1-2025 7.7.1-7.7.4，PDF 第 110-113 页。
- 设备连接还应绑定 7.7.6，PDF 第 113 页。

### 当前配置问题

AC-R53-01 与 AC-R53-02 是同一业务检查被句号机械拆分后的残句，不是两个完整原子项。AC-R53-02 的设备连接要求来自 7.7.6，但当前专业条款包只绑定 7.7.1-7.7.4，条款覆盖不完整。

GB/T 20801.1-2025 7.7.1.6 已明确：管道穿越墙、道路或铁路时应设套管，套管内不宜有环焊缝；存在环焊缝时应按第 8 章进行 100% 无损检测。该条款应绑定到 R53，同时也可作为 R48 的专业依据，替代 R48 当前错误的 G.6.7.2 绑定。

### 建议 atomicCheck

- 轴测图、自由管段、封闭管段和实际布管一致。
- 连接方式与设计、材料和工况一致。
- 禁止强力对口、加热管子、加偏垫或多层垫消除偏差。
- 法兰、螺纹和其他连接安装符合对应条款。
- 管道与设备在设备定位并紧固后自然连接，设备接口偏差合格。
- 穿墙/道路/铁路套管和套管内环焊缝 100% NDT 条件检查。
- 安装后不存在设计外附加载荷。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R53-01` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R53-01` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R53-01` | 3 | `extract_table_records` | 读取指定文档版本的表格和标准化行记录，可按业务表 Schema 过滤。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R53-01` | 4 | `check_required` | 检查事实对象中的必需字段是否存在；字段缺失返回 `failed`，规则未配置返回证据不足。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R53-01` | 5 | `check_sampling_requirement` | 按总体数、比例和最小数量计算抽样数，并校验实际样本。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R53-01` | 6 | `evaluate_pipeline_installation` | 按穿跨越、套管、绝缘支撑、预制、连接、补偿器或支吊架 profile 执行判断。 | 已实现（通用规则解释器；仍需规则参数装配）；绑定：`implemented` |
| `AC-R53-01` | 7 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R53-02` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R53-02` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R53-02` | 3 | `evaluate_pipeline_installation` | 按穿跨越、套管、绝缘支撑、预制、连接、补偿器或支吊架 profile 执行判断。 | 已实现（通用规则解释器；仍需规则参数装配）；绑定：`implemented` |
| `AC-R53-02` | 4 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R53-03` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R53-03` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |

## 62. R54 补偿装置

### 业务审查描述

先识别自然补偿、预拉伸/预压缩、金属波纹膨胀节或其他补偿装置，再按设计和产品文件检查型号、方向、安装长度、预变形、固定/导向支架、临时约束和安装记录。

### 固定标准条款

- TSG D7006-2020 D2.11（2），PDF 第 31 页。
- GB/T 20801.1-2025 7.7.5、7.7.11，PDF 第 113-114 页。
- 金属波纹膨胀节适用附录 P P.4，PDF 第 274 页。

当前专业条款包缺少 7.7.5，而 AC-R54-01 的预拉伸/预压缩要求主要需要该条款支持。

### 适用性

不是所有管道都设置独立补偿装置。无补偿装置且设计采用自然柔性时，装置安装原子项为 `NOT_APPLICABLE`，但仍应确认设计布置与柔性分析一致。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R54-01` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R54-01` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R54-01` | 3 | `extract_table_records` | 读取指定文档版本的表格和标准化行记录，可按业务表 Schema 过滤。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R54-01` | 4 | `check_required` | 检查事实对象中的必需字段是否存在；字段缺失返回 `failed`，规则未配置返回证据不足。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R54-01` | 5 | `check_sampling_requirement` | 按总体数、比例和最小数量计算抽样数，并校验实际样本。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R54-01` | 6 | `evaluate_pipeline_installation` | 按穿跨越、套管、绝缘支撑、预制、连接、补偿器或支吊架 profile 执行判断。 | 已实现（通用规则解释器；仍需规则参数装配）；绑定：`implemented` |
| `AC-R54-01` | 7 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R54-02` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R54-02` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |

## 63. R55 支撑件

### 业务审查描述

按支吊架编号检查形式、材质、位置、标高、固定/导向/滑动功能、弹簧设定、焊接质量、临时固定件拆除和热位移适应性。

### 固定标准条款

- TSG D7006-2020 D2.11（2），PDF 第 31 页。
- GB/T 20801.1-2025 7.3.8、7.7.12，PDF 第 92、114-115 页。

### 建议 atomicCheck

- 支吊架清单和设计位置完整。
- 形式、材质、加工尺寸和防腐符合设计。
- 固定、导向、滑动支架功能和安装方向正确。
- 弹簧支吊架安装高度、锁定和解锁时点正确。
- 支吊架焊缝由合格焊工施焊且外观合格。
- 不限制主管热位移，不存在卡涩或违规点焊。
- 试压、隔热后临时固定件按要求拆除。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R55-01` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R55-01` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R55-01` | 3 | `extract_table_records` | 读取指定文档版本的表格和标准化行记录，可按业务表 Schema 过滤。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R55-01` | 4 | `check_required` | 检查事实对象中的必需字段是否存在；字段缺失返回 `failed`，规则未配置返回证据不足。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R55-01` | 5 | `check_sampling_requirement` | 按总体数、比例和最小数量计算抽样数，并校验实际样本。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R55-01` | 6 | `evaluate_pipeline_installation` | 按穿跨越、套管、绝缘支撑、预制、连接、补偿器或支吊架 profile 执行判断。 | 已实现（通用规则解释器；仍需规则参数装配）；绑定：`implemented` |
| `AC-R55-01` | 7 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R55-02` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R55-02` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |

## 64. R56 安全阀、爆破片和紧急切断阀的安装位置、规格和型号

### 业务审查描述

按每台安全附件分别识别安全阀、爆破片或紧急切断阀分支，将产品编号、铭牌、规格型号、整定参数和安装位号与设计文件、产品文件及现场见证关联。

### 固定标准条款

- TSG D7006-2020 D2.12（1），PDF 第 31 页。
- GB/T 20801.1-2025 7.7.10，PDF 第 114 页。
- TSG 92-2026 第 5 章及附件 D/E/F。

TSG 92-2026 自 2026 年 7 月 1 日施行，已在当前日期生效。安装或检验发生在 2026 年 7 月 1 日前的历史事实仍需根据旧规和过渡条款判断。官方发布信息：[湖南省政府转载市场监管总局公告](https://www.hunan.gov.cn/zqt/zcsd/202603/t20260316_33934178.html)。

### 节点边界

R56 的主结论是安装位置、规格、型号和安装方式。制造许可、型式试验和产品质量证明属于 R12-R20 的主审查内容，可在 R56 作为前置依赖引用，避免同一证书在两个节点重复独立判定。

### 建议 atomicCheck

- 项目配置的安全附件类型和数量完整。
- 产品编号、型号、规格、压力参数与设计一致。
- 安装位号、方向和现场铭牌与设计一致。
- 安全阀垂直安装、出口至安全地点，隔离阀锁定全开。
- 爆破片方向、夹持器和安装时点符合要求。
- 紧急切断阀安装方向及控制接口符合产品和设计要求。
- 产品资料前置节点结果合格。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R56-01` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R56-01` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R56-01` | 3 | `check_required` | 检查事实对象中的必需字段是否存在；字段缺失返回 `failed`，规则未配置返回证据不足。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R56-01` | 4 | `evaluate_safety_accessory` | 按安全附件清单、产品资料、安装位置和校验/性能报告 profile 执行判断。 | 已实现（通用规则解释器；仍需规则参数装配）；绑定：`implemented` |
| `AC-R56-01` | 5 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R56-02` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R56-02` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R56-02` | 3 | `check_required` | 检查事实对象中的必需字段是否存在；字段缺失返回 `failed`，规则未配置返回证据不足。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R56-02` | 4 | `check_cross_document_match` | 按文本/机构名归一化或数值容差比较跨文档字段。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R56-02` | 5 | `check_sampling_requirement` | 按总体数、比例和最小数量计算抽样数，并校验实际样本。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R56-02` | 6 | `evaluate_safety_accessory` | 按安全附件清单、产品资料、安装位置和校验/性能报告 profile 执行判断。 | 已实现（通用规则解释器；仍需规则参数装配）；绑定：`implemented` |
| `AC-R56-02` | 7 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R56-03` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R56-03` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |

## 65. R57 安全阀校验报告

### 业务审查描述

按已安装安全阀的产品编号匹配校验报告，检查校验机构、校验日期、整定/开启压力、回座压力、密封试验压力、结果和铅封等是否满足设计和 TSG 92。

### 固定标准条款

- TSG D7006-2020 D2.12（2），PDF 第 31 页。
- TSG 92-2026 附件 D 及附录 db。

当前 AC-R57-01 只写开启压力和密封压力，遗漏 D2.12（2）明确要求检查的回座压力。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R57-01` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R57-01` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R57-01` | 3 | `check_required` | 检查事实对象中的必需字段是否存在；字段缺失返回 `failed`，规则未配置返回证据不足。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R57-01` | 4 | `check_numeric_range` | 对一个或多个数值执行开闭区间上下限检查。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R57-01` | 5 | `evaluate_safety_accessory` | 按安全附件清单、产品资料、安装位置和校验/性能报告 profile 执行判断。 | 已实现（通用规则解释器；仍需规则参数装配）；绑定：`implemented` |
| `AC-R57-01` | 6 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R57-02` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R57-02` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |

报告没有与现场阀门产品编号匹配时，即使参数表面合格也不能判符合。

## 66. R58 紧急切断阀性能测试报告

### 业务审查描述

将每台紧急切断阀测试报告与现场产品编号和控制系统关联，按阀门类型检查动作、联锁、切断时间、关闭性能、密封和失效安全等适用项目。

### 固定标准条款

- TSG D7006-2020 D2.12（3），PDF 第 31 页。
- TSG 92-2026 附件 F。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R58-01` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R58-01` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R58-01` | 3 | `check_required` | 检查事实对象中的必需字段是否存在；字段缺失返回 `failed`，规则未配置返回证据不足。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R58-01` | 4 | `evaluate_safety_accessory` | 按安全附件清单、产品资料、安装位置和校验/性能报告 profile 执行判断。 | 已实现（通用规则解释器；仍需规则参数装配）；绑定：`implemented` |
| `AC-R58-01` | 5 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R58-02` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R58-02` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |

测试项目必须由阀门类型和设计联锁要求驱动，不能使用一个固定项目清单覆盖所有紧急切断阀。

## 67. R59 耐压试验方案

### 业务审查描述

先确定每个管道系统采用液压、气压、液压-气压、初始运行压力试验、免除压力试验或无需试验的路线，再检查对应试验方案的系统边界、准备条件、参数、程序、安全措施、审批和合格标准。

### 固定标准条款

- TSG D7006-2020 D2.13.1（1），PDF 第 31 页。
- GB/T 20801.1-2025 8.6.1.1-8.6.1.7，PDF 第 125-129 页。

### 当前适用性问题

R59 当前配置为 `always`，但排放且连通大气的特定管线可无需压力试验；GC3 可在同意后采用初始运行压力试验；液压和气压均不实际时可进入免除路线。R59 应始终执行“试验路线判定”，但传统耐压试验方案原子项应按路线条件适用。

### 建议 atomicCheck

- 管道系统边界和试验路线。
- 替代或免除路线的触发条件和书面同意。
- 试验前检查、热处理、NDT 完成状态。
- 接头外露、盲板隔离、临时支撑、膨胀节处理。
- 介质、压力、温度、升压程序、保压和检查程序。
- 气压试验风险评估、泄压装置和安全区域。
- 压力表、温度计和其他仪器要求。
- 方案审批和版本。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R59-01` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R59-01` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R59-01` | 3 | `extract_table_records` | 读取指定文档版本的表格和标准化行记录，可按业务表 Schema 过滤。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R59-01` | 4 | `recognize_signatures_and_seals` | 读取签字角色、签章、印章文字、位置和置信度；不判断签字是否齐全。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R59-01` | 5 | `check_signature_completeness` | 核验识别出的签字角色是否覆盖规则要求角色。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R59-01` | 6 | `check_numeric_range` | 对一个或多个数值执行开闭区间上下限检查。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R59-01` | 7 | `evaluate_pressure_test` | 核验耐压试验方案必需内容和审批签字角色。 | 已实现（专用业务规则）；绑定：`implemented` |
| `AC-R59-01` | 8 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R59-02` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R59-02` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |

## 68. R60 试验用压力表、试验介质、介质温度、环境温度

### 业务审查描述

现场核对实际压力表和试验介质，检查仪表数量、精度、量程、校验有效期、安装位置，以及介质种类、洁净度、氯离子、温度和环境条件是否符合所选试验方法。

### 固定标准条款

- TSG D7006-2020 D2.13.1（2），PDF 第 31 页。
- GB/T 20801.1-2025 8.6.1.2.5、8.6.1.3-8.6.1.4，PDF 第 127-129 页。

GB/T 20801.1-2025 明确：压力表不少于 2 块，精度不低于 1.6 级，满刻度为最大试验压力 1.5-2.0 倍，至少一块位于液位最高点。D2.13.1 还要求检查表盘直径，但当前专业条款包未绑定表盘直径的具体限值；在补到明确标准前不能自行设置直径阈值。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R60-01` | 1 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R60-01` | 2 | `extract_table_records` | 读取指定文档版本的表格和标准化行记录，可按业务表 Schema 过滤。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R60-01` | 3 | `check_pressure_gauge_requirements` | 核验耐压试验压力表数量、检定日期、精度、量程、表盘直径记录、最高点安装及介质温度。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R60-01` | 4 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R60-02` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R60-02` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |

## 69. R61 耐压试验压力、保压时间及结果

### 业务审查描述

按管道系统和试验路线计算应达到的试验压力，核查实际升压、保压、降压检查和最终结果，并处理材料应力、组成件额定值及设备共同试验的限制。

### 固定标准条款

- TSG D7006-2020 D2.13.1（3），PDF 第 31 页。
- GB/T 20801.1-2025 8.6.1.1.3-8.6.1.1.4、8.6.1.3-8.6.1.6，PDF 第 125-129 页。

### 当前规则错误

AC-R61-01 写成“液压试验压力为设计压力 1.5 倍，不考虑温度系数”，与 GB/T 20801.1-2025 8.6.1.3 冲突。设计温度高于试验温度时，最低试验压力为：

```text
pT = 1.5 × p × S1 / S2
```

并受屈服强度、组成件额定值和设备共同试验等条件限制。

气压试验也不是只判断“不低于 1.1 倍设计压力”：其压力还不得超过 1.33 倍设计压力与屈服强度极限 90% 对应压力两者中的较小值，并应按 50%、每级 10%、每级稳压 3 分钟的步骤执行。

### 建议 atomicCheck

- 液压/气压/组合/初始运行试验类型。
- 设计压力、试验温度和设计温度许用应力比。
- 最低压力和上限限制计算。
- 压力试验保压不少于 10 分钟。
- 保压后降至设计压力并检查全部接头和连接处。
- 气压试验分级升压和每级稳压。
- 无泄漏、无异常、无永久变形等结果。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R61-01` | 1 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R61-01` | 2 | `check_pressure_test_parameters` | 核验液压/气压试验压力上下限、温度许用应力比、保压、分级升压和结果。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R61-01` | 3 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R61-02` | 1 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R61-02` | 2 | `extract_table_records` | 读取指定文档版本的表格和标准化行记录，可按业务表 Schema 过滤。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R61-02` | 3 | `check_pressure_test_parameters` | 核验液压/气压试验压力上下限、温度许用应力比、保压、分级升压和结果。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R61-02` | 4 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R61-03` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R61-03` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |

## 70. R62 耐压试验记录（报告）

### 业务审查描述

检查每个管道系统的试验记录能否完整还原试验边界、日期、介质、压力、温度、仪表、保压、检查过程和结果，并与方案及现场见证一致。

### 固定标准条款

- TSG D7006-2020 D2.13.1（4），PDF 第 31 页。
- GB/T 20801.1-2025 8.6.1.1.8、8.7，PDF 第 126、130 页。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R62-01` | 1 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R62-01` | 2 | `check_pressure_test_report_consistency` | 比较耐压试验报告、方案与现场见证记录的关键参数和结果。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R62-01` | 3 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |
| `AC-R62-02` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`pilot_implemented` |
| `AC-R62-02` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`pilot_implemented` |

方案、现场过程和报告参数冲突时应返回 `NON_COMPLIANT` 或 `INSUFFICIENT_EVIDENCE`，不能以最终“合格”字样覆盖冲突。

## 71. R63 管道系统的柔性(应力)分析

### 业务审查描述

当申请免除液压和气压试验时，确认免除条件成立，并检查设计单位出具的柔性分析覆盖整个免压管道系统，载荷、边界、支撑、位移和结论满足设计要求。

### 固定标准条款

- TSG D7006-2020 D2.13.2（1），PDF 第 31 页。
- GB/T 20801.1-2025 6.7.5.5、8.6.1.1.2（c）、8.6.1.7，PDF 第 76、125、128 页。

### 与 R06 的分工

R06 审查常规设计计算书的存在、覆盖、参数和审批；R63 只在压力试验免除路线触发，确认柔性分析是免压的法定前置条件之一，并与免压系统边界一致。计算模型的实质正确性不能由 LLM 直接判断，可由专业计算 Tool 或人工设计审查确认。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R63-01` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R63-01` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R63-01` | 3 | `check_required` | 检查事实对象中的必需字段是否存在；字段缺失返回 `failed`，规则未配置返回证据不足。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R63-01` | 4 | `evaluate_stress_analysis` | 按免除耐压路线下的系统边界、模型输入、审批和分析结论规则执行判断。 | 已实现（通用规则解释器；仍需规则参数装配）；绑定：`implemented` |
| `AC-R63-01` | 5 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R63-02` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R63-02` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |

## 72. R64 现场检查替代性试验的过程

### 业务审查描述

在压力试验免除路线下，现场监督敏感性泄漏试验，按实际采用的气泡、卤素、氦质谱、氨等方法检查介质、压力、灵敏度、升压程序、环境和结果。

### 固定标准条款

- TSG D7006-2020 D2.13.2（2），PDF 第 31 页。
- GB/T 20801.1-2025 8.6.1.7、8.6.2.2，PDF 第 128-130 页。
- NB/T 47013.8-2025，按实际泄漏检测方法适用。

NB/T 47013.8-2025 已于 2026 年 6 月 18 日实施并代替 2012 版。当前工程规则已引用 2025 版，但本地标准目录只有已被替代的 2012 版 PDF，导致条款包无法完整支撑当前日期后的检测。必须补入 2025 版原文和条款定位。官方状态：[全国标准信息公共服务平台](https://std.samr.gov.cn/hb/search/stdHBDetailed?id=48DE71C8B275D227E06397BE0A0A27D3)。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R64-01` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R64-01` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R64-01` | 3 | `check_required` | 检查事实对象中的必需字段是否存在；字段缺失返回 `failed`，规则未配置返回证据不足。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R64-01` | 4 | `evaluate_leak_test` | 按具体泄漏试验方法、压力、灵敏度、环境、报告和验收 profile 执行判断。 | 已实现（通用规则解释器；仍需规则参数装配）；绑定：`implemented` |
| `AC-R64-01` | 5 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R64-02` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R64-02` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |

## 73. R65 无损检测报告和底片

### 业务审查描述

在压力试验免除路线下，先确认施工单位已对所有规定焊接接头实施 100% 体积和表面无损检测，再按 TSG D7006 对不少于焊接接头总数 50% 的报告和底片实施监检审查。

### 固定标准条款

- TSG D7006-2020 D2.13.2（3），PDF 第 31 页：监检审查不少于焊接接头总数 50% 的无损检测报告和底片。
- GB/T 20801.1-2025 8.6.1.7（a），PDF 第 128-129 页：环向、纵向、螺旋焊缝 100% RT/UT；其他焊接接头 100% MT/PT。
- 实际检测方法对应的 NB/T 47013 分册。

### 必须区分的比例

- 施工/检测要求：规定焊接接头 100% 体积检测和 100% 表面检测。
- 监检审查要求：不少于焊接接头总数 50% 的报告和底片。

当前配置虽分别出现 100% 和 50%，但必须用两个独立分母和结果字段，避免把“抽查 50%”误解释为只需做 50% NDT。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R65-01` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R65-01` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R65-01` | 3 | `check_required` | 检查事实对象中的必需字段是否存在；字段缺失返回 `failed`，规则未配置返回证据不足。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R65-01` | 4 | `evaluate_rt_film` | 核验底片/数字影像与焊口、报告、影像质量和抽样要求的关联。 | 已实现（专用业务规则）；绑定：`implemented` |
| `AC-R65-01` | 5 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R65-02` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R65-02` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R65-02` | 3 | `check_required` | 检查事实对象中的必需字段是否存在；字段缺失返回 `failed`，规则未配置返回证据不足。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R65-02` | 4 | `check_sampling_requirement` | 按总体数、比例和最小数量计算抽样数，并校验实际样本。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R65-02` | 5 | `evaluate_rt_film` | 核验底片/数字影像与焊口、报告、影像质量和抽样要求的关联。 | 已实现（专用业务规则）；绑定：`implemented` |
| `AC-R65-02` | 6 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R65-03` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R65-03` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |

## 74. R66 试验用压力表、试验介质、介质温度、环境温度、试验压力

### 业务审查描述

先判断是否需要泄漏试验及采用敏感性泄漏试验还是气密性试验，再按方法检查仪表、介质、温度、试验压力和灵敏度。

### 固定标准条款

- TSG D7006-2020 D2.14（1），PDF 第 32 页。
- GB/T 20801.1-2025 8.6.2.1-8.6.2.3，PDF 第 129-130 页。
- 敏感性泄漏试验绑定 NB/T 47013.8-2025。

### 当前适用性和数值错误

当前配置为 `always`，但 GB/T 20801.1-2025 8.6.2.1 主要对输送泄漏危害性介质的管道要求压力试验后进行泄漏试验；经气压试验合格且试验后未拆卸的系统可不再进行泄漏试验。应改为条件适用。

AC-R66-01 把所有泄漏试验压力写成设计压力，只适用于气密性试验。敏感性泄漏试验的最低压力和灵敏度随方法而变，例如气泡直接加压通常由建设/使用或设计要求确定且不小于 25% 设计压力；其他方法还有不同压力和灵敏度要求。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R66-01` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R66-01` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R66-01` | 3 | `extract_table_records` | 读取指定文档版本的表格和标准化行记录，可按业务表 Schema 过滤。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R66-01` | 4 | `check_date_covers` | 判断证照或报告有效期是否覆盖闭区间业务周期。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R66-01` | 5 | `check_numeric_range` | 对一个或多个数值执行开闭区间上下限检查。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R66-01` | 6 | `evaluate_leak_test` | 按具体泄漏试验方法、压力、灵敏度、环境、报告和验收 profile 执行判断。 | 已实现（通用规则解释器；仍需规则参数装配）；绑定：`implemented` |
| `AC-R66-01` | 7 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R66-02` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R66-02` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |

## 75. R67 泄漏试验方法和试验报告

### 业务审查描述

检查泄漏试验方法是否与设计介质危害、所需灵敏度和批准方案一致，报告是否完整记录系统边界、方法、介质、压力、温度、灵敏度、保压/检测过程、泄漏位置、处理和结论。

### 固定标准条款

- TSG D7006-2020 D2.14（2），PDF 第 32 页。
- GB/T 20801.1-2025 8.6.2.2-8.6.2.3、8.7，PDF 第 129-130 页。
- NB/T 47013.8-2025。

### 当前文本问题

AC-R67-01 末尾写成“耐压试验结果”，应改为“泄漏试验结果”。敏感性泄漏和气密性试验的记录字段、压力和验收准则不同，不能共用一个固定 outputSchema。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R67-01` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R67-01` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R67-01` | 3 | `check_required` | 检查事实对象中的必需字段是否存在；字段缺失返回 `failed`，规则未配置返回证据不足。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R67-01` | 4 | `check_numeric_range` | 对一个或多个数值执行开闭区间上下限检查。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R67-01` | 5 | `evaluate_leak_test` | 按具体泄漏试验方法、压力、灵敏度、环境、报告和验收 profile 执行判断。 | 已实现（通用规则解释器；仍需规则参数装配）；绑定：`implemented` |
| `AC-R67-01` | 6 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R67-02` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R67-02` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |

## 76. R53-R67 第一轮共性问题清单

1. R53 atomicCheck 是残句式机械拆分；应补绑 GB/T 20801.1-2025 7.7.6。
2. R48 的套管内焊缝规则应绑定 7.7.1.6，不能继续使用 G.6.7.2。
3. R54 缺少 7.7.5 预拉伸/预压缩条款和条件适用。
4. R56-R58 需加入 TSG 92-2026 自 2026-07-01 起的版本切换和历史事实规则。
5. R56 不应重复独立判定 R12-R20 的制造资质和产品证书。
6. R57 缺少回座压力原子项和现场阀门序列号匹配。
7. R59 必须先判定压力试验路线，不能把传统耐压试验方案设为无条件适用。
8. R60 的表盘直径尚缺具体专业条款限值。
9. R61 “不考虑温度系数的 1.5 倍”必须删除，气压试验也需上下限和分级升压算法。
10. R63-R65 是压力试验免除的三个并列必要条件，应共享同一免压系统边界。
11. R64、R66、R67 缺少已于 2026-06-18 生效的 NB/T 47013.8-2025 原文。
12. R65 必须严格区分施工 100% NDT 与监检 50% 抽查。
13. R66、R67 应条件适用；气密性和敏感性泄漏试验必须使用不同参数和输出结构。

## 77. R68 吹扫、清洗

### 业务审查描述

按管道系统和介质确定人工清理、水冲洗、空气吹扫、蒸汽吹扫、脱脂、化学清洗或其他方法，核查方案、实施时点、隔离保护、介质、压力、顺序、安全措施、验收标准、记录和防再污染措施。

### 固定标准条款

- TSG D7006-2020 D2.15，PDF 第 32 页。
- GB/T 20801.1-2025 7.9.1-7.9.5，PDF 第 116-117 页。
- GB 50235-2010 第 9 章的适用方法条款。

### 时点和方法边界

TSG D7006 重点抽查耐压试验后的吹扫或清洗记录，但 GB/T 20801.1 还规定强氧化性介质管道应在装配后、安装前分段或单件脱脂。系统不能把所有清洗都固定到耐压试验后，应按清理方法和介质确定时点。

### 建议 atomicCheck

- 吹洗方法与管径、介质和污染风险相适用。
- 方案在安装前提出并与 P&ID/施工图预留位置一致。
- 不参与吹洗的设备、仪表、安全阀和重要阀门得到隔离或保护。
- 介质、压力、温度、顺序和排放地点符合要求。
- 蒸汽吹扫暖管、排水和循环程序完整。
- 化学清洗配方、安全防护和废液处理合格。
- 按设计/方法标准的靶板、清洁度或其他合格标准判定。
- 完成后封口或充氮，防止再污染。

### Tool 调用链

> 以下为当前工程正式绑定的实际运行时 Tool。按 atomicCheck 和调用顺序展开；同一 Tool 在不同 atomicCheck 中重复出现表示实际会分别执行，不做节点级去重。

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R68-01` | 1 | `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R68-01` | 2 | `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R68-01` | 3 | `extract_table_records` | 读取指定文档版本的表格和标准化行记录，可按业务表 Schema 过滤。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R68-01` | 4 | `check_required` | 检查事实对象中的必需字段是否存在；字段缺失返回 `failed`，规则未配置返回证据不足。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R68-01` | 5 | `check_sampling_requirement` | 按总体数、比例和最小数量计算抽样数，并校验实际样本。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R68-01` | 6 | `check_numeric_range` | 对一个或多个数值执行开闭区间上下限检查。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R68-01` | 7 | `evaluate_blowing_cleaning` | 按冻结的吹扫、清洗方法、参数、顺序、安全和验收规则执行判断。 | 已实现（通用规则解释器；仍需规则参数装配）；绑定：`implemented` |
| `AC-R68-01` | 8 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R68-02` | 1 | `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R68-02` | 2 | `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | 已实现（专用确定性判断）；绑定：`implemented` |

## 78. R69 施工单位质量保证体系实施状况的评价

### 业务审查描述

结合本工程 R01-R68 的监检结果，按《受检单位质量保证体系实施状况评价工艺》对施工单位进行项目级评价并出具评价报告。本节点的评价主体是监检人员；AI 不得自动生成、修改或替代人工评价结论。

### 固定标准条款

- TSG D7006—2020 第 2.2.4 条，PDF 第 9 页：质量保证体系实施状况评价应符合附件 G。
- TSG D7006—2020 附件 G G1-G5，PDF 第 38-39 页：评价基本要求、资源条件变化、质量保证体系保持和改进、许可制度执行以及发现问题与处理。
- `files/checklist.docx` 序号 69：结合监检项目按评价工艺对施工单位进行评价，并出具评价报告。

### requiredFacts 与结论边界

- `qualitySystemEvaluation.report`：监检人员签发的评价报告。
- `qualitySystemEvaluation.result`、`evaluator`、`evaluationDate`、`coveredProjectId`：报告中的评价结果、评价人员、评价日期和覆盖工程。
- `reviewRun.nodeResults`：R01-R68 的冻结节点结果及其证据链。
- 报告不存在、字段不全、未覆盖当前工程、未由监检人员确认或与项目证据冲突时，返回 `INSUFFICIENT_EVIDENCE` 或 `REQUIRES_HUMAN_REVIEW`。
- 即使报告完整，Tool 也只确认“人工评价报告证据完整”，最终 R69 业务结论只能采用监检人员签发的评价结果；`automatedDecisionAllowed=false`。

### Tool 调用链

| atomicCheck | 顺序 | Tool | 中文 description | 实现状态 |
| --- | ---: | --- | --- | --- |
| `AC-R69-01` | 1 | `locate_evidence_fragment` | 定位人工评价报告和 R01-R68 项目证据，保留文件版本、页码、原文和置信度。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R69-01` | 2 | `extract_document_fields` | 抽取评价结果、评价人员、评价日期、覆盖工程和签发信息。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R69-01` | 3 | `check_document_set_completeness` | 仅检查人工评价报告及必需字段是否齐全，不计算评价结果。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R69-01` | 4 | `validate_evidence_grounding` | 校验评价报告字段和项目证据是否可追溯、无冲突。 | 已实现（专用确定性判断）；绑定：`implemented` |
| `AC-R69-02` | 1 | `locate_evidence_fragment` | 定位结论所引用的报告和项目证据。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R69-02` | 2 | `extract_document_fields` | 读取人工报告字段并保留来源定位。 | 已实现（证据读取）；绑定：`implemented` |
| `AC-R69-02` | 3 | `check_document_set_completeness` | 检查证据及人工报告字段完整性。 | 已实现（通用确定性判断）；绑定：`implemented` |
| `AC-R69-02` | 4 | `validate_evidence_grounding` | 执行最终证据追溯门禁。 | 已实现（专用确定性判断）；绑定：`implemented` |

## 79. R68-R69 第一轮共性问题清单

1. R68 的时点应按吹洗/脱脂方法区分，不能全部固定为耐压试验后。
2. R69 必须保持人工评价边界，Tool 只能汇总 R01-R68 结果并校验评价报告，不能根据前序节点自动生成评价等级或结论。

## 80. 二次检查结果

### 79.1 结构和数据库审计

第二次检查执行了项目内置校验和独立数据库审计：

- Business Pack 校验：PASS，三个业务包均无结构错误或警告。
- 标准条款制品审计：PASS。
- 工程监检包覆盖：R01-R69 共 69 个规则、69 个主条款绑定、69 个条款包。
- 专业条款：112 条；专业定位：202 个；atomicCheck：194 个。
- 本地数据库发布审计：PASS。
- 数据库发布包含 29 个标准文档版本、167 个去重条款引用、236 个去重定位、69 个条款包、181 个条款包项目。
- 演示数据中的 3 个工程项目均绑定全部 69 个条款包，共 207 条项目节点绑定。

上述 PASS 只能证明 YAML、条款包和数据库快照结构完整，不能证明业务条款语义正确。本次人工原文复核发现的误绑定、错误公式和适用性问题仍必须修正。

### 79.2 Tool 实现状态复核

全量 R01-R69 共 194 条 atomicCheck → Tool 绑定，其中：

- `implemented`：119 条。
- `pilot_implemented`：75 条。

75 条试点绑定集中在 R01-R03、R06-R07、R09、R12-R19、R24、R60-R62。R69 的 2 条绑定状态为 `implemented`，但该状态只说明证据 Tool 可执行，不表示允许自动生成评价结论。二次代码检查发现：

1. R24 的焊工资格解析只支持极少数项目代号形态，当前仅内置有限厚度/直径代码和简化位置覆盖，未覆盖填充金属、附加工艺因素及 TSG Z6002-2026。它只能作为受限试点，不能用于全量生产判定。
2. R60 的压力表 Tool 已检查数量、精度、量程比和校验日期，但尚未核验最高点安装、表盘直径来源和不同试验方法的完整介质条件。
3. R61 的试点 Tool 直接采用液压 `1.5 × 设计压力`、气压 `1.1 × 设计压力`。它没有液压试验的 `S1/S2` 温度许用应力比，也没有气压试验的 1.33 倍上限、90% 屈服限制和分级升压规则，存在错误放行或危险超压判断风险。
4. R62 的报告一致性 Tool 只比对有限字段，不能替代试验边界、现场过程和全部接头检查证据。

因此，**R61 的 `check_pressure_test_parameters` 在修复前不得作为生产放行依据**；R24、R60、R62 也必须保持试点标识并限制使用场景。

### 79.3 跨节点依赖复核

| 上游事实/结果 | 下游节点 | 固定关系 |
| --- | --- | --- |
| R32 单焊口是否需要热处理 | R33、R34 | R33/R34 必须继承相同焊口级适用性，不能各自重新判断 |
| R35 无损检测机构和现场体系 | R36-R42 | 每家机构分别执行人员、方案、工艺、记录、底片和现场抽查判断 |
| R36 检测方案要求 | R39-R42 | 方法、比例、级别、时机和标准版本应共用同一计划事实 |
| R43 防腐/保温材料批次 | R44-R46 | 施工、漏点和阴极保护记录必须追溯到同一批材料/设备 |
| R48/R49 穿跨越类型 | R50、R51 | 套管、防腐和绝缘支撑只在相应结构存在时触发 |
| R56 安全附件清单 | R57、R58 | 每台安全阀/紧急切断阀报告必须匹配现场产品编号 |
| R59 压力试验路线 | R60-R67 | 常规耐压、免压替代和泄漏试验必须共享同一管道系统边界 |
| R63-R65 | 压力试验免除结论 | 柔性分析、敏感性泄漏和 100% NDT 三项缺一不可 |
| R12 产品监管分类 | R13-R20 | 先确定许可/监检/型式试验/境外/新材料分支，再调用各节点 |
| R16 产品和批号身份 | R17-R21 | 到货验收、复验、标志移植必须使用同一材料追溯链 |
| R22 材料代用 | R04、R07、R25、竣工图 | 设计批准、图纸版本、WPS/PQR 和竣工资料必须同步更新 |

### 79.4 现行标准库复核

- TSG Z6002-2026 已发布，将于 2026 年 8 月 1 日施行；当前标准目录只有 2010 版。
- TSG 92-2026 已于 2026 年 7 月 1 日施行，本地已存在正式 PDF。
- GB/T 19285-2026、GB/T 33378-2025 均已于 2026 年 5 月 1 日施行，本地已有正式 PDF。
- NB/T 47013.8-2025 已于 2026 年 6 月 18 日替代 2012 版。当前本地只有旧版 PDF和一份“更新变化记录”Markdown；该 Markdown 明确声明不等同于标准正文，不能用于冻结专业条款或生产判定。
- GB/T 13927-2022、GB/T 26480-2011 当前均现行；GB/T 26480-2011 处于修订计划阶段，但新版本尚未发布。

### 79.5 按安全风险排序的修订队列

#### P0：投入生产前必须修正

1. 停止用当前 R61 试点 Tool 作放行判定，补齐液压温度应力比、气压上限和分级升压算法。
2. 补入 NB/T 47013.8-2025 正式原文，重做 R64、R66、R67 条款和参数绑定。
3. 为 R24 增加 TSG Z6002-2026 及 2026-08-01 版本切换，重写示例式 atomicCheck。
4. 修正 R30 施工目视检查比例与监检抽查比例的双分母模型。
5. 修正 R34 按材料表格判断硬度，删除统一 200HB/225HB 阈值。
6. 修正 R43 的 GB/T 19285-2026 条款定位。
7. 修正 R48/R53 的穿越套管及环焊缝条款绑定。
8. 修正 R15 的 TSG 31-2025 条款误绑定；R13 已在本轮关闭。
9. 补绑 R23 的 GB/T 20801.1-2025 7.2.4 和阀门试验数量算法。

#### P1：正式联调前完成

1. 重构 R32-R34 的热处理适用性和共享事实。
2. 实现 R41、R42 的分层抽样及加倍抽查。
3. 按防腐、阴极保护、连接、穿跨越和材料产品类型拆分专业 Tool。
4. 将 R47、R52、R59、R66、R67、R21、R22 等节点改为正确的条件适用；R18 已完成条件适用修正。
5. 明确 R51 绝缘支撑、R56 产品资料、R16 电子质量证明和 R23 标准优先级的业务口径。

### 79.6 二次检查结论

R11-R69 已完成逐节点第一轮分析和第二轮交叉审计，分析文档已覆盖业务描述、固定条款、适用性、原子项、requiredFacts、Tool 调用链、结论边界和疑问。现有工程在结构和数据库一致性方面通过审计，但专业语义仍存在多项 P0 风险，不能将“配置已发布”解释为“业务规则已经可以安全自动放行”。R69 已按附件正式补录，并固定为监检人员人工评价节点。

## 81. `业务节点分析.md` 与 `tools规划.md` 的 Tool 对应审计

### 80.1 审计结论

两份文档已经统一为同一套运行时 Tool 口径：

- 各节点原有的 475 个概念步骤名称已经从“Tool 调用链”移除，调用链现在只包含正式绑定的运行时 Tool。
- `tools规划.md` 绑定 R01-R69 共 194 个 atomicCheck，使用 92 个不同的运行时 Tool；92 个名称都已进入 `runtime_tool_catalog()`，也都包含在 `ALLOWED_AGENT_TOOLS` 中。
- 当前绑定共包含 721 次 Tool 能力绑定；确定性节点按计划执行，R19 由 Agent 按证据需要动态调用并在提交时强制校验。
- 当前运行时 Tool 分为证据读取、确定性基础判断、通用业务判断和领域聚合判断四层。
- “已注册”“计划可编译”只证明 Tool 名称存在。它不证明参数已经从业务事实正确组装，也不证明专业规则已达到生产放行质量。
- 当前 `build_tool_arguments()` 已为 R01-R04、R06-R07、R09、R13-R18 实现专用参数装配；R19 使用独立文档上下文 Builder、动态证据 Tool 和语义提交校验器。其余多数判断 Tool 的顶层参数仍依赖 `atomicToolArguments`/Tool Argument Assembler。没有参数装配器时应返回 `evidence_insufficient`，不得让 LLM 补猜参数。
- 尚未专门落地的领域 Tool 仍可能走通用 `evaluate_rule_profile()`：必须同时提供 `facts`、`requiredFields` 和 `ruleChecks` 才会执行。R16-R18 已从该通用路径迁移到专用确定性 Tool。

因此，Agent 调用时必须采用以下优先级：

```text
atomic_check_tool_bindings.yaml（运行时唯一绑定源）
→ 默认节点：FactSnapshot / EvidenceRef → Tool Argument Assembler → 确定性 Tool Result
→ R19：Document Context / EvidenceRef → LLM atomicCheck Judgment → Schema/EvidenceRef 校验
→ 固定节点聚合器
```

节点章节中的 Tool 调用链可以作为 Agent 编排的绑定说明，但运行时仍必须按当前 ReviewRun 冻结的 `atomic_check_tool_bindings` 加载，不能仅解析 Markdown，也不能由 Agent增删 Tool 或改变顺序。

### 80.2 业务节点与主要可执行判断 Tool

下表只列节点的主要专业判断 Tool。OCR、字段/表格抽取、签章识别、通用校验和证据门禁仍按每个 atomicCheck 的正式绑定执行。

| 业务节点 | 主要可执行判断 Tool | 实现性质 |
| --- | --- | --- |
| R01 | `check_design_license_scope` | 专用确定性实现 |
| R02 | `check_installation_license_scope`、`check_date_covers` | 专用确定性实现 |
| R03 | `decode_ndt_approval_item_codes`、`evaluate_ndt_agencies` | 专用确定性实现 |
| R04 | `check_document_set_completeness`、`evaluate_design_document_approval` | 专用确定性与专用业务实现 |
| R06 | `evaluate_calculation_document_consistency`、`evaluate_design_document_approval` | 专用确定性实现 |
| R07 | `evaluate_design_change_approval`、`verify_design_license_seals` | 专用确定性实现 |
| R22 | `evaluate_design_approval_level` | 专用业务实现 |
| R05 | `check_document_set_completeness` | 通用确定性实现 |
| R08 | `check_standard_version_active` | 通用确定性实现 |
| R09 | `evaluate_design_special_requirements` | 专用确定性实现 |
| R10 | `evaluate_alternative_standard` | 规则配置驱动 |
| R11 | `evaluate_construction_plan` | 规则配置驱动 |
| R24 | `decode_welder_qualification`、`check_welder_work_coverage` | 专用确定性实现 |
| R25 | `check_wps_pqr_coverage` | 专用业务实现 |
| R26 | `evaluate_welding_consumable` | 规则配置驱动 |
| R27 | `evaluate_welding_consumable_control` | 规则配置驱动 |
| R28 | `evaluate_pipe_fit_up` | 规则配置驱动 |
| R29 | `evaluate_welding_process` | 规则配置驱动 |
| R30 | `evaluate_weld_appearance` | 规则配置驱动 |
| R31 | `evaluate_weld_repair` | 规则配置驱动 |
| R32、R34 | `evaluate_heat_treatment` | 规则配置驱动 |
| R33 | `evaluate_heat_treatment_instruments` | 规则配置驱动 |
| R35 | `evaluate_ndt_quality_system` | 规则配置驱动 |
| R36、R39、R40 | `evaluate_ndt_process` | 规则配置驱动 |
| R37 | `evaluate_ndt_nonconformance` | 规则配置驱动 |
| R38 | `check_ndt_personnel_coverage` | 专用业务实现 |
| R41、R42、R65 | `evaluate_rt_film` | 专用业务实现 |
| R43-R47、R50 | `evaluate_corrosion_protection` | 规则配置驱动 |
| R48、R49、R51-R55 | `evaluate_pipeline_installation` | 规则配置驱动 |
| R56-R58 | `evaluate_safety_accessory` | 规则配置驱动 |
| R59 | `evaluate_pressure_test` | 专用业务实现 |
| R60 | `check_pressure_gauge_requirements` | 专用确定性实现 |
| R61 | `check_pressure_test_parameters` | 专用确定性实现，受第 79.5 节 P0 限制 |
| R62 | `check_pressure_test_report_consistency` | 专用确定性实现 |
| R63 | `evaluate_stress_analysis` | 规则配置驱动 |
| R64、R66、R67 | `evaluate_leak_test` | 规则配置驱动 |
| R68 | `evaluate_blowing_cleaning` | 规则配置驱动 |
| R12 | `evaluate_component_manufacturer_scope` | 规则配置驱动 |
| R13 | `classify_r13_component_requirements`、`evaluate_r13_supervision_certificate_completeness`、`evaluate_r13_type_test_coverage` | R13 专用法规目录分类、逐批/逐台监检证书齐全性和型式试验覆盖矩阵 |
| R14 | `classify_r14_component_applicability`、`evaluate_r14_component_design_match`、`resolve_r14_required_inspection_items`、`evaluate_r14_special_report_coverage`、`evaluate_r14_pressure_compatibility` | R14 专用适用性、等级材质、必检项目、专项报告和压力等级判断 |
| R16-R20 | `evaluate_material_component` | 规则配置驱动 |
| R15 | `classify_r15_foreign_manufacturing_applicability`、`classify_r15_regulatory_requirements`、`evaluate_r15_manufacturing_license_coverage`、`evaluate_r15_type_test_coverage`、`evaluate_r15_manufacturing_inspection_route` | R15 专用境外制造适用性、监管目录、许可/型式试验覆盖和制造监检路径实现 |
| R21 | `check_traceability` | 通用确定性实现 |
| R23 | `evaluate_valve_test` | 专用业务实现 |
| R69 | `check_document_set_completeness`、`validate_evidence_grounding` | 证据完整性实现；业务评价结论必须人工确认 |

### 80.3 Agent Tool 上下文格式

Agent 上下文应使用标准 Function Tool 结构。当前代码中的 `capability` 对应 `description`，`inputSchema` 应转换为真正的 JSON Schema `parameters`，不能把字符串形式的 `"string?"` 直接提交给模型。

以下是 `extract_document_fields` 的完整上下文示例：

```json
{
  "name": "extract_document_fields",
  "description": "读取指定文档版本的已解析结构化字段，返回字段值及其文件版本、页码、坐标和置信度。只读取已有解析结果，不判断业务是否符合。",
  "parameters": {
    "type": "object",
    "properties": {
      "documentVersionIds": {
        "type": "array",
        "items": { "type": "string" },
        "minItems": 1,
        "description": "本次复核允许读取的文档版本 ID。"
      },
      "fieldCodes": {
        "type": "array",
        "items": { "type": "string" },
        "description": "需要读取的字段代码；省略时读取选中文档的全部已解析字段。"
      }
    },
    "required": ["documentVersionIds"],
    "additionalProperties": false
  }
}
```

调用约束：

1. `documentVersionIds` 必须来自当前 ReviewRun，不允许 Agent 输入任意文件路径，也不允许跨项目读取。
2. `facts`、数值、日期、机构名称、人员身份和材料批次必须来自冻结的 FactSnapshot；Agent 不得自行从对话文本构造“已确认事实”。
3. `requiredFields`、`ruleChecks`、阈值、覆盖关系、签字角色和抽样比例必须来自冻结规则配置或 Tool Argument Assembler；Agent 不得临时制定。
4. Tool 返回 `evidence_insufficient` 时，Agent只能说明缺失参数/证据并建议补证，不能二次推理成 `passed`。
5. Agent 不得直接调用节点分析中未在下表出现的候选步骤名称。

### 80.4 证据读取 Tool 上下文

| name | description | parameters（JSON Schema 摘要） |
| --- | --- | --- |
| `get_document_ocr_result` | 读取指定文档版本的 OCR 文本、字段、表格、印章、签名和原文片段；只返回解析证据，不做符合性判断。 | 必填 `documentVersionIds:string[]` |
| `extract_document_fields` | 读取指定文档版本的结构化字段，并保留页码、坐标和置信度。 | 必填 `documentVersionIds:string[]`；可选 `fieldCodes:string[]` |
| `extract_table_records` | 读取指定文档版本的表格和标准化行记录，可按业务表 Schema 过滤。 | 必填 `documentVersionIds:string[]`；可选 `businessSchemas:string[]` |
| `recognize_signatures_and_seals` | 读取签字角色、签章、印章文字、位置和置信度；不判断签字是否齐全。 | 必填 `documentVersionIds:string[]`；可选 `expectedIssuer:string` |
| `locate_evidence_fragment` | 按查询词定位字段或原文片段，生成文件版本、页码、坐标、原文和置信度证据引用。 | 必填 `documentVersionIds:string[]`、`queryTerms:string[]`；可选 `minConfidence:number` |
| `extract_welder_certificate` | 从焊工资格证 OCR 结果抽取证件号、档案号、发证机关和作业项目代码。 | 必填 `documentVersionIds:string[]` |

### 80.5 专用确定性 Tool 上下文

| name | description | parameters（必填；`?` 表示可选） |
| --- | --- | --- |
| `check_all_equal` | 对多个来源的值按指定归一化方式比较一致性；有效值不足时返回证据不足。 | `values:{source:string,value:any}[]`；`normalizer:string?`；`requiredCount:integer?` |
| `check_date_covers` | 判断证照或报告有效期是否覆盖闭区间业务周期。 | `validUntil:date`、`periodStart:date`、`periodEnd:date`；`validFrom:date?` |
| `check_design_license_scope` | 按 GC1、GC2、GCD 设计许可覆盖规则核验许可范围。 | `licenseScopes:string[]`、`requiredPipelineGrades:string[]` |
| `decode_welder_qualification` | 解析焊工资格作业项目代码，形成方法、材料、位置、厚度和管径覆盖事实。 | `qualificationCodes:string[]` |
| `check_welder_work_coverage` | 核验焊工资格项目是否覆盖实际方法、材料、位置、壁厚和管径。 | `qualificationCodes:string[]`、`workItems:object[]` |
| `check_pressure_gauge_requirements` | 核验耐压试验压力表数量、检定日期、精度、量程、表盘直径记录、最高点安装及介质温度。 | `gauges:object[]`、`maxTestPressure:number`、`testDate:date`、`medium:string`、`mediumTemperature:number`、`ambientTemperature:number` |
| `check_pressure_test_parameters` | 核验液压/气压试验压力上下限、温度许用应力比、保压、分级升压和结果。 | `method:liquid\|gas`、`designPressure:number`、`testPressure:number`、`holdMinutes:number`、`testResult:string`、`maximumAllowableTestPressure:number`；其余应力和气压步骤参数按方法条件提供 |
| `check_pressure_test_report_consistency` | 比较耐压试验报告、方案与现场见证记录的关键参数和结果。 | `report:object`、`observed:object`；`tolerance:number?` |
| `validate_evidence_grounding` | 校验声明事实是否都有页码、坐标或原文、置信度及无冲突的 EvidenceRef。 | `facts:object[]`、`evidenceRefs:object[]`；`minConfidence:number?` |

### 80.6 通用业务判断 Tool 上下文

| name | description | parameters（必填；`?` 表示可选） |
| --- | --- | --- |
| `check_required` | 检查事实对象中的必需字段是否存在；字段缺失返回 `failed`，规则未配置返回证据不足。 | `facts:object`、`requiredFields:string[]` |
| `check_scope_coverage` | 按显式覆盖映射核验已授予范围是否覆盖要求范围。 | `grantedScopes:string[]`、`requiredScopes:string[]`；`coverageMap:object?` |
| `check_cross_document_match` | 按文本/机构名归一化或数值容差比较跨文档字段。 | `comparisons:{code?,values:any[],normalizer?,tolerance?,requiredCount?}[]` |
| `check_signature_completeness` | 核验识别出的签字角色是否覆盖规则要求角色。 | `actualRoles:string[]`、`requiredRoles:string[]` |
| `check_numeric_range` | 对一个或多个数值执行开闭区间上下限检查。 | `ranges:{code?,value,min?,max?,includeMin?,includeMax?}[]` |
| `check_conditional_requirement` | 条件为真时检查必需事实，条件为假返回不适用。 | `condition:boolean`、`facts:object`、`requiredFields:string[]` |
| `check_sampling_requirement` | 按总体数、比例和最小数量计算抽样数，并校验实际样本。 | `populationCount:integer`、`sampledCount:integer`；`requiredRatio:number?`、`minimumCount:integer?`、`selectedIds:string[]?`，比例和最小数量至少提供一项 |
| `check_document_set_completeness` | 核验所需文件本体是否已上传且可解析；目录列名不能代替文件本体。 | `requiredDocumentTypes:string[]`、`uploadedDocumentTypes:string[]`；`parseableDocumentTypes:string[]?` |
| `check_standard_version_active` | 按评审日期核验标准的实施日期、废止日期和状态。 | `standardReferences:{standardRef?,effectiveFrom?,withdrawnOn?,status?}[]`、`reviewDate:date` |
| `check_traceability` | 核验原标志、移植标志、批号及移植记录之间的追溯关系。 | `items:{originalMark,transferredMark,batchNo,transferRecord}[]` |

### 80.7 已有专用业务 Tool 上下文

| name | description | parameters（必填；`?` 表示可选） |
| --- | --- | --- |
| `check_ndt_personnel_coverage` | 按工作日期、注册状态、检测方法和等级核验每项 NDT 任务的人员覆盖。 | `personnel:object[]`、`workItems:object[]`、`workDate:date` |
| `check_wps_pqr_coverage` | 按方法、材料、位置、填充金属、厚度和管径核验 PQR/WPS 覆盖实际焊接任务。 | `qualifiedRanges:object[]`、`workItems:object[]` |
| `check_installation_license_scope` | 按 GC1、GC2、GCD 和 A级锅炉安装资质的固定覆盖关系核验安装许可证范围。 | `licenseScopes:string[]`、`requiredPipelineGrades:string[]` |
| `decode_ndt_approval_item_codes` | 按 TSG Z7002—2022 附件 A 表 A-1 解码检测机构核准项目代码；未知代码返回证据不足。 | `approvalItemCodes:string[]` |
| `evaluate_ndt_agencies` | 按检测机构分别核验机构名称、检测方法覆盖或施工计划工期有效期。 | `agencies:object[]`、`evaluationMode:identity\|method_coverage\|date_coverage`；`failureAction:string?` |
| `evaluate_installation_license_scope` | 按 A 级锅炉安装资质及 GC1/GC2/GCD 规则核验安装许可范围，并以计划/实际结束较晚者核验有效期。 | `licenseScopes:string[]`、`requiredPipelineGrades:string[]`、`validUntil:date`、`periodStart:date`、`plannedPeriodEnd:date`；`validFrom:date?`、`actualPeriodEnd:date?` |
| `evaluate_ndt_organization_scope` | 分机构核验核准证机构名、检测方法范围和施工计划工期有效期。 | `licenseOrganizationName:string`、`planOrganizationName:string`、`licensedMethods:string[]`、`requiredMethods:string[]`、`validUntil:date`、`periodStart:date`、`plannedPeriodEnd:date`；`validFrom/actualPeriodEnd?` |
| `evaluate_design_approval_level` | 逐文件核验文件本体及其三级/四级签字角色。 | `documents:{signatureRoles:string[],requiredRoles?:string[],bodyUploaded?:boolean}[]`；`requiredRoles:string[]?` |
| `evaluate_design_document_approval` | 逐份核验主要设计文件本体和签字角色，并根据文件覆盖的管道级别、设计压力和设计温度确定三级或四级批准程序。 | `approvalMode:three_level\|four_level_conditional`、`documents:object[]`、`pipelines:object[]`、`targetDocumentTypes:string[]`、`requiredRoles:string[]` |
| `evaluate_calculation_document_consistency` | 逐份核验强度计算书和应力计算书本体、覆盖管线以及与设计文件的结构化参数比较。 | `documents:object[]`、`targetDocumentTypes:string[]` |
| `evaluate_design_change_approval` | 核验设计变更适用性、原设计单位书面批准，并按被变更文件类型及管道条件逐份确定三级或四级签字。 | `hasDesignChanges:boolean`、`documents:object[]`、`pipelines:object[]` |
| `verify_design_license_seals` | 按 TSG 31-2025 3.1.2 逐份核验重新出具目录和布置图的原始压力管道设计许可印章。 | `hasDesignChanges:boolean`、`documents:object[]`、`requiredDocumentTypes:string[]`、`expectedSealName:string` |
| `evaluate_design_special_requirements` | 逐领域核验设计说明是否对无损检测、防腐、耐压试验和泄漏试验给出具体要求，并仅按冻结标准规则返回四个领域子结果和节点结果。 | `requirements:object`、`standardRules:object`、`requiredPathsByDomain:object`、`domains:string[]` |
| `evaluate_rt_film` | 核验底片/数字影像与焊口、报告、影像质量和抽样要求的关联。 | `films:object[]`；`reportWeldIds:string[]?`、`sampling:object?` |
| `evaluate_pressure_test` | 核验耐压试验方案必需内容和审批签字角色。 | `facts:object`、`requiredRoles:string[]`，facts 至少包含 timing、medium、pressurizationRate、instrumentRequirements、safetyMeasures、acceptanceCriteria |
| `evaluate_valve_test` | 按管道等级计算阀门抽检比例，或核验工厂逐台见证豁免条件，并检查试验记录和结果。 | `pipelineGrade:string`、`lotSize:integer`、`testedCount:integer`；试验记录字段或工厂见证豁免字段按分支提供 |

### 80.8 规则配置驱动领域 Tool 上下文

以下 Tool 在当前代码中共用 `evaluate_rule_profile()`，名称表达领域用途，但尚无独立专业算法。它们必须使用同一个参数 Schema：

```json
{
  "type": "object",
  "properties": {
    "facts": {
      "type": "object",
      "description": "Fact Builder 生成并冻结的节点业务事实。"
    },
    "requiredFields": {
      "type": "array",
      "items": { "type": "string" },
      "minItems": 1,
      "description": "必须存在的事实路径，由 atomicCheck 规则配置提供。"
    },
    "ruleChecks": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "code": { "type": "string" },
          "operator": { "enum": ["present", "equals", "gte", "lte", "gt", "lt", "contains_all", "accepted"] },
          "actual": {},
          "expected": {},
          "normalizer": { "type": "string" }
        },
        "required": ["code", "operator", "actual"]
      },
      "minItems": 1,
      "description": "由冻结规则和参数装配器生成的确定性检查，不允许 Agent 自行编写。"
    },
    "applicable": { "type": "boolean" },
    "profile": { "type": "string" },
    "ruleVersion": { "type": "string" }
  },
  "required": ["facts", "requiredFields", "ruleChecks"],
  "additionalProperties": false
}
```

| name | description |
| --- | --- |
| `evaluate_alternative_standard` | 按冻结的替代标准符合性声明、对照表和附录 B 检查规则执行判断。 |
| `evaluate_blowing_cleaning` | 按冻结的吹扫、清洗方法、参数、顺序、安全和验收规则执行判断。 |
| `evaluate_component_manufacturer_scope` | 按制造许可类别、产品范围和本项目元件清单规则执行判断。 |
| `classify_r13_component_requirements` | 依据 TSG D7006-2020 1.2.1、A1.2 和2021年第41号公告附件1注三、注四，逐项判断元件是否触发制造监检、型式试验及监检粒度；未知类别返回证据不足。 |
| `classify_r14_component_applicability` | 确认元件不需要制造许可、制造监检和型式试验后才进入 R14；需许可转 R12，需监检或型式试验转 R13，无法分类返回证据不足。 |
| `evaluate_construction_plan` | 按施工组织设计的审批、项目匹配、工序覆盖和标准版本规则执行判断。 |
| `evaluate_corrosion_protection` | 按具体防腐、保温、电火花、阴极保护或静电接地 profile 执行判断；不同 profile 不得混用。 |
| `classify_r15_foreign_manufacturing_applicability` | 仅依据制造国家、制造地点或明确业务事实判断境外制造，不得用境外材料牌号代替。 |
| `classify_r15_regulatory_requirements` | 依据 TSG 31-2025 第1.10、2.2.1.5条及 TSG D7006-2020 附件D D2.4.1，分类制造许可、型式试验和制造监检要求。 |
| `evaluate_r15_manufacturing_license_coverage` | 对需要制造许可的境外制造产品核对实际制造单位、官网核验状态及许可范围覆盖。 |
| `evaluate_r15_type_test_coverage` | 对有型式试验要求的境外制造产品核对制造单位、产品类别、材料、结构、工艺及规格/压力范围。 |
| `evaluate_r15_manufacturing_inspection_route` | 对需要制造监检的境外产品核验境外制造监检、到岸检验或随整机安全性能检验路径。 |
| `evaluate_heat_treatment` | 按焊口级适用性、热处理参数、曲线和硬度规则执行判断。 |
| `evaluate_heat_treatment_instruments` | 按热处理仪表身份、量程、精度和检定有效期规则执行判断。 |
| `evaluate_leak_test` | 按具体泄漏试验方法、压力、灵敏度、环境、报告和验收 profile 执行判断。 |
| `evaluate_material_component` | 按元件/材料类型、批次、质量证明、复验、型式试验或新材料 profile 执行判断。 |
| `evaluate_r13_supervision_certificate_completeness` | 对需制造监检的元件按批号或产品编号核对监检证书编号、制造单位、产品类别和合格结论，返回逐项覆盖矩阵。 |
| `evaluate_r13_type_test_coverage` | 将设计材料表元件与型式试验证书/报告的制造单位、产品类别、材料、结构、工艺和规格/压力范围逐项匹配，返回覆盖矩阵。 |
| `evaluate_r14_component_design_match` | 按元件类型、规格、管线号和批号关联出厂检验报告，逐项核对等级、材质和合格结论与设计材料表是否一致。 |
| `resolve_r14_required_inspection_items` | 根据设计明确要求和冻结产品标准规则确定光谱、硬度、金相、无损检测和耐压试验等必检项目；规则未覆盖时返回证据不足。 |
| `evaluate_r14_special_report_coverage` | 按元件、规格和批号核对必需专项报告是否齐全、关联正确且结论合格。 |
| `evaluate_r14_pressure_compatibility` | 按管线号关联材料表、管道特性表、出厂报告和专项报告，判断元件压力等级是否覆盖管线要求；不支持的压力等级换算返回证据不足。 |
| `evaluate_ndt_nonconformance` | 按 NDT 问题通知、处置、扩检、复检和闭环规则执行判断。 |
| `evaluate_ndt_process` | 按 NDT 方案、工艺或记录报告 profile 的方法、比例、级别、时机和版本规则执行判断。 |
| `evaluate_ndt_quality_system` | 按每家 NDT 机构的现场质量体系、任命、设备和记录追溯规则执行判断。 |
| `evaluate_pipe_fit_up` | 按接头形式、错边、壁厚过渡、组对间隙和禁止强力组对规则执行判断。 |
| `evaluate_pipeline_installation` | 按穿跨越、套管、绝缘支撑、预制、连接、补偿器或支吊架 profile 执行判断。 |
| `evaluate_safety_accessory` | 按安全附件清单、产品资料、安装位置和校验/性能报告 profile 执行判断。 |
| `evaluate_stress_analysis` | 按免除耐压路线下的系统边界、模型输入、审批和分析结论规则执行判断。 |
| `evaluate_weld_appearance` | 按管道等级、接头类型、外观缺陷和相应验收限值规则执行判断。 |
| `evaluate_weld_repair` | 按同一物理位置的返修次数、审批、工艺、复检和闭环规则执行判断。 |
| `evaluate_welding_consumable` | 按焊材批次、牌号、规格、质量证明、产品标准和库存期限规则执行判断。 |
| `evaluate_welding_consumable_control` | 按焊材验收、保管、烘干、发放、回收和焊口追溯规则执行判断。 |
| `evaluate_welding_process` | 按焊口、焊工、WPS、焊材和实际施焊参数关联规则执行判断。 |

### 80.9 Agent 调用示例

以 R02 有效期判断为例，原调用链中的 `extract_installation_license_facts`、`calculate_project_end_date` 等未注册名称已经删除。参数装配器应先把事实转换为专用 Tool 参数，然后 Agent 只消费返回结果：

```json
{
  "name": "evaluate_installation_license_scope",
  "description": "核验安装许可证是否覆盖项目管道等级，并核验证照有效期是否覆盖整个施工计划工期；施工结束日采用计划结束日和实际结束日中较晚者。",
  "parameters": {
    "type": "object",
    "properties": {
      "licenseScopes": { "type": "array", "items": { "type": "string" } },
      "requiredPipelineGrades": { "type": "array", "items": { "type": "string" } },
      "validFrom": { "type": "string", "format": "date" },
      "validUntil": { "type": "string", "format": "date" },
      "periodStart": { "type": "string", "format": "date" },
      "plannedPeriodEnd": { "type": "string", "format": "date" },
      "actualPeriodEnd": { "type": "string", "format": "date" }
    },
    "required": ["licenseScopes", "requiredPipelineGrades", "validUntil", "periodStart", "plannedPeriodEnd"],
    "additionalProperties": false
  }
}
```

默认确定性 Tool 的返回值是原子判断结论，不是交给 LLM 再判断的依据；LLM 只能把 `result`、`checks`、`facts`、`ruleVersion`、EvidenceRef 和 ClauseRef 组织成可读说明。R19 是已显式配置的例外：LLM 形成原子项语义判断，服务端校验其固定 ClauseRef 和 EvidenceRef 后再固定聚合节点结果。
