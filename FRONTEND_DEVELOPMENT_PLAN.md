# 前端开发计划

更新时间：2026-06-27

更新依据：当前工作区 `frontend/`、`API_DOCUMENTATION.md`、`FRONTEND_DEVELOPMENT_PLAN.md` 和最近一次本地验证结果。注意：上述文件当前在 git 中仍为未跟踪文件，开发不受阻塞，但进入连续开发前建议先纳入明确分支或提交检查点。

## 1. 结论

当前结论：前端功能开发、真实后端联调基座和 live smoke 已进入可持续迭代阶段。前端仍保留 `mock-first` 作为快速交互开发方式，但 `/api/*` 已可切到本地 FastAPI live 模式，核心工作台、后台、知识库和写回恢复流程已通过真实后端 smoke。

最新 UI/UX 口径：业务页面必须以 `ui/` 目录中的静态稿为 **1:1 克隆基准**，同时业务交互控件尽量使用 Element Plus。`vue-element-plus-admin` 保留工程脚手架、登录、路由、权限、请求和 mock 能力；Element Plus 优先承载按钮、表单、表格、弹窗、抽屉、分页、标签等功能控件；自定义 HTML/CSS 仅用于 `ui/` 静态稿 1:1 外壳、只读静态表格和文档预览等 Element DOM 会破坏截图级密度的区域。

继续开发评分：**97 / 100，Go**。UI/UX 1:1 克隆完成度单独计为 **93 / 100**，工作台、管理后台和知识库主入口已切到 `ui` 静态稿的顶栏和两列工作区外壳；工作台四类角色中栏主业务块和辅助细项详情已按 `ui/*workbench.html` 补齐到 Vue 静态展示层，管理后台和知识库中栏深层页面顺序与右栏细项卡片已按 `admin_backend.html`、`ai_knowledge_base_admin.html` 补齐到静态展示层；静态壳导航已切为 Element Plus Menu 并接入后台/知识库深链路由，工作台项目节点已切为 Element Plus Tree 并默认折叠、放大展开图标、压缩子节点缩进，长节点标题完整换行且选中态无左侧边线；顶栏项目选择器字体已压小、锐化并允许项目名换行完整展示，顶栏/页面动作/预览动作继续使用 Element Plus Button；工作台左/中两段各自纵向滚动，页面级横向溢出已禁止，文件预览已改为 Element Plus Drawer 覆盖式展示，不再占用工作区宽度。当前所有前端计划模块均已达到 90+；主要剩余差距是截图级对照、局部文案密度、部分截图级间距和少量可安全 Element 化的静态只读区域仍需逐项收敛。

不阻塞继续开发的剩余风险：截图级 1:1 仍需继续收敛，文件预览/下载在后端 live 模式已可走 MinIO signed GET，归档包下载已走后端导出产物；复杂 Office 转换/缩略图预览服务仍待后续接入，真实 OCR/PaddleOCR、MinIO、LiteLLM provider 和生产 Docker 环境仍需目标环境验收，其中 OCR 对象级解析已有 `verify_deployment.py --write-probes --ocr-object-probe` 实测探针，LiteLLM provider 已有 `verify_deployment.py --litellm-provider-probes` 实测探针。

开工判断：

| 检查项          | 状态   | 结论                                                                                                                                |
| --------------- | ------ | ----------------------------------------------------------------------------------------------------------------------------------- |
| 业务范围        | 已明确 | 四类工作台、管理后台、AI 知识库、文件/证据/报告/归档闭环清晰                                                                        |
| UI 原型         | 已具备 | `ui/` 内已有静态页面、交互映射、工作台原型和知识库后台页面；后续业务页必须按这些 HTML/CSS 逐页 1:1 对齐                             |
| API 路径        | 已覆盖 | `API_DOCUMENTATION.md` 已覆盖静态入口、mock/live 预留接口和旧接口别名                                                               |
| API 参数和输出  | 已补齐 | `API_DOCUMENTATION.md` 第 22、23 节可支撑前端 mock 开发                                                                             |
| Mock 策略       | 已具备 | 已定义 seed、handlers、状态流转、错误态、权限矩阵、幂等和并发口径                                                                   |
| 前端技术栈      | 已确定 | 基于 `vue-element-plus-admin` 实施                                                                                                  |
| Vue 工程目录    | 已创建 | `frontend/` 已接入 `vue-element-plus-admin` 模板                                                                                    |
| 前端 API client | 已建立 | `frontend/src/api/aicheck/index.ts` 已覆盖核心 mock 接口                                                                            |
| 前端 mock 基座  | 已建立 | `frontend/mock/aicheck/seed.ts`、`index.mock.ts` 已接入模板 mock 体系，支持核心 mutation 写回和基础错误态                           |
| 初版业务页面    | 已建立 | 工作台、管理后台、知识库已接入业务路由，功能链路第一版可运行；视觉仍需按 `ui/*.html` 和 `ui/workbench_v2.css` 重构为 1:1 静态稿外观 |
| 版本管理状态    | 需处理 | `frontend/`、`API_DOCUMENTATION.md`、`FRONTEND_DEVELOPMENT_PLAN.md` 当前未跟踪；不阻塞开发，但建议建立提交点                        |
| 后端服务        | 已就绪 | 本地 FastAPI live 模式已支撑 `aicheck-smoke.spec.ts` 51 条通过；生产外部依赖仍需部署验收                                            |

总体评估：可以继续开发。第 0 阶段已完成，第 1 阶段核心基础已落地，工作台已拆出项目树、节点包、动作栏、右侧信息栏、上传抽屉、资料挂载弹窗、提交批次弹窗、提交历史抽屉、人工审查面板、证据定位弹窗、补正详情弹窗、报告归档面板、报告复核详情抽屉、无损检测流程面板、无损检测详情抽屉、归档详情抽屉、导出任务详情抽屉、全局入口弹窗、文件详情弹窗和角色信息区。文件预览/下载、归档包、证据包、标准依据、日期比对、上传失败抽屉内保留文件列表并重试、资料挂载失败弹窗内保留选择并重试、施工方提交批次、提交/撤回失败弹窗内保留输入并重试、提交后撤回历史追溯、施工方跨节点挂载/草稿恢复/草稿覆盖提示/提交历史/范围提交、NDT 记录导入/资料提交失败后保留待提交集合并重试/补正反馈失败后保留说明并重试/抽查详情、NDT 报告上传失败表单内保留输入并重试、报告生成/导出/归档、归档浏览详情、导出任务详情、核心 mock 异常态、页面级状态、管理后台项目详情/成员授权/批量授权/配置导出、立项向导、权限/节点/流程配置编辑和差异预览、配置发布联动追溯/跨页面消息和待办联动、待办规则、消息模板、工具源、字段映射、真实联调字段差异清单、管理后台 Prompt 版本详情、管理后台规则差异、知识库任务重试/取消、知识库配置保存、多模型对比、规则版本差异和知识库二期管理操作已完成第一版。下一轮开发优先级调整为“`ui/` 截图级 1:1 收敛 -> Element 可替换点审计 -> 保持既有 mock 功能闭环 -> 更多异常恢复收尾 -> OpenAPI/codegen 准备”。

可立即开始的开发范围：

- 工作台剩余交互：更多弹窗级和操作级失败恢复；管理后台项目详情/成员授权/配置导出/立项向导/配置差异、知识库标准库编辑/规则发布/配置审计、NDT 检测记录导入/抽查详情、归档浏览详情、导出任务详情、日期比对、标准依据详情、报告复核详情、跨节点挂载、草稿详情和提交批次详情已完成第一版。
- 建设方只读能力：归档浏览详情、导出任务状态、归档包、证据定位包、报告预览、单项下载和最近只读导出入口已完成 mock 第一版。
- Mock 完整度：`FORBIDDEN`、`TASK_RUNNING`、`IDEMPOTENCY_KEY_CONFLICT`、`ETAG_CONFLICT` 已完成第一版；前端已接入 live 错误码解析，真实联调字段差异清单已完成 mock 对账第一版。
- 管理后台二期：项目详情、项目成员授权、成员批量授权、配置导出任务详情、立项向导、权限矩阵/节点模板/流程状态机配置编辑和差异预览、配置发布联动追溯、配置发布后跨页面消息/待办联动、待办规则、消息模板、工具源、字段映射新增/编辑、真实联调字段差异清单、Prompt 版本详情、规则版本差异、审计分页、live 错误码映射和主要局部错误恢复已完成第一版；后续补真实字段对账深化和更多跨页面联动验收。
- AI 知识库二期：标准库新增/编辑/启停、规则版本发布/回滚、规则版本差异、知识库配置、审计、分页、主要分区局部错误恢复、操作级失败恢复、live 错误码映射和端到端基础验收已完成第一版；任务重试/取消、配置保存、多模型对比已纳入 E2E，后续扩展更多管理类写回流程。

暂不建议开始的范围：

- 生产外部依赖联调：真实 MinIO、PaddleOCR/Seal OCR、LiteLLM provider、统一 PostgreSQL。
- OpenAPI SDK/codegen。
- 真实对象存储上传、真实 OCR、真实向量检索、真实 LLM 调用。
- 复杂报表模板编辑器。

当前验证结果：

- `pnpm ts:check`：通过。
- `pnpm build:dev`：通过，产物在 `frontend/dist-dev`。
- `AICHECK_BASE_URL=http://127.0.0.1:4100 pnpm playwright test e2e/aicheck-smoke.spec.ts --reporter=list`：通过，51 条 Playwright live 用例覆盖四类工作台、管理后台、知识库、建设方只读边界、390px 移动视口、业务错误码恢复链路、联调字段差异清单 0 blocker 筛选、配置发布联动追溯和跨页面消息/待办联动，以及施工方补正反馈、上传失败后抽屉内保留文件列表并重试、资料挂载失败后弹窗内保留选择并重试、施工方提交批次、施工方提交失败后弹窗内保留输入并重试、施工方提交后撤回历史追溯、施工方撤回失败后保留原因并重试、施工方跨节点挂载/草稿恢复/草稿覆盖提示/提交历史/范围提交、NDT 检测记录导入、NDT 底片新增失败后表单内保留输入并重试、NDT 记录导入失败后表单内保留输入并重试、NDT 报告上传失败后表单内保留输入并重试、NDT 检测资料提交失败后保留待提交集合并重试、NDT 补正反馈失败后保留说明并重试、NDT 检测资料提交、报告详情加载失败后抽屉内保留上下文并重试、归档资料详情加载失败后抽屉内保留上下文并重试、导出任务详情加载失败后抽屉内保留上下文并重试、管理后台待办规则新增、管理后台配置包导出、项目成员授权、项目立项、成员批量授权、权限矩阵更新、流程状态机版本更新、消息模板新增、工具源更新、字段映射更新、知识库任务重试/取消、知识库配置保存、多模型对比排队状态展示、监检报告导出、监检报告生成、监检报告归档。
- 本轮 Element Plus 与 1:1 CSS 调整验证：`Workbench.vue` 顶栏搜索、待办/消息、文件预览入口、页面动作和预览动作已改为 `ElButton`，文件预览已改为 `ElDrawer` 覆盖式展示；顶栏项目选择器的 Element Select 输入字体已压小、锐化，选中项目名允许换行完整展示；`ProjectNodeTree.vue` 已按 Element Plus Tree 实现项目节点控件，默认折叠、展开图标放大并压缩子节点缩进，长节点标题完整换行且选中态无左侧边线；`StaticPageShell.vue` 静态壳搜索已改为 `ElButton`，静态壳菜单已按 Element Plus Menu 实现；`src/styles/index.less` 新增 AICheck 作用域 Element Plus token，统一主色、表格 hover、卡片半径、按钮权重和焦点状态；源码审计显示 `views/AICheck` 已无裸 `<button>`，静态只读表格仍保留原 HTML 结构以维护 `ui/` 截图级密度。桌面量测覆盖 `/workbench/inspection`、`/workbench/contractor`、`/workbench/ndt`、`/workbench/owner`、`/admin/overview`、`/knowledge/overview`，均无页面级横向溢出；390px 量测覆盖工作台、后台、知识库，`scrollWidth === viewport`；工作台左/中两段各自纵向滚动，文件预览 Drawer 不参与工作区列宽；抽样 hover/selected/reduced-motion 显示搜索、树节点、表格行、辅助卡片、selected 行均有状态反馈，reduced-motion 下 transition 为 `0s`。
- 本轮全面 90+ 推进：后台新增 `/admin/projects|org|permission|rules|fine-config|integration|audit` 深链路由，知识库新增 `/knowledge/sources|files|tasks|rules|retrieval|reasoning|compare|config` 深链路由；`StaticPageShell` 菜单可按 route 跳转并保持单项高亮，后台/知识库 `ElTabs` 与 URL 双向同步。
- 本轮预览/只读导出推进：文档详情、preview-url 和 download-url 已改为优先基于当前版本 MinIO 对象生成 signed GET，`verify_deployment.py --write-probes --strict-production` 会实际 GET 新上传文档的预览/下载地址；归档包、证据定位包和归档单项下载已统一进入导出任务详情抽屉；`ExportTaskDrawer` 增加只读导出提示、导出类型文案、下载地址展示、失败/过期任务提示和重新加载入口；mock 导出任务已覆盖排队中、生成中、可下载、失败、已过期状态，下载 URL 接口对未就绪/过期任务返回对应业务错误。
- 本轮 Mock seed 扩展：项目样例从 4 个扩展到 8 个，覆盖草稿/立项中、资料提交中、AI 预审中、监检审查中、退回补正中、报告生成/复核中、已归档；新增报告复核和归档资料样例，并补充多状态导出任务样例。
- 本轮 UI/UX 1:1 外壳、角色中栏、右栏和后台/知识库深层页面实现：新增 `AICheckStaticLayout`、`StaticPageShell`、`WorkbenchRoleStaticSections`、`WorkbenchRightStaticDetails` 和 `AdminKnowledgeStaticDeepSections`，`/workbench/*`、`/admin/overview`、`/knowledge/overview` 已脱离 `vue-element-plus-admin` 默认后台侧栏/标签栏；`Workbench.vue` 已按 `ui/workbench_v2.css` 建立 68px 顶栏、项目切换、全局搜索、待办消息、`404px / minmax(850px, 1fr) / 552px` 三栏工作区、静态稿卡片/表格/pill/右侧预览区，并保留既有上传、挂载、提交、审查、NDT、报告和抽屉恢复逻辑；工作台中栏已按 `inspection_workbench.html`、`contractor_workbench.html`、`ndt_workbench.html`、`owner_workbench.html` 补齐监检“四到七”静态业务说明、施工方一到八段、无损检测一到六段、建设方一到五段；右栏已补齐监检“当前证据定位/文件属性/节点审查状态”、施工方“当前反馈摘要/业务链路只读摘要/文件属性/处理进度”、无损检测“链路补充提示/资料属性/办理进度”、建设方“当前节点资料摘要/业务链路状态/最近异常提醒”；管理后台按 `admin_backend.html` 补齐后台菜单、后台边界、模板详情右栏，以及合同功能入口、模板概览、规则模板列表、当前模板核验步骤、外部核验工具源、动作级权限矩阵 6 个中栏深层静态段落；知识库按 `ai_knowledge_base_admin.html` 补齐知识库菜单、后台边界、运行状态右栏，以及知识库总览、标准规范库、项目文件知识库、文件知识详情、OCR/向量任务中心、知识检索测试、业务规则版本、推理链路日志、多模型反馈、配置和审计 11 个中栏深层静态段落；`StaticPageShell` 已支持右栏进度条和版本时间线，后台右栏补齐“基础信息/字段映射/版本记录/后台限制”，知识库右栏补齐“处理进度/最近失败任务/当前规则快照/多模型评估摘要/后台限制”；布局检查显示工作台、管理后台和知识库在桌面与 390px 视口均无 body 级横向溢出，中栏和右栏均无自身横向溢出，1680px+ 业务画布由内部滚动容器承载。
- 本轮管理后台 Prompt 版本验证：`GET /api/admin/config-overview` 的规则版本摘要已返回 `ruleKey/promptVersion/outputSchemaVersion/description/publishedAt/actions`；`/#/admin/overview` 登录后打开“规则与流程”，桌面和 390px 移动视口均可查看 `prompt-welder-v2.1`/`schema-review-v1.3`，规则详情抽屉展示动作权限和影响节点，规则差异抽屉展示 `Prompt 版本` 差异，页面级横向溢出为 `false`。
- 本轮管理后台局部错误恢复实现：`/#/admin/overview` 已补基础数据、配置发布/导出、审计日志、项目详情、规则差异、配置编辑抽屉、项目立项和成员授权的就近错误条；失败时保留当前表格/抽屉上下文，并提供重新加载或重试操作入口；`POST /api/admin/config-overview/publish` 已接入 `mockError` 强制错误机制。
- 本轮管理后台收尾实现：`/#/admin/overview` 审计日志已接入 `page/pageSize/total` 分页器，筛选/重置会回到第一页；项目详情抽屉已新增“批量授权”，支持按角色一次选择多个用户并写入统一节点范围、动作权限和到期时间，失败时保留选择并展示成功/失败汇总。
- 本轮 live 错误码映射实现：新增 `src/utils/aicheckError.ts`，axios 非 0 业务响应会记录 `code/message/data.reason/operationId/serverTime` 并映射全局 toast；工作台、管理后台和知识库页面的 `!res`、catch、就近错误条和主操作 toast 已接入 `FORBIDDEN`、`ARCHIVED_READONLY`、`TASK_RUNNING`、`IDEMPOTENCY_KEY_CONFLICT`、`ETAG_CONFLICT`、`FILE_TOO_LARGE`、`UNSUPPORTED_FILE_TYPE`、`*_NOT_FOUND`、`*_REQUIRED` 等恢复建议。
- 本轮 E2E 集成验收基座实现：新增 `@playwright/test`、`playwright.config.ts` 和 `frontend/e2e/aicheck-smoke.spec.ts`，支持自动启动 mock dev server；已覆盖 `/workbench/inspection`、`/workbench/contractor`、`/workbench/ndt`、`/workbench/owner`、`/admin/overview`、`/knowledge/overview` 的渲染验收，建设方写操作不可见边界，核心页面 390px 移动视口无页面级横向溢出，以及管理后台发布、知识库检索、工作台 AI 复核的 live 错误码恢复提示。
- 本轮 E2E 业务写回验收扩展：`frontend/e2e/aicheck-smoke.spec.ts` 已覆盖施工方补正反馈提交后节点进入 `复审中`、施工方提交节点资料后打开提交批次详情并进入 `AI 预审中`、施工方提交后撤回资料并在提交历史/详情追溯原因、施工方跨节点挂载后保存范围草稿、从提交历史恢复草稿并按节点范围提交、草稿恢复后的覆盖提示、NDT 检测记录导入后表格出现新记录、NDT 底片新增失败后保留底片编号并按原表单重试、NDT 记录导入失败后保留记录编号并按原表单重试、NDT 报告上传失败后保留文件名并按原表单重试、NDT 检测资料提交失败后保留待提交报告/底片并按原集合重试、NDT 补正反馈失败后保留反馈说明并按原表单重试、NDT 检测资料提交后报告进入 `待审查`、报告详情/归档详情/导出任务详情首次加载失败后抽屉内保留上下文并按当前 ID 重试、管理后台新增待办规则后弹出配置差异、管理后台导出配置包后展示可下载任务、项目成员新增管理角色授权后刷新成员表、项目立项后打开成员授权详情并生成 4 名初始成员、成员批量授权后刷新共享节点范围、权限矩阵项目范围更新后展示配置差异、流程状态机版本更新后展示配置差异、消息模板新增后展示配置差异、工具源接口地址更新后展示配置差异、字段映射阈值更新后展示配置差异、知识库任务失败重试生成排队任务、排队任务取消进入 `已取消`、知识库配置保存、多模型对比写入历史、监检报告导出后打开导出任务详情、监检报告生成后新增 `V1` 复核版本、监检报告归档后项目进入只读归档状态；同步修正 mock 监检角色缺失 `report:archive` 动作的问题，并为报告类用例显式选择原始项目，避免立项写回影响后续验收。
- 本轮联调字段差异清单实现：新增 `GET /api/admin/integration-contract`、`IntegrationContractPayload` 类型、mock 模块/状态筛选和管理后台“联调清单”页签；当前样例覆盖工作台、文件与预览、提交与补正、监检审查、无损/建设方/报告、知识库与后台 6 个模块，并在 E2E 中验证 `后端缺失` 筛选和 390px 移动视口无页面级横向溢出。
- 本轮配置发布联动追溯实现：`POST /api/admin/config-overview/publish` 返回 `impactSummary/impacts`，并写入非归档项目工作台消息、监检角色字段映射复核待办；管理后台系统配置摘要展示“最近发布”卡片、推送消息数和复核待办数，弹窗可追溯权限矩阵、流程状态机、待办规则、消息模板和字段映射的同步/复核状态；E2E 覆盖发布后打开“发布联动追溯”、跳转监检工作台查看消息/待办，并验证 390px 移动视口无页面级横向溢出。
- 本轮上传抽屉失败恢复实现：`UploadSessionDrawer` 已补就近错误条、文件列表保留、响应式抽屉宽度、表格局部滚动和“重试创建”；`Workbench` 在上传会话失败时保留抽屉上下文，`FILE_TOO_LARGE` 等 live 错误码映射为恢复建议；E2E 覆盖首次上传失败后按原文件重试入库，并验证 390px 移动视口无页面级横向溢出。
- 本轮 NDT 报告上传失败恢复实现：`NdtWorkflowPanel` 已补报告上传局部错误条、文件名和关联底片保留、“重试上传会话”以及移动端错误内容纵向布局；`Workbench` 接入 `NDT_FILE_TOO_LARGE`/`UNSUPPORTED_NDT_FILE_TYPE` live 错误码恢复建议；E2E 覆盖首次上传失败后按原表单重试并增加待提交报告数，390px 移动视口无页面级横向溢出。
- 本轮 NDT 检测资料提交失败恢复实现：`NdtWorkflowPanel` 已补提交动作区局部错误条、待提交报告/底片集合保留和“重试提交”；`Workbench` 接入 `TASK_RUNNING`、`ETAG_CONFLICT`、`NDT_REPORT_REQUIRED` 等 live 错误码恢复建议；E2E 覆盖首次提交失败后按原待提交集合重试，390px 移动视口无页面级横向溢出。
- 本轮 NDT 补正反馈失败恢复实现：`NdtWorkflowPanel` 已补补正反馈表单局部错误条、反馈事项和说明保留、“重试补正反馈”；`Workbench` 接入 `TASK_RUNNING`、`ETAG_CONFLICT`、`NDT_RECTIFICATION_REQUIRED` 等 live 错误码恢复建议；E2E 覆盖首次补正反馈失败后按原表单重试，390px 移动视口无页面级横向溢出。
- 本轮 NDT 底片/记录表单失败恢复实现：`NdtWorkflowPanel` 已补底片新增和检测记录导入局部错误条、表单输入保留、“重试新增底片”和“重试导入记录”；`Workbench` 接入 `TASK_RUNNING`、`ETAG_CONFLICT`、`NDT_FILM_REQUIRED`、`NDT_RECORD_REQUIRED` 等 live 错误码恢复建议；mock 对完全缺少检测记录必填字段返回 `NDT_RECORD_REQUIRED`；E2E 覆盖首次失败后按原表单重试，390px 移动视口无页面级横向溢出。
- 本轮只读/导出详情抽屉失败恢复实现：`ReportDetailDrawer`、`ArchiveDetailDrawer`、`ExportTaskDrawer` 已补抽屉内错误条和重试入口；`Workbench` 保留最近请求的报告、归档资料和导出任务 ID，接入 `REPORT_NOT_FOUND`、`ARCHIVE_NOT_FOUND`、`EXPORT_TASK_NOT_FOUND` 等 live 错误码恢复建议；E2E 覆盖首次加载失败后按当前 ID 重试，390px 移动视口无页面级横向溢出。
- 本轮管理后台局部错误恢复验证：拦截配置保存返回 `ETAG_CONFLICT` 时，配置抽屉保留并展示“角色权限矩阵保存失败”和“重试操作”；拦截配置发布返回 `TASK_RUNNING` 时，页面顶部展示“配置发布失败”和“重试操作”，项目清单与系统配置摘要仍可见；桌面和 390px 移动视口页面级横向溢出均为 `false`。
- 本轮管理后台细项配置 HTTP mock 验证：`GET /api/admin/config-overview` 返回待办规则、消息模板、工具源、字段映射各 3 项；`POST /api/admin/config-items/todo-rule` 可新增规则并返回配置差异和审计；`PUT /api/admin/config-items/tool-source/{id}` 可更新工具源状态；`PATCH /api/admin/field-mappings/{id}` 可更新字段映射阈值。
- 本轮页面验证：`/#/admin/overview` 登录后可打开“细项配置”页签，桌面和 390px 移动视口均能渲染细项表格，页面级横向溢出为 `false`。
- 本轮异常态验证：`/#/knowledge/overview` 正常态桌面和 390px 移动视口均可渲染且无页面级横向溢出；拦截 `GET /api/knowledge/overview` 返回 500 时，页面展示“知识库数据加载失败”和“重新加载”按钮。
- 本轮规则差异验证：`GET /api/rules/versions/{versionId}/diff` 已接入 mock；`/#/knowledge/overview` 的“规则配置”页签可打开规则版本差异抽屉，桌面和 390px 移动视口均可渲染且无页面级横向溢出。
- 本轮工作台局部错误验证：节点资料包接口失败时，中间栏显示“节点资料包加载失败”和“重新加载节点”，项目切换、指标、节点树和右侧上下文仍保留；接口恢复后可重试加载当前节点。
- 本轮知识库分页实现：知识源、项目文件、任务中心、规则版本、知识库审计和推理日志均已使用 `page/pageSize/total`，筛选变化会回到第一页，表格标题显示服务端总数。
- 本轮知识库分页验证：拦截 `GET /api/knowledge/project-files` 返回 12 条数据后，项目文件库分页控件可请求 `page=2` 并渲染第二页数据；桌面和 390px 移动视口页面级横向溢出均为 `false`。
- 本轮知识库局部错误恢复实现：知识源、项目文件、任务中心、规则版本、知识库配置、知识库审计、推理日志和多模型历史均有分区级错误条和“重新加载”入口；非总览接口失败不再把整页切到失败态。
- 本轮知识库局部错误验证：受控拦截 `GET /api/knowledge/project-files` 首次返回 500 后，项目文件库展示“项目文件加载失败”和局部“重新加载”；重试后接口调用达到 2 次、错误条消失并渲染 `钢管质量证明书.pdf`，桌面和 390px 移动视口页面级横向溢出均为 `false`。
- 本轮知识库操作级失败恢复实现：`/#/knowledge/overview` 已补知识源新增/编辑/启停、规则发布/回滚、规则差异、知识库配置保存、批量/单项重建索引、文件重建、任务重试/取消、检索测试、多模型对比、文件详情和推理详情的就近错误条；失败时保留当前输入、表格、结果或抽屉上下文，并提供重试或刷新入口；`POST /api/knowledge/retrieval-test` 和 `POST /api/llm/compare` 已接入 `mockError` 强制错误机制。
- mock mutation 验证：保存节点 24 审查意见后，节点状态从 `待人工确认` 变为 `已通过`，审查记录和资料绑定状态同步更新。
- mock 交互验证：上传资料后进入项目资料池，挂载后节点绑定数增加，AI 建议采纳只生成审查草稿，超大文件和不支持格式返回非成功错误码。
- mock 交互验证：证据链接口返回文件/页码/字段定位数据；提交补正反馈后节点 16 进入 `复审中`，绑定资料状态变为 `已提交`。
- mock 交互验证：撤回 `BIND-24-001` 后节点 24 变为 `部分提交`、绑定变为 `草稿挂载`，提交历史和提交详情可追溯撤回数量、原因和时间；重新提交批次后进入 `AI 预审中`；生成报告草稿后报告数从 1 增至 2，归档项从 3 增至 4。
- mock 交互验证：新增无损检测底片、创建检测报告上传会话、提交节点 40 检测资料、提交无损检测补正反馈均已写回 mock 内存态。
- mock 交互验证：报告导出可生成导出任务并更新下载地址；报告归档后报告状态变为 `已归档`，项目进入只读归档状态。
- mock 交互验证：全局搜索、待办列表、待办完成、消息列表、单条已读和全部已读均已接入 mock 并写回项目计数。
- mock 交互验证：文件详情接口返回版本、OCR 字段、证据定位、预览 URL 和下载 URL；归档包和证据包接口返回可下载 mock 地址、导出编号和包内数量。
- mock 异常态验证：owner 写操作返回 `FORBIDDEN`；AI 复核/归档包可模拟 `TASK_RUNNING`；重复幂等键返回 `IDEMPOTENCY_KEY_CONFLICT`；过期 `If-Match` 返回 `ETAG_CONFLICT`。
- mock 管理后台验证：配置总览返回指标、组织、用户、权限矩阵、节点模板、规则版本和流程状态机；项目详情返回成员、参建单位、节点概况和导出任务；成员授权/启停写回内存态；配置导出生成可查询导出任务并写审计；发布配置会生成版本和审计日志；审计日志支持 `keyword`、`result`、`objectType`、分页筛选。
- mock 管理后台验证：项目立项会写入项目列表、69 个监督检验节点、初始四类成员授权、消息和审计；配置差异预览不写状态；保存权限矩阵、节点模板、流程状态机、待办规则、消息模板、工具源和字段映射会更新配置总览并写审计；细项配置新增会生成配置差异和审计日志。
- mock 知识库验证：总览、知识源新增/编辑/启停、项目文件、文件详情、切片、向量摘要、推理引用、任务重试/取消、文件重建索引、检索测试、规则发布/回滚/差异、知识库配置保存、知识库审计、推理日志详情/证据、多模型对比和历史记录均已接入并可写回内存态。
- 页面验证：`/#/knowledge/overview` 使用登录态 Playwright 截图通过，桌面和移动宽度均能渲染知识库页面。
- mock 工作台详情验证：跨节点挂载可一次写入多个节点；保存草稿后可读取草稿详情；提交历史可返回草稿、已提交批次摘要和撤回追溯；提交批次后可读取提交快照、节点、资料项、后续待办和撤回详情。
- mock 监检详情验证：标准依据、日期比对和报告复核详情接口返回结构化数据；监检工作台右侧栏可查看标准/日期比对，报告版本可打开复核详情抽屉并定位证据。
- mock NDT/建设方详情验证：检测记录导入写回记录列表，NDT 报告详情和反馈抽查详情可读取关联底片/报告/记录；归档资料详情和导出任务详情可查询。
- 页面验证：`/#/workbench/contractor` 使用登录态 Playwright 截图通过，施工方工作台可正常渲染。
- 构建警告：Browserslist 数据过旧、mockjs 使用 `eval`，均来自模板依赖，不阻塞当前开发。

## 1.1 当前开发进度

| 模块           | 进度 | 说明                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| -------------- | ---: | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 模板接入       | 100% | 已 clone `vue-element-plus-admin` 到 `frontend/`，安装依赖并接入业务标题、主题色、静态业务路由                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| API 类型       | 100% | 已覆盖项目、节点、项目详情、项目立项、成员授权、文件详情/预览/下载、跨节点挂载、草稿详情、提交历史、提交快照详情、提交/撤回、证据、标准依据、日期比对、AI 审查、报告生成/导出/归档/详情、报告章节保存、归档包/证据包、归档浏览/详情、导出任务详情、无损检测记录/报告/反馈详情、全局搜索、待办、消息、管理后台配置总览/发布/导出/差异预览/配置保存/细项配置新增、配置发布联动追溯与跨页面消息/待办计数、联调字段差异清单、管理后台规则版本 Prompt 摘要、审计筛选、知识源维护、规则发布/回滚/差异、知识库配置/审计、知识库文件/任务、检索/推理/多模型对比、mutation 返回；已落地 `openapi/` 拆分合同、`contract-index.json`、`scripts/openapi_contract.py` 和前端生成的 `operation-map.ts`，后续主要做真实后端字段对账                                                                                                                                                                                                                                                                                                                                                                                    |
| Mock seed      |  92% | 已录入 8 个项目、69 个节点、核心资料、挂载、OCR 字段、证据、AI 审查、报告、归档、导出任务、项目成员、无损检测底片/记录/报告/反馈、待办、消息、可编辑管理后台核心配置、待办规则、消息模板、工具源、字段映射、知识源、知识文件、知识任务、规则版本、Prompt/输出结构版本、知识库配置、多模型对比历史；项目状态覆盖草稿/立项中、资料提交中、AI 预审中、监检审查中、退回补正中、报告生成/复核中、已归档；导出任务覆盖排队中、生成中、可下载、失败、已过期；后续补真实联调样例                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Mock handlers  | 100% | 已覆盖工作台、项目树、节点包、文件详情/预览/下载、上传会话、跨节点资料挂载、草稿详情、提交历史、提交批次详情、撤回、补正、审查、AI 复核、AI 采纳/驳回、标准依据、日期比对、报告生成/导出/归档/详情、报告列表、归档包/证据包、归档浏览/详情、导出任务详情、无损检测底片新增、记录导入、报告详情、反馈详情、全局搜索、待办、消息、知识库 source/rule/config/audit、规则版本差异、知识库文件/任务/检索/推理/多模型对比、管理后台配置、配置发布联动追溯和跨页面消息/待办写回、联调字段差异清单、审计筛选，并支持核心状态写回、权限不足、任务运行中、幂等冲突、ETag 冲突、NDT 必填校验和基础错误态                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| UI/UX 1:1 克隆 |  93% | `ui/` 静态稿、交互映射和 `workbench_v2.css` 已完成盘点；`/workbench/*`、`/admin/overview`、`/knowledge/overview` 已使用 68px 顶栏、左栏 + 中栏工作区、静态稿卡片/表格/pill/Drawer 预览/辅助详情基线，并保留 mock 写回组件；工作台已通过 `WorkbenchRoleStaticSections` 按四类角色补齐中栏主业务块顺序和标题，并通过 `WorkbenchRightStaticDetails` 补齐主要细项详情卡；管理后台和知识库已通过 `AdminKnowledgeStaticDeepSections` 按 `admin_backend.html`、`ai_knowledge_base_admin.html` 补齐中栏深层页面顺序，`StaticPageShell` 已补右栏进度条、版本时间线和静态表格 ARIA 隔离；工作台顶栏/动作/预览按钮和静态壳搜索已切为 Element Plus Button，静态壳导航已切为 Element Plus Menu，工作台项目节点已切为 Element Plus Tree 且默认折叠、展开图标放大、子节点缩进压缩；工作区已补左/中独立滚动、页面级横向溢出限制和文件预览 Drawer 覆盖式展示；监检、施工方、无损检测、建设方、管理后台、知识库均通过 E2E 与桌面/390px 布局量测；后续继续做截图级对照、文案/间距密度和可安全 Element 化区域收敛。                                                                                                                                          |
| 路由菜单       |  92% | 已接入四类工作台、管理后台、AI 知识库；后台已补项目、组织用户、权限节点、规则流程、细项配置、联调清单、审计日志深链路由；知识库已补知识源、项目文件、任务中心、规则配置、检索测试、推理日志、多模型对比、配置审计深链路由；静态壳 Element Plus Menu 与页面 Tab 已按路由同步，高亮和无横向溢出已纳入 E2E                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 工作台初版     |  99% | 已实现并拆分统一项目树、节点包、资料要求、资料池、文件详情/预览/下载、上传抽屉、上传失败抽屉内保留文件列表并重试、跨节点挂载弹窗、资料挂载失败弹窗内保留选择并重试、提交批次弹窗节点范围选择、草稿详情抽屉、提交历史抽屉、草稿恢复、草稿覆盖提示、提交批次详情抽屉、提交/撤回失败弹窗内保留输入并重试、撤回未提交和撤回历史追溯、AI 审查、AI 采纳/驳回、人工审查、证据定位、标准依据、日期比对、补正详情、报告生成/导出/归档/详情/只读下载面板、报告详情失败恢复、归档详情失败恢复、导出任务详情失败恢复、无损检测流程/底片新增失败恢复/记录导入失败恢复/报告上传失败恢复/资料提交失败恢复/补正反馈失败恢复/详情面板、归档详情和导出任务详情、全局入口弹窗、角色信息区、待办消息、基础动作按钮、页面级 error/empty/forbidden/readonly 状态；施工方上传失败恢复、施工方补正、资料挂载失败恢复、施工方提交批次、施工方提交失败恢复、施工方提交后撤回追溯、施工方撤回失败恢复、施工方跨节点挂载/草稿恢复/提交历史/范围提交、NDT 记录导入、NDT 底片新增失败恢复、NDT 记录导入失败恢复、NDT 报告上传失败恢复、NDT 资料提交失败恢复、NDT 补正反馈失败恢复、NDT 资料提交、报告/归档/导出详情失败恢复、监检报告生成/导出/归档已纳入 E2E 写回验收 |
| 管理后台初版   |  99% | 已实现项目清单、项目详情抽屉、参建单位、节点概况、项目成员授权/启停/批量授权、项目立项向导、配置导出任务、系统配置摘要、组织用户、权限矩阵、节点模板、规则版本、流程状态机、Prompt/输出结构版本展示、规则版本详情抽屉、规则版本差异抽屉、权限/节点/流程配置编辑、配置差异预览、配置发布联动追溯、配置发布后工作台消息/待办联动、待办规则、消息模板、工具源、字段映射新增/编辑、真实联调字段差异清单、审计筛选/分页、配置发布，以及基础数据/审计/项目详情/规则差异/配置保存/发布/导出/立项/成员授权的局部错误恢复和 live 错误码映射；待办规则新增、配置包导出、配置发布联动追溯/跨页面联动、项目成员授权、项目立项、批量授权、权限矩阵更新、流程状态机版本更新、消息模板新增、工具源更新、字段映射更新已纳入 E2E 写回验收，后续补真实后端对账深化                                                                                                                                                                                                                                                                                                                                                                                         |
| 知识库初版     |  99% | 已实现知识库指标、索引列表、知识源新增/编辑/启停、项目文件库、文件详情、切片、向量摘要、推理引用、任务中心、失败重试、排队取消、文件重建索引、规则发布/回滚、规则版本差异抽屉、知识库配置、知识库审计、检索测试、推理日志、多模型对比和历史记录；知识源、项目文件、任务、规则、审计和推理日志已接入服务端分页，并补齐知识库初始加载失败/全空数据页面级状态、主要分区局部错误恢复、主要操作级失败恢复、live 错误码映射和端到端基础覆盖；任务重试/取消、配置保存、多模型对比已纳入 E2E，后续扩展更多管理写回流程验收                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 异常态覆盖     |  99% | 已有校验失败、未找到、状态冲突、文件过大、格式不支持、归档只读、权限不足、任务运行中、幂等冲突、ETag 冲突、工作台页面级 error/empty/forbidden/readonly、节点资料包局部失败可重试、上传抽屉失败就近错误条和按当前文件列表重试、报告详情/归档详情/导出任务详情失败就近错误条和按当前 ID 重试、NDT 底片新增失败就近错误条和按当前表单重试、NDT 记录导入失败就近错误条和按当前表单重试、NDT 报告上传失败就近错误条和按当前表单重试、NDT 检测资料提交失败就近错误条和按当前待提交集合重试、NDT 补正反馈失败就近错误条和按当前说明重试、资料挂载弹窗失败就近错误条和按当前选择重试、提交批次弹窗提交/保存/撤回失败就近错误条和按当前表单重试、工作台主操作 live 错误码提示、知识库初始加载失败可重试、知识库全空数据提示、知识库主要分区局部失败可重试、知识库主要操作级失败可重试，以及管理后台基础数据/审计/项目详情/规则差异/配置保存/发布/导出/立项/成员授权局部失败恢复；已补关键集成验收用例，后续扩展其它弹窗级错误恢复和更多真实错误码样例                                                                                                                                                                                             |
| 预览/只读导出  |  96% | 文件详情/预览/下载、报告预览、归档单项下载、归档浏览详情、归档包下载、证据包下载和导出任务详情已完成 mock 第一版；真实后端文档详情、preview-url 和 download-url 已基于当前版本 MinIO 对象生成 signed GET，strict write probes 会实际 GET 新上传文档；归档包、证据定位包已改为真实 zip 产物，包含 manifest、JSON/CSV 和 manifestHash；报告导出和归档单项下载已统一进入只读导出任务抽屉，归档面板已显示最近只读导出任务入口；导出任务生命周期已覆盖排队中、生成中、可下载、失败、已过期，失败/过期抽屉内有原因和重新加载入口；报告详情、归档详情和导出任务详情已补局部失败恢复并纳入 E2E；复杂 Office 转换、缩略图和页面级真实预览渲染服务待补                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 集成验收       |  97% | 已新增 Playwright E2E 基座，覆盖四类工作台、管理后台、知识库、建设方只读、390px 移动视口、三条 live 错误码恢复链路、联调字段差异清单按状态筛选、配置发布联动追溯和跨页面消息/待办联动，以及施工方补正反馈、施工方上传失败恢复、资料挂载失败恢复、施工方提交批次、施工方提交失败恢复、施工方提交后撤回追溯、施工方撤回失败恢复、施工方跨节点挂载/草稿恢复/草稿覆盖提示/提交历史/范围提交、NDT 检测记录导入、NDT 底片新增失败恢复、NDT 记录导入失败恢复、NDT 报告上传失败恢复、NDT 检测资料提交失败恢复、NDT 补正反馈失败恢复、NDT 检测资料提交、报告详情失败恢复、归档详情失败恢复、导出任务详情失败恢复、管理后台待办规则新增、配置包导出、项目成员授权、项目立项、成员批量授权、权限矩阵更新、流程状态机版本更新、消息模板新增、工具源更新、字段映射更新、知识库任务重试/取消、知识库配置保存、多模型对比、监检报告导出、监检报告生成、监检报告归档 35 条写回/恢复/联动流程；后续扩展其它异常恢复连续流程、配置联动细节收敛和 OpenAPI/codegen 准备                                                                                                                                                                                      |

## 2. 开发目标

前端一期目标是做出可运行、可交互、可 mock 流转的压力管道施工监督检验协作系统管理端。

一期必须覆盖：

- 登录后动态路由、角色菜单、动作权限。
- 项目切换、顶部栏、全局搜索、待办、消息。
- 统一项目树，覆盖 69 个监督检验节点。
- 施工方工作台：上传、挂载、草稿、提交、补正反馈、撤回未提交项。
- 监检工作台：节点文件包、AI 审查、证据链、审查意见、采纳/驳回 AI、退回补正、报告生成/复核。
- 无损检测工作台：底片、检测记录、报告上传、检测资料提交、监检反馈、补正反馈。
- 建设方工作台：只读概况、节点摘要、报告预览、归档浏览。
- 管理后台：项目、组织用户、权限、流程、节点模板、规则、工具源、字段映射、审计。
- AI 知识库：总览、标准库、项目文件库、文件知识详情、任务中心、规则版本、检索测试、推理日志、多模型对比、配置、审计。

一期不做：

- OpenAPI SDK/codegen。
- 生产级真实文件存储、真实 OCR、真实向量检索、真实 LLM provider 压测。
- 复杂报表模板编辑器。

## 3. 技术路线

| 方向     | 方案                                                                                                                                                                                                  |
| -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 基础框架 | `vue-element-plus-admin`                                                                                                                                                                              |
| UI 组件  | Vue 组件承载数据与交互，视觉按 `ui/` 静态稿 1:1 复刻；Element Plus 优先用于按钮、表单、功能表格、弹窗、抽屉、分页和标签；自定义 HTML/CSS 仅保留在静态壳、只读复刻表格和文档预览等需要截图级密度的区域 |
| 状态管理 | Pinia                                                                                                                                                                                                 |
| 路由     | 现阶段使用模板静态业务路由，后续再打开动态路由/服务端菜单                                                                                                                                             |
| 请求层   | 统一 `request` 封装，兼容 mock/live                                                                                                                                                                   |
| Mock     | 沿用模板 `vite-plugin-mock`，mock 文件放在 `frontend/mock/**/*.mock.ts`                                                                                                                               |
| 类型     | TypeScript 类型先从 `API_DOCUMENTATION.md` 第 4、22、23 节手写整理                                                                                                                                    |
| 样式     | 以 `ui/workbench_v2.css` 为业务页视觉基准，复刻 `app-shell/topbar/workspace`、三栏宽度、卡片、表格、pill、预览区和后台/知识库页面布局                                                                 |
| 文件预览 | PDF/图片/下载链路已支持后端 signed GET；复杂 Office 转换、缩略图和页级预览渲染服务后续接入                                                                                                            |

## 4. 当前工程目录

```text
frontend/
  src/
    api/
      aicheck/
        index.ts
    types/
      aicheck.ts
    stores/
      modules/
        app.ts
        user.ts
        permission.ts
    router/
      index.ts
    views/
      AICheck/
        Workbench.vue
        AdminOverview.vue
        KnowledgeOverview.vue
  mock/
    aicheck/
      seed.ts
      index.mock.ts
```

已拆分核心组件：

- `views/AICheck/components/ProjectNodeTree.vue`：项目树、节点状态和必传进度。
- `views/AICheck/components/NodePackagePanel.vue`：节点资料包、资料要求、项目资料池和绑定列表。
- `views/AICheck/components/WorkbenchActionBar.vue`：基于 `actions` 的工作台动作入口。
- `views/AICheck/components/UploadSessionDrawer.vue`：上传会话。
- `views/AICheck/components/DocumentBindDialog.vue`：资料挂载。
- `views/AICheck/components/SubmissionBatchDialog.vue`：提交批次、节点范围选择和保存为草稿。
- `views/AICheck/components/SubmissionHistoryDrawer.vue`：提交历史、草稿恢复和提交快照入口。
- `views/AICheck/components/ReviewDecisionPanel.vue`：监检人工审查。
- `views/AICheck/components/EvidenceLocatorDialog.vue`：证据链定位。
- `views/AICheck/components/RectificationDetailDialog.vue`：补正详情和反馈。
- `views/AICheck/components/ReportArchivePanel.vue`：报告生成、导出、归档和归档浏览。
- `views/AICheck/components/NdtWorkflowPanel.vue`：无损检测底片、报告、提交和补正。
- `views/AICheck/components/GlobalQuickAccessDialog.vue`：全局搜索、待办、消息。
- `views/AICheck/components/RoleContextPanel.vue`：角色上下文摘要。
- `views/AICheck/components/WorkbenchRoleStaticSections.vue`：按 `ui/*workbench.html` 输出四类角色中栏静态业务段落，并复用现有 mock 数据。
- `views/AICheck/components/WorkbenchRightStaticDetails.vue`：按 `ui/*workbench.html` 输出四类角色右栏细项详情卡，并保持操作按钮由原有组件承接。
- `views/AICheck/components/AdminKnowledgeStaticDeepSections.vue`：按 `ui/admin_backend.html` 和 `ui/ai_knowledge_base_admin.html` 输出后台/知识库中栏深层静态段落，并复用现有 mock 数据。

下一步新增/深化组件：

- `AdminErrorPanels`：管理后台配置表格、抽屉、发布/导出/保存操作的局部失败恢复。
- `KnowledgeOperationFeedback`：检索运行、配置保存、规则发布/回滚等操作级 loading/error/retry。
- `FilePreviewShell`：真实预览服务接入后的统一预览容器；当前 `FileDetailDialog` 已完成 mock 第一版。
- `src/stores/modules/aicheck.ts`：项目上下文、当前节点、待办消息缓存；当前先用页面内状态支撑开发，后续再抽。

## 5. 阶段计划

### 阶段 0：工程初始化

状态：已完成。

目标：把 `vue-element-plus-admin` 工程跑起来，并建立项目基础规范。

任务：

- 已创建 `frontend/` 工程目录。
- 已接入 `vue-element-plus-admin` 模板。
- 已跑通 `pnpm install`、`pnpm ts:check`、`pnpm build:dev`。
- 已保留布局、菜单、权限、请求、mock 机制。
- 已配置业务标题、主题色、静态业务路由和 mock/live 开关。
- 已建立 `src/types/aicheck.ts`、`src/api/aicheck/`、`mock/aicheck/`、`views/AICheck/`。

验收：

- 登录页和模板布局可用。
- 业务菜单可展示监检、施工、无损检测、建设方、管理后台、AI 知识库入口。
- `pnpm ts:check` 不报错。
- `pnpm build:dev` 不报错。

### 阶段 1：API 类型和 Mock 基座

状态：进行中，核心工作台链路、管理后台、知识库和主要 mutation 写回已可用。

目标：让前端可以不依赖后端完成所有页面数据流。

任务：

- 已根据 `API_DOCUMENTATION.md` 第 4、22、23 节整理第一批 TypeScript 类型。
- 已复用模板 `IResponse<T>`，建立角色、动作权限、状态枚举和核心 payload。
- 已建立 mock seed，覆盖项目、69 节点、文件、挂载、OCR、证据、AI、补正、报告、归档、无损检测、知识库、审计。
- 已建立 mock handlers，覆盖工作台和核心状态写入。
- 已增加 mock 内存态，上传会话、资料挂载、节点提交、提交项撤回、补正提交、AI 复核、AI 采纳/驳回、保存审查意见、退回补正、报告生成、无损检测资料提交会同步更新节点、绑定、报告、归档、待办、消息和审计/审查记录。
- 已实现基础错误态：校验失败、未找到、状态冲突、文件过大、格式不支持、归档只读、权限不足、任务运行中、幂等冲突、`If-Match`/ETag 冲突。
- 待继续细化：业务细分错误样例、组件级 retry、弹窗级失败恢复和 live 后端错误码映射。

验收：

- 所有 `api/*` 方法可在 mock 下返回稳定数据。
- mutation 后列表和详情能同步变化。
- owner 写操作返回 `FORBIDDEN`。
- 归档项目写操作返回 `ARCHIVED_READONLY`。
- 重复幂等键返回 `IDEMPOTENCY_KEY_CONFLICT`，过期 `If-Match` 返回 `ETAG_CONFLICT`。

### 阶段 2：应用壳和通用能力

状态：进行中，已完成第一版页面级实现、基础组件拆分、全局搜索、待办、消息、详情定位和主要页面状态；最新调整为优先复刻 `ui/` 静态稿业务壳，下一步细化局部错误恢复。

目标：完成四类工作台共用底座。

任务：

- 业务页外壳：四类工作台、管理后台和知识库从 `vue-element-plus-admin` 默认后台壳中剥离，使用 `ui/` 静态稿的 `app-shell/topbar/workspace` 结构。
- 顶部栏：项目名称、状态、用户角色、全局搜索、待办、消息和文件预览 Drawer 入口已在业务页内实现；已按 `ui/*.html` 的 68px 顶栏、品牌标识、项目切换和通知区完成 1:1 基线，并将全局搜索、待办、消息和预览入口切为 Element Plus Button。
- 项目切换：授权项目列表、切换后刷新上下文已实现。
- 左侧项目树：69 节点、业务大类、状态、必传进度已实现；已纳入 `ui/` 的 404px 左栏、节点树、节点文件包/反馈定位/只读资料摘要结构，节点控件已切为 Element Plus Tree，默认折叠、展开图标放大并压缩子节点缩进，保持模板三列密度。
- 动作权限：基于 `actions` 控制按钮显隐已实现，禁用原因和只读态提示待补。
- 通用组件：已拆出 `ProjectNodeTree`、`NodePackagePanel`、`WorkbenchActionBar`、`WorkbenchSidePanel`、`UploadSessionDrawer`、`DocumentBindDialog`、`SubmissionDetailDrawer`、`SubmissionHistoryDrawer`、`ReviewDecisionPanel`、`EvidenceLocatorDialog`、`RectificationDetailDialog`、`ReportArchivePanel`、`ReportDetailDrawer`、`NdtWorkflowPanel`、`NdtDetailDrawer`、`ArchiveDetailDrawer`、`ExportTaskDrawer`、`FileDetailDialog`、`GlobalQuickAccessDialog`、`RoleContextPanel`、`WorkbenchStateBanner` 和状态工具；后续补更多弹窗级失败恢复。
- 全局搜索、待办中心、消息中心。已完成第一版。

验收：

- 四类角色可进入对应工作台。
- 切换项目后顶部栏、树、主区数据刷新。
- 项目树节点状态和权限随角色变化。
- 搜索、待办、消息可打开详情并定位业务对象。

### 阶段 2.1：UI/UX 1:1 静态稿克隆

状态：P0，进行中；工作台、后台和知识库静态稿外壳基线已完成，工作台四类角色中栏主业务块和辅助细项详情已补齐，后台/知识库中栏深层页面顺序和右栏细项卡已补齐，Element Plus Button/Menu/Tree/Drawer 关键替换、工作台两段独立滚动、页面级无横向溢出和文件预览覆盖式弹出已完成；截图级对照、局部文案/间距密度和可安全 Element 化区域仍需继续 1:1。

目标：在保留 Vue/mock 交互的前提下，让业务页面视觉和交互布局 1:1 对齐 `ui/` 静态稿。

任务：

- Element Plus 使用边界：功能性交互控件优先使用 Element Plus，并通过 AICheck 作用域 CSS 变量贴合 `ui/` 视觉；只读静态表格、文档预览和静态稿外壳在 Element DOM 会破坏截图级密度时保留自定义结构。
- 工作台统一外壳：按 `ui/workbench_v2.css` 复刻 `app-shell`、68px 顶栏、`404px / minmax(0, 1fr)` 两列工作区、左侧节点树、中间业务区和辅助详情区；文件预览使用 Element Plus Drawer 覆盖式弹出，不占用工作区列宽。
- 监检页：按 `ui/inspection_workbench.html` 对齐“业务核验链路 -> 监检工作区 -> AI 建议结果 -> 审查对象/过程文件/证据链/规则说明/指标”的顺序。
- 施工方页：按 `ui/contractor_workbench.html` 对齐项目文件库、上传区、批量挂载、业务链路反馈、整改说明、文件批次摘要和右侧办理操作。
- 无损检测页：按 `ui/ndt_workbench.html` 对齐检测任务摘要、核验链路、底片记录、检测报告、上传区、补正材料和右侧资料预览。
- 建设方页：按 `ui/owner_workbench.html` 对齐只读项目概况、节点状态总览、业务链路摘要、报告归档状态和强制限制说明。
- 管理后台：按 `ui/admin_backend.html` 已对齐后台菜单、合同功能入口、模板概览、规则模板、当前模板核验步骤、外部核验工具源、动作级权限矩阵、右侧模板详情、字段映射、版本记录和后台限制，并移除工作台业务办理按钮；后续补截图级密度和静态交互态。
- AI 知识库：按 `ui/ai_knowledge_base_admin.html` 已对齐知识库总览、标准规范库、项目文件知识库、文件知识详情、任务中心、检索测试、规则版本、推理日志、多模型对比、配置审计布局，以及右侧运行状态、失败任务、规则快照和多模型评估摘要；后续补截图级密度和静态交互态。
- 验收：每个业务路由至少保留一张桌面截图与对应 `ui/*.html` 对照；关键 DOM 类名、三栏比例、卡片顺序、按钮状态和只读边界通过 Playwright 校验。

### 阶段 3：施工方工作台

状态：进行中，资料上传、跨节点挂载、跨节点提交范围选择、草稿保存与详情、提交历史、草稿恢复、草稿覆盖提示、提交批次与快照详情、撤回未提交项、撤回历史追溯和补正反馈已完成第一版。

目标：跑通施工方资料提交流程。

任务：

- 项目文件库列表。
- 上传会话、上传完成、文件入库。已完成第一版。
- 选择节点挂载。已完成第一版，跨节点批量挂载已完成第一版。
- 保存草稿。已接入接口，草稿详情和从提交弹窗保存范围草稿已完成第一版。
- 提交批次。已完成第一版，提交快照详情和跨节点范围提交已完成第一版。
- 提交历史。已完成第一版，支持草稿列表、已提交批次列表、恢复草稿、草稿覆盖提示、打开快照详情和撤回追溯。
- 撤回未提交项。已完成第一版，撤回后自动打开提交历史并可在批次详情追溯原因。
- 查看反馈详情、提交补正反馈。已完成第一版。
- 文件详情、历史版本、OCR 字段、下载/预览入口。已完成第一版。

验收：

- 上传 3 个文件后进入项目文件库。
- 同一文件可挂载多个节点。
- 提交后节点进入 `AI 预审中` 或 `待审查`。
- 退回补正后施工方可提交补正反馈，节点进入 `复审中`。

### 阶段 4：监检工作台

状态：进行中，AI 审查、证据链、标准依据、日期比对、人工审查、退回补正、报告生成/复核详情和报告导出/归档动作已完成第一版。

目标：跑通审查和补正闭环。

任务：

- 节点文件包聚合视图。
- 项目文件包选择并挂载到节点。
- 上传监检资料。
- AI 审查运行历史、重新核验、建议结论。
- 证据链、标准依据、日期比对、OCR 字段定位。已完成第一版。
- 采纳/驳回 AI 建议。已完成第一版。
- 保存人工审查意见。已完成第一版。
- 退回补正。已支持原因输入，补正详情已完成第一版。
- 报告生成/复核入口和复核详情抽屉。已完成第一版。
- 报告导出/归档动作。已完成第一版。

验收：

- AI 建议只能作为草稿，不自动通过。
- 保存审查意见必须提交当前人工编辑内容。
- 退回补正能生成责任单位待办和消息。
- 证据链可回到文件、页码、字段或标准条款。

### 阶段 5：无损检测和建设方

状态：进行中，无损检测底片/报告/提交/补正反馈流程、建设方只读概览、报告预览和归档浏览已完成第一版。

目标：补齐非施工方/监检方角色。

任务：

- 无损检测：底片编号、检测记录导入、抽查详情、报告上传、报告详情、资料提交、监检反馈详情、补正反馈。已完成第一版。
- 建设方：项目概况、节点摘要、报告预览、归档浏览、归档详情、导出任务详情、只读说明。已完成第一版。
- 权限只读边界：建设方无上传、审查、退回、归档确认按钮。

验收：

- 无损检测资料可提交到节点 40/41/42。
- 监检退回后无损检测可补正。
- 建设方只能查看和导出只读资料。

### 阶段 6：管理后台

状态：进行中，项目清单、项目详情、成员授权/批量授权、项目立项、配置导出、系统配置摘要、组织用户、权限矩阵、节点模板、规则版本、Prompt/输出结构版本、规则版本详情、规则版本差异、流程状态机、待办规则、消息模板、工具源、字段映射、真实联调字段差异清单、配置编辑、差异预览、审计筛选/分页、配置发布、live 错误码映射和主要局部错误恢复已完成第一版。

目标：完成系统配置和项目管理入口。

任务：

- 已完成：项目列表、项目详情、参建单位、节点概况、项目成员授权/启停/批量授权、项目立项向导。
- 已完成：系统配置摘要、配置发布、配置导出和导出任务详情。
- 已完成：组织用户、角色权限矩阵。
- 已完成：流程状态机、节点模板、规则版本，权限矩阵/节点模板/流程状态机/待办规则/消息模板/工具源/字段映射编辑和配置差异预览。
- 已完成：待办规则、消息模板、工具源、字段映射新增表单和 mock 写回。
- 已完成：规则版本 Prompt/输出结构摘要、规则与 Prompt 版本详情抽屉、规则版本差异抽屉。
- 已完成：基础数据、审计日志、项目详情、规则差异、配置编辑抽屉、配置发布/导出、项目立项和成员授权的局部错误恢复。
- 已完成：审计日志筛选和分页。
- 已完成：live 错误码映射第一版。
- 已完成：真实联调字段差异清单第一版，支持模块/状态筛选、阻塞项统计和 390px 移动视口验证。

验收：

- 管理员可维护项目、用户、权限、规则、节点。
- 配置类 mutation 产生审计日志。
- 管理后台不代替业务角色办理审查意见。

### 阶段 7：AI 知识库

状态：进行中，知识库指标、索引列表、知识源、项目文件库、文件知识详情、任务中心、检索测试、规则版本发布/回滚/差异、知识库配置、审计、推理日志、多模型对比、分页、live 错误码映射、主要分区局部错误恢复、主要操作级失败恢复和端到端基础验收已完成第一版。

目标：实现知识库管理后台。

任务：

- 已完成：知识库总览、知识源列表、项目文件知识库。
- 已完成：文件知识详情、切片、向量索引、推理引用。
- 已完成：OCR/切片/向量任务中心、失败重试、排队取消、文件重建索引。
- 已完成：知识检索测试、推理日志、多 LLM 对比。
- 已完成：标准规范库新增/编辑/启停。
- 已完成：规则版本发布、回滚、差异。
- 已完成：知识库配置、审计、服务端分页和主要分区局部错误恢复。
- 已完成：检索测试、多模型对比、规则发布/回滚、规则差异、配置保存、任务重试/取消、重建索引、文件详情和推理详情的操作级失败恢复。
- 已完成：live 错误码映射第一版。
- 已完成：端到端验收基础覆盖第一版。
- 待补：更多知识库写回流程 E2E 覆盖和真实后端字段对账深化。

验收：

- 任务失败可重试，排队任务可取消。
- 文件知识详情能关联项目文件和证据链。
- 推理日志能展示模型、规则、Prompt、输入版本和证据。
- 多模型对比不写入正式审查结论。

### 阶段 8：集成验收和联调准备

目标：保持 mock 开发效率，同时用本地 FastAPI live 模式持续验证真实后端合同。

任务：

- 整理前端 API 方法和类型。
- 导出 mock seed 和联调样例。
- 按 `API_DOCUMENTATION.md` 第 23.9 节拆 OpenAPI 计划。
- 已完成第一版：建立 Playwright E2E 基座、关键页面/错误恢复覆盖、联调字段差异清单 0 blocker 验收、配置发布联动追溯/跨页面联动验收和 51 条 live smoke。
- 建立接口覆盖测试清单。
- 跑通 build、E2E 和关键页面检查。
- 梳理后端联调缺口。

验收：

- 关键业务闭环可在 mock 模式完整演示。
- 所有业务页面有 loading、empty、error、forbidden、readonly 状态。
- 可以用本地 FastAPI live 模式跑通核心业务 smoke。

## 6. 开发优先级

| 优先级 | 模块                                 | 原因                                                        |
| ------ | ------------------------------------ | ----------------------------------------------------------- |
| P0     | 版本管理检查点                       | 当前前端文件未跟踪，需要避免连续开发期间丢失上下文          |
| P0     | `ui/` 静态稿 1:1 克隆                | 用户已明确 UI/UX 必须 1:1 克隆 `ui`，这是新的视觉验收硬门槛 |
| P0     | Element Plus 优先组件化              | 功能控件尽量使用 Element，并通过 CSS 维持 `ui/` 截图级视觉  |
| P0     | 工程初始化、类型、mock、权限、项目树 | 所有页面依赖                                                |
| P0     | 施工方提交闭环、监检审查闭环         | 核心业务价值                                                |
| P0     | 异常态、局部 retry、联调错误映射     | 当前质量闭环缺口                                            |
| P1     | 无损检测、建设方只读                 | 角色完整性                                                  |
| P1     | 管理后台更多 live 错误码映射         | 系统可配置性和可维护性                                      |
| P1     | AI 知识库操作级失败恢复              | AI 可追溯能力和任务可恢复性                                 |
| P2     | 报告编辑细节、归档包、高级导出       | 可在一期后段完善                                            |
| P2     | OpenAPI/codegen                      | 真实联调前再做                                              |

## 7. 是否能开始开发

可以，并且已经开始。当前代码已具备继续开发条件，最新顺序应调整为“`ui/` 截图级 1:1 收敛 -> Element Plus 可替换点审计 -> 保持既有 mock/live E2E 不回退 -> 更多异常恢复连续 E2E -> OpenAPI/codegen 准备 -> 生产外部依赖验收”。

- 已完成：`frontend/` 工程初始化、mock seed、types、request、权限路由和工作台第一版。
- 已完成：工作台基础组件拆分、核心 mock mutation 写回、管理后台配置第一版、知识库二期管理操作第一版。
- 可以立即继续：`ui/` 静态稿截图级 1:1 克隆、Element Plus 可替换点审计、更多异常恢复连续 E2E、配置联动深化和 OpenAPI/codegen 准备。
- 可以并行继续：弹窗级错误恢复、关键页面截图检查、OpenAPI/codegen 准备。
- 暂不建议作为前端任务内直接承诺：生产 MinIO、PaddleOCR/Seal OCR、LiteLLM provider、统一 PostgreSQL 的目标环境压测。
- 必须继续补齐：更细的弹窗级错误态、组件级 retry、真实预览服务和生产上传协议替换点。

启动条件评估：

| 条件                           | 结论   | 说明                                                       |
| ------------------------------ | ------ | ---------------------------------------------------------- |
| 需求是否足够清楚               | 通过   | 角色、节点、资料、报告、归档、知识库边界明确               |
| UI 模板是否可用                | 通过   | `vue-element-plus-admin` 已接入并通过构建                  |
| `ui/` 静态稿是否可作为验收基准 | 通过   | 工作台、管理后台、知识库静态页和 `workbench_v2.css` 已具备 |
| API/mock 是否能支撑页面开发    | 通过   | 核心接口可用，剩余接口可在 mock-first 中补齐               |
| 是否需要等后端                 | 不需要 | 当前已有本地 FastAPI live smoke，生产外部依赖另行验收       |
| 是否存在硬阻塞                 | 无     | 剩余都是范围和质量风险，不是开工阻塞                       |
| 是否需要先做版本检查点         | 建议   | 当前前端和文档未跟踪，建议开发前纳入 git 或明确交付分支    |

开工风险：

| 风险                                      | 影响                          | 应对                                                                                                                                |
| ----------------------------------------- | ----------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| mock/live 异常态仍需覆盖更多局部组件 | 局部表格/弹窗错误恢复还不完整 | 已补 `FORBIDDEN`、`TASK_RUNNING`、`IDEMPOTENCY_KEY_CONFLICT`、`ETAG_CONFLICT` 第一版；后续补组件级 retry 和 live 错误映射           |
| 静态原型是 HTML/CSS，不是 Vue 组件        | 视觉复刻工作量上升            | 以 `ui/workbench_v2.css` 为基准样式，Vue 组件承载数据和交互，逐页按静态稿顺序组件化                                                 |
| 当前 Vue 页面仍未完成截图级 1:1           | 不能直接作为最终 UI 验收      | 已完成业务页外壳、工作台中栏/右栏、后台/知识库中栏深层顺序和基础交互态，后续按截图对照继续收敛密度、右栏细节和可安全 Element 化区域 |
| OpenAPI/codegen 仍可升级为完整 SDK        | 当前生成 operation map        | 已落 `openapi/aicheck.yaml`、分片 YAML、`contract-index.json`、校验脚本和前端 `operation-map.ts`；后续可替换为 orval/openapi-typescript 完整 SDK |
| 生产外部依赖未在目标环境验证              | 真实 OCR/LLM/对象存储效果不确定 | 本地 FastAPI live smoke 已通过，生产用 `verify_deployment.py --strict-production --write-probes` 补验收                            |
| 复杂预览渲染服务未落地                    | Office/缩略图/页级预览仍依赖后续服务 | 文档原件预览/下载已使用 signed GET，后续补 Office 转换、缩略图和页级真实预览服务                                                       |
| 前端目录和文档仍未被 git 跟踪             | 容易丢失或难以审计改动        | 先建立本地提交点或确认当前未跟踪文件纳入交付范围                                                                                    |

最终判断：**具备继续开发条件，可以继续下一轮前端开发**。第 0 阶段已完成，第 1-5 阶段已有可运行基础，第 6 阶段已完成管理后台核心二期、细项配置、真实联调字段差异清单、配置发布联动追溯、Prompt 版本详情和规则差异第一版，第 7 阶段已完成知识库二期主链路，第 8 阶段已有 51 条本地 FastAPI live E2E 基线。下一轮不需要再做立项评估，直接进入 `ui/` 截图级静态稿克隆和 Element 可替换点审计，并在每个视觉切片后回归既有 mock/live 写回 E2E 基线。

## 8. 下一步执行清单

1. 建立开发检查点：确认 `frontend/`、`API_DOCUMENTATION.md`、`FRONTEND_DEVELOPMENT_PLAN.md` 纳入本轮交付范围。
2. 收敛 1:1 截图差距：对 `/workbench/*`、`/admin/overview`、`/knowledge/overview` 做桌面截图对照，逐项修正卡片密度、表格间距、标题字号和状态色。
3. 审计 Element 可替换点：在不破坏 `ui/` 截图级密度的前提下，把仍可替换的功能按钮、表格、表单和状态组件继续切为 Element Plus。
4. 扩展视觉验收：为每个业务路由补关键 DOM 比例、静态段落数量、面板溢出和按钮状态校验，避免后续功能开发偏离 `ui/`。
5. 扩展连续主流程 E2E：其它弹窗异常恢复、配置发布后跨页面联动校验。
6. 联调准备收尾：基于真实后端字段继续深化联调差异清单，并将 `openapi/aicheck.yaml` 接入 orval/openapi-typescript 生成前端 SDK。
7. 集成验收：每完成一个切片跑 `pnpm ts:check`、`pnpm build:dev`、`pnpm test:e2e`，并启动本地服务做页面检查。

建议下一轮开发切片：

| 顺序 | 切片         | 验收口径                                                                |
| ---- | ------------ | ----------------------------------------------------------------------- |
| 1    | 截图收敛     | 六个业务路由桌面截图与 `ui/*.html` 卡片密度、字号、间距和状态色逐项对齐 |
| 2    | Element 审计 | 可替换功能控件继续使用 Element Plus，静态复刻区域不破坏截图级密度       |
| 3    | 异常恢复     | 更多弹窗级错误恢复和连续失败/重试流程可演示                             |
| 4    | 集成验收     | 四类角色主流程可连续演示，构建、类型检查和 E2E 通过                     |
