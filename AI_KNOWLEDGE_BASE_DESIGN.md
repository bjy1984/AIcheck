# AI 知识库功能体系与页面设计

## 1. 设计目标

AI 知识库管理用于统一管理标准规范、项目文件库、业务规则、推理证据和多模型反馈结果。它服务于监检工作台的 AI 业务审查链路，但不替代监检人员审查，不承载采纳 AI 建议、退回补正、报告复核等业务办理动作。

核心目标：

- 让标准规范、项目文件和业务规则具备可检索、可追溯、可版本化的知识资产状态。
- 让 OCR、切片、向量化、索引重建和失败重试具备可见的运行状态。
- 让每次 AI 业务审查链路能回溯到文件版本、OCR 版本、向量索引版本、规则版本和模型版本。
- 让多 LLM 反馈对比用于质量评估和规则优化，而不是直接生成最终审查结论。

## 2. 功能体系

| 模块 | 管理对象 | 核心功能 |
|---|---|---|
| 知识库总览 | 标准规范库、项目文件库、业务规则库、推理证据库 | 查看文件总量、OCR 状态、向量化状态、失败任务、最近更新 |
| 标准规范库 | 国家标准、行业规范、监管规则、检查清单 | 上传、分类、条文拆分、版本、OCR、向量化、启停 |
| 项目文件知识库 | 施工方、无损检测、监检上传的项目资料 | 按项目、节点、单位、文件版本管理 OCR、切片、向量化和索引状态 |
| 文件知识详情 | 单个文件及其知识加工结果 | 文件预览、OCR 文本、切片结果、向量索引、关联节点、引用历史 |
| OCR/向量任务中心 | 文件解析、OCR、切片、Embedding、索引 | 队列状态、失败重试、处理耗时、模型版本、任务日志 |
| 业务规则版本管理 | 审查规则模板、Prompt、字段映射、工具源 | 版本管理、发布、回滚、差异对比、适用节点 |
| 知识检索测试 | 标准条文、项目文件、节点规则 | 输入问题，查看召回片段、来源文件、页码、相似度和命中规则 |
| 推理链路历史日志 | 每次 AI 业务审查链路 | 输入文件、规则版本、召回证据、模型输出、人工处理结果 |
| 多 LLM 反馈对比 | 多模型对同一问题的分析结果 | 对比结论、证据引用、风险点、置信度、耗时、成本 |
| 知识库配置 | OCR 引擎、Embedding 模型、切片策略、索引策略 | 模型参数、处理队列、保留周期、重建索引策略 |
| 操作审计日志 | 知识库管理操作 | 上传、删除、重跑、发布、回滚、人工修正等审计记录 |

## 3. 知识库类型

| 类型 | 来源 | 用途 |
|---|---|---|
| 标准规范库 | TSG、GB、NB/T、项目规范、检查清单 | AI 审查时引用标准条文、生成标准依据、支持证据定位 |
| 项目文件知识库 | 项目文件库、节点文件包、无损检测报告、监检资料 | 支撑项目内业务核验和跨文件一致性检查 |
| 业务规则知识库 | 后台维护的 AI 业务审查规则模板、Prompt、字段映射、工具源 | 保证同一节点审查链路可版本化、可回溯、可复现 |
| 推理证据库 | EvidenceLink、召回片段、模型输出、人工确认记录 | 审计、复盘、多模型对比、规则优化 |

## 4. 页面结构

```text
AI 知识库管理
├─ 知识库总览
├─ 标准规范库
├─ 项目文件知识库
├─ 文件知识详情
├─ OCR/向量任务中心
├─ 业务规则版本管理
├─ 知识检索测试
├─ 推理链路历史日志
├─ 多 LLM 反馈对比
├─ 知识库配置
└─ 操作审计日志
```

## 5. 关键页面字段

### 5.1 知识库总览

| 区域 | 字段 |
|---|---|
| 顶部指标 | 标准文件数、项目文件数、OCR 完成率、向量化完成率、失败任务、推理日志数 |
| 知识库状态表 | 知识库类型、文件数、切片数、向量数、索引版本、健康状态、最近更新 |
| 运行提醒 | 最近失败任务、最近规则发布、最近模型对比、索引过期提醒 |

### 5.2 项目文件知识库

| 字段 | 说明 |
|---|---|
| 项目 | 项目编号和项目名称 |
| 节点 | 项目审核节点编号和名称 |
| 文件 | 文件名、文件用途、文件版本 |
| 来源 | 上传单位、上传人、上传时间 |
| OCR 状态 | 未识别、排队中、识别中、已识别、识别失败、人工修正 |
| 切片状态 | 未切片、切片中、已切片、切片失败 |
| 向量状态 | 未向量化、向量化中、已向量化、向量化失败、索引过期 |
| 操作 | 查看详情、重跑 OCR、重建向量、查看推理引用 |

### 5.3 业务规则版本管理

| 字段 | 说明 |
|---|---|
| 规则模板 | 例如 Welder-Qualification-B、Material-Certificate-C |
| 适用节点 | 绑定项目审核节点 |
| 规则版本 | 当前发布版本和草稿版本 |
| Prompt 版本 | 与规则版本绑定的 Prompt |
| 字段映射版本 | OCR 字段到业务字段的映射版本 |
| 工具源版本 | 外部核验工具源版本 |
| 操作 | 查看差异、复制版本、发布、回滚 |

### 5.4 推理链路历史日志

| 字段 | 说明 |
|---|---|
| 推理时间 | AI 业务审查链路执行时间 |
| 项目/节点 | 项目和审核节点 |
| 审查对象 | 材料证明、焊工资格、无损检测报告等 |
| 规则版本 | 本次推理使用的规则和 Prompt 版本 |
| 模型 | 参与推理的 LLM 或多模型组合 |
| 召回证据 | 引用标准条文、项目文件页码、OCR 字段、EvidenceLink |
| AI 建议 | 建议结论、风险点、需人工确认项 |
| 人工处理 | 采纳、修改、驳回、重新分析，仅记录业务工作台结果 |

### 5.5 多 LLM 反馈对比

| 字段 | 说明 |
|---|---|
| 模型 | 模型名称和版本 |
| 建议结论 | 模型给出的业务建议 |
| 证据引用 | 引用的文件、页码、标准条文 |
| 风险点 | 识别的缺失、冲突、过期、低置信度问题 |
| 置信度 | 模型输出的业务置信度 |
| 性能 | 耗时、Token、成本 |
| 人工评价 | 仅用于规则优化和模型质量评估 |

## 6. 状态规范

| 状态类型 | 状态值 |
|---|---|
| OCR 状态 | 未识别、排队中、识别中、已识别、识别失败、人工修正 |
| 切片状态 | 未切片、切片中、已切片、切片失败 |
| 向量化状态 | 未向量化、向量化中、已向量化、向量化失败、索引过期 |
| 规则状态 | 草稿、待发布、已发布、已停用、已回滚 |
| 推理状态 | 待推理、推理中、完成、失败、已人工确认 |
| 知识状态 | 启用、停用、过期、待复核 |

## 7. 权限与边界

- 系统管理员可以维护知识源、规则版本、OCR/向量任务和配置。
- 监检人员可以查看与授权项目、节点相关的推理链路、证据引用和多模型对比结果。
- 施工方只能查看本单位上传文件的知识入库状态，不可查看其他单位文件向量内容。
- 建设方保持只读，只能查看授权项目状态摘要和报告相关证据。
- AI 知识库管理不提供退回补正、采纳 AI 建议、保存审查意见、报告生成/复核等业务办理按钮。

## 8. 后端接口预留

| 接口 | 用途 |
|---|---|
| `GET /api/knowledge/overview` | 获取知识库总览指标 |
| `GET /api/knowledge/sources` | 获取知识源列表 |
| `GET /api/knowledge/project-files` | 获取项目文件知识库列表 |
| `GET /api/knowledge/files/{fileId}` | 获取单文件知识详情 |
| `POST /api/knowledge/tasks/{taskId}/retry` | 重试 OCR、切片或向量化任务 |
| `POST /api/knowledge/files/{fileId}/reindex` | 重建单文件向量索引 |
| `GET /api/rules/versions` | 获取业务规则版本列表 |
| `POST /api/rules/versions/{versionId}/publish` | 发布规则版本 |
| `POST /api/rules/versions/{versionId}/rollback` | 回滚规则版本 |
| `GET /api/reasoning/logs` | 获取推理链路历史日志 |
| `GET /api/reasoning/logs/{logId}` | 获取单次推理链路详情 |
| `POST /api/llm/compare` | 发起多 LLM 反馈对比 |
